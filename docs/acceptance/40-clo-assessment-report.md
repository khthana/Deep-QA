# ตรวจรับด้วยมือ — #40 CLO assessment report

`docs/06-implementation-plan.md` §Acceptance กำหนดว่า ticket หนึ่งจะเสร็จเมื่อ automated test ผ่าน
**และ** checklist ที่ทำด้วยมือถูกเดินจนครบ เอกสารนี้คือครึ่งหลังของ #40

ครึ่งแรกอยู่ที่ `backend/test/clo-assessment.test.js` (14 subtests) — ผลการเรียนรู้ของการเปิดสอน
เกณฑ์สี่ระดับของแต่ละข้อ เส้นแบ่งทั้งสองเส้นที่ถูกยืนยันจากทั้งสองด้าน ค่าที่ต้องตรงกับ #38
และเกณฑ์ข้อ 7 ที่ระบุ seam ไว้เอง (*enforced at the server*)

seam ที่สองคือเบราว์เซอร์ — `e2e/tests/40a-clo-assessment.spec.js` (8 แถว) ดูหัวข้อ
*มัตแตนต์ที่พิสูจน์แถว ⚙* ท้ายเอกสาร

## สิ่งที่ต้องเข้าใจก่อนเดิน

**หน้านี้ไม่เก็บอะไรเลย** ทุกอย่างบนจอคือคะแนนที่ #34 บันทึกไว้ อ่านผ่านกฎของ
`backend/lib/attainment.js` การเดินใบนี้จึงไม่เขียนอะไร ยกเว้นแถวที่จงใจสร้างสถานการณ์แล้วคืนค่า

**เกณฑ์ในคอลัมน์คือกฎ ไม่ใช่รูบริก** และนี่คือการตัดสินใจเดียวของตั๋วนี้

`subject_clo_achievement_criteria` ที่ #29 เขียนไว้ เก็บ**สี่ประโยค**ต่อหนึ่งข้อ —
ดีเยี่ยม / ดี / พอใช้ / ต้องปรับปรุง — และไม่มีตัวเลขอยู่ในนั้นเลยสักตัว ไม่มีอะไรในตารางนั้น
ตัดสินได้ว่านักศึกษาคนหนึ่งบรรลุผลการเรียนรู้ข้อหนึ่งหรือไม่ และ `lib/attainment.js` เขียนไว้ตรง ๆ
ว่าเส้นแบ่งจริงคือ `PASS = 3` จากห้า ซึ่ง *ไม่ได้มาจากตารางเกณฑ์* — ตกลงกันไว้ก่อนสร้าง #38
เพราะตารางนั้นเก็บประโยค ส่วน BR-17 พูดถึงแต่สัดส่วน

คอลัมน์ *เกณฑ์การบรรลุ* จึงเขียนกฎที่ตัดสินจริง และรูบริกสี่ระดับอยู่ใต้แผ่นพับเป็นข้อมูลอ้างอิง
ถ้าเอาประโยครูบริกไปวางในคอลัมน์นั้นแทน ผู้อ่านทุกคนจะเข้าใจว่าตัวเลขข้าง ๆ เกิดจากประโยคนั้น —
ความผิดพลาดชนิดเดียวกับที่ #39 เกือบวาดเส้นจากคะแนนใต้หัวเรื่องที่เขียนว่าร้อยละ

**เส้นแบ่งมีสองเส้น ไม่ใช่เส้นเดียว** `PASS` ตัดสินนักศึกษาหนึ่งคนต่อผลการเรียนรู้หนึ่งข้อ
ส่วน 60% ของ BR-17 ตัดสินผลการเรียนรู้ข้อนั้นต่อทั้งชั้น และ BR-17 อ่านว่า **มากกว่า** อย่างเคร่งครัด
ข้อที่มีผู้ผ่านพอดี 60% จึง **ไม่ผ่าน**

**seed ผ่านทุกข้ออย่างสบาย ๆ** ทุกข้อในนั้นอยู่ห่างจากทั้งสองเส้นมาก หน้าจอที่พิมพ์ *ผ่าน*
ไปเรื่อย ๆ จะทำให้ทุกแถวที่เขียนบน seed อย่างเดียวเขียวหมด แถวที่เกี่ยวกับ *ไม่ผ่าน*
กับ *ยังไม่ประเมิน* จึงต้องสร้างสถานการณ์เองทั้งหมด

## สถานการณ์ที่ seed ให้ และที่ต้องสร้างเอง

| สถานการณ์ | มีใน seed ไหม | ใช้กับเกณฑ์ข้อไหน |
|---|---|---|
| ตอนเรียน 1 ปี 2569 มี 9 CLO · 57 คน · คะแนนครบ | มี | ข้อ 1, 2, 3 |
| เกณฑ์การบรรลุสี่ระดับของทุก CLO | มี (ประโยคเดียวกันทุกข้อ) | ข้อ 1 |
| ผลการเรียนรู้ที่ **ไม่ผ่าน** | **ไม่มี** ต้องล้างคะแนนข้อหนึ่งเป็น 0 | ข้อ 2 |
| ผลการเรียนรู้ที่ **ยังไม่มีใครถูกวัด** | **ไม่มี** ต้องเพิ่ม CLO ใหม่ | ข้อ 2 |
| ผู้ผ่านพอดี 60% | **ไม่มี** ต้องจัดคะแนนให้ได้ 3 ใน 5 คน | ข้อ 2, 8 |
| ตอนเรียนที่ยังไม่มีคะแนน | **ไม่มี** ต้องสร้างเอง | ข้อ 6 |
| รายวิชาที่ยังไม่มี CLO เลย | **ไม่มี** ต้องสร้างการเปิดสอนใหม่ | ข้อ 6 |
| ตอนเรียนของคนอื่น | `teacher.two@` ไม่ได้สอนตอนเรียน 1 | ข้อ 7 |

**สี่แถวต้องสร้างสถานการณ์เอง** และการสร้างแล้วคืนค่าคือส่วนหนึ่งของการเดินตามที่ `CLAUDE.md` เขียนไว้

## Checklist

- **☑** เดินจบแล้ว
- **◐** ครึ่งของ server เดินแล้วและผ่าน ส่วนที่เหลือคือสิ่งที่หน้าจอวาด (สี ถ้อยคำ) ยังต้องมีคนดู
- **☐** ยังไม่ได้เดิน
- **⚙** ครอบคลุมด้วย browser seam แล้ว (#65) ไม่ต้องเดินด้วยมือซ้ำ แถวบอกไฟล์ที่ครอบคลุมไว้

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| — | ทางเข้าหน้านี้อยู่ที่เมนูของตอนเรียน | เข้าด้วย `teacher.one@` เลือกตอนเรียน 1 แล้วกด **การประเมินผลการเรียนรู้** | หน้าเปิดขึ้นจริง ไม่ใช่หน้า *ยังไม่ได้สร้าง* — เนื้อหาของเมนูเป็นสิ่งที่คนต้องดู | ☐ |
| 1 | ตารางแสดงทุก CLO พร้อมเกณฑ์และสัดส่วนผู้ผ่าน | เปิดหน้า แล้วนับแถว | หนึ่งแถวต่อหนึ่งข้อ ครบทุกข้อของการเปิดสอน และเศษส่วนตรงกับร้อยละข้าง ๆ — ครอบคลุมโดย `40a` แถว 1 | ⚙ |
| 1 | เกณฑ์ที่พิมพ์คือกฎที่ตัดสินจริง ไม่ใช่ค่าที่พิมพ์ทิ้งไว้ | อ่านคอลัมน์ *เกณฑ์การบรรลุ* | เขียน *คะแนน ≥ 3.00 จาก 5* และ *ผู้ผ่านมากกว่าร้อยละ 60 ของผู้มีคะแนน* โดยตัวเลข**และคำว่า มากกว่า** มาจากกฎ ไม่ใช่พิมพ์ทิ้งไว้ — ครอบคลุมโดย `40a` แถว 1 | ⚙ |
| 1 | ข้อที่ยังไม่มีใครถูกวัดไม่หายไปจากตาราง | เพิ่ม CLO ที่ยังไม่มีคะแนนลงในการเปิดสอน แล้วเปิดหน้า | แถวของมันอยู่ในตาราง — ครอบคลุมโดย `40a` แถว 3 | ⚙ |
| 1 | ถ้อยคำของคอลัมน์เกณฑ์อ่านแล้วไม่สับสนกับรูบริก | อ่านหัวตารางกับกล่องเกณฑ์เหนือตาราง | ชัดว่าอะไรตัดสิน และรูบริกสี่ระดับเป็นข้อมูลอ้างอิงคนละส่วน — ถ้อยคำเป็นสิ่งที่คนต้องดู | ☐ |
| 2 | ผ่าน/ไม่ผ่าน คิดจากกฎ 60% | ยืนยันที่ seam ของ server ด้วยคะแนนที่ขอบทั้งสองด้าน | 60% พอดี = ไม่ผ่าน · 80% = ผ่าน — `backend/test/clo-assessment.test.js` | ☑ |
| 2 | ผลการประเมินเป็น**คำ** ไม่ใช่แค่สี | ล้างคะแนนข้อหนึ่งเป็น 0 แล้วดูคอลัมน์สุดท้าย | อ่านว่า *ไม่ผ่าน* เป็นตัวอักษร ไม่ใช่แค่ช่องสีแดง — ครอบคลุมโดย `40a` แถว 2 | ⚙ |
| 2 | ข้อที่ยังไม่มีใครถูกวัดขึ้นว่า *ยังไม่ประเมิน* ไม่ใช่ *ไม่ผ่าน* | เพิ่ม CLO ที่ยังไม่มีคะแนน แล้วดูแถวของมัน | *ยังไม่ประเมิน* · `0 / 0` · เฉลี่ยเป็น `—` — ครอบคลุมโดย `40a` แถว 3 | ⚙ |
| 2 | ข้อที่ไม่ผ่านถูกเอ่ยชื่อ ไม่ใช่ปล่อยให้ไปหาเอาจากสี | ล้างคะแนนข้อหนึ่ง แล้วอ่านประโยคเหนือตาราง | ชื่อข้อนั้นอยู่ในประโยค — ครอบคลุมโดย `40a` แถว 4 · เกินตัวอักษรของตั๋ว รับนิสัยมาจาก #38 | ⚙ |
| 2 | สามคำนั้นอ่านแล้วรู้ว่าต่างกันอย่างไร | อ่าน *ผ่าน* · *ไม่ผ่าน* · *ยังไม่ประเมิน* บนหน้าจอ | คำที่สามต้องไม่อ่านเหมือนคำที่สอง — ถ้อยคำเป็นสิ่งที่คนต้องดู | ☐ |
| 3 | คะแนนใช้มาตราส่วนห้าเดียวกับหน้าจออื่น | เทียบค่าเฉลี่ยและอัตราผ่านของทุกข้อกับ `/learning-details` ของตอนเรียนเดียวกัน | เลขเดียวกันทุกข้อ — `backend/test/clo-assessment.test.js` ยึดสอง endpoint ไว้ด้วยกัน | ☑ |
| 3 | หัวคอลัมน์บอกว่าเป็นสเกลเต็ม 5 | อ่านหัวตาราง | เขียนว่า *คะแนนเฉลี่ย (เต็ม 5)* โดยเลข 5 มาจากคำตอบ — ถ้อยคำเป็นสิ่งที่คนต้องดู `40a` ไม่ได้อ่านหัวคอลัมน์ | ☐ |
| 4 | ไฟล์ PDF ออกมาจริงและ**ฝัง**ฟอนต์ไทยไว้ในตัวมัน | กด *บันทึกเป็น PDF* | ได้ไฟล์ และในไบต์ของไฟล์มีทั้งชื่อ `THSarabun` และ `/FontFile2` — ครอบคลุมโดย `40a` แถว 5 | ⚙ |
| 4 | **ตัวอักษรไทยใน PDF วางถูกจริง** | เปิดไฟล์ที่ได้ แล้วอ่าน | สระบน สระล่าง วรรณยุกต์ อยู่ถูกตำแหน่ง ไม่มีกล่องสี่เหลี่ยม — **ไม่มีอะไรในเบราว์เซอร์เห็นเรื่องนี้ได้** | ☐ |
| 4 | ตัวอักษรใน PDF ใหญ่พอสำหรับภาษาไทย | เปิดไฟล์ แล้วดูขนาดตัวอักษรในตาราง | ไม่เล็กกว่า 14pt ทุกจุด — บทเรียนจาก #103 ซึ่งพบว่า 10pt เล็กเกินไปสำหรับเอกสารหลักฐาน | ☐ |
| 5 | PDF ระบุรายวิชา ตอนเรียน ปีการศึกษา และภาคการศึกษา | ยืนยันที่ seam ของ server ว่าค่าทั้งสี่อยู่ในคำตอบ | ชื่อรายวิชา**ภาษาไทย** เลขตอนเรียน ปี และภาค — `backend/test/clo-assessment.test.js` | ☑ |
| 5 | และทั้งสี่อย่างนั้นพิมพ์อยู่บนหน้ากระดาษจริง | เปิด PDF แล้วอ่านหัวกระดาษ | เห็นครบทั้งสี่ — **`40a` อ่านแต่ไบต์ของฟอนต์ ไม่ได้อ่านข้อความในหน้า** | ☐ |
| 5 | ชื่อไฟล์บอกได้ว่าเป็นของตอนเรียนไหน ปีไหน | ดูชื่อไฟล์ในโฟลเดอร์ดาวน์โหลด | มีรหัสวิชา เลขตอนเรียน และปีการศึกษาอยู่ในชื่อ — ครอบคลุมโดย `40a` แถว 5 | ⚙ |
| 6 | รายวิชาที่ยังไม่มี CLO เลย ขึ้นคนละประโยคกับ *ยังไม่มีคะแนน* | ยืนยันที่ seam ของ server ด้วยการเปิดสอนที่ไม่มี CLO | `no_outcomes` เป็นจริง และ `empty` เป็นเท็จ — `backend/test/clo-assessment.test.js` (รีวิวเจอ `[].every()` คืน `true`) | ☑ |
| 6 | และถ้อยคำของประโยคนั้นบอกได้ว่าต้องทำอะไรต่อ | เปิดหน้าของรายวิชาที่ยังไม่มี CLO | บอกว่ายังไม่ได้กำหนดผลการเรียนรู้ ไม่ใช่ว่ายังไม่มีคะแนน — ถ้อยคำเป็นสิ่งที่คนต้องดู | ☐ |
| 6 | ตอนเรียนที่ยังไม่มีคะแนนขึ้นสถานะว่าง | สร้างตอนเรียนใหม่ที่ยังไม่มีคะแนน แล้วเปิดหน้า | ข้อความบอกว่ายังไม่มีคะแนน และ**ไม่มีตาราง** — ครอบคลุมโดย `40a` แถว 7 | ⚙ |
| 6 | และปุ่ม PDF กดไม่ได้เมื่อไม่มีอะไรให้พิมพ์ | ดูปุ่มบนตอนเรียนที่ยังไม่มีคะแนน | ปุ่มอยู่ในสถานะกดไม่ได้ — ครอบคลุมโดย `40a` แถว 7 | ⚙ |
| 7 | ตอนเรียนของคนอื่นถูกปฏิเสธที่เซิร์ฟเวอร์ | เข้าด้วย `teacher.two@` แล้วพิมพ์ที่อยู่ของตอนเรียน `1` ตามด้วย `/AssessmentCLO` | *ไม่พบตอนเรียนที่ระบุ* ไม่ใช่ตาราง และไม่มี *กำลังโหลดข้อมูล…* ค้างอยู่ใต้คำปฏิเสธ — ครอบคลุมโดย `40a` แถว 8 | ⚙ |
| 8 | เทสต์ยืนยันการตัดสินที่ขอบและทั้งสองด้านของขอบ | อ่านชุดเทสต์ | สี่แถว: 3.00 พอดีนับว่าผ่าน · ต่ำกว่านับว่าไม่ผ่าน · 60% พอดีคือไม่ผ่าน · 80% คือผ่าน — `backend/test/clo-assessment.test.js` | ☑ |
| — | รูบริกสี่ระดับอยู่หลังแผ่นพับ ไม่ปนกับตัวเลข | เปิดหน้า แล้วกดแผ่นพับ *เกณฑ์การบรรลุผลสี่ระดับ* | ปิดอยู่ตอนเปิดหน้า และเปิดออกมาแล้วเห็นสี่ระดับของแต่ละข้อ — ครอบคลุมโดย `40a` แถว 6 | ⚙ |
| — | ที่จอแคบ ตารางยังอ่านออกและเลื่อนในกรอบตัวเอง | ย่อหน้าต่างให้เหลือประมาณครึ่งจอ | ตารางเลื่อนในกรอบของมัน หน้าไม่เลื่อนข้าง (#98) ตัวอักษรยังอ่านออก — เป็นสิ่งที่คนต้องดู | ☐ |
| — | สีของชิปไม่ใช่ข้อมูลเพียงอย่างเดียว | ดูคอลัมน์ผลการประเมิน | ทุกชิปมีคำอยู่ในนั้น สีเป็นแค่ตัวช่วย — สีเป็นสิ่งที่คนต้องดู | ☐ |

## บันทึกการเดินด้วยมือ

ยังไม่ได้เดิน

## สิ่งที่รีวิวสองแกนเจอ และแก้ไปแล้ว

รีวิวสองแกนเจอเก้าอย่าง แก้ไปทั้งหมด สองอย่างในนั้นทั้งสองแกนเจอตรงกัน

**แกน spec**

1. **`empty` เป็นจริงแบบว่างเปล่า** `lines.every(...)` บนอาร์เรย์ว่างคืน `true` การเปิดสอนที่ยัง
   ไม่มี CLO เลยจึงรายงานตัวเองว่า *ยังไม่มีคะแนน* ทั้งที่ปัญหาจริงคือยังไม่มีอะไรให้วัด
   ตอนนี้ `empty` มี `lines.length > 0` คุม และมี `no_outcomes` เป็นสถานะของตัวเอง
   พร้อมประโยคของตัวเองบนหน้าจอ และเทสต์ที่สร้างการเปิดสอนใหม่ที่ไม่มี CLO
2. **เกณฑ์ข้อ 8 พิสูจน์ขอบล่างไม่ครบ** เส้น 60% ถูกยืนยันที่ *พอดี 60* กับ *80* แต่ไม่เคยยืนยัน
   ที่ต่ำกว่า — แถวที่จัดคะแนนเป็น 0 อ่าน `passed_count` กับ `pass_rate` แต่ไม่เคยอ่าน `passed`
   เพิ่ม `assert.equal(bottom.passed, false)`
3. **สามเอกสารบอกสองอย่างเรื่องตารางตอนไม่มีคะแนน** JSDoc ของ route กับคอมเมนต์ในเทสต์
   เขียนว่า *หน้าจอเก็บตารางไว้แล้วบอกว่าทำไมทุกช่องว่าง* ส่วนหน้าจอจริง ใบตรวจรับ และ `40a`
   ล้วนบอกว่าไม่มีตาราง — แก้คอมเมนต์ทั้งสองให้พูดเฉพาะสิ่งที่ route ตัดสิน คือคำตอบยังพา
   ผลการเรียนรู้ไปด้วย ส่วนหน้าจอจะวาดอะไรไม่ใช่เรื่องของ route
4. **ประโยคเตือนอ้างถึงหน้าจอที่ยังไม่มี** *ต้องระบุแนวทางปรับปรุงในรายงานการปรับปรุงอย่างต่อเนื่อง*
   ชี้ไปที่ #41 ซึ่งยังเป็นหน้า *กำลังพัฒนา* — บอกให้ผู้สอนไปที่ที่ไปไม่ได้ ตัดออกเหลือ
   *ยังไม่ผ่านเกณฑ์ที่ระบุไว้ข้างต้น*
5. **แถวปฏิเสธอ้างการเทียบที่ไม่ได้ทดสอบ** คอมเมนต์เขียนว่าพิสูจน์ *บัญชีที่สอนตอนเรียนนั้นได้ผล*
   แต่ตัวแปรชื่อ `mine` กลับเป็นตอนเรียนของ U_MULTI ที่ขอด้วยคุกกี้ของ U_TEACH2 — 404 นั้นถูก
   แต่การเทียบที่อ้างไม่ได้ถูกยืนยันตรงไหน แก้ชื่อและคอมเมนต์ให้ตรงกับสิ่งที่ยืนยันจริง

**แกน standards**

6. **แถว ⚙ ข้อ 1 อ้างสองอย่าง แต่มีมัตแตนต์ค้ำอย่างเดียว** ครึ่ง *หนึ่งแถวต่อหนึ่งข้อ ครบทุกข้อ*
   ไม่มีอะไรฆ่า — รอบกวาดเองบันทึกไว้แล้วว่า `unmeasuredoutcomenotlisted` ฆ่ามันไม่ได้
   เพิ่ม `onerowmissingfromthetable` ที่ตัดแถวแรกทิ้งโดยไม่มีเงื่อนไข
7. **คำว่า *มากกว่า* ไม่เคยถูกยืนยัน** `40a` อ่านแค่ substring `ร้อยละ 60` ทั้งที่ `bands.js`
   เขียนไว้เองว่า *มากกว่า* กับ *ไม่น้อยกว่า* คนละกฎกัน แก้เป็นยืนยันทั้งประโยค
   แล้วเพิ่ม `criterionsaysnotlessthan`
8. **คำตัดสินสามสถานะเขียนไว้สองที่สองรูป** หน้าจอใช้ map ส่วน PDF ใช้ if-cascade
   และคำไทยสามคำถูกพิมพ์ซ้ำ — มีแต่ฝั่งหน้าจอที่มีมัตแตนต์ ทุบฝั่ง PDF แล้วไม่มีอะไรแดง
   ย้ายไปเป็น `verdictLabel` ใน `lib/bands.js` เหตุผลเดียวกับที่ตั๋วนี้ยกให้ `criterionLines`
   เอาไว้แล้ว สีของชิปยังอยู่กับหน้าจอ เพราะ PDF ไม่มีชิป
9. **คอมเมนต์ที่ export ระบุตัวหารผิด** เขียนว่า *60% ของชั้น* ทั้งที่ทั้งโค้ดและคอมเมนต์ของ
   `passed_count` ยืนยันว่าเป็น *ของผู้มีคะแนน* — และคอมเมนต์นั้นคือเหตุผลเดียวที่ export
   `OUTCOME_PASS_PERCENT` ออกมา

**และอีกสองอย่างที่รีวิวชี้ว่าไม่ใช่ข้อบกพร่อง** สำเนาที่สี่ของ query ของ #38 ซึ่ง JSDoc ประกาศไว้
พร้อมเหตุผลของ #39 และ #104 อยู่แล้ว · กับหัวไฟล์มัตแตนต์ที่ยังเขียนว่า *Eleven mutants*
ซึ่งเป็นตัวเลขค้าง แก้แล้ว

## หมายเหตุที่ไม่ใช่เกณฑ์

**route นี้เรียกด่านทีละตัว ไม่ใช่ `Promise.all` แบบ #39** และเหตุผลไม่ใช่ความเร็ว
มีแต่ `sectionOf` ที่ใช้ `integerId`; `offeringOf` ยังกันด้วย `/^\d+$/` เอง รหัสที่เป็นตัวเลขล้วน
แต่ใหญ่เกิน `integer` จึงวิ่งเข้า query ของมันแล้วกลับมาเป็น 22003 — ห้าร้อยสำหรับ URL ที่คนพิมพ์ผิด
นั่นคือ [#107](https://github.com/khthana/Deep-QA/issues/107) ซึ่งไล่ไว้สิบเอ็ดจุดและไม่ควรแก้ครึ่งเดียว
จากในตั๋วนี้ การถามด่านที่ตัดสินก่อนเป็นรูปที่ถูกกว่าอยู่แล้ว และ `backend/test/clo-assessment.test.js`
มีแถวปักไว้

**`columnOf` ได้ `passed_count` เพิ่มมาหนึ่งฟิลด์** เพราะรายงานพิมพ์ *55 / 57* ข้าง ๆ *96.5%*
ถ้า route นี้นับเอง มันจะเป็นที่ที่สองที่ `PASS` อยู่ เหตุผลเดียวกับที่ `summaryOf` พา
`scored_count` กับ `passed_count` ไปด้วยตั้งแต่ #38 เพียงแต่ลึกลงมาหนึ่งชั้น
#38 กับ #36 ได้ฟิลด์นี้ติดไปด้วยและไม่มีใครใช้ ซึ่งไม่เป็นไร — มันคือฟิลด์เพิ่ม ไม่ใช่ฟิลด์เปลี่ยน

**`SCALE` กับ `OUTCOME_PASS_PERCENT` ถูก export แล้ว** โดยคอมเมนต์ในไฟล์เขียนเส้นแบ่งไว้ว่า
export มาเพื่อ **พิมพ์** ไม่ใช่เพื่อ **ใช้คำนวณ** — เหมือนที่ `BAND_FLOORS` เดินทางไปหา legend ของ #38
อยู่แล้ว ไม่มีอะไรนอกไฟล์นั้นหารด้วยห้าหรือเทียบกับหกสิบ

**`outcomesOf` เป็นสำเนาตรงตัวที่สี่ของ query ของ #38** ต่อจาก `sectionResults.js` และ
`outcomeActivityMap.js` กฎของ `lib/fields.js` คือแยกที่ครั้งที่สาม และ #39 ไม่แยกไปแล้วครั้งหนึ่ง
ด้วยเหตุผลของ `enrolment.js` — อีกสองสำเนาอยู่ในไฟล์ที่อยู่ในชุดมัตแตนต์ของตั๋วอื่น
[#104](https://github.com/khthana/Deep-QA/issues/104) ตอนนี้มีผู้เรียกสี่รายแล้ว

**PDF ตั้งตัวอักษรไว้ไม่ต่ำกว่า 14pt ทุกจุด** ซึ่งเป็นบทเรียนจาก
[#103](https://github.com/khthana/Deep-QA/issues/103) ที่พบว่า PDF ของ #20 ตั้งไว้ 10pt
ฟอนต์ไทยมี x-height เตี้ยกว่าละตินที่ point size เดียวกันและมีสระบนล่างที่ต้องมีที่ว่าง
PDF ของ #20 ยังเป็น 10pt และมีตั๋วของมันเอง — ใบนี้เขียนโดยรู้เรื่องนั้น ไม่ใช่ทำซ้ำแล้วได้ตั๋วใบที่สอง

**ตารางรูบริกใน PDF เรียงตามข้อ ไม่ได้พิมพ์ครั้งเดียว** ถึงแม้ seed จะให้ทุกข้อมีสี่ประโยคเหมือนกัน
`subject_clo_achievement_criteria` เป็นของราย CLO และ #29 เขียนทีละข้อ ตารางเดียวที่พาดหัวว่า
*รูบริกของรายวิชา* จึงเป็นคำกล่าวอ้างถึงข้อที่มันไม่เคยอ่าน ทันทีที่สองข้อต่างกัน

## มัตแตนต์ที่พิสูจน์แถว ⚙

`mutation/40-clo-assessment.py` — 13 ตัว ทั้งหมดอยู่ในเบราว์เซอร์ เพราะ route ถูกคุมด้วย
`backend/test/clo-assessment.test.js` อยู่แล้ว การทำเลขให้ผิดจะฆ่าชุด backend ไม่ใช่ `40a`

รอบกวาดวันที่ 4 กันยายน 2569 ทุกตัวฆ่าแถวที่มันบอกไว้ และ **รอบกวาดจับคำอ้างของตัวเองผิดสองที่
ก่อนที่ใบนี้จะถูกเขียน**:

- `unmeasuredoutcomenotlisted` เขียนไว้ว่าฆ่าแถว 1 กับ 3 แต่ฆ่าแถว 3 ตัวเดียว เหตุเพราะทุกข้อใน seed
  ถูกวัดหมดแล้ว ตัวกรองจึงไม่ได้ตัดอะไรออก และแถว 1 ที่นับแถวเทียบกับคำตอบก็ยังเห็นเลขสองตัวตรงกัน
- `verdictisonlyacolour` เขียนไว้ว่าฆ่าแถว 2 แต่ฆ่าแถว 3 ด้วย ซึ่งไม่ใช่อุบัติเหตุ: แถว 3 คือเรื่องของ
  คำที่สาม *ยังไม่ประเมิน* ซึ่งไม่มีสีไหนเคยสื่อได้ ทั้งสองแถวอ่านคำเพราะคำคือคำกล่าวอ้าง

คอมเมนต์ในไฟล์มัตแตนต์ถูกแก้ให้ตรงกับสิ่งที่รอบกวาดตอบ ไม่ใช่สิ่งที่เขียนไว้ตอนแรก

**และการตรวจว่าแถว ⚙ ทุกแถวมีมัตแตนต์ค้ำจริง จับได้อีกสองที่** ซึ่งเป็นความผิดพลาดชนิดเดียวกับ
#42 #36 #37 และ #39 — ตั๋วที่ห้าติดกัน คราวนี้จับได้ตอนนับแถวในใบเทียบกับรายชื่อมัตแตนต์:

- แถวเรื่อง **ชื่อไฟล์ PDF** ติด ⚙ ไว้ทั้งที่ไม่มีมัตแตนต์ตัวไหนแตะชื่อไฟล์ **และ** assertion เดิม
  เขียนว่า `assessment-.+\.pdf` ซึ่งอ่อนกว่าที่แถวอ้างมาก — ชื่อที่ไม่มีรหัสวิชา เลขตอนเรียน
  หรือปีเลยก็ผ่าน แก้เป็นเทียบชื่อทั้งชื่อกับค่าจากคำตอบ แล้วเพิ่ม `pdffilenamesaysnothing`
- แถวเรื่อง **การปฏิเสธตอนเรียนของคนอื่น** ติด ⚙ ไว้โดยไม่มีมัตแตนต์เลย เพิ่ม `refusalkeepsloading`
  ซึ่งเป็นข้อบกพร่องที่การเดินของ #43 เจอ และ #36 #37 #39 ต่างมีตัวของตัวเอง
