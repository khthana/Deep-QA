# ตรวจรับด้วยมือ — #17 ข้อมูลนักศึกษากลาง

`docs/06-implementation-plan.md` §Acceptance กำหนดว่า ticket หนึ่งจะเสร็จเมื่อ automated test ผ่าน
**และ** checklist ที่ทำด้วยมือถูกเดินจนครบ เอกสารนี้คือครึ่งหลังของ #17

ครึ่งแรกอยู่ที่ `backend/test/students.test.js` (14 subtests) — ทุกเกณฑ์ที่พิสูจน์ได้ที่ seam เดียว
ของเราคือ HTTP surface ของ backend ถูกพิสูจน์ที่นั่นแล้ว รวมทั้งข้อที่ต้องเดินสองทางจึงจะมีความหมาย

- **ข้อ 1 (ตัวกรอง)** — นักศึกษาทั้ง 173 คนใน seed อยู่หลักสูตร `0501` ทั้งหมด `?program_id=0501`
  จึงคืนครบทุกคนไม่ว่า route จะอ่านพารามิเตอร์หรือไม่ test จึงสร้างนักศึกษาใน `0503` ก่อน
  แล้วยืนยันว่าหน้าของ `0501` **ไม่มี** คนนั้น ส่วนตัวกรองภาควิชายืนยันว่า `?department_id=01`
  คืน `total: 0` ทั้งที่ผู้เรียกเพิ่งเพิ่มนักศึกษาของตัวเองไป — หน้าว่างจึงแปลว่าตัวกรองปฏิเสธ
  ไม่ใช่ตารางว่าง
- **ข้อ 7 (ปีที่เข้าศึกษา)** — พิสูจน์ด้วยการ *ส่ง* `admission_year: '2599'` ทั้งทาง POST และทาง
  คอลัมน์พิเศษในไฟล์ CSV แล้วยืนยันว่าค่าที่เก็บคือค่าที่คำนวณเอง ถ้าไม่ส่ง เกณฑ์นี้จะผ่านแม้ route
  จะอ่านจาก body
- **ข้อ 8 (ขอบเขต)** — เดินสองทาง คือ `dept.admin.01@` ไม่เห็นและเขียนไม่ได้ **และ**
  รหัสนักศึกษาของภาควิชาอื่นถูกปฏิเสธเมื่อ `dept.admin.05@` พยายาม import ทับ (ถ้าเช็คแต่หลักสูตร
  ที่ส่งมา การ import จะกลายเป็นช่องทางย้ายนักศึกษาของภาควิชาอื่นเข้ามาเงียบ ๆ)

เกณฑ์สองข้อพิสูจน์ที่อื่น และควรรู้ว่าที่ไหน

- **ข้อ 3 (ปุ่มดาวน์โหลดแบบฟอร์ม)** ครึ่งหนึ่งเป็นการคลิกปุ่มและได้ไฟล์ลงเครื่อง ซึ่ง docs/06
  ระบุว่าไม่ทำ unit test ฝั่ง frontend ตัวไฟล์ที่ server ส่งถูกยืนยันใน test แล้ว (หัวคอลัมน์สี่ช่อง
  และไฟล์นั้น import กลับเข้าไปได้จริง) เหลือแต่การคลิก ซึ่งอยู่ในตารางข้างล่าง
- **ข้อ 9 (นักศึกษาที่เพิ่มที่นี่พร้อมให้ลงทะเบียนใน Section)** ยังไม่มี endpoint ให้เดิน —
  การลงทะเบียนคือ #25 ซึ่งถูก block ด้วยตั๋วนี้ — test จึงพิสูจน์ที่จุดที่ข้ออ้างนี้อยู่จริง คือ
  foreign key `student_course.student_id` โดยสร้างนักศึกษาผ่าน API อ่านกลับผ่าน API แล้วจึง
  ลงทะเบียน และยืนยันว่ารหัสที่ register ไม่รู้จักลงทะเบียนไม่ได้เลย (23503)

## ตั้งต้น

```
docker start deep-core-postgres
cd db      && npm run reset && npm run migrate && npm run seed
cd backend && npm start          # http://localhost:3000
cd frontend && npm start         # http://localhost:5000
```

ทุกบัญชีใน seed ใช้รหัสผ่านเดียวกัน คือค่า `PASSWORD` ใน `db/seed.js`
เมนูที่ใช้ตลอดเอกสารนี้คือ *หลักสูตร → ข้อมูลนักศึกษากลาง* (`/main/student-data`)

**ต้อง restart backend ก่อนเดิน** — process ที่ค้างจากตั๋วก่อนจะตอบ 404 ให้ทุก route ของ #17

seed มีนักศึกษาอยู่แล้ว **173 คน** (รุ่น `66…` 113 คน และรุ่น `65…` 60 คน) อยู่ภาควิชา `05`
หลักสูตร `0501` ทั้งหมด — จำนวนนี้เกินสิบอยู่แล้ว การแบ่งหน้าจึงเห็นได้ทันทีโดยไม่ต้องเตรียมอะไร
และเป็นเหตุผลที่ตัวกรอง *หลักสูตร* ต้องเดินด้วยนักศึกษาที่เพิ่มเข้า `0503` เอง

**รายการเรียงจากคนที่ถูกเพิ่มล่าสุดก่อน** (`created_at` มากไปน้อย และใช้รหัสตัดสินเมื่อเวลาเท่ากัน)
ข้อมูลใน seed ถูกเขียนในธุรกรรมเดียว `created_at` จึงเท่ากันทั้ง 173 แถว แถวบนสุดของหน้าแรกตอนตั้งต้น
จึงเป็นรหัสมากที่สุดคือรุ่น `66…` แต่คนที่ *เพิ่งเพิ่ม* จะอยู่บนสุดเสมอ **ไม่ว่ารหัสจะเป็นรุ่นใด** —
ข้อ 5 จึงใช้รหัสที่เก่ากว่าทุกคนใน seed โดยตั้งใจ ถ้าเรียงด้วยรหัสอย่างเดียว แถวนั้นจะไปอยู่หน้าสุดท้าย

**อย่า upload แบบฟอร์มโดยไม่แก้** — แถวตัวอย่างในแบบฟอร์มใช้รหัส `66010001` ซึ่งเป็นรหัสของนักศึกษาใน seed จริง การ import ทับรหัสที่มีอยู่ ดังนั้นการ upload แบบฟอร์มที่ยังไม่ได้แก้ จะเปลี่ยนชื่อนักศึกษาจริงเป็น สมชาย ใจดี โดยไม่มีอะไรเตือน — ดู #67 แก้นามสกุลในไฟล์ก่อนส่งทุกครั้ง

หลายข้อข้างล่างเขียนข้อมูลจริงลงฐานข้อมูล — **`npm run seed` ใหม่หลังเดินจบ** ก่อนเริ่มตั๋วถัดไป

## รายการที่ต้องเดิน

ช่องสุดท้ายมีสี่ค่า

- **☑** เดินจบแล้ว
- **◐** ครึ่งของ server เดินแล้วและผ่าน ส่วนที่เหลือคือสิ่งที่หน้าจอวาด (แถบสี ตาราง dropdown) ยังต้องมีคนดู
- **☐** ยังไม่ได้เดิน
- **⚙** ครอบคลุมด้วย browser seam แล้ว (#65) ไม่ต้องเดินด้วยมือซ้ำ แถวบอกไฟล์ที่ครอบคลุมไว้ ถ้าแถวนั้นพูดถึงสิ่งที่วาดด้วย (เมนู ถ้อยคำ สี) ครึ่งนั้นยังเป็นงานของคนเดิน และแถวจะเขียนกำกับไว้

**สิบแถวที่คนต้องเดิน เดินครบแล้วเมื่อ 22 ส.ค. 2569** — ข้อ 1–9, ข้อ 18 และครึ่งที่เป็นเมนูของข้อ 19–22 ทั้งหมดพูดถึงสิ่งที่หน้าจอ *วาด* (กล่องเทาที่พิมพ์ไม่ได้ ปุ่มที่ถูกปิด ถ้อยคำ สี ตัวเลือกใน dropdown) ซึ่ง #65 กันไว้ให้คนดูด้วยตาโดยตั้งใจ ส่วนข้อที่เป็นจำนวน การหายไป และการปฏิเสธ ย้ายไป browser seam แล้ว

หมายเหตุข้อ 5: ครึ่งที่ว่า *แถวใหม่อยู่บนสุดแม้รหัสจะเก่ากว่าทุกคนใน seed* ถูกยึดไว้แล้วใน `e2e/tests/17c-students-register.spec.js` (ส่วนตั้งต้นของ row 10) สิ่งที่ยังต้องดูด้วยตาคือแถบเขียวและการกลับมาหน้ารายการ

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| 1 | รายการแสดงนักศึกษาและแบ่งหน้าเกินสิบแถว | เข้าด้วย `dept.admin.05@` → *ข้อมูลนักศึกษากลาง* | ตารางมี 10 แถว บรรทัดล่างเขียน *ทั้งหมด 173 รายการ · หน้า 1 จาก 18* แถวบนสุดเป็นรหัสรุ่น `66…` | ☑ |
| 2 | เดินหน้า–ถอยหลังได้ | กด *ถัดไป* สองครั้ง แล้ว *ก่อนหน้า* หนึ่งครั้ง | เลขหน้าเปลี่ยนตาม รายชื่อไม่ซ้ำกับหน้าก่อน ปุ่ม *ก่อนหน้า* ถูกปิดเมื่ออยู่หน้า 1 | ☑ |
| 3 | ภาควิชาเป็นข้อความ ไม่ใช่ dropdown | ดูแถบหัวตาราง | เห็นคำว่า *ภาควิชา* ตามด้วยกล่องเทา `05 วิศวกรรมคอมพิวเตอร์` ไม่มีตัวเลือกให้กด — บัญชีนี้ดูแลภาควิชาเดียว dropdown ที่ทุกตัวเลือกคืนแถวเดิมคือ control ที่ไม่ทำอะไร | ☑ |
| 4 | ตัวกรองหลักสูตรใช้ได้ | เลือก *หลักสูตร* = `0503` | ตารางว่าง ข้อความ *ยังไม่มีนักศึกษาในหลักสูตรนี้* และบรรทัดล่างเขียน *ทั้งหมด 0 รายการ* (ยังไม่มีใครใน `0503`) | ☑ |
| 5 | เพิ่มนักศึกษาทีละคนได้ | เลือกกลับเป็น *ทุกหลักสูตร* → กด *เพิ่มนักศึกษา* → รหัส `61010001` ชื่อ `สมหญิง` นามสกุล `เรียนดี` หลักสูตร `0501` → บันทึก | แถบเขียว *บันทึกข้อมูลนักศึกษาเรียบร้อยแล้ว* กลับมาหน้ารายการ และแถวใหม่อยู่ **บนสุด** ของหน้าแรก — รหัสรุ่น `61` ต่ำกว่าทุกคนใน seed โดยตั้งใจ ถ้ารายการเรียงด้วยรหัส แถวนี้จะไปอยู่หน้า 18 และข้อนี้จะจับข้อผิดพลาดนั้นได้ | ☑ |
| 6 | ปีที่เข้าศึกษาคำนวณให้ ไม่ให้กรอก | กด *เพิ่มนักศึกษา* อีกครั้ง แล้วพิมพ์รหัส `67010002` ช้า ๆ | ช่อง *ปีที่เข้าศึกษา* เป็นกล่องเทาพิมพ์ไม่ได้ ตอนแรกเขียน *คำนวณจากรหัสนักศึกษา* แล้วเปลี่ยนเป็น `2567` เมื่อพิมพ์ครบแปดหลัก | ☑ |
| 7 | ภาควิชามาจากหลักสูตร ไม่ให้กรอก | ในฟอร์มเดิม สลับ *หลักสูตร* ระหว่าง `0501` กับ `0503` | ช่อง *ภาควิชา* เป็นกล่องเทาพิมพ์ไม่ได้ และแสดง `05 วิศวกรรมคอมพิวเตอร์` ทั้งสองกรณี — ไม่มีช่องให้พิมพ์ภาควิชาที่ขัดกับหลักสูตร | ☑ |
| 8 | รหัสต้องเป็นตัวเลขแปดหลัก | ในฟอร์มเดิม แก้รหัสเป็น `6701` แล้วกด *บันทึก* | เบราว์เซอร์ไม่ยอมส่ง ขึ้นคำเตือนที่ช่องรหัส (server ก็ปฏิเสธเช่นกัน ดู test) | ☑ |
| 9 | นักศึกษาใหม่ปรากฏในรายการ | แก้รหัสกลับเป็น `67010002` เลือกหลักสูตร `0503` ชื่อ `ชาย` `ทดสอบ` → บันทึก → เลือกตัวกรอง *หลักสูตร* = `0503` | ตารางมีหนึ่งแถว คือ `67010002` คอลัมน์ *หลักสูตร* เขียนชื่อหลักสูตรนานาชาติ *ปีที่เข้าศึกษา* `2567` สถานะ *กำลังศึกษา* | ☑ |
| 10 | ตัวกรองหลักสูตรกรองจริง | เลือกตัวกรองเป็น `0501` | `67010002` **หายไป** และ *ทั้งหมด 174 รายการ* (173 + `61010001` จากข้อ 5) — ครอบคลุมโดย `e2e/tests/17c-students-register.spec.js` (row 10) ซึ่งนับจากจำนวนที่ register มีจริงขณะนั้น ไม่ใช่เลข 174 ตายตัว | ⚙ |
| 11 | เพิ่มรหัสซ้ำไม่ได้ | เลือก *ทุกหลักสูตร* → *เพิ่มนักศึกษา* → รหัส `61010001` ชื่ออะไรก็ได้ หลักสูตร `0501` → บันทึก | แถบแดง *รหัสนักศึกษานี้มีอยู่ในระบบแล้ว* และข้อมูลเดิมของ `61010001` ไม่ถูกทับ (กด *ยกเลิก* แล้วหาแถว `61010001` ในหน้าแรก — ยังเป็น `สมหญิง เรียนดี`; แถวบนสุดตอนนี้คือ `67010002` จากข้อ 9 เพราะเพิ่มทีหลัง) — ครึ่งที่ server ปฏิเสธ (409) และข้อมูลเดิมไม่ถูกทับ ครอบคลุมโดย `e2e/tests/17c-students-register.spec.js` (row 11) ครึ่งที่เป็นแถบสีแดงและถ้อยคำ เดินด้วยมือแล้วในการเดินของ #64 (2026-08-22) — แถบแดงขึ้นตรงถ้อยคำ และแถว `61010001` ยังเป็น `สมหญิง เรียนดี` ไม่ถูกทับ | ⚙ |
| 12 | ดาวน์โหลดแบบฟอร์มได้ | เลื่อนลงไปที่กล่อง *นำเข้ารายชื่อนักศึกษาจากไฟล์* → กด *ดาวน์โหลดแบบฟอร์ม* | ได้ไฟล์ `students-template.csv` ลงเครื่อง เปิดแล้วมีสี่คอลัมน์ `student_id,first_name_th,last_name_th,program_id` กับตัวอย่างหนึ่งแถว — **ไม่มี** คอลัมน์ภาควิชาและไม่มีคอลัมน์ปีที่เข้าศึกษา — ครอบคลุมโดย `e2e/tests/17b-students-import.spec.js` (row 12) รวมถึง BOM ที่ #62 ใส่กลับ | ⚙ |
| 13 | ไฟล์ที่ถูกต้องนำเข้าได้ครบทุกแถว | แก้ไฟล์ที่ดาวน์โหลดมาให้เหลือสามแถว รหัส `68020001`–`68020003` หลักสูตร `0501` → อัปโหลด | กล่องขึ้น *นำเข้าสำเร็จ 3 รายการ* รายการกลับไปหน้า 1 และรหัสทั้งสามอยู่บนสุด — ครอบคลุมโดย `e2e/tests/17b-students-import.spec.js` (row 13) | ⚙ |
| 14 | ไฟล์ที่มีแถวผิดรายงานทุกแถวและไม่บันทึกอะไรเลย | ทำไฟล์ห้าแถว: `68030001` (ถูก) · `6803000X` (มีตัวอักษร) · `68030003` เว้นนามสกุลว่าง · `68030004` หลักสูตร `9999` · `68030005` (ถูก) → อัปโหลด | กล่องแดงบอกว่าไม่ได้บันทึกรายการใดเลย และมีสี่บรรทัด ระบุ *บรรทัดที่ 3, 4, 5* พร้อมเหตุผลของแต่ละแถว จากนั้นค้นหา `68030001` และ `68030005` ในรายการ — **ต้องไม่มี** ทั้งคู่ — ครอบคลุมโดย `e2e/tests/17b-students-import.spec.js` (row 14) | ⚙ |
| 15 | รหัสซ้ำกันเองในไฟล์เดียวถูกปฏิเสธ | ทำไฟล์สองแถวที่ใช้รหัส `68040001` เหมือนกัน → อัปโหลด | ข้อความบอก *บรรทัดที่ 3* ว่ารหัสซ้ำกันภายในไฟล์ *(ซ้ำกับบรรทัดที่ 2)* และไม่มีใครถูกเพิ่ม — ครอบคลุมโดย `e2e/tests/17b-students-import.spec.js` (row 15) | ⚙ |
| 16 | รหัสที่มีอยู่แล้วถูกปรับปรุง ไม่เพิ่มซ้ำ | จำจำนวนรายการทั้งหมดไว้ → ทำไฟล์แถวเดียว รหัส `61010001` นามสกุล `แก้ไขแล้ว` หลักสูตร `0501` → อัปโหลด | *นำเข้าสำเร็จ 1 รายการ* จำนวนรายการทั้งหมด **ไม่เปลี่ยน** และแถว `61010001` แสดงชื่อ `สมหญิง แก้ไขแล้ว` — ครอบคลุมโดย `e2e/tests/17b-students-import.spec.js` (row 16) | ⚙ |
| 17 | ปีที่เข้าศึกษาในไฟล์ก็ถูกละทิ้ง | ทำไฟล์แถวเดียว รหัส `65020001` แล้วเติมคอลัมน์ที่ห้าชื่อ `admission_year` ค่า `2599` → อัปโหลด | นำเข้าสำเร็จ และแถว `65020001` แสดง *ปีที่เข้าศึกษา* `2565` ไม่ใช่ `2599` — ครอบคลุมโดย `e2e/tests/17b-students-import.spec.js` (row 17) | ⚙ |
| 18 | ผู้ดูแลภาควิชาอื่นไม่เห็นข้อมูลนี้ | ออกจากระบบ → เข้าด้วย `dept.admin.01@` → *ข้อมูลนักศึกษากลาง* | ตารางว่าง *ยังไม่มีข้อมูลนักศึกษาในระบบ* กล่อง *ภาควิชา* เขียน `01` และ dropdown *หลักสูตร* ไม่มีหลักสูตรของภาควิชา `05` ให้เลือกเลย | ☑ |
| 19 | ผู้ดูแลระดับคณะเข้าหน้านี้ไม่ได้ | ออกจากระบบ → เข้าด้วย `faculty.admin@` | เมนู *หลักสูตร* **ไม่มี** รายการ *ข้อมูลนักศึกษากลาง* และถ้าพิมพ์ `/main/student-data` ตรง ๆ หน้าจะขึ้นแถบแดง *คุณไม่มีสิทธิ์…* ตารางว่าง (#17 ตัดบทบาทนี้ออก เหมือนที่ #61 ตัดออกจากหน้ารายวิชา) — ครึ่งที่ server ปฏิเสธ ครอบคลุมโดย `e2e/tests/17a-students-refusals.spec.js` (row 19) ครึ่งที่เมนูไม่มีรายการ เดินด้วยมือแล้วในการเดินของ #64 (2026-08-22) — กางเมนู *หลักสูตร* แล้วไม่มี *ข้อมูลนักศึกษากลาง* ให้เห็น | ⚙ |
| 20 | กรรมการหลักสูตรเข้าหน้านี้ไม่ได้ | ออกจากระบบ → เข้าด้วย `prog.manager@` | เหมือนข้อ 19 ทุกประการ — เมนูไม่มีรายการนี้ และการพิมพ์ URL ตรง ๆ ถูกปฏิเสธที่ server — ครึ่งที่ server ปฏิเสธ ครอบคลุมโดย `e2e/tests/17a-students-refusals.spec.js` (row 20) ครึ่งที่เมนูไม่มีรายการ เดินด้วยมือแล้วในการเดินของ #64 (2026-08-22) — กางเมนู *หลักสูตร* แล้วไม่มี *ข้อมูลนักศึกษากลาง* ให้เห็น | ⚙ |
| 21 | ผู้ดูแลระบบกลางเข้าหน้านี้ไม่ได้ | ออกจากระบบ → เข้าด้วย `admin@` แล้วพิมพ์ `/main/student-data` | ถูกปฏิเสธเช่นกัน — ADR-0002 กำหนดว่าบทบาทนี้ดูแลบัญชีและสิทธิ์ ไม่ใช่ข้อมูลหลักสูตร — ครึ่งที่ server ปฏิเสธ ครอบคลุมโดย `e2e/tests/17a-students-refusals.spec.js` (row 21) ครึ่งที่เมนูไม่มีรายการ เดินด้วยมือแล้วในการเดินของ #64 (2026-08-22) — กางเมนู *หลักสูตร* แล้วไม่มี *ข้อมูลนักศึกษากลาง* ให้เห็น | ⚙ |
| 22 | อาจารย์ผู้สอนเข้าหน้านี้ไม่ได้ | ออกจากระบบ → เข้าด้วย `teacher.one@` แล้วพิมพ์ `/main/student-data` | ถูกปฏิเสธ — อาจารย์ลงทะเบียนนักศึกษาเข้า Section ของตัวเอง (#25) ไม่ใช่ดูแลทะเบียนกลาง — ครึ่งที่ server ปฏิเสธ ครอบคลุมโดย `e2e/tests/17a-students-refusals.spec.js` (row 22) ครึ่งที่เมนูไม่มีรายการ เดินด้วยมือแล้วในการเดินของ #64 (2026-08-22) — กางเมนู *หลักสูตร* แล้วไม่มี *ข้อมูลนักศึกษากลาง* ให้เห็น | ⚙ |

## การเดินด้วยมือ — 22 ส.ค. 2569

สิบแถวที่เหลือเป็น ☐ (ข้อ 1–9 และข้อ 18) เดินรวดเดียวโดยคนที่นั่งอยู่หน้าจอ ไม่ใช่โดยตัวขับอัตโนมัติ
ฐานข้อมูลถูก `reset` + `migrate` + `seed` ก่อนเริ่ม เพราะ `npm run seed` เฉย ๆ เป็น upsert
แถวที่ค้างจากตั๋วก่อนจะทำให้ตัวเลข *ทั้งหมด 173 รายการ* ในข้อ 1 เพี้ยนโดยไม่มีอะไรบอก

การเดินถูกจัดใหม่เป็นห้าช่วงเพื่อให้สถานะไหลไปทางเดียว และมีลำดับหนึ่งที่ห้ามสลับ —
**ข้อ 4 ต้องมาก่อนข้อ 9** เพราะข้อ 4 ต้องเห็นหลักสูตร `0503` ว่างเปล่า ซึ่งจะไม่ว่างอีกเลย
หลังข้อ 9 เขียน `67010002` ลงไป ถ้าเดินสลับ ข้อ 4 จะผ่านโดยไม่ได้พิสูจน์อะไรและต้อง seed ใหม่ทั้งรอบ

สามแถวที่ให้ผลมากที่สุด

- **ข้อ 5 (เพิ่มนักศึกษาทีละคน)** รหัส `61010001` ที่ใช้ต่ำกว่าทุกคนใน seed โดยตั้งใจ และแถวใหม่
  ขึ้นบนสุดของหน้าแรกจริง — รายการเรียงด้วย `created_at DESC` ไม่ใช่เรียงด้วยรหัสแล้วบังเอิญดูถูก
  เพราะข้อมูล seed มีรหัสเรียงตามเวลาอยู่แล้ว ถ้าเรียงด้วยรหัส แถวนี้จะไปอยู่หน้า 18
- **ข้อ 6 กับข้อ 7 อ่านคู่กัน** — กล่องเทาสองกล่องที่พิมพ์ไม่ได้ ทั้งคู่เป็นค่าที่ระบบอนุมานจากสิ่งที่
  กรอกไปแล้ว (ปีจากรหัส ภาควิชาจากหลักสูตร) ข้อ 7 ให้สลับหลักสูตรไปกลับเพื่อดูว่ากล่องนั้นไม่ได้แค่
  *เทา* แต่ *ตามค่าจริง* — ช่องเทาที่ค้างค่าเดิมไว้ผิดพอ ๆ กับช่องที่พิมพ์ได้ ถ้าสองช่องนี้กรอกได้
  ระบบจะมีข้อมูลสองแหล่งที่ขัดกันเองได้ทันที คือรหัส `67…` อยู่แถวเดียวกับปี `2599`
- **ข้อ 18 (ผู้ดูแลภาควิชาอื่น)** เป็นแถวเดียวที่พิสูจน์ว่าขอบเขตถูกบังคับที่ข้อมูล ไม่ใช่ที่การซ่อนปุ่ม
  `dept.admin.01@` เข้าหน้านี้ได้ตามสิทธิ์ของบทบาท สิ่งที่กันเขาออกคือ `department_id` ที่ server
  อ่านจาก grant แล้วเอาไปกรอง ที่สำคัญกว่าตารางว่างคือ dropdown *หลักสูตร* ที่กางออกมาแล้ว
  ไม่มี `0501` และไม่มี `0503` — ตารางว่างอย่างเดียวยังแปลได้ว่า "ภาควิชา 01 ยังไม่มีใครกรอก"
  ซึ่งเป็นความจริงพอดีและจะทำให้แถวนี้ผ่านโดยไม่ได้พิสูจน์อะไร

ข้อ 8 พิสูจน์ครึ่งเดียวโดยตั้งใจ เบราว์เซอร์ไม่ยอมส่ง `6701` เป็นความสะดวก ไม่ใช่กฎ — กฎอยู่ที่ server
และ `backend/test/students.test.js` ยึดไว้แล้ว แถวนี้พิสูจน์ว่าหน้าจอไม่ปล่อยให้คนเดินไปเจอ 400
โดยไม่จำเป็น ไม่ใช่ว่ารหัสสี่หลักเข้าฐานไม่ได้

การเดินรอบนี้เก็บครึ่งที่เป็นหน้าตาของแถว ⚙ ห้าแถวไปด้วย เพราะสถานะฐานข้อมูลระหว่างทางพอดีกับมัน
อยู่แล้ว — ข้อ 11 (แถบแดงและถ้อยคำ) และข้อ 19–22 (เมนู *หลักสูตร* ไม่มีรายการนี้ให้เห็นตั้งแต่แรก)
ทั้งห้าแถวยังคงเป็น ⚙ และเขียนบันทึกต่อท้ายไว้ในช่องหลักฐานของแถวเอง กติกาสี่ค่าไม่มีเครื่องหมาย
สำหรับแถวที่ครึ่งหนึ่งเป็นเครื่องและอีกครึ่งเป็นคน การเปลี่ยนเป็น ☑ จะทำให้คนอ่านทีหลังคิดว่าไม่มี
test ยึดมันไว้ ส่วนการปล่อย ⚙ เฉย ๆ จะทำให้ครึ่งที่เพิ่งเดินหายไปเงียบ ๆ

**การเดินรอบนี้ไม่พบข้อบกพร่องใหม่** ต่างจากการเดิน #14 ที่เปิด
[#91](https://github.com/khthana/Deep-QA/issues/91) และ
[#92](https://github.com/khthana/Deep-QA/issues/92) ออกมา ทั้งสิบแถวถูกรายงานกลับมาเป็นช่วง
(A, B, C, D, E) ว่าตรงกับสิ่งที่ช่อง *ต้องเห็นอะไร* เขียนไว้ ไม่มีแถวใดถูกรายงานว่าต่างไปจากนั้น
ข้อความในช่องหลักฐานจึงเป็นสิ่งที่คนเดินยืนยัน ไม่ใช่สิ่งที่อนุมานจากโค้ด

ข้อ 9 ของเอกสารนี้ (นักศึกษาที่เพิ่มที่นี่พร้อมให้ลงทะเบียนใน Section) ยังพิสูจน์ที่ foreign key
`student_course.student_id` เหมือนเดิม แถวที่เดินคือครึ่งที่ว่านักศึกษาใหม่ปรากฏในรายการจริง
ครึ่งที่เป็นการลงทะเบียนจะเดินได้เมื่อ #25 มีหน้าจอ และไม่ควรอ่านว่าเดินแล้ว

## หลังเดินจบ

`npm run seed` ใหม่ — ข้อ 5 ถึงข้อ 17 เขียนนักศึกษาจริงลงฐานข้อมูล และตั๋วถัดไปคาดว่า
register มีเฉพาะสองรุ่นที่ seed สร้าง

> **หมายเหตุเรื่องชื่อบัญชี** — ตั้งแต่รอบที่แก้ seed ให้ตรงกับ `docs/acceptance/18-program-subjects.md`
> อีเมลของกรรมการหลักสูตรสองคนเปลี่ยนเป็น `prog.manager@kmitl.ac.th` และ `prog.manager.0503@kmitl.ac.th`
> แถวในเอกสารนี้ที่ยังเขียนว่า `committee.0501@` / `committee.0503@` คือ **บันทึกของการเดินที่เกิดขึ้นจริง**
> ใต้ชื่อเดิม จึงไม่ถูกเขียนทับ อ่านเป็นบัญชีเดียวกัน (alias `U_COM` และ `U_COM2` ไม่เปลี่ยน)

## มัตแตนต์จากใบอื่นที่ค้ำแถวของใบนี้ — #123 (7 ก.ย. 2569)

`30:importlineoff` ใน `mutation/30-weighting-scheme.py` เลื่อนเลขบรรทัดในรายงานของ
`backend/lib/importer.js` ไปหนึ่ง ซึ่งเป็นโมดูลนำเข้าที่หกหน้าจอใช้ร่วมกัน **มันฆ่าแถว 14 ของ `17b-students-import.spec.js` ด้วย**
วัดเมื่อ 7 ก.ย. 2569 ตอนที่ [#123](https://github.com/khthana/Deep-QA/issues/123) เล็งจุดยึดของมันใหม่
แล้วกวาดข้ามใบเป็นครั้งแรก

เขียนไว้ที่นี่เพราะ `CLAUDE.md` บอกว่าต้องอ่านสองทาง — จากแถวไปหามัตแตนต์ และจากมัตแตนต์ไปหาแถว
**ใบตรวจรับของ #30 อ้างว่ามันฆ่าแถวนี้ ใบนี้จึงต้องรู้จักชื่อมัน** ไม่งั้นคนที่ไล่จากฝั่งนี้จะไม่มีทางเจอ
มัตแตนต์ที่ค้ำข้ออ้างเรื่องเลขบรรทัดของตัวเองเลย
