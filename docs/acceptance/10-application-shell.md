# ตรวจรับด้วยมือ — #10 Application shell, role switching and sidebar

`docs/06-implementation-plan.md` §Acceptance กำหนดว่า ticket หนึ่งจะเสร็จเมื่อ automated test ผ่าน
**และ** checklist ที่ทำด้วยมือถูกเดินจนครบ เอกสารนี้คือครึ่งหลังของ #10

เหตุที่ต้องมีครึ่งนี้: docs/06 §Testing Decisions ระบุว่า seam เดียวที่ทดสอบอัตโนมัติคือ HTTP surface
ของ backend และ **"Frontend components are not unit-tested"** เกณฑ์ที่มีส่วนที่มองเห็นได้ —
ข้อ 1, 2, 5 และครึ่งหนึ่งของข้อ 8 — จึงพิสูจน์ด้วยเทสต์ไม่ได้โดยการออกแบบ ไม่ใช่โดยการละเลย

## ตั้งต้น

```
docker start deep-core-postgres
cd db      && npm run seed
cd backend && npm start          # http://localhost:3000
cd frontend && npm start         # http://localhost:5000
```

ทุกบัญชีใน seed ใช้รหัสผ่านเดียวกัน คือค่า `PASSWORD` ใน `db/seed.js`

## รายการที่ต้องเดิน

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| 1 | Shell แสดง navigation, sidebar, breadcrumb, content | เข้าด้วย `admin@kmitl.ac.th` → เลือก Deep-QA | เห็นครบสี่ส่วนในหน้าเดียว, breadcrumb ตรงกับเมนูที่ยืนอยู่ | ☐ |
| 2 | เมนูตรงกับบทบาทที่สวมอยู่ | เข้าทีละบัญชี: `faculty.admin@`, `dept.admin.05@`, `committee.0501@`, `teacher.one@` | ชุดเมนูตรงกับ `docs/05-screen-api-mapping.md` ของบทบาทนั้น ไม่มีเมนูของบทบาทอื่นปน | ☐ |
| 2 | (ข้อยกเว้นที่ยังไม่ปิด) | เข้าด้วย `external.assessor@` | เห็นเมนูเดียว — ดูหมายเหตุ "สิ่งที่ยังไม่ปิด" ข้างล่าง **ข้อนี้ยังไม่ผ่าน** | ☐ |
| 3 | สลับบทบาทได้ และตั้งต้นที่บทบาทสูงสุด | เข้าด้วย `multi.role@kmitl.ac.th` | ตัวเลือกบทบาทตั้งต้นที่ *กรรมการหลักสูตร 0501* (priority 4) ไม่ใช่ *อาจารย์* (priority 5), และกดเปลี่ยนเป็นอาจารย์ได้ | ☐ |
| 4 | สลับบทบาทเปลี่ยนสิ่งที่ server ยอม ไม่ใช่แค่เมนู | ยังเป็น `multi.role@` — สลับเป็นอาจารย์ แล้วเปิด DevTools → Network กดเมนูใด ๆ | request หลังสลับต้องสะท้อนบทบาทใหม่ ไม่ใช่แค่เมนูที่วาดใหม่ (ครึ่ง server พิสูจน์แล้วที่ `backend/test/shell.test.js`) | ☐ |
| 5 | Central Admin เห็นเฉพาะเมนูจัดการผู้ใช้ | เข้าด้วย `admin@kmitl.ac.th` | sidebar มีรายการเดียว คือ *ผู้ใช้งานระบบ* | ☐ |
| 6 | Session หมดอายุแล้วมีคำอธิบาย | เข้าระบบ → DevTools → Application → Cookies → ลบ cookie `token` → กดเมนูใด ๆ | กล่อง *Session หมดอายุ* ขึ้นพร้อมข้อความและปุ่มเข้าสู่ระบบใหม่ **ไม่ใช่** การเด้งออกเงียบ ๆ | ☐ |
| 6 | ลบ cookie แล้ว **โหลดหน้าใหม่** (ไม่ใช่กดเมนู) | เข้าระบบ → ลบ cookie `token` → กด F5 | ยังขึ้นกล่อง *Session หมดอายุ* — request แรกของ shell แยก "ยังไม่เคยเข้า" กับ "หมดอายุ" ด้วย `reason` ที่ server ส่งมา | ☐ |
| 6 | 401 กับ 403 ไม่ใช่เรื่องเดียวกัน | เข้าด้วย `teacher.one@` แล้วเปิด `http://localhost:3000/api/users` | ได้ 403 พร้อมข้อความ *ไม่มีสิทธิ์* — ไม่ใช่กล่อง session หมดอายุ | ☐ |
| 7 | เปลี่ยนรหัสผ่านตัวเองได้ และใช้ได้ครั้งถัดไป | เข้าด้วย `teacher.two@` → เมนูผู้ใช้ → เปลี่ยนรหัสผ่าน → ออกจากระบบ → เข้าด้วยรหัสใหม่ | เข้าได้ด้วยรหัสใหม่, รหัสเดิมถูกปฏิเสธ (**อย่าลืม `npm run seed` ใหม่หลังทดสอบข้อนี้**) | ☐ |
| 7 | รหัสเดิมผิด | ในกล่องเดียวกัน ใส่รหัสเดิมผิด | ขึ้น *รหัสผ่านเดิมไม่ถูกต้อง* ในกล่อง ไม่ใช่การเด้งออก | ☐ |
| 8 | ซ่อนเมนูไม่ใช่สิ่งเดียวที่กันไว้ | ครึ่ง server อยู่ที่ `shell.test.js` แล้ว ครึ่งนี้: เข้าด้วย `teacher.one@` แล้วพิมพ์ `http://localhost:5000/main/users` ตรง ๆ | เข้าถึงข้อมูลผู้ใช้ไม่ได้ แม้จะข้ามเมนูไปแล้ว | ☐ |

## สิ่งที่ยังไม่ปิดใน #10

1. **เมนูของ EXT_ASSESSOR ยังไม่มีข้อกำหนดรองรับ** — `docs/01` ROLE-6 และ R005 พูดถึงเฉพาะว่า
   บัญชีนี้มีอายุจำกัด ไม่มีเอกสารฉบับใดระบุว่าผู้ประเมินภายนอกเห็นหน้าจอใด
   `frontend/src/components/SidebarItem/ExtAssessor.js` จึงใส่ไว้หนึ่งรายการเป็นที่วางชั่วคราว
   **เกณฑ์ข้อ 2 จึงผ่านเพียงบางส่วน** ชุดเมนูจริงเป็นการตัดสินใจของเจ้าของข้อกำหนด
2. **`/main/users` กับสิทธิ์ที่ไม่ตรงกัน** — sidebar ของ FACULTY_ADMIN และ DEPT_ADMIN มีรายการนี้
   ตาม `docs/05` A11 ("ทุก admin role") แต่ `backend/routes/users.js` กันไว้ที่ `FULL_ADMIN` อย่างเดียว
   วันนี้ยังไม่เห็นผลเพราะเส้นทางนั้นแสดง `NotBuiltYet` ต้องตัดสินให้ตรงกันที่ #11
3. **การต่ออายุ token ทับการสลับบทบาท** — ถ้า request ที่ค้างอยู่จบหลังการสลับ และตอนนั้นเหลือ
   อายุน้อยกว่า 10 นาที cookie ที่ต่ออายุจะพา `acting` ตัวเก่ากลับมา เป็น race ที่แคบ
   แต่แก้ไม่ได้ด้วยเทสต์ที่ seam เดียว — ต้องมีตั๋วแยก
4. **หน้าจอเข้าสู่ระบบของ #8 ถูกสร้างขึ้นใน #10** — `Login.js`, `LoginForm.js`, `LoginGoogle.js`,
   `UserNotFound.js` และ `SelectApp.js` เป็นครึ่ง UI ของ #8 แต่เกิดในตั๋วนี้ #8 ปิดไปแล้ว
   เกณฑ์ฝั่งหน้าจอของมันจึงไม่มีตั๋วใดถือไว้ ควรเปิดตั๋วเก็บตก
5. **ไม่มีการปรับ acting ให้ตรงกันข้ามแท็บ** — สลับบทบาทในแท็บหนึ่ง แท็บที่เปิดค้างอีกแท็บยังวาด
   sidebar เดิมจนกว่าจะโหลดใหม่ server ไม่ได้ผิด (มันบังคับตาม cookie ล่าสุดเสมอ) แต่คนที่หน้าจอ
   เห็นเมนูที่ไม่ตรงกับสิ่งที่กดได้
6. **URL ของระบบ portfolio ฝังไว้ในโค้ด** — `https://portfolio.deep-core.net/teacher` อยู่ใน
   `Navbar.js:146` และ `SelectApp.js:411` ควรมาจาก environment เหมือน `REACT_APP_API_URL`
7. **เส้นทางต่ออายุ token ยังไม่มีเทสต์** — `RENEW_BELOW_SECONDS` ไม่มีเทสต์ใดเดินผ่าน และเกณฑ์ข้อ 4
   ส่วนใหญ่พิสูจน์กับ `guardedApp` ที่เป็น route จำลอง มากกว่ากับ `/api/users` ที่ส่งมอบจริง
8. **โค้ดที่ไปไม่ถึง** — `Mainpage.js:321` มีสาขา `alert.messages` ที่ไม่มีใครตั้งค่าให้
   และ `/user-not-found` อ่าน `?reason` ได้แต่ไม่มีที่ใดพาไปที่นั่น
