# ตรวจรับด้วยมือ — #18 Program Subjects

`docs/06-implementation-plan.md` §Acceptance กำหนดว่า ticket หนึ่งจะเสร็จเมื่อ automated test ผ่าน
**และ** checklist ที่ทำด้วยมือถูกเดินจนครบ เอกสารนี้คือครึ่งหลังของ #18

ครึ่งแรกอยู่ที่ `backend/test/program-subjects.test.js` (12 subtests) — ทุกเกณฑ์ที่พิสูจน์ได้ที่ seam
เดียวของเราคือ HTTP surface ของ backend ถูกพิสูจน์ที่นั่นแล้ว รวมทั้งเกณฑ์ข้อ 8 ที่เดินสองทาง คือ
`prog.manager.0503@` ถูกปฏิเสธบนหลักสูตร `0501` **และ** ผู้ดูแลภาควิชากับคณะเข้าถึง `0501` ได้จริง
(reach ที่ปฏิเสธทุกคนก็ผ่านครึ่งแรกได้ จึงต้องเดินทั้งสองทาง)

ตั้งแต่ #65 มี seam ที่สองคือเบราว์เซอร์ — `e2e/tests/18a-program-subjects.spec.js`,
`18b-program-subjects-import.spec.js` และ `18c-program-subjects-refusals.spec.js` รวม 18 ข้อทดสอบ
รับแถวข้างล่างไปเกือบทั้งใบ ดูหัวข้อ *แถวที่ browser seam รับไป* ท้ายเอกสาร

เกณฑ์สองข้อพิสูจน์ที่อื่น และควรรู้ว่าที่ไหน

- **ข้อ 6 (ถามยืนยันก่อนลบ)** เป็น dialog ล้วน ๆ ไม่มีอะไรให้ server ยืนยัน — request ที่มาถึงคือ
  request ที่ตั้งใจส่ง จึงอยู่ในตารางข้างล่างอย่างเดียว
- **ข้อ 7 (import ผ่านโมดูลกลาง)** เป็นข้อความเกี่ยวกับ *ผู้เรียกห้าราย* และสี่รายคือหน้าผู้ใช้งาน
  หน้าภาควิชา หน้าหลักสูตร และหน้ารายวิชา พิสูจน์โดย suite ของตัวเอง ซึ่ง **ไม่ถูกแก้เลย**ในตั๋วนี้ —
  นั่นคือความหมายของคำว่าใช้ซ้ำ

## ตั้งต้น

```
docker start deep-core-postgres
cd db      && npm run reset && npm run migrate && npm run seed
cd backend && npm start          # http://localhost:3000
cd frontend && npm start         # http://localhost:5000
```

ทุกบัญชีใน seed ใช้รหัสผ่านเดียวกัน คือค่า `PASSWORD` ใน `db/seed.js`
เมนูที่ใช้ตลอดเอกสารนี้คือ *ข้อมูลหลัก → รายวิชาในหลักสูตร* (`/main/course-in-program`)

**ต้อง restart backend ก่อนเดิน** — process ที่ค้างจากตั๋วก่อนจะตอบ 404 ให้ทุก route ของ #18

seed มีหลักสูตรสองหลักสูตรอยู่ในภาควิชา `05` เดียวกัน คือ `0501` (ของ `prog.manager@`) และ `0503`
(ของ `prog.manager.0503@`) — สองหลักสูตรนี้คือคู่ที่ใช้เดินข้อ 8 และเป็นคู่ที่ reach แบบหยาบ ๆ
จะปล่อยผ่าน เพราะอยู่ภาควิชาเดียวกันและคณะเดียวกัน

seed มีรายวิชาเดียวคือ `01076105` และ **คู่ (`0501`, `01076105`) มีข้อมูลอ้างอิงอยู่แล้ว**
(การเปิดสอน → CLO → คะแนน) จึงเป็นแถวที่ใช้เดินข้อ 5 ครึ่งที่ *ปิดการใช้งานแทนการลบ*
ส่วนครึ่งที่ *ลบได้จริง* ต้องเพิ่มคู่ใหม่เองในข้อ 1

**คลังรายวิชามีวิชาเดียวตอนเริ่ม** ข้อ 1 ถึงข้อ 4 จึงต้องไปเพิ่มรายวิชาที่หน้า *ข้อมูลรายวิชา* ก่อน
อย่างน้อยสามวิชา ใช้บัญชี `dept.admin.05@` เพิ่ม (ตั้งแต่ #61 เป็นบัญชีเดียวที่เข้าหน้า *ข้อมูลรายวิชา*
ได้ และรหัส `0107xxxx` เป็นของภาควิชา `05` พอดี) แนะนำ `01076106`, `01076107`
และวิชาของภาควิชา `01` อีกหนึ่งวิชา (เช่น `01016101`) — วิชาสุดท้ายคือตัวที่ใช้เดินข้อ 10

หัวข้อตั้งต้นนี้เป็นของ**คนเดิน** ชุด e2e ไม่ใช้มัน — แต่ละไฟล์ spec สร้างคลังรายวิชาของตัวเองใน
`beforeAll` ด้วยช่วงรหัสของตัวเอง เพราะ `16a-subjects.spec.js` รันก่อนบน schema เดียวกันและทิ้ง
รายวิชาของมันไว้ แถวที่ไปยืมของไฟล์อื่นคือแถวที่ความหมายขึ้นกับไฟล์ที่มันไม่ได้เอ่ยถึง

หลายข้อข้างล่างเขียนข้อมูลจริงลงฐานข้อมูล — **`npm run seed` ใหม่หลังเดินจบ** ก่อนเริ่มตั๋วถัดไป

## รายการที่ต้องเดิน

ช่องสุดท้ายมีสี่ค่า

- **☑** เดินจบแล้ว
- **◐** ครึ่งของ server เดินแล้วและผ่าน ส่วนที่เหลือคือสิ่งที่หน้าจอวาด (แถบสี ตาราง dropdown) ยังต้องมีคนดู
- **☐** ยังไม่ได้เดิน
- **⚙** ครอบคลุมด้วย browser seam แล้ว (#65) ไม่ต้องเดินด้วยมือซ้ำ แถวบอกไฟล์ที่ครอบคลุมไว้ ถ้าแถวนั้นพูดถึงสิ่งที่วาดด้วย (เมนู ถ้อยคำ สี) ครึ่งนั้นยังเป็นงานของคนเดิน และแถวจะเขียนกำกับไว้

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| 1 | กรรมการหลักสูตรเพิ่มรายวิชาเข้าหลักสูตรตัวเองได้ | เข้าด้วย `prog.manager@` → *รายวิชาในหลักสูตร* → *เพิ่มรายวิชาเข้าหลักสูตร* → พิมพ์ `01076106` ในช่องค้นหา → เลือกวิชาที่ขึ้นมา → ประเภท *วิชาบังคับ* → บันทึก | แถบเขียว *บันทึกข้อมูลเรียบร้อยแล้ว* และแถวใหม่โผล่ในตาราง ประเภท *วิชาบังคับ* สถานะ *ใช้งานอยู่* — ครอบคลุมโดย `e2e/tests/18a-program-subjects.spec.js` ซึ่งค้นคลังรายวิชาผ่านฟอร์มจริง เลือกจากผลค้นหา บันทึก แล้ว assert แถบเขียว จำนวนรวมที่เพิ่มขึ้นหนึ่ง และแถวที่ตารางวาดหลังโหลดใหม่ ทั้งช่องประเภทและช่องสถานะ | ⚙ |
| 1 | ช่องหลักสูตรไม่ต้องเลือกเมื่อมีหลักสูตรเดียว | ดูฟอร์มเดิมด้วยบัญชี `prog.manager@` | ช่อง *หลักสูตร* มีตัวเลือกเดียวคือ `0501` และถูกเลือกไว้ให้แล้ว — สิ่งที่เลือกได้คือสิ่งที่ server จะรับ ฟอร์มจึงสร้างคำขอที่ถูกปฏิเสธไม่ได้ — ครอบคลุมโดย `18a-program-subjects.spec.js` ซึ่ง assert ว่าหน้ารายการบอกชื่อหลักสูตรโดย**ไม่มี** dropdown ให้เลือกเลย และตัวเลือกในฟอร์มคือ `['', '0501']` พอดี พร้อมถูกเลือกไว้แล้ว | ⚙ |
| 2 | เปลี่ยนประเภทได้ภายหลัง | กด *แก้ไข* ที่แถว `01076106` → เปลี่ยนเป็น *วิชาเลือก* → บันทึก | ป้ายในคอลัมน์ *ประเภท* เปลี่ยนเป็น *วิชาเลือก* และเปลี่ยนสี — ครอบคลุมโดย `18a-program-subjects.spec.js` เฉพาะครึ่งที่เป็น**ถ้อยคำ** คือป้ายอ่านว่า *วิชาเลือก* จริงหลังโหลดใหม่ ส่วน**สี**เป็นสิ่งที่หน้าจอวาดจาก class ที่ผูกกับคำนั้น ยังเป็นงานของคนเดิน | ⚙ |
| 2 | รหัสวิชาและหลักสูตรแก้ไม่ได้ | ดูฟอร์ม *แก้ไขรายวิชาในหลักสูตร* | ทั้งช่อง *หลักสูตร* และช่อง *รายวิชา* เป็นสีเทาแก้ไม่ได้ พร้อมข้อความว่าย้ายข้ามหลักสูตรไม่ได้ — คู่นี้คือ primary key และ CLO กับคะแนนทั้งหมดห้อยอยู่ใต้มัน — ครอบคลุมโดย `18a-program-subjects.spec.js` ซึ่ง assert attribute `disabled` ของทั้งสองช่อง (สีเทาคือสิ่งที่เบราว์เซอร์วาด**จาก** attribute นั้น seam จึง assert ที่ต้นเหตุ) และ assert ข้อความว่าย้ายข้ามหลักสูตรไม่ได้ | ⚙ |
| 3 | รหัสวิชาที่ไม่มีในคลังถูกปฏิเสธ พร้อมข้อความที่บอกเหตุ | พิมพ์รหัสมั่ว เช่น `09999999` ในช่องค้นหาของฟอร์ม *เพิ่ม* | รายการว่างเปล่า พร้อมข้อความ *ไม่พบรายวิชาที่ตรงกับคำค้น หากยังไม่มีในคลังรายวิชา ต้องเพิ่มที่หน้าข้อมูลรายวิชาก่อน* — ฟอร์มกันไว้ก่อนถึง server และ server ก็ปฏิเสธซ้ำอีกชั้น (ทดสอบไว้แล้ว) — ครอบคลุมโดย `18a-program-subjects.spec.js` ซึ่ง assert ว่าตัวเลือกที่เหลือคือช่องว่างช่องเดียว และข้อความนั้นขึ้นจริง | ⚙ |
| 4 | วิชาเดิมใส่หลักสูตรเดิมซ้ำไม่ได้ | *เพิ่มรายวิชาเข้าหลักสูตร* → เลือก `01076106` ที่เพิ่งเพิ่มไปแล้ว → บันทึก | แถบแดง *รายวิชานี้อยู่ในหลักสูตรนี้แล้ว* และไม่มีแถวใหม่ — ครอบคลุมโดย `18a-program-subjects.spec.js` ซึ่ง assert ทั้งถ้อยคำของแถบและจำนวนรวมที่ไม่ขยับ | ⚙ |
| 5 | คู่ที่ยังไม่มีใครอ้างอิง ลบออกได้จริง | กด *นำออก* ที่แถว `01076106` → ยืนยัน | แถบเขียว *นำรายวิชาออกจากหลักสูตรเรียบร้อยแล้ว* และแถวหายไปจากตาราง — ครอบคลุมโดย `18a-program-subjects.spec.js` | ⚙ |
| 5 | คู่ที่มีข้อมูลอ้างอิง ถูกปิดการใช้งานแทนการลบ | กด *นำออก* ที่แถว `01076105` (คู่ที่ seed สร้างไว้และมีการเปิดสอน) → ยืนยัน | แถบเขียวที่บอกว่า *ระบบจึงปิดการใช้งานแทนการลบ* และแถวยัง**อยู่**ในตาราง สถานะเปลี่ยนเป็น *ปิดใช้งาน* — ครอบคลุมโดย `18a-program-subjects.spec.js` ซึ่งนับจำนวนรวมด้วย ไม่ใช่ดูแต่ถ้อยคำ | ⚙ |
| 5 | เปิดกลับได้จากหน้าเดิม | กด *แก้ไข* ที่แถว `01076105` → ช่อง *สถานะ* → *ใช้งานอยู่* → บันทึก | สถานะกลับเป็น *ใช้งานอยู่* — นี่คือทางกลับทางเดียว เพราะเพิ่มซ้ำจะชนกับแถวที่มีอยู่ — ครอบคลุมโดย `18a-program-subjects.spec.js` (ต่อจากแถวบน ในข้อทดสอบเดียวกัน) | ⚙ |
| 6 | ถามยืนยันก่อนนำออก | กด *นำออก* ที่แถวใดก็ได้ แล้วกด *ยกเลิก* | กล่องยืนยันขึ้นมาก่อน ข้อความมีรหัสวิชา ชื่อวิชา และรหัสหลักสูตรอยู่ในประโยค และบอกด้วยว่าถ้ามีข้อมูลอ้างอิงจะปิดการใช้งานแทน กด *ยกเลิก* แล้วไม่มีอะไรเปลี่ยน | ⚙ |
| 7 | ดาวน์โหลดแบบฟอร์มได้ และ Excel อ่านภาษาไทยออก | เลื่อนลงถึงกล่อง *นำเข้ารายวิชาในหลักสูตรจากไฟล์* → *ดาวน์โหลดแบบฟอร์ม* → เปิดด้วย Excel | ได้ไฟล์ `program-subjects-template.csv` หัวตารางสามคอลัมน์ `program_id`, `subject_id`, `subject_type` พร้อมแถวตัวอย่างหนึ่งแถว และ**ภาษาไทยไม่เพี้ยน** (BOM — ดู #62) — ครอบคลุมโดย `e2e/tests/18b-program-subjects-import.spec.js` ซึ่งอ่าน**ไบต์ที่ลงดิสก์จริง**ผ่าน download แล้ว assert ชื่อไฟล์ BOM หัวคอลัมน์ทั้งสาม และแถวตัวอย่างหนึ่งแถว — BOM คือต้นเหตุของข้อความ *ภาษาไทยไม่เพี้ยน* seam จึง assert ที่ต้นเหตุ ส่วนการเปิดด้วย Excel จริงยังเป็นสิ่งที่คนเท่านั้นทำได้ | ⚙ |
| 7 | นำเข้าไฟล์ที่ถูกต้อง | กรอกสองแถวลงแบบฟอร์ม ใช้ `0501` กับรหัสวิชาที่มีในคลังและยังไม่อยู่ในหลักสูตร → บันทึกเป็น CSV → อัปโหลด | แถบเขียวบอกจำนวนที่นำเข้า และทั้งสองแถวโผล่ในตาราง — ครอบคลุมโดย `18b-program-subjects-import.spec.js` ซึ่งสร้างไฟล์จากหัวตารางที่ปุ่มแบบฟอร์มดาวน์โหลดมาจริง | ⚙ |
| 7 | ไฟล์ที่มีแถวผิด ไม่บันทึกอะไรเลย | แก้ไฟล์เดิมให้แถวที่สองใช้รหัสวิชาที่ไม่มีในคลัง แล้วอัปโหลดใหม่ | รายงานรายแถวขึ้นมา ระบุ **เลขบรรทัด** และเหตุผล *ไม่พบรหัสวิชานี้ในคลังรายวิชา* และ**แถวแรกก็ไม่ถูกบันทึก** — ทั้งไฟล์หรือไม่เอาเลย — ครอบคลุมโดย `18b-program-subjects-import.spec.js` ซึ่ง assert เลขบรรทัด เหตุผล จำนวนรวมที่ไม่ขยับ และแถวแรกที่ไม่โผล่ | ⚙ |
| 8 | กรรมการหลักสูตรถูกปฏิเสธบนหลักสูตรอื่น (บังคับที่ server) | เข้าด้วย `prog.manager.0503@` → หน้านี้ | เห็นแต่ `0503` ป้ายหลักสูตรบอกว่า `0503` และตารางไม่มีแถวของ `0501` เลย — แถวที่ `prog.manager@` เพิ่งเพิ่มก็ไม่เห็น — ครอบคลุมโดย `e2e/tests/18c-program-subjects-refusals.spec.js` ซึ่งวางคู่หนึ่งไว้ใน `0503` ก่อน แล้ว assert ว่าเห็นคู่ของตัวเอง (ตารางจึงโหลดขึ้นจริง) และไม่เห็นคู่ของ `0501` เลย | ⚙ |
| 8 | เดินอีกด้าน — ผู้ดูแลเหนือหลักสูตรเข้าถึงได้ | เข้าด้วย `dept.admin.05@` แล้ว `faculty.admin@` | ทั้งสองเห็น dropdown *หลักสูตร* ที่มี `0501` และ `0503` (คนละความหมายกับป้ายของกรรมการ) และแก้ไขแถวของทั้งสองหลักสูตรได้ — ครอบคลุมโดย `18c-program-subjects-refusals.spec.js` ซึ่งเปิดฟอร์มแก้ไขบนแถวของทั้งสองหลักสูตร (การเปิดฟอร์มคือการอ่านคู่กลับจาก server คู่ที่อยู่นอก reach ตอบ 404) และบันทึกจริงหนึ่งครั้งแล้วแก้กลับ | ⚙ |
| 8 | บัญชีที่ไม่ควรเข้าถึงเลย | เข้าด้วย `admin@` (ผู้ดูแลระบบกลาง) แล้ว `teacher.one@` | ไม่มีเมนู *รายวิชาในหลักสูตร* ในแถบข้าง และถ้าพิมพ์ `/main/course-in-program` ตรง ๆ ก็ได้แถบแดง *บัญชีนี้ไม่มีสิทธิ์ใช้งานส่วนนี้* — หลักสูตรไม่ใช่งานของผู้ดูแลระบบกลาง (ADR-0002) — ครึ่งหลัง (403 กับแถบแดง) assert แล้วที่ `18c-program-subjects-refusals.spec.js` ครึ่งที่เหลือคือ**รายการในเมนูซ้าย** ซึ่งเป็นสิ่งที่วาด ยังต้องมีคนดู | ☐ |
| 9 | รายการแบ่งหน้าเมื่อเกินสิบแถว | ด้วย `prog.manager@` เพิ่มรายวิชาเข้าหลักสูตร `0501` จนเกินสิบแถว (ใช้ import ไฟล์เดียวเร็วที่สุด) | ตารางแสดงสิบแถว มีตัวเลือกหน้าอยู่ด้านล่าง กด *ถัดไป* แล้วเห็นแถวที่เหลือ ไม่มีแถวซ้ำระหว่างสองหน้า และเรียงตามรหัสวิชา — ครอบคลุมโดย `18b-program-subjects-import.spec.js` ซึ่ง assert ทั้งสี่ข้อ คือสิบแถวพอดี บรรทัดของ Pager กดหน้าถัดไปแล้วได้ส่วนที่เหลือ ไม่มีรหัสซ้ำ และสองหน้าต่อกันเป็นลำดับเดียว | ⚙ |
| 10 | เลือกรายวิชาข้ามภาควิชาได้ | ด้วย `prog.manager@` → *เพิ่มรายวิชาเข้าหลักสูตร* → ค้นหาวิชาของภาควิชา `01` ที่เตรียมไว้ → เลือก → บันทึก | บันทึกสำเร็จ — หลักสูตรวิศวกรรมคอมพิวเตอร์มีวิชาคณิตศาสตร์และวิชาศึกษาทั่วไปของภาควิชาอื่นได้ ถ้าข้อนี้ถูกปฏิเสธแปลว่า catalogue ถูกจำกัดผิด (ดูหมายเหตุท้ายเอกสาร) — ครอบคลุมโดย `18a-program-subjects.spec.js` ซึ่งให้ `dept.admin.01@` สร้างวิชาของภาควิชา `01` เองใน `beforeAll` แล้ว assert ว่ากรรมการหลักสูตรของ `0501` ค้นเจอและบันทึกได้ | ⚙ |
| 10 | วิชาที่ปิดการใช้งานแล้ว เลือกไม่ได้ | ด้วย `dept.admin.05@` ไปที่ *ข้อมูลรายวิชา* ปิดการใช้งานวิชา `01076107` → กลับมาหน้านี้ด้วย `prog.manager@` → ค้นหา `01076107` | ไม่ขึ้นในรายการให้เลือก — วิชาที่มหาวิทยาลัยเลิกสอนแล้วไม่ควรถูกผูกเข้าหลักสูตรใหม่ — ครอบคลุมโดย `18a-program-subjects.spec.js` ซึ่งค้นเจอวิชานั้น**ก่อน** ปิดการใช้งานในเบราว์เซอร์ของผู้ดูแลภาควิชา แล้วค้นใหม่และไม่เจอ — การหายไปจึงเป็นการปิดใช้งาน ไม่ใช่คำค้นที่ไม่เคยตรง | ⚙ |

## แถวที่ browser seam รับไป และแถวที่ยังเป็นของคนเดิน (#65, 2026-08-20)

สิบแปดในสิบเก้าแถวถูกรับไปด้วยไฟล์ spec สามไฟล์ รวม 18 ข้อทดสอบ ผ่านทั้งหมด
**ตอนนี้จึงเป็น 18 ⚙ และ 1 ☐**

- `e2e/tests/18a-program-subjects.spec.js` — เกณฑ์ข้อ 1 ถึง 6 และข้อ 10 (11 ข้อทดสอบ)
- `e2e/tests/18b-program-subjects-import.spec.js` — เกณฑ์ข้อ 7 และข้อ 9 (4 ข้อทดสอบ)
- `e2e/tests/18c-program-subjects-refusals.spec.js` — เกณฑ์ข้อ 8 (3 ข้อทดสอบ)

แถวเดียวที่ยังเป็น ☐ เป็นโดยตั้งใจ ไม่ใช่เพราะเขียนไม่ถึง — **บัญชีที่ไม่ควรเข้าถึงเลย**
(เกณฑ์ข้อ 8) ครึ่งที่เป็นกฎ คือ 403 กับแถบแดงเมื่อพิมพ์ที่อยู่ตรง ๆ ถูก assert แล้วทั้งสองบัญชี
ที่เหลือคือรายการในเมนูซ้าย ซึ่งเป็นสิ่งที่วาด #65 ให้ครึ่งนี้เป็นงานของคนเดิน — เหมือนแถว 75
ของ `16-subjects.md`

สองแถวที่ติด ⚙ แต่ยังมีครึ่งที่เป็นของคนเดิน และเขียนกำกับไว้ในแถวแล้ว

- **เปลี่ยนประเภทได้ภายหลัง** (ข้อ 2) — seam assert ถ้อยคำของป้าย ส่วน**สี**ของป้ายเป็นสิ่งที่วาด
- **ดาวน์โหลดแบบฟอร์ม** (ข้อ 7) — seam assert BOM บนไบต์ที่ลงดิสก์จริง ซึ่งเป็น*ต้นเหตุ*ของ
  "Excel อ่านภาษาไทยออก" ส่วนการเปิดด้วย Excel จริงยังเป็นสิ่งที่คนเท่านั้นทำได้

ที่อยู่ในแถวของข้อ 8 ถูกแก้จาก `teacher@` เป็น `teacher.one@` — ช่องนั้นเป็นคำสั่งที่ยังมีคนต้องเดินตาม
และ `teacher@` ไม่ใช่บัญชีใน seed

### mutation test

ทุกข้ออ้างใหม่ในไฟล์ spec ถูกพิสูจน์ด้วย mutation test ก่อนติด ⚙ ยี่สิบสามตัวกลายพันธุ์
ยี่สิบสามตัวถูกฆ่าตรงข้ออ้างที่ตั้งใจ และทุกข้อทดสอบที่อยู่**ก่อนหน้า**ในไฟล์เดียวกันยังผ่าน
(ไฟล์เหล่านี้เป็น serial การตายที่ข้อแรกจะบังข้อที่เหลือ จำนวนข้อที่ผ่านก่อนตายจึงถูกนับทุกครั้ง)

| กลายพันธุ์ | แก้อะไร | ฆ่าข้ออ้างไหน |
|---|---|---|
| `noautoprogram` | ฟอร์มไม่เลือกหลักสูตรเดียวที่มีให้ล่วงหน้า | 18a ข้อ 1 บรรทัด 122 |
| `alwaysfilter` | หน้ารายการวาด dropdown เสมอ แม้มีหลักสูตรเดียว | 18a ข้อ 1 บรรทัด 113 |
| `nolabelcode` | ป้ายหลักสูตรไม่บอกรหัส บอกแต่ชื่อ | 18a ข้อ 1 บรรทัด 114 และ 18c ข้อ 8 บรรทัด 83 |
| `alwayselective` | insert เขียนประเภทเป็น `elective` เสมอ | 18a ข้อ 1 บรรทัด 146 |
| `typeignored` | PUT เขียนประเภทเดิมกลับไป | 18a ข้อ 2 บรรทัด 161 |
| `nofreeze` | ช่องหลักสูตรไม่ถูก disable ตอนแก้ไข | 18a ข้อ 2 บรรทัด 172 |
| `nohint` | ตัดข้อความว่าย้ายข้ามหลักสูตรไม่ได้ออก | 18a ข้อ 2 บรรทัด 180 |
| `ignorequery` | catalogue ไม่สนใจคำค้น คืนทุกแถว | 18a ข้อ 3 บรรทัด 197 |
| `duplicatesilent` | POST ไม่แปลง unique violation เป็นข้อความ | 18a ข้อ 4 บรรทัด 217 |
| `terseconfirm` | กล่องยืนยันไม่บอกรหัสวิชาและชื่อวิชา | 18a ข้อ 6 บรรทัด 237 |
| `nodelete` | `deleteOrDeactivate` ไม่ลบจริง แต่ตอบว่าลบแล้ว | 18a ข้อ 5 บรรทัด 256 |
| `nodeactivate` | ไม่ปิดการใช้งาน แต่ตอบว่าปิดแล้ว | 18a ข้อ 5 บรรทัด 280 |
| `noreopen` | PUT ไม่รับ `is_active` กลับมา | 18a ข้อ 5 บรรทัด 287 |
| `catalogueowndept` | จำกัด catalogue ไว้ที่ภาควิชา `05` | 18a ข้อ 10 บรรทัด 303 |
| `catalogueretired` | catalogue คืนวิชาที่ปิดการใช้งานแล้วด้วย | 18a ข้อ 10 บรรทัด 341 |
| `nobom` | `saveAsFile` ไม่เติม BOM กลับ | 18b ข้อ 7 บรรทัด 95 |
| `importdrops` | import รายงานสำเร็จโดยไม่เขียนแถว | 18b ข้อ 7 บรรทัด 120 |
| `wrongcataloguereason` | เหตุผลของแถวผิดเปลี่ยนเป็นข้อความกลาง ๆ | 18b ข้อ 7 บรรทัด 149 |
| `keepgood` | `lib/importer` COMMIT แทน ROLLBACK เมื่อมีแถวผิด | 18b ข้อ 7 บรรทัด 158 |
| `nolimit` | รายการไม่จำกัดสิบแถวต่อหน้า | 18b ข้อ 9 บรรทัด 190 |
| `unordered` | เรียงตามประเภทแทนรหัสวิชา | 18b ข้อ 9 บรรทัด 202 |
| `listeveryone` | รายการไม่ถูกจำกัดด้วย reach | 18c ข้อ 8 บรรทัด 90 |
| `maintainerall` | `MAINTAINERS` รวม `FULL_ADMIN` กับ `TEACHER` | 18c ข้อ 8 บรรทัด 151 |

สามตัวแรกฆ่าข้อทดสอบเดียวกัน (18a ข้อ 1) แต่คนละ assertion — `alwaysfilter` ฆ่าบรรทัดที่ว่า
ไม่มี dropdown ให้เลือก, `nolabelcode` ฆ่าบรรทัดที่ว่าป้ายบอกรหัสหลักสูตร และ `noautoprogram`
ฆ่าบรรทัดที่ว่าหลักสูตรถูกเลือกไว้ให้แล้ว — สาม assertion ต้องมีตัวฆ่าของตัวเอง เพราะไฟล์เหล่านี้เป็น serial
การตายที่ assertion แรกจะบัง assertion ที่อยู่ถัดไป เช่นเดียวกับ `nofreeze`/`nohint`
และ `nodeactivate`/`noreopen`

`keepgood` เจอข้อบกพร่องของ spec จริง — ฉบับแรกของข้อ 7 อ่านจำนวนรวมจากหน้าจอที่ค้างอยู่
(import ที่ถูกปฏิเสธไม่โหลดตารางใหม่) ตัวเลขจึงเป็นจริงไม่ว่า rollback จะทำงานหรือไม่ แก้โดยอ่านรายการใหม่
จาก server ก่อนนับ แล้ว `keepgood` จึงฆ่ามันได้

ข้ออ้างที่ว่า import **ทั้งไฟล์หรือไม่เอาเลย** เป็นกลไกของ `lib/importer` ที่ทุกหน้าจอใช้ร่วมกัน
เอกสารตรวจรับของ #11 กับ #14 ไม่มี mutation ที่แตะขอบเขต transaction เลย ตั๋วนี้จึงเพิ่ม `keepgood`
ซึ่งเป็นตัวเดียวที่พิสูจน์ข้อนี้ ส่วนกฎของ*แถว* — คู่ที่ซ้ำในไฟล์ หลักสูตรที่อยู่นอก reach
และรหัสวิชาที่ไม่มีในคลัง — เป็นสิ่งที่ `wrongcataloguereason` กับ `importdrops` ฆ่า

สคริปต์ที่ใช้กลายพันธุ์อยู่ใน scratchpad ของ session ไม่ได้อยู่ใน repo — เป็นเรื่องเดียวกับที่บันทึกไว้ใน
`13-user-activity-history.md`

## หมายเหตุ — ขอบเขตของ catalogue

ตั๋วเขียนเกณฑ์ข้อ 3 ว่า *"รหัสวิชาที่ไม่มีในคลังถูกปฏิเสธ"* เท่านั้น ไม่ได้บอกว่าคลังที่เลือกได้ควร
แคบแค่ไหน การตัดสินใจของตั๋วนี้คือ **เลือกได้ทุกภาควิชา** ด้วยเหตุผลสามข้อ

1. หลักสูตรวิศวกรรมคอมพิวเตอร์จริง ๆ มีวิชาคณิตศาสตร์ ฟิสิกส์ และวิชาศึกษาทั่วไปของภาควิชาอื่น
   ถ้าจำกัดไว้ที่ภาควิชาของหลักสูตร หน้าจอนี้จะแสดงหลักสูตรจริงไม่ได้เลย
2. ของเดิมที่นักศึกษาส่งมอบไม่ได้จำกัดอะไรเลย (`getAllProgramSubjects` คืนทุกแถว)
3. การ*อ่าน* คลังรายวิชาไม่ใช่การ*เขียน* — ทุกการเขียนยังถูกตรวจกับ reach ของหลักสูตรตามเดิม

ถ้าเจ้าของโดเมนเห็นว่าควรแคบกว่านี้ จุดที่แก้คือ `catalogueRefusal` และ route
`/api/program-subjects/catalogue` ใน `backend/routes/programSubjects.js` ที่เดียว
การตัดสินใจข้อนี้ตอนนี้มี seam ยึดไว้แล้ว — `catalogueowndept` คือ mutation ที่ทำให้มันแคบลง
และมันฆ่าข้ออ้างของข้อ 10 ทันที
