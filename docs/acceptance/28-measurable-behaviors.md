# ตรวจรับด้วยมือ — #28 Measurable Behaviors

`docs/06-implementation-plan.md` §Acceptance กำหนดว่า ticket หนึ่งจะเสร็จเมื่อ automated test ผ่าน
**และ** checklist ที่ทำด้วยมือถูกเดินจนครบ เอกสารนี้คือครึ่งหลังของ #28

ครึ่งแรกอยู่ที่ `backend/test/behaviors.test.js` (16 subtests) — ทุกเกณฑ์ที่พิสูจน์ได้ที่ seam ของ
HTTP surface ถูกพิสูจน์ที่นั่นแล้ว รวมทั้งสามข้อที่**ไม่มีหน้าจอไหนแสดงได้เลย** คือ ค่า enum
นอกรายการถูกปฏิเสธ (dropdown ส่งค่านอกไม่ได้) การจับคู่พฤติกรรมกับ CLO ใน address
(ทดสอบด้วย CLO สองตัวของบัญชีเดียวกัน — บทเรียนของ #22) และ `clo_id` ใน body ที่ถูกเมิน

seam ที่สองคือเบราว์เซอร์ — `e2e/tests/28a-measurable-behaviors.spec.js` (8 แถว) ดูหัวข้อ
*มัตแตนต์ที่พิสูจน์แถว ⚙* ท้ายเอกสาร ซึ่งเป็นเหตุผลว่าทำไมแถวเหล่านั้นถึงถือว่าครอบแล้วจริง

## สิ่งที่ต้องเข้าใจก่อนเดิน — พฤติกรรมบ่งชี้เป็นของ CLO และของอย่างอื่นไม่มีเลย

`subject_clo_measurable_behavior` มีแค่ `clo_id` — ไม่มีตอนเรียน ไม่มีปี ไม่มีหลักสูตร ไม่มีเจ้าของ
ทั้ง ADR-0003 จึงมาถึงหน้านี้โดยการสืบทอด: เข้าผ่านตอนเรียนไหนของการเปิดสอนก็เห็นชุดเดียวกัน
และ CLO รหัสเดียวกันของคนละปีถือคนละชุด `behaviors.js` ไม่ได้ถามคำถามสิทธิ์เอง —
มัน import `offeringOf` กับ `cloOf` จาก `clos.js` แบบเดียวกับที่ #22 import `reachableRubric`
(การที่ join ตัวนี้เคยถูกเขียนซ้ำสามที่คือ [#104](https://github.com/khthana/Deep-QA/issues/104)
— #28 ตั้งใจไม่เป็นสำเนาที่สี่)

**ทุกการอ่านแถวเป็น `WHERE id = $1 AND clo_id = $2` เสมอ** ไม่ใช่ `WHERE id = $1` เดี่ยว ๆ —
ขาดครึ่งหลังเมื่อไหร่ address ของ CLO-1 จะแก้พฤติกรรมของ CLO-2 ได้ทั้งที่ route ดูถูกต้องทุกบรรทัด

**เลขลำดับเป็นของเซิร์ฟเวอร์ ไม่ใช่ของฟอร์ม** เพิ่มแล้วได้เลขถัดไป ลบแล้วที่เหลือถูกจัดใหม่เป็น 1..N
ไม่มีช่องว่าง — พฤติกรรมที่สืบทอดจากโค้ดเดิม ซึ่ง migration 0002 ยึดตอนตั้ง `UNIQUE (clo_id,
behavior_no)` ไว้แล้ว เลขที่ส่งมาใน body ถูกเมินแบบเดียวกับปีที่ส่งมาหา #27

**ไม่มีใครถูกบันทึกว่าเป็นคนแก้** ตารางไม่มี `updated_by` (ADR-0001 ชั้น 2 — แบบเดียวกับ
`student_course` ของ #25) หน้าจอจึงไม่มีบรรทัด *แก้ไขล่าสุดโดย* และนั่นคือการตัดสินของ schema
ไม่ใช่ของหน้านี้

**การลบไม่มีด่านกั้น และนั่นคือคำตอบของ schema เช่นกัน** ไม่มีตารางไหนอ้างถึงพฤติกรรมบ่งชี้ —
มันเป็นคำอธิบาย ไม่ใช่สิ่งที่คะแนนไปเกาะ กล่องยืนยันของหน้าจอจึงเป็นด่านเดียวระหว่างมือลั่นกับของหาย
และประโยคในกล่องต้องบอกว่ากำลังจะลบข้อไหน

**สองรายการปิดถูกตรวจในโค้ดก่อนถึงฐานข้อมูล** enum จะปฏิเสธค่านอกรายการอยู่แล้ว แต่เป็น 22P02
ที่ไปถึง error handler แล้วกลายเป็น *เกิดข้อผิดพลาดในระบบ* — ประโยคผิดสำหรับค่าที่คนเลือกจากรายการ
route ตอบ `invalidBehavior` แทน ทั้ง POST และ PUT ค่าหกระดับคือ R064 และสี่ประเภทคือ R063
(Quiz เป็นคำของ §8 ที่ตั้งใจไม่อยู่ในรายการ — ดูโน้ตใน migration 0002)

## ตั้งต้น

```
docker start deep-core-postgres
cd db      && npm run reset && npm run migrate && npm run seed
cd backend && npm start          # http://localhost:3000
cd frontend && npm start         # http://localhost:5000
```

**ต้อง reset + seed ใหม่จริง ๆ ก่อนเดินใบนี้** — `db/term.js` ทำให้ seed เดินตามนาฬิกา

**ต้อง kill แล้ว start backend ใหม่ก่อนเดิน** — `node server.js` ไม่มี reload ตรวจด้วยการ login
แล้วยิง `GET /api/teaching/sections/1/clos/<clo_id>/behaviors` หนึ่งครั้ง ต้องได้ 200 พร้อมรายการ
(ยิงแบบไม่ login ตรวจอะไรไม่ได้ — ทุก path ใต้ `/api` ตอบ 401 เหมือนกันหมด)

ทุกบัญชีใน seed ใช้รหัสผ่านเดียวกัน คือ `deep-core-local`

ทางเข้า: เลือกตอนเรียนจากหน้า *รายวิชา* → เมนู **ผลการเรียนรู้รายวิชา** → กดลิงก์
**พฤติกรรมบ่งชี้** บนการ์ดของ CLO ที่ต้องการ ที่อยู่ที่ได้คือ
`/teacher/teacherDashboard/<section_id>/courseOutcomes/<clo_id>/behaviors`

seed ให้ **CLO ทุกตัวของทั้งสองปีมีพฤติกรรมบ่งชี้คนละ 2 ข้อ** (ข้อ 1 กับ ข้อ 2) ระดับกับประเภท
ไล่สลับกันตามสูตรใน `db/seed.js` — สองข้อของ CLO เดียวกันจึงถือค่าไม่ซ้ำกัน

| `section_id` (ที่อยู่) | ปีการศึกษา | ผู้สอน | ใช้เดินอะไร |
|---|---|---|---|
| `1` | 2569 (ปัจจุบัน) | `teacher.one@` | เกือบทุกข้อ |
| `2` | 2569 (ปัจจุบัน) | `multi.role@` (สลับเป็น *อาจารย์ผู้สอน* ก่อน) | ข้อชุดเดียวกันข้ามตอนเรียน |
| `3` | **2568 (ปีก่อน)** | `teacher.one@` | ข้อปีอื่นมีชุดของตัวเอง |

## รายการที่ต้องเดิน

ช่องสุดท้ายมีสี่ค่า — **☑** เดินจบแล้ว · **◐** ครึ่งของ server เดินแล้วและผ่าน ส่วนที่เหลือคือสิ่งที่
หน้าจอวาด · **☐** ยังไม่ได้เดิน · **⚙** ครอบคลุมด้วย browser seam แล้ว (แถวบอกไฟล์)

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| 1 | ทางเข้าอยู่บนการ์ดของ CLO | เข้าด้วย `teacher.one@` เปิด *ผลการเรียนรู้รายวิชา* ดูการ์ด `CLO-1` | ลิงก์อ่านว่า **พฤติกรรมบ่งชี้** — ถ้อยคำเป็นสิ่งที่คนต้องดู | ☑ |
| 1 | กดแล้วถึงหน้าของ CLO ตัวนั้นจริง | กดลิงก์นั้น | ที่อยู่ลงท้าย `/<เลข clo>/behaviors` และรายการขึ้น — ครอบคลุมโดย `28a` แถว 1 (`linkdropsclo`) | ⚙ |
| 1 | หัวเรื่องบอกว่าเป็นของ CLO ไหน | ดูหัวเรื่อง | *พฤติกรรมบ่งชี้ของ CLO-1* และรายละเอียดของ CLO อยู่ใต้หัว — ถ้อยคำ | ☑ |
| 5 | หน้าจอบอกเรื่อง grain เป็นคำพูด | ดูบรรทัดใต้หัว | *ปีการศึกษา 2569 · ทุกตอนเรียนของรายวิชานี้ในปีการศึกษาเดียวกันใช้ชุดเดียวกันนี้ และปีการศึกษาอื่นมีชุดของตัวเอง* — ประโยคเดียวกับ #27 และต้องไม่มีช่องว่างกลางคำ (บทเรียน 23 ส.ค.) | ☑ |
| 2 | รายการเรียงเป็นข้อ 1..N | ดูรายการ | *ข้อ 1* แล้ว *ข้อ 2* — ครอบคลุมโดย `28a` แถว 1 (`orderbyid`) | ⚙ |
| 2 | ค่าบนแถวเป็นคำไทย ไม่ใช่ค่าดิบ | ดูแถวใดก็ได้ | ระดับเป็น *จำ/เข้าใจ/…* ไม่ใช่ `remember` — ครอบคลุมโดย `28a` แถว 4 (`taglabelraw`) | ⚙ |
| 2 | แถวแสดงส่วนประกอบครบใต้ป้ายของมัน | ดูแถวเดียวกัน | ป้าย *ระดับพุทธิพิสัย* กับ *กิจกรรมการเรียนรู้* นำหน้าค่า — ถ้อยคำ | ☑ |
| 3 | dropdown ระดับพุทธิพิสัยมีหกค่าตามที่กำหนด | เปิดฟอร์มเพิ่ม เปิด dropdown *ระดับพุทธิพิสัย* | จำ · เข้าใจ · ประยุกต์ใช้ · วิเคราะห์ · ประเมินค่า · สร้างสรรค์/ออกแบบ — หกค่า ไม่มีอื่น (TC-CLO-006) | ☑ |
| 3 | ค่านอกหกระดับถูกปฏิเสธ | — | ครอบครึ่งของ server แล้ว (`behaviors.test.js` — 400 `invalidBehavior` และไม่มีอะไรถูกเขียน) หน้าจอส่งค่านอกไม่ได้เพราะเป็น dropdown — ที่เหลือคือยืนยันว่า dropdown ไม่มีช่องพิมพ์เอง | ☑ |
| 4 | dropdown กิจกรรมการเรียนรู้มีสี่ค่าตามที่กำหนด | เปิด dropdown *กิจกรรมการเรียนรู้* | ข้อสอบ · แบบฝึกหัด · การบ้าน · งานที่มอบหมาย — สี่ค่า และ**ไม่มี Quiz** (TC-CLO-005 · R063) | ☑ |
| 4 | ค่านอกสี่ประเภทถูกปฏิเสธ | — | ครอบครึ่งของ server แล้ว (ยิง `quiz` ทั้ง POST และ PUT — 400 ทั้งคู่) | ☑ |
| 1 | เพิ่มแล้วได้เลขถัดไป และแถวอยู่ในรายการ | — | ครอบคลุมโดย `28a` แถว 4 (`nextnumber` · `savenoreload`) | ⚙ |
| 1 | ฟอร์มบังคับครบสามช่อง | เปิดฟอร์มเปล่า | ปุ่ม *บันทึก* จาง กดไม่ได้ จนกรอกรายละเอียดและเลือกทั้งสอง dropdown | ☑ |
| 1 | แก้ได้ และการแก้ไม่ขยับเลข | — | ครอบคลุมโดย `28a` แถว 3 (`noupdate`) ส่วน *เลขไม่ขยับแม้ body จะส่งมา* พิสูจน์ที่ seam ของ HTTP | ⚙ |
| 2 | ลบแล้วเลขที่เหลือถูกจัดใหม่เป็น 1..N โดยแถวยังเป็นแถวเดิม | — | ครอบคลุมโดย `28a` แถว 5 (`norenumber` ค้ำเรื่องเลข · `removeslast` ค้ำเรื่องเนื้อหาตามแถว · `silentdelete` ค้ำว่าการลบเกิดจริง) | ⚙ |
| 5 | สองตอนเรียนของการเปิดสอนเดียวกันอ่านรายการเดียวกัน | — | ครอบคลุมโดย `28a` แถว 2 — **แถวนี้ไม่มีมัตแตนต์ และตั้งใจบันทึกไว้แทนการทำเนียน** ดูหัวข้อมัตแตนต์ | ⚙ |
| 5 | แก้จากตอนเรียนหนึ่ง อีกตอนเรียนเห็น | — | ครอบคลุมโดย `28a` แถว 3 (สอง browser context จริง) | ⚙ |
| 5 | ปีการศึกษาอื่นมีชุดของตัวเอง | แก้ address เป็น `section_id` ของปีก่อน (`3`) เปิดพฤติกรรมของ `CLO-1` ที่นั่น | หัวบอก *ปีการศึกษา 2568* และรายละเอียดพฤติกรรมเป็นคนละข้อความกับของปี 2569 — ครอบครึ่งของ server แล้ว (ids ไม่ทับกันเลย) หน้าจอยังต้องดู | ☑ |
| 6 | การลบถามก่อน และประโยคบอกครบ | กดถังขยะที่ข้อใดก็ได้ | กล่องเอ่ย *ข้อ N* กับรหัส CLO บอกว่าข้อที่เหลือจะถูกจัดลำดับใหม่ และทุกตอนเรียนจะไม่เห็นข้อนี้อีก — ถ้อยคำ | ☑ |
| 6 | กด *ยกเลิก* แล้วไม่มีใครถูกลบ | ในกล่องนั้นกด *ยกเลิก* | ครอบคลุมโดย `28a` แถว 6 (`cancelremoves`) ซึ่ง assert ว่า**ไม่มีคำขอ DELETE ถูกส่ง** | ⚙ |
| 7 | ตอนเรียนของคนอื่นถูกปฏิเสธที่เซิร์ฟเวอร์ | — | ครอบคลุมโดย `28a` แถว 7 (`anysection` ค้ำ 404 · `swallowrefusal` ค้ำว่าประโยคถึงจอ) | ⚙ |
| 7 | CLO ที่การเปิดสอนนี้ไม่มี ถูกปฏิเสธเป็นประโยคของ CLO | — | ครอบคลุมโดย `28a` แถว 8 (`wrongsentence`) ส่วน *CLO ของปีอื่นตอบประโยคเดียวกัน* พิสูจน์ที่ seam ของ HTTP | ⚙ |
| — | สถานะว่างบอกชื่อ CLO | สร้าง CLO ใหม่ (ยังไม่มีพฤติกรรม) แล้วเปิดลิงก์พฤติกรรมของมัน | *ยังไม่มีพฤติกรรมบ่งชี้ของ CLO-…* ไม่ใช่กรอบเปล่า — เสร็จแล้วลบ CLO นั้นทิ้ง | ☑ |

**เก็บกวาดหลังเดิน** พฤติกรรมที่เพิ่มระหว่างเดินให้ลบออก (การลบจัดเลขให้เอง) และ CLO
ที่สร้างสำหรับแถวสถานะว่างให้ลบทิ้ง หรือ reset + seed ใหม่ซึ่งเร็วกว่า **นับจำนวนข้อก่อนและหลัง
ข้อที่มีการลบ** — ใบนี้มีขั้นตอนลบ และการลบผิดแถวมองไม่เห็นถ้าไม่นับ (บทเรียนจาก #20/#101
และมัตแตนต์ `removeslast` ของใบนี้เองคือรูปของมัน)

## บันทึกการเดินด้วยมือ

**เดินเมื่อ 28 ส.ค. 2569** — ผ่านทั้ง 12 แถวมือ (9 ☐ + 3 ◐) ฐานข้อมูลกลับสู่สภาพ seed เมื่อจบ
(พฤติกรรมของ CLO-1 นับ 2 ก่อนและหลังการยกเลิกลบ · CLO-10 ที่สร้างสำหรับแถวสถานะว่างถูกลบ
การ์ดกลับเป็น 9 · ข้อความปี 2568 ที่แก้เพื่อพิสูจน์ถูกคืนตรงตามตัวอักษรของ seed)

**วิธีเดินเหมือน #25** — Claude คุมเบราว์เซอร์ (ส่วนขยาย Claude ใน Chrome) โดยผู้ใช้ดูอยู่
หน้าต่างแคบจากแถวจอแคบของ #25 ทำให้คลิกด้วยพิกัดพลาดบ่อย แถวถ้อยคำ/ตัวเลือก/สถานะปุ่ม
จึงอ่านจาก DOM ที่วาดจริงและสั่งกดผ่าน handler ของหน้าจอเอง — ทุกการเขียนวิ่งผ่านฟอร์ม
ของหน้าจอและ HTTP จริง ไม่มีการเขียนลง schema ตรง

สิ่งที่พบระหว่างเดินซึ่งใบตรวจไม่ได้คาดไว้ หนึ่งเรื่อง:

- **แถวปีอื่น (เกณฑ์ 5) เดินตามตัวอักษรไม่ได้กับ seed นี้** — สูตรของ `db/seed.js` (บรรทัด 935)
  สร้างรายละเอียดพฤติกรรมจาก `clo.detail` เดียวกันทั้งสองปี ข้อความของ CLO-1 ปี 2568 กับ 2569
  จึง**เหมือนกันโดยสร้าง** ไม่ใช่บั๊ก หัวบอก *ปีการศึกษา 2568* ผ่านตามตัวอักษร ส่วน
  *คนละชุดของตัวเอง* พิสูจน์เป็นพฤติกรรมแทน: แก้ข้อ 1 ของปี 2568 ผ่านฟอร์มของหน้าจอ
  (เติมสร้อย) → หน้า 2569 ไม่ขยับ → แก้คืนเป็นข้อความ seed ตรงตัวอักษร ซึ่งแรงกว่าการอ่าน
  ข้อความนิ่ง ๆ สองหน้าเทียบกัน

ถ้อยคำที่เห็นจริงของแถวสำคัญ (ตรงตามใบทุกแถว): ประโยค grain ตรงทุกคำและไม่มีช่องว่างกลางคำ ·
กล่องลบอ่านว่า *ต้องการลบข้อ 1 ของ CLO-1 หรือไม่ ข้อที่เหลือจะถูกจัดลำดับใหม่
และทุกตอนเรียนของรายวิชานี้จะไม่เห็นข้อนี้อีก* · สถานะว่างอ่านว่า *ยังไม่มีพฤติกรรมบ่งชี้ของ CLO-10* ·
ฟอร์มเปล่าปุ่ม *บันทึก* จาง และติดเมื่อครบสามช่องเท่านั้น (ลองครบทั้งสี่ลำดับการกรอก
รวมทั้งลบรายละเอียดออกแล้วปุ่มกลับจาง) · dropdown ทั้งสองเป็น `<select>` แท้ ไม่มีช่องพิมพ์เอง

## มัตแตนต์ที่พิสูจน์แถว ⚙

`mutation/28-measurable-behaviors.py` สิบสามตัว อ่านวิธีใช้ที่ `mutation/README.md` ก่อน
กวาดจริงเมื่อ 28 ส.ค. 2569 ด้วย `npx playwright test 28a` (8 tests) ทีละตัว log อยู่ที่
`_local/mutation-28/` เกณฑ์ตัดสินสามข้อของฉบับ `2026-08-25-c` ผ่านครบทุกตัว —
แถวเป้าหมายอยู่ในรายการที่ตาย · log ไม่มี `error:` จากเซิร์ฟเวอร์ ·
การเก็บกวาดของ 28a เขียนผ่าน endpoint ของหน้าจอก็จริง แต่แถวที่ใช้มัน (แถว 4 กับ 5)
คือแถวที่กำลังทดสอบการลบอยู่เอง และมัตแตนต์ฝั่งลบทุกตัวถูกวัดจากบรรทัดของแถวนั้น
ไม่ใช่จากแถวถัดไป

ตารางบันทึก**สิ่งที่มัตแตนต์ฆ่าได้จริง** คอลัมน์สุดท้ายคือบรรทัดที่ตายจริง

| มัตแตนต์ | แตะอะไร | ทำอะไร | แถวที่มันค้ำ · ตายที่ | แถวอื่นที่ตายไปด้วย |
|---|---|---|---|---|
| `linkdropsclo` | การ์ด #27 | ลิงก์ทิ้ง clo id | แถว 1 · บรรทัด 73 — GET ที่รอไม่มีวันมา | ไม่มี |
| `orderbyid` | route | รายการเรียงตาม id ใหม่ก่อน | แถว 1 · บรรทัด 88 `[1, 2]` ได้ `[2, 1]` | 4 (148), 5 (189), 6 (208) — ลำดับผิดตัวเดียวกันถูกอ่านทุกที่ **แถว 2 รอด** เพราะสองจอวาดลำดับผิดเหมือนกัน ซึ่งบอกว่าแถว 2 ไม่เคยพูดเรื่องลำดับ |
| `noupdate` | route | การแก้ตอบ 200 แต่ไม่เขียน (`COALESCE` — พารามิเตอร์ยังผูกครบ ตามบทเรียน `27:nodetail`) | แถว 3 · บรรทัด 128 | ไม่มี |
| `nextnumber` | route | เลขถัดไปข้ามหนึ่ง | แถว 4 · บรรทัด 162 `behaviorCard(3)` | 5 (189) — การปนเปื้อน: แถว 4 ตายก่อนเก็บกวาดของตัวเอง CLO-9 จึงมีข้อค้าง ไม่ใช่หลักฐานของแถว 5 |
| `savenoreload` | หน้าจอ | บันทึกแล้วไม่โหลดรายการใหม่ | แถว 4 · บรรทัด 162 | 3 (128) — การแก้ที่ไม่ถูกวาดคือความพังเดียวกันคนละกริยา · 5 (189) — การปนเปื้อนแบบเดียวกับ `nextnumber` |
| `taglabelraw` | หน้าจอ | แถวแสดงค่า enum ดิบแทนคำไทย | แถว 4 · บรรทัด 163 `getByText('ประเมินค่า')` | 5 (189) — การปนเปื้อนแบบเดียวกัน |
| `norenumber` | route | ลบแล้วไม่จัดเลขใหม่ | แถว 5 · บรรทัด 192 `[1, 2, 3]` ได้ `[1, 2, 4]` | ไม่มี |
| `removeslast` | route | DELETE เมินแถวที่ถูกขอ ไปเอาแถวท้ายสุด — เลขทุกตัวถูกหมดหลังจากนั้น | แถว 5 · บรรทัด 193 — assertion เรื่อง**เนื้อหา**เป็นตัวเดียวที่เห็น | ไม่มี — แถว 4 ลบแถวท้ายอยู่แล้วจึงรอด ซึ่งคือเหตุผลที่แถว 5 ต้องลบจากกลาง |
| `silentdelete` | route | DELETE ไม่ลบอะไรแต่ตอบ 204 | แถว 4 · บรรทัด 168 poll เก็บกวาดที่ไม่ยอมกลับเป็น `[1, 2]` | 5 (189) — รายการที่ควรหดไม่หด |
| `cancelremoves` | หน้าจอ | ปุ่ม *ยกเลิก* ต่อสายไปที่การลบ | แถว 6 · บรรทัด 206 `toEqual([])` | ไม่มี |
| `anysection` | **`clos.js`** | ทะเบียนผู้สอนหลุดจาก WHERE ของ `offeringOf` | แถว 7 · บรรทัด 227 — 404 กลายเป็น 200 | ไม่มี |
| `swallowrefusal` | หน้าจอ | จอกลืนคำปฏิเสธตอนโหลด | แถว 7 · บรรทัด 228 — แถบไม่ขึ้น | 8 (242) — การกลืนเดียวกัน คนละประโยคข้างใน |
| `wrongsentence` | route | CLO ที่ไม่พบตอบประโยคของตอนเรียน | แถว 8 · บรรทัด 242 `getByText(cloNotFound)` | ไม่มี — แถว 7 ยังยืน ซึ่งคือสิ่งที่แยกข้ออ้างของสองแถวออกจากกัน |

**`anysection` แตะไฟล์นอกตั๋วโดยตั้งใจ** — join ของทะเบียนผู้สอนอยู่ใน `clos.js` และ #28
import คำถามมาแทนที่จะถามซ้ำ ทางเดียวที่ซื่อสัตย์ในการทำ *ตอนเรียนของคนอื่นถูกปฏิเสธ* ให้พัง
คือพังที่ที่มันถูกตัดสิน `23:firstrole` เป็นแบบอย่างของการออกนอกไฟล์แบบมีเหตุผลกำกับ

**แถวรายการร่วม (แถว 2) ไม่มีมัตแตนต์ และบันทึกไว้ตรง ๆ แทนการทำเนียน** พฤติกรรมบ่งชี้ถือแค่
`clo_id` — ไม่มีคอลัมน์ตอนเรียน ไม่มีเจ้าของ — จึงไม่มีอะไรให้มัตแตนต์กรองเพื่อให้สองตอนเรียน
ได้คนละสำเนา คำโกหกที่ใกล้ที่สุด (`ORDER BY random()`) แกว่งบนรายการสองแถวมากกว่าจะพิสูจน์อะไร
สิ่งที่ค้ำข้ออ้างนี้จริงคือ schema เอง บวกกับ `behaviors.test.js` ที่เทียบ id ทีละตัวข้ามสองบัญชี
นี่คือสภาพเดียวกับ `noinsert` ของ #25

**`orderbyid` ฉบับแรก MISS** — ข้อความที่ผูกไว้ตรงกับทั้ง query ของรายการและ query
ของลูปจัดเลขใหม่ harness ปฏิเสธตามหน้าที่ ฉบับที่ใช้ผูกทั้ง SELECT ของ `listOf` แล้วรันใหม่
ตายที่บรรทัด 88 ตามตาราง

**การตายแบบปนเปื้อนถูกบันทึกเป็นการปนเปื้อน** สามมัตแตนต์ฝั่งเพิ่ม (`nextnumber`,
`savenoreload`, `taglabelraw`) ฆ่าแถว 5 ด้วย — ไม่ใช่เพราะแตะสิ่งที่แถว 5 อ้าง แต่เพราะแถว 4
ตายก่อนบรรทัดเก็บกวาดของตัวเอง แล้ว CLO-9 ส่งข้อค้างต่อให้แถว 5 ซึ่งเริ่มด้วยการนับ
เหตุผลเดียวกับที่ `27:swallowdelete` เคยส่งความพังให้ข้ออื่นผ่านรหัสที่ใช้ร่วมกัน
บรรทัดที่เชื่อได้ของแต่ละมัตแตนต์คือบรรทัดในคอลัมน์ *แถวที่มันค้ำ* เท่านั้น

## สิ่งที่ตัดสินใจไว้และควรรู้ก่อนแก้

**ฟอร์มเดียวใช้ทั้งเพิ่มและแก้** เหตุผลเดียวกับ `CloForm` — สองฟอร์มคือสองที่ที่ต้องเพิ่มช่องที่สี่
และหนึ่งในนั้นจะถูกลืม

**dropdown ไม่มีตัวเลือกว่างเป็นสถานะจริง** ต่างจาก PLO ของ #27 — คอลัมน์เป็น NOT NULL
และระดับคือครึ่งหนึ่งของเหตุผลที่ระเบียนนี้มีอยู่ ช่องว่างเป็นแค่คำเชิญให้เลือก และปุ่ม *บันทึก*
ไม่ติดจนกว่าจะเลือกครบ

**ป้ายไทยของสองรายการปิดอยู่ที่เดียว** — `frontend/src/components/behaviors/labels.js`
ใช้ร่วมกันระหว่าง dropdown ของฟอร์มกับแท็กบนแถว เพื่อไม่ให้สองที่เพี้ยนจากกัน
ค่า enum อยู่บนสาย ป้ายอยู่บนจอ

**การเพิ่มล็อกแถว CLO ก่อนนับเลข** (`SELECT … FOR UPDATE`) — ผู้สอนสองคนเพิ่มพร้อมกันได้
ข้อ 3 กับข้อ 4 แทนที่จะแข่งกันเป็นข้อ 3 แล้วคนแพ้ได้ 23505 ที่ไม่มีประโยคของตัวเอง

**ลูปจัดเลขใหม่เดินจากน้อยไปมากเท่านั้น** การลบปล่อยเลขที่ต่ำกว่าให้ว่างเสมอ แต่ละแถวจึงย้ายเข้า
เลขที่ว่างแล้ว และ `UNIQUE (clo_id, behavior_no)` ยืนอยู่ได้ตลอดทาง — เขียนลูปให้เดินกลับทาง
เมื่อไหร่ constraint จะเป็นคนจับ (โน้ตของ migration 0002 ซึ่ง route นี้พึ่งอยู่)
