# ตรวจรับด้วยมือ — #34 Activity marks

`docs/06-implementation-plan.md` §Acceptance กำหนดว่า ticket หนึ่งจะเสร็จเมื่อ automated test ผ่าน
**และ** checklist ที่ทำด้วยมือถูกเดินจนครบ เอกสารนี้คือครึ่งหลังของ #34

ครึ่งแรกอยู่ที่ `backend/test/activity-scores.test.js` (25 subtests) — เพดานทั้งสองแบบ การตรวจไฟล์
ทั้งสี่ข้อ และการที่บันทึกซ้ำเป็นการแก้ ถูกพิสูจน์ที่นั่นแล้ว รวมทั้งเกณฑ์ข้อ 10 ที่ระบุ seam ไว้เอง
(*enforced at the server*)

seam ที่สองคือเบราว์เซอร์ — `e2e/tests/34a-activity-scores.spec.js` (14 แถว) ดูหัวข้อ
*มัตแตนต์ที่พิสูจน์แถว ⚙* ท้ายเอกสาร

## สิ่งที่ต้องเข้าใจก่อนเดิน

**สวิตช์สองตัวไม่ใช่การเก็บสองแบบ** `activity_scores.clo_id` เป็น NOT NULL — migration 0003
อธิบายไว้ยาวว่าทำไม — จึงไม่มีสิ่งที่เรียกว่า *คะแนนของกิจกรรม* ในฐานข้อมูล มีแต่คะแนนต่อผลการเรียนรู้
สวิตช์ *ต่อกิจกรรม / ต่อผลการเรียนรู้* จึงเป็นเรื่องของ **การพิมพ์** ไม่ใช่การเก็บ: พิมพ์ทีเดียวแล้ว
เซิร์ฟเวอร์แบ่งตามน้ำหนักของแต่ละแถว และอ่านกลับด้วยการบวกกลับ

เพราะอ่านกลับด้วยการบวก **การแบ่งจึงต้องลงตัวพอดี ไม่ใช่ใกล้เคียง** เศษจากการปัดจึงถูกยกไปไว้ที่
แถวแรก ไม่ปล่อยให้หายไป คนที่พิมพ์ 61 แล้วเปิดมาเห็น 60.99 มีสิทธิ์ไม่เชื่อหน้าจอนี้ทั้งหน้า

**คะแนนของกลุ่มเป็นของนักศึกษา ไม่ใช่ของกลุ่ม** สวิตช์ *รายกลุ่ม* เขียนเลขเดียวกันให้สมาชิกทุกคน และ
ไม่มีอะไรจำไว้ว่ามันมาด้วยกัน ซึ่งถูกแล้ว — คนที่ถูกย้ายกลุ่มทีหลัง (#26) ยังถือคะแนนที่เคยได้ ไม่ใช่คะแนน
ของกลุ่มใหม่ ผลที่ตามมาบนจอคือ **ช่องของกลุ่มจะมีเลขก็ต่อเมื่อสมาชิกทุกคนถือเลขเดียวกัน** ถ้าไม่ตรงกัน
ช่องจะว่าง ไม่ใช่หยิบของคนแรกมาแสดงแทนทุกคน

**ช่องว่างไม่ใช่ศูนย์** ช่องที่เว้นไว้คือ *ยังไม่ได้ให้คะแนน* และถูกบันทึกเป็น null คนที่ตั้งใจให้ศูนย์
ต้องพิมพ์ศูนย์ หน้าจอที่แปลงช่องว่างเป็นศูนย์จะตัดคะแนนทั้งห้องตั้งแต่ครั้งแรกที่เปิดแล้วกดบันทึก

**ไฟล์นำเข้าเป็นรายคนเสมอ** เกณฑ์ข้อ 7 ให้ตรวจสี่อย่าง — จำนวนนักศึกษา รหัส ชื่อ และคอลัมน์ CLO —
ทั้งสี่เป็นเรื่องของนักศึกษา ไฟล์จึงเป็นบัญชีรายชื่อ และสวิตช์ต่อ-CLO เปลี่ยน *คอลัมน์* ของไฟล์
ไม่ใช่ *แถว* ส่วนสวิตช์กลุ่มเป็นวิธีพิมพ์บนจอเท่านั้น (การตัดสินใจของผู้ใช้ 2 ก.ย. 2569)

**สวิตช์กลุ่มตั้งต้นตามชนิดของกิจกรรม แล้วปล่อยให้เลือกเอง** งานกลุ่มเปิดมาที่ *รายกลุ่ม* งานเดี่ยว
เปิดมาที่ *รายคน* แต่เปลี่ยนได้ทั้งคู่ — ชนิดของงานเป็นข้อเท็จจริง ส่วนวิธีกรอกเป็นเรื่องของผู้สอน

**กิจกรรมที่ยังไม่ผูกผลการเรียนรู้บันทึกคะแนนไม่ได้** และหน้าจอบอกไว้ตรงนั้น ไม่ใช่ปล่อยให้กดบันทึก
แล้วค่อยปฏิเสธ กิจกรรม *ใบงานที่เพิ่งสร้าง…* ของ seed เป็นตัวนี้พอดี

## ตั้งต้น

```
docker start deep-core-postgres
cd db      && npm run reset && npm run migrate && npm run seed
cd backend && npm start          # http://localhost:3000
cd frontend && npm start         # http://localhost:5000
```

**ต้อง kill แล้ว start backend ใหม่ก่อนเดิน** — `node server.js` ไม่มี reload กระบวนการที่ค้างจาก
ตั๋วก่อนจะตอบ 404 ให้ทุก route ของ #34 ในขณะที่หน้าจอ hot-reload ตามปกติ **จอจึงดูใหม่ทั้งที่
server เป็นของเก่า**

ทุกบัญชีใน seed ใช้รหัสผ่าน `deep-core-local` — ค่าของตัวแปร `PASSWORD` ใน `db/seed.js`

ทางเข้าคือ เลือกตอนเรียนจากหน้า *รายวิชา* แล้วกดเมนู **คะแนนกิจกรรมการเรียนรู้** ทางซ้าย
ที่อยู่คือ `/teacher/teacherDashboard/<section_id>/activityScores`

seed หลังเดินคำสั่งข้างบนมีรูปร่างที่ใบนี้ต้องใช้พอดี

| กิจกรรม | ชนิด | คะแนนเต็ม | ผลการเรียนรู้ | แบ่งข้อละ |
|---|---|---|---|---|
| โครงงานย่อยที่ 1 — คลาสและอ็อบเจกต์ | กลุ่ม | 100 | CLO-1, CLO-2 | 50 / 50 |
| โครงงานย่อยที่ 2 — การสืบทอดและพอลิมอร์ฟิซึม | กลุ่ม | 100 | CLO-3, CLO-4 | 50 / 50 |
| โครงงานปลายภาค | กลุ่ม | 100 | CLO-5, CLO-8, CLO-9 | 34 / 33 / 33 |
| สอบกลางภาค | เดี่ยว | 100 | CLO-1, CLO-3, CLO-6 | 34 / 33 / 33 |
| สอบปลายภาค | เดี่ยว | 100 | CLO-5, CLO-6, CLO-7 | 34 / 33 / 33 |
| ใบงานที่เพิ่งสร้าง… | เดี่ยว | 20 | **ไม่มีเลย** | — |

**นักศึกษาทุกคนมีคะแนนครบทุกกิจกรรมอยู่แล้ว** seed เขียนไว้ให้ทั้ง 2249 แถว ใบนี้จึงเป็นการ *แก้*
คะแนน ไม่ใช่การกรอกจากศูนย์ ซึ่งตรงกับเกณฑ์ข้อ 4 พอดี — และแปลว่า **ต้องนับก่อนและหลัง**

ตอนเรียน 1 มี **6 กลุ่ม · 57 คน** และกิจกรรมละ `57 × จำนวน CLO` แถวคะแนน

## รายการที่ต้องเดิน

ช่องสุดท้ายมีสี่ค่า

- **☑** เดินจบแล้ว
- **◐** ครึ่งของ server เดินแล้วและผ่าน ส่วนที่เหลือคือสิ่งที่หน้าจอวาด (เมนู ถ้อยคำ) ยังต้องมีคนดู
- **☐** ยังไม่ได้เดิน
- **⚙** ครอบคลุมด้วย browser seam แล้ว (#65) ไม่ต้องเดินด้วยมือซ้ำ แถวบอกไฟล์ที่ครอบคลุมไว้

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| — | ทางเข้าหน้านี้อยู่ที่เมนูของตอนเรียน | เข้าด้วย `teacher.one@` เลือกตอนเรียน 1 แล้วกด **คะแนนกิจกรรมการเรียนรู้** | หน้าเปิดขึ้นจริง ไม่ใช่หน้า *ยังไม่ได้สร้าง* — เนื้อหาของเมนูเป็นสิ่งที่คนต้องดู | ☑ |
| 1 | กรอกและบันทึกคะแนนให้นักศึกษาทุกคนที่ลงทะเบียนได้ | เลือก *สอบกลางภาค* สวิตช์ *รายคน* แล้วดูตาราง | ทุกแถวของทะเบียน 57 คน มีช่องกรอก และช่องมีเลขอยู่แล้ว (seed) — ครอบคลุมโดย `34a` แถว 1 | ⚙ |
| 1 | ช่องว่างคือยังไม่ให้คะแนน ไม่ใช่ศูนย์ | ล้างช่องของคนหนึ่งแล้วบันทึก แล้วดูฐานข้อมูล | `score` เป็น NULL ไม่ใช่ 0 — ถ้อยคำใต้หัวข้อบอกไว้ด้วย และเป็นสิ่งที่คนต้องดู | ☑ |
| 2 | สวิตช์ต่อ-CLO เปลี่ยนช่องกรอกและเก็บได้ทั้งสองแบบ | สลับ *ต่อกิจกรรม* ↔ *ต่อผลการเรียนรู้* | คอลัมน์เปลี่ยนเป็นหนึ่งช่องต่อ CLO ของกิจกรรมนี้ พร้อมคะแนนเต็มของแต่ละข้อ — ครอบคลุมโดย `34a` แถว 2 | ⚙ |
| 2 | เลขที่พิมพ์ต่อกิจกรรมอ่านกลับได้เท่าเดิม | พิมพ์ `61` ให้คนหนึ่ง บันทึก แล้วโหลดหน้าใหม่ | ช่องยังเป็น `61` ไม่ใช่ `60.99` — ครอบคลุมโดย `34a` แถว 3 | ⚙ |
| 2 | คะแนนรวมที่อ่านกลับมาเป็นคะแนน ไม่ใช่เศษทศนิยมของเครื่อง | เปิดตารางที่ *ต่อกิจกรรม* แล้วดูช่องของคนที่ส่วนแบ่งบวกกันไม่ลงตัวในฐานสอง | ช่องอ่านได้ `88.67` ไม่ใช่ `88.66999999999999` — ครอบคลุมโดย `34a` แถว 14 | ⚙ |
| 3 | สวิตช์กลุ่มเปลี่ยนแถว และคะแนนของกลุ่มตกถึงสมาชิกทุกคน | เลือก *โครงงานย่อยที่ 1* สวิตช์ *รายกลุ่ม* พิมพ์ `84` ให้กลุ่มหนึ่ง บันทึก แล้วสลับเป็น *รายคน* | สมาชิกทุกคนของกลุ่มนั้นมี `84` — ครอบคลุมโดย `34a` แถว 4 | ⚙ |
| 3 | สวิตช์กลุ่มตั้งต้นตามชนิดของกิจกรรม | สลับกิจกรรมไปมาระหว่างงานกลุ่มกับงานเดี่ยว | งานกลุ่มเปิดมาที่ *รายกลุ่ม* งานเดี่ยวเปิดมาที่ *รายคน* และเปลี่ยนเองได้ทั้งคู่ — พฤติกรรมของ default เป็นสิ่งที่คนต้องดู | ☑ |
| 3 | สวิตช์กลุ่มที่ผู้สอนเปลี่ยนเองอยู่ที่เดิมหลังบันทึก | ที่งานกลุ่ม สลับเป็น *รายคน* พิมพ์คะแนนให้คนหนึ่ง แล้วกด **บันทึกคะแนน** | ตารางยังเป็น *รายคน* ไม่ดีดกลับไป *รายกลุ่ม* — ครอบคลุมโดย `34a` แถว 11 | ⚙ |
| 3 | ช่องของกลุ่มว่างเมื่อสมาชิกถือคะแนนไม่ตรงกัน | ที่ *รายคน* แก้คะแนนสมาชิกคนเดียวของกลุ่มนั้น แล้วสลับกลับไป *รายกลุ่ม* | ช่องของกลุ่มว่าง ไม่ใช่เลขของใครคนหนึ่ง — ครอบคลุมโดย `34a` แถว 12 | ⚙ |
| 4 | ช่องที่ว่างเพราะสมาชิกไม่ตรงกันไม่ถูกลบทิ้งตอนบันทึก | ที่ *รายกลุ่ม* พิมพ์คะแนนให้กลุ่ม**อื่น** แล้วกด **บันทึกคะแนน** | คะแนนของสมาชิกกลุ่มที่ช่องว่างเท่าเดิมทุกคน ไม่ถูกเขียนทับด้วย NULL — ครอบคลุมโดย `34a` แถว 12 | ⚙ |
| 1 | คนที่ได้คะแนนมาบางผลการเรียนรู้ยังไม่มีคะแนนรวม | ที่ *ต่อผลการเรียนรู้* กรอกเฉพาะข้อแรกให้คนหนึ่ง บันทึก แล้วสลับเป็น *ต่อกิจกรรม* | ช่องคะแนนรวมของคนนั้นว่าง ไม่ใช่ผลบวกของเท่าที่กรอกไป — ครอบคลุมโดย `34a` แถว 13 | ⚙ |
| 4 | บันทึกซ้ำเป็นการแก้ ไม่ใช่การเพิ่มแถวใหม่ | บันทึกคนเดิมสองครั้งด้วยเลขต่างกัน แล้วนับแถว | จำนวนแถวใน `activity_scores` เท่าเดิม และเลขเป็นค่าล่าสุด — ครอบคลุมโดย `34a` แถว 7 | ⚙ |
| 5 | คะแนนเกินคะแนนเต็มของกิจกรรมถูกปฏิเสธ | พิมพ์ `101` ในกิจกรรมที่เต็ม 100 แล้วบันทึก | *คะแนนต้องไม่เกินคะแนนเต็มของกิจกรรมนี้ ซึ่งเท่ากับ 100* และไม่มีอะไรถูกเขียน — ครอบคลุมโดย `34a` แถว 5 | ⚙ |
| 5 | คะแนนเกินส่วนแบ่งของ CLO ข้อนั้นถูกปฏิเสธ พร้อมบอกส่วนแบ่ง | ที่ *ต่อผลการเรียนรู้* พิมพ์เกินคะแนนเต็มของข้อแรก | ประโยคเอ่ยทั้งเลขข้อและส่วนแบ่ง — ครอบคลุมโดย `34a` แถว 6 | ⚙ |
| 5 | แถบปฏิเสธเป็นประโยคของระบบ ไม่ใช่ข้อผิดพลาด | ดูแถบปฏิเสธทุกแบบที่เดินมา | ต้อง **ไม่ใช่** *เกิดข้อผิดพลาดในระบบ กรุณาลองใหม่อีกครั้ง* สักครั้งเดียว | ☑ |
| 6 | ดาวน์โหลดแบบฟอร์มได้ และแบบฟอร์มสะท้อนสวิตช์ | กด **ดาวน์โหลดแบบฟอร์ม** ที่สวิตช์ *ต่อกิจกรรม* แล้วสลับเป็น *ต่อผลการเรียนรู้* แล้วกดอีกครั้ง | ไฟล์แรกมีคอลัมน์ `student_id,full_name_th,score` ไฟล์ที่สองมีคอลัมน์ CLO ของกิจกรรมนี้แทน — ครอบคลุมครึ่งของ server แล้ว ส่วนการที่ปุ่มอ่านสวิตช์ปัจจุบันเป็นสิ่งที่คนต้องดู | ☑ |
| 7 | ไฟล์ที่จำนวนคนไม่ตรงถูกปฏิเสธ และประโยคบอกว่าเพราะจำนวน | ลบบรรทัดหนึ่งออกจากแบบฟอร์มที่กรอกแล้ว อัปโหลด | *จำนวนนักศึกษาในไฟล์ไม่ตรงกับตอนเรียนนี้ …* — ครอบคลุมโดย `34a` แถว 8 | ⚙ |
| 7 | ไฟล์ที่มีรหัสนอกห้องถูกปฏิเสธ และประโยคบอกรหัส | เปลี่ยนรหัสหนึ่งเป็น `99019999` | ประโยคเอ่ย `99019999` — ครอบคลุมครึ่งของ server แล้ว | ☑ |
| 7 | ไฟล์ที่ชื่อไม่ตรงทะเบียนถูกปฏิเสธ และบอกว่าเป็นเรื่องชื่อ | แก้ชื่อคนหนึ่งในไฟล์ | ประโยคเอ่ยรหัสคนนั้นและชื่อที่ทะเบียนมี — ครอบคลุมครึ่งของ server แล้ว | ☑ |
| 7 | ไฟล์ที่คอลัมน์ CLO ไม่ตรงถูกปฏิเสธ และบอกว่าควรเป็นคอลัมน์อะไร | เอาแบบฟอร์มต่อ-CLO ของกิจกรรมอื่นมาอัปโหลด | ประโยคเอ่ยรายชื่อคอลัมน์ที่ควรเป็น ตามลำดับ — ครอบคลุมครึ่งของ server แล้ว | ☑ |
| 8 | ไฟล์ที่ถูกปฏิเสธไม่เขียนอะไรเลย | หลังทุกไฟล์ที่ถูกปฏิเสธข้างบน ดูคะแนนของคนแรก | เลขเท่าเดิมทุกครั้ง — ครอบคลุมโดย `34a` แถว 8 และ 9 | ⚙ |
| 8 | คะแนนเกินในไฟล์ถูกรายงานเป็นรายบรรทัด | ใส่ `101` ที่บรรทัดที่ 3 ของไฟล์ที่ถูกต้อง | ตาราง *บรรทัดที่ 3* พร้อมประโยคเรื่องคะแนนเต็ม ไม่ใช่ประโยคเดียวทั้งไฟล์ — ครอบคลุมโดย `34a` แถว 9 | ⚙ |
| 9 | ไฟล์ที่ถูกต้องบันทึกคะแนนครบทุกคน | อัปโหลดแบบฟอร์มที่กรอกครบ | *นำเข้าสำเร็จ 57 รายการ* และคะแนนบนจอเปลี่ยนตามไฟล์ — ครอบคลุมโดย `34a` แถว 8 | ⚙ |
| 10 | ตอนเรียนของคนอื่นถูกปฏิเสธที่เซิร์ฟเวอร์ | เข้าด้วยผู้สอนคนหนึ่ง แล้วพิมพ์ที่อยู่ของตอนเรียนที่ไม่ใช่ของตัวเอง ตามด้วย `/activityScores` | เดินแล้ว 3 ก.ย. 2569 — `teacher.one@` พิมพ์ `/teacher/teacherDashboard/2/activityScores` ได้ *ไม่พบตอนเรียนที่ระบุ* และ **`document.querySelectorAll('table').length === 0`** ไม่ใช่ตารางว่างหรือตารางของคนอื่น · **ใบเขียนขั้นตอนไว้เป็น `teacher.two@` ซึ่งไม่ได้สอนตอนเรียนใดเลย** รอบนี้ใช้ `teacher.one@` แทนเพราะเป็นเคสที่คมกว่า — คนที่สอนจริง มีจอนี้ใช้จริง แต่ไม่ใช่ตอนเรียนนี้ · ยังไม่มีมัตแตนต์ค้ำแถว `34a` ตามเดิม ซึ่งไม่ใช่สิ่งที่การเดินแก้ให้ได้ | ☑ |
| 10 | id ของกิจกรรมที่เป็นของตอนเรียนอื่นถูกปฏิเสธ | — | ตอบ *ไม่พบกิจกรรมที่ระบุ* ทุก route — ไม่มีปุ่มบนจอที่ยิงแบบนี้ได้ พิสูจน์ที่ seam HTTP · **ตรวจซ้ำ 3 ก.ย. 2569** `AppRoutes.js` มี `:sectionId` เป็นตัวแปรเดียวใน URL ของจอผู้สอน id ของกิจกรรมไม่เคยอยู่ในนั้น และรายการที่จอเสนอให้กดล้วนเป็นของตอนเรียนที่ยืนอยู่ — ◐ ถาวรโดยโครงสร้าง | ◐ |
| — | กิจกรรมที่ยังไม่ผูกผลการเรียนรู้บอกไว้บนจอ | เลือก *ใบงานที่เพิ่งสร้าง…* | ข้อความสีเหลืองอำพันบอกว่ายังบันทึกคะแนนไม่ได้ และไม่มีตารางให้กรอก — ถ้อยคำและสีเป็นสิ่งที่คนต้องดู | ☑ |
| — | ตารางที่กว้างเกินจอเลื่อนในกรอบของตัวเอง | ย่อหน้าต่างให้เหลือประมาณครึ่งจอ ที่สวิตช์ *ต่อผลการเรียนรู้* | ตารางมีแถบเลื่อนของตัวเอง และหน้าไม่เลื่อนข้าง (#98) | ☑ |
| 11 | ชุดทดสอบครอบคลุมสวิตช์ทั้งสอง การตรวจไฟล์ทั้งสี่ และการบันทึกซ้ำ | อ่าน `backend/test/activity-scores.test.js` และ `e2e/tests/34a-activity-scores.spec.js` | 25 subtests + 14 แถว ครบทั้งสามอย่างที่เกณฑ์ระบุ | ☑ |

**เก็บกวาดหลังเดิน** reset + migrate + seed ใหม่ ซึ่งเร็วกว่าการไล่แก้คะแนนกลับ
**นับจำนวนแถวคะแนนก่อนและหลัง** — ใบนี้มีขั้นตอนที่นำเข้าไฟล์ทั้งห้อง และการเขียนผิดที่มองไม่เห็น
ถ้าไม่นับ

ตัวเลขที่ต้องกลับมาเท่าเดิมเมื่อจบ: `SELECT count(*) FROM activity_scores` = **2249**

## บันทึกการเดินด้วยมือ

**เดินจบแล้ว 2 ก.ย. 2569** โดย Claude ขับ Chrome ให้ผู้ใช้ดู (ผู้ใช้เป็นคนลงชื่อเข้าใช้เอง)
บนสแตกเดินมือ พอร์ต 3000/5000 สคีมา `deep_core`

**นับก่อน–หลัง** `SELECT count(*) FROM activity_scores` = **2249** ทั้งสองครั้ง และ
`WHERE score IS NULL` = **0** ตอนจบ คะแนนของ `66010001` ที่ถูกล้างเพื่อเดินแถว *ช่องว่างคือ
ยังไม่ให้คะแนน* ถูกเขียนกลับเป็น 31.48/26.49/25.41 ตามเดิม

สิ่งที่เห็นจริงบนจอ

- เข้าทางเมนู **คะแนนกิจกรรมการเรียนรู้** ของตอนเรียน 1 แล้วหน้าเปิดจริง ไม่ใช่ *ยังไม่ได้สร้าง*
- ล้างช่องของ `66010001` แล้วบันทึก — ทั้งสาม CLO เป็น **NULL ไม่ใช่ 0** และจำนวนแถวไม่ขยับ
  (upsert แก้ ไม่ใช่เพิ่ม) ส่วนถ้อยคำใต้หัวข้อเขียนไว้ว่า *ช่องที่เว้นว่างหมายถึง ยังไม่ให้คะแนน ไม่ใช่ศูนย์*
- งานกลุ่มเปิดมาที่ *รายกลุ่ม* งานเดี่ยวเปิดมาที่ *รายคน* และสลับเองได้ทั้งคู่
- แถบปฏิเสธห้าแบบ ไม่มีแบบไหนเป็น *เกิดข้อผิดพลาดในระบบ* สักครั้ง — สองแบบจากหน้าจอ
  (`bg-red-50` `text-red-800`) และสามแบบจากไฟล์ ทุกแบบเอ่ยสิ่งที่ผิดของตัวเอง:
  `99019999` · `66010005` พร้อมชื่อในทะเบียน *"วรเมธ พงษ์เจริญ"* · `CLO-1, CLO-3, CLO-6 ตามลำดับ`
- ปุ่มแบบฟอร์มอ่านสวิตช์ปัจจุบันจริง — ยิง `?mode=clo` แล้ว `?mode=activity` ตามที่กดไว้
  และหัวตารางของสองไฟล์ต่างกัน (`student_id,full_name_th,score` กับ
  `student_id,full_name_th,CLO-1,CLO-3,CLO-6`)
- *แบบฝึกหัดท้ายบท* ซึ่งยังไม่ผูก CLO ขึ้นแถบ `bg-amber-50` `text-amber-700` ว่ายังบันทึกไม่ได้
  และ**ไม่มีตารางกับไม่มีปุ่มบันทึก**
- ที่ 607px ตารางเลื่อนในกรอบ `overflow-x-auto` ของตัวเอง (576 > 510) และหน้าไม่เลื่อนข้าง
- ไฟล์ที่ถูกปฏิเสธทั้งสามไม่เขียนอะไรเลย — ไม่มีแถว `score = 10` อยู่ในตารางสักแถว

### สิ่งที่การเดินจับได้ และสาม seam ไม่จับ

**ช่องคะแนนรวมของ `66010003` อ่านได้ `88.66999999999999`**

ส่วนแบ่งที่เก็บไว้ตรงเป๊ะ — 32.08 · 31.27 · 25.32 — แต่หน้าจออ่านคะแนนรวมด้วยการ**บวกกลับ
ใน JavaScript** และผลบวกนั้นไม่ได้ปัดเศษ ฝั่งเซิร์ฟเวอร์ปัดทั้งตอนหารและตอนเก็บ ทางอ่านกลับไม่เคยปัด

มันโผล่เฉพาะกับคะแนนที่ส่วนแบ่งบวกกันไม่ลงตัวในฐานสอง ซึ่งเป็นเหตุผลว่าทำไม 25 subtests
14 แถวเบราว์เซอร์ และ 14 มัตแตนต์ถึงไม่เจอ — `61` กับ `12.5` ที่ชุดทดสอบพิมพ์นั้นหารแล้วบวกกลับ
ได้พอดีทุกครั้ง **ต้องใช้ข้อมูลจริงถึงจะเห็น** และนี่คือสิ่งที่ seam ที่สามมีไว้เพื่อ

เป็นมาตรฐานของตั๋วนี้เองที่ตกด้วย — header ของ route เขียนไว้ว่า *"คนที่พิมพ์ 61 แล้วอ่านกลับได้
60.99 มีสิทธิ์ไม่เชื่อหน้าจอนั้นอีกเลย"*

แก้ด้วยการปัดผลบวกเป็นสองตำแหน่งเหมือนฝั่งเซิร์ฟเวอร์ ทั้ง 57 ช่องอ่านสะอาดหมด และได้
`34a` แถว 14 กับมัตแตนต์ `totalkeepsfloattail` ค้ำไว้

### สองแถวที่ยังเป็น ◐ และการเดินไม่ทำให้ดีขึ้น

*ตอนเรียนของคนอื่น* กับ *id ของกิจกรรมที่เป็นของตอนเรียนอื่น* — ◐ ของสองแถวนี้ไม่ได้แปลว่า
ยังไม่มีใครเดิน แต่แปลว่า**ไม่มีมัตแตนต์ค้ำ** (แถวแรกถูกปฏิเสธที่ route ของ #32 แถวที่สองไม่มีปุ่ม
บนจอที่ยิงแบบนั้นได้) การเอาคนไปเดินซ้ำจึงไม่เปลี่ยนเครื่องหมาย ทั้งคู่พิสูจน์ที่ seam HTTP แล้ว

## มัตแตนต์ที่พิสูจน์แถว ⚙

`mutation/34-activity-scores.py` สิบห้าตัว อ่านวิธีใช้ที่ `mutation/README.md` ก่อน

ตารางนี้บันทึก **สิ่งที่มัตแตนต์ฆ่าได้จริง** ไม่ใช่สิ่งที่ควรจะฆ่าได้ ช่อง *แถวที่มันค้ำ* คือแถวเดียว
ที่ตัวนั้นเป็นหลักฐานให้ ส่วนแถวอื่นที่ตายไปด้วยเป็นผลพลอยได้ของความพังเดียวกัน ไม่ใช่หลักฐานเพิ่ม

| มัตแตนต์ | แถวที่มันค้ำ (เกณฑ์ข้อ) | ตายที่ | แถวอื่นที่ตายไปด้วย |
|---|---|---|---|
| `gridopensempty` | 1 — ตารางเปิดมาพร้อมคะแนนที่มีอยู่ | `not.toHaveValue('')` ของแถว 1 | 2–13 ทุกแถว (ทุกแถวเริ่มด้วยการอ่านหรือพิมพ์ในช่อง) |
| `totalkeepsfloattail` | 2 — คะแนนรวมอ่านกลับเป็นคะแนน ไม่ใช่เศษทศนิยม | `toHaveValue('0.3')` ของแถว 14 ได้ `0.30000000000000004` | ไม่มี |
| `partialreadsastotal` | 1 — คนที่ได้คะแนนบางผลการเรียนรู้ยังไม่มีคะแนนรวม | `toHaveValue('')` ของแถว 13 ได้ `31.48` | ไม่มี |
| `columnsareeveryclo` | 2 — คอลัมน์เป็น CLO ของกิจกรรมนี้ | จำนวนคอลัมน์ของแถว 2 กลายเป็น CLO ทั้ง Offering | 1, 3, 4, 6, 7, 8, 11, 12, 13 |
| `markwholeoneveryclo` | 2 — เลขที่พิมพ์ต่อกิจกรรมอ่านกลับได้เท่าเดิม | `totalOf` ของแถว 3 ได้ 183 แทน 61 | 4, 7, 8, 11, 12 |
| `driftuncorrected` | 2 — การแบ่งลงตัวพอดี | `totalOf` ของแถว 3 ได้ 12.51 แทน 12.5 | 7 |
| `grouptofirstmember` | 3 — คะแนนกลุ่มตกถึงสมาชิกทุกคน | `toBe(84)` ของสมาชิกคนที่สองในแถว 4 ได้ 75.61 (ค่าเดิม) | ไม่มี |
| `entryresetsonsave` | 3 — สวิตช์กลุ่มอยู่ที่เดิมหลังบันทึก | `whoColumn` ของแถว 11 ได้ *กลุ่ม* แทน *รหัสนักศึกษา* | ไม่มี |
| `insertnotupsert` | 4 — บันทึกซ้ำเป็นการแก้ | `toBe(12.5)` ของแถว 7 ได้ค่าของ seed | 3, 4, 8, 11, 12 |
| `savesendseveryrow` | 4 — ช่องที่ว่างเพราะสมาชิกไม่ตรงกันไม่ถูกลบทิ้ง | `toEqual(before)` ของสมาชิกกลุ่มในแถว 12 | ไม่มี |
| `noactivityceiling` | 5 — เพดานของกิจกรรม **และ** 8 — คะแนนเกินในไฟล์รายงานรายบรรทัด | `toBe(400)` ของแถว 5 ได้ 200 และ `reportedLines` ของแถว 9 ได้ `[]` | ไม่มี |
| `nocloceiling` | 5 — เพดานของ CLO แต่ละข้อ | `toBe(400)` ของแถว 6 ได้ 200 | ไม่มี |
| `importcountunchecked` | 7 — จำนวนคนในไฟล์ถูกตรวจ | ประโยค *จำนวนนักศึกษาในไฟล์…* ของแถว 8 ไม่ปรากฏ | ไม่มี |
| `refusedfilestillwrites` | 8 — ไฟล์ที่ถูกปฏิเสธไม่เขียนอะไรเลย | `toBe(72)` ของแถว 8 ได้ 5 (ไฟล์ที่ถูกปฏิเสธถูกเขียนลงไป) | ไม่มี |
| `importwritesnothing` | 9 — ไฟล์ที่ถูกต้องบันทึกคะแนนครบทุกคน | `toBe(72)` ของแถว 8 ได้ 83.38 (ค่าของ seed ไม่ขยับ) | ไม่มี |

**`gridopensempty` ฆ่าทั้งสิบสามแถว และนั่นคือสิ่งที่บันทึกไว้ตรง ๆ** ตารางที่ไม่เคยเติมค่าลงช่องทำให้
ทุกแถวที่อ่านหรือพิมพ์ในช่องล้มพร้อมกัน แถวที่มันเป็นหลักฐานให้คือแถว 1 ซึ่งเป็นแถวเดียวที่ *ข้ออ้าง*
ของมันคือ "ช่องมีค่าอยู่แล้ว" ส่วนอีกสิบสองแถวเป็นความพังเดียวกันมองจากที่อื่น

**`driftuncorrected` เป็นเศษหนึ่งสตางค์ ไม่ใช่การแบ่งที่พัง** และเป็นรูปที่มองไม่เห็นถ้าไม่จงใจหา:
น้ำหนัก 34/33/33 กับคะแนนจำนวนเต็มหารลงตัวเสมอ รอบแรกของการกวาดมัตแตนต์ตัวนี้จึงรอด ทั้งที่
แถว 3 มีไว้ค้ำเรื่องนี้พอดี — แถว 3 ถูกเขียนเพิ่มให้พิมพ์ `12.5` ด้วย แล้วมัตแตนต์จึงตายที่แถวของตัวเอง
บทเรียนเดียวกับ #33: มัตแตนต์ที่รอดคือคำถามว่าแถวนั้นตรวจสิ่งที่มันอ้างจริงหรือเปล่า

**สามตัวสุดท้ายมาจาก `/code-review` ของตั๋วนี้ ไม่ใช่จากการเขียนเทสต์** — `entryresetsonsave`,
`savesendseveryrow` และ `partialreadsastotal` ต่างคืนสภาพหน้าจอกลับไปเป็นก่อนแก้ ทั้งสามเป็น
ความผิดเดียวกันคนละที่: **ช่องว่างแปลว่ายังไม่ให้คะแนน** และหน้าจอนี้วาดช่องให้ว่างด้วยเหตุผลของ
ตัวเองสองกรณี (กลุ่มที่สมาชิกถือคะแนนไม่ตรงกัน กับคนที่ได้คะแนนมาบางข้อ) การส่งทุกแถวขึ้นไปตอน
บันทึกจึงเขียน NULL ทับคะแนนที่ทำให้ช่องนั้นว่างตั้งแต่แรก — ลบงานของทั้งกลุ่มด้วยการกดปุ่มที่เล็ง
ไปที่แถวอื่น ไม่มีอะไรปฏิเสธ และแถบแจ้งผลบอกว่าบันทึกแล้ว

**`refusedfilestillwrites` บังคับให้ต้องสลับลำดับ assertion ของแถว 8** ฉบับแรกของมันตายที่
*ประโยค* ไม่ใช่ที่ *คะแนนไม่ขยับ* เพราะทั้งสองครึ่งของ "ปฏิเสธทั้งไฟล์" เดินผ่านโค้ดเส้นเดียวกัน
มัตแตนต์จึงตายที่อันที่อยู่ก่อน แล้วเกณฑ์ข้อ 8 ก็จะติด ⚙ ด้วยหลักฐานของเกณฑ์ข้อ 7 — ซึ่งเป็น
ความผิดชนิดเดียวกับที่ review ทักมาพอดี ย้าย `toBe(72)` ขึ้นก่อนประโยค แล้วทั้งสองตัวจึงตาย
คนละที่: `importcountunchecked` ที่ประโยค `refusedfilestillwrites` ที่คะแนน

**แถว 10 ไม่มีมัตแตนต์ค้ำ** และเกณฑ์ข้อ 10 จึงไม่ติด ⚙ จากฝั่งเบราว์เซอร์ สิ่งที่แถวนั้นยืนยันคือ
ตอนเรียนของคนอื่นถูกปฏิเสธก่อนตารางจะถูกวาด — แต่คำขอที่ถูกปฏิเสธก่อนเป็น route ของ **#32**
การมัตแตนต์มันคือการมัตแตนต์ #32 เกณฑ์ข้อนี้พิสูจน์ที่ seam ของ HTTP แทน ซึ่ง
`activity-scores.test.js` ถาม route ของไฟล์นี้ตรง ๆ

ไม่มีมัตแตนต์ตัวไหนทำให้เกิด `error:` ฝั่งเซิร์ฟเวอร์ในบันทึกการรัน — ตรวจแล้วทั้งสิบห้าไฟล์ที่
`_local/mutation-34/`

## มัตแตนต์จากใบอื่นที่ค้ำแถวของใบนี้ — #123 (7 ก.ย. 2569)

`30:importlineoff` ใน `mutation/30-weighting-scheme.py` เลื่อนเลขบรรทัดในรายงานของ
`backend/lib/importer.js` ไปหนึ่ง ซึ่งเป็นโมดูลนำเข้าที่หกหน้าจอใช้ร่วมกัน **มันฆ่าแถว 9 ของ `34a-activity-scores.spec.js` ด้วย**
วัดเมื่อ 7 ก.ย. 2569 ตอนที่ [#123](https://github.com/khthana/Deep-QA/issues/123) เล็งจุดยึดของมันใหม่
แล้วกวาดข้ามใบเป็นครั้งแรก

เขียนไว้ที่นี่เพราะ `CLAUDE.md` บอกว่าต้องอ่านสองทาง — จากแถวไปหามัตแตนต์ และจากมัตแตนต์ไปหาแถว
**ใบตรวจรับของ #30 อ้างว่ามันฆ่าแถวนี้ ใบนี้จึงต้องรู้จักชื่อมัน** ไม่งั้นคนที่ไล่จากฝั่งนี้จะไม่มีทางเจอ
มัตแตนต์ที่ค้ำข้ออ้างเรื่องเลขบรรทัดของตัวเองเลย
