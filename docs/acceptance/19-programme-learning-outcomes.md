# ตรวจรับด้วยมือ — #19 Programme Learning Outcomes

`docs/06-implementation-plan.md` §Acceptance กำหนดว่า ticket หนึ่งจะเสร็จเมื่อ automated test ผ่าน
**และ** checklist ที่ทำด้วยมือถูกเดินจนครบ เอกสารนี้คือครึ่งหลังของ #19

ครึ่งแรกอยู่ที่ `backend/test/plos.test.js` (19 subtests) — ทุกเกณฑ์ที่พิสูจน์ได้ที่ seam ของ
HTTP surface ถูกพิสูจน์ที่นั่นแล้ว รวมทั้ง **เกณฑ์ข้อ 9 ทั้งข้อ** ซึ่งเป็นข้อความเกี่ยวกับเทสต์เอง คือ
รหัสซ้ำภายในหลักสูตรเดียวกันถูกปฏิเสธ (409) รหัสเดียวกันในอีกหลักสูตรหนึ่ง**ถูกรับ** (201) และ
กรรมการหลักสูตรของ `0503` ถูกปฏิเสธทุกกริยาบนหลักสูตร `0501` — reach ที่ปฏิเสธทุกคนก็ผ่านครึ่งเดียวได้
จึงต้องเดินทั้งสองทาง

ตั้งแต่ #65 มี seam ที่สองคือเบราว์เซอร์ — `e2e/tests/19a-plos.spec.js` 12 ข้อทดสอบ รับแถวข้างล่างไปเกือบทั้งใบ
ดูหัวข้อ *มัตแตนต์* ท้ายเอกสารว่าแต่ละแถว ⚙ ถูกพิสูจน์ด้วยการทำอะไรพัง

## สิ่งที่ตั๋วเขียนไว้แล้วโค้ดไม่ทำตาม และทำไม

**เกณฑ์ข้อ 8 ของตั๋วเขียนว่า *"Faculty and Department Admins can manage PLOs within their scope"*
แต่โค้ดไม่ให้ `FACULTY_ADMIN` เข้าหน้านี้เลย** — [#79](https://github.com/khthana/Deep-QA/issues/79)
กลับคำตัดสินนั้นหลังจากตั๋วนี้ถูกเขียน และระบุ **A09 ซึ่งคือหน้านี้** ไว้ตรง ๆ ว่า
*"เมื่อสร้าง ห้ามใส่ `FACULTY_ADMIN` ลงใน MAINTAINERS ของ route และห้ามใส่รายการเมนูลง `FacultyAdmin.js`"*
เหตุผลคือคณะถือ *รายชื่อ* ภาควิชาและหลักสูตร ส่วนสิ่งที่อยู่ *ข้างใน* หลักสูตรเป็นของภาควิชากับกรรมการหลักสูตร
ข้อความในตั๋วเก่ากว่า #79 จึงถือ #79 เป็นข้อยุติ และเขียนไว้ตรงนี้แทนที่จะให้ checklist ขัดกับโค้ดอย่างเงียบ ๆ

**การลบข้อหลักที่ยังมีข้อย่อย ถูกปฏิเสธ ไม่ใช่ปิดการใช้งาน** — ตั๋วบอกกรณีอ้างอิงไว้สองกรณี
(subject mapping กับ CLO) แต่ `parent_outcome_id` ก็เป็น `ON DELETE RESTRICT` เหมือนกัน
ถ้าปล่อยให้ `deleteOrDeactivate` จัดการ ข้อหลักที่มีข้อย่อยจะถูก *ปิดการใช้งาน* เงียบ ๆ
ทั้งที่คนกดคือคนที่ตั้งใจจะ *ลบ* และข้อย่อยยังเรียงอยู่ใต้มันเหมือนเดิม จึงถามเรื่องข้อย่อยก่อน
แล้วตอบด้วยประโยคที่บอกว่าต้องทำอะไร — เป็นแถวหนึ่งใน checklist นี้ ไม่ใช่ผลข้างเคียงของ helper ที่หยิบมาใช้

**หน้านี้ไม่มีการแบ่งหน้า** ทุกหน้าข้อมูลหลักอื่นแบ่งสิบแถว หน้านี้ไม่แบ่งโดยตั้งใจ —
ข้อย่อยที่อยู่หน้าสองขณะที่ข้อหลักอยู่หน้าหนึ่งไม่ใช่ต้นไม้ และไม่มีวิธีประกอบกลับที่ฝั่งหน้าจอ

ย่อหน้าข้างบนคือสิ่งที่ [#101](https://github.com/khthana/Deep-QA/issues/101) อธิบายผิด — ตั๋วนั้นเขียนว่า
*ห้าหน้าของ `0501` มาก่อนแถวแรกของ `0503`* ซึ่งเป็นคำอธิบายของหน้าที่**ไม่มีอยู่จริง**
ความจริงแย่กว่านั้น คือ 56 แถวต่อกันในการเลื่อนเดียว โดยไม่มี *หน้า 1 จาก 6* บอกว่ายังมีอะไรต่อ
**การวัดก่อนเชื่อคำวินิจฉัยของตั๋ว** — `grep -c Pager frontend/src/pages/Plos.js` ตอบ 0

**หน้าพี่น้องสี่หน้าที่ตั๋วสั่งให้อ่าน ถูกอ่านแล้ว และไม่มีหน้าไหนเปลี่ยน** ([#101](https://github.com/khthana/Deep-QA/issues/101)) — `Offerings`, `ProgramSubjects`,
`Rubrics` และ `Students` ยังมี *ทุกหลักสูตร* ทั้งสี่หน้า และทั้งสี่หน้า**แบ่งหน้า** — หน้านี้กับ #20
เป็นสองหน้าเดียวที่ไม่แบ่ง สิ่งที่ตัดสินอยู่ที่ schema ไม่ใช่รสนิยม คือ `UNIQUE (program_id, outcome_code)`:
รหัส PLO เป็นอันเดียวในห้าหน้าที่ชี้แถวได้*เฉพาะภายในหลักสูตร* (`rubric_code` เป็น `UNIQUE` ทั้งระบบ)
สี่หน้านั้นจึงไม่ถูกแก้ในตั๋วนี้ และการอ่านนั้นถูกบันทึกไว้ตรงนี้ ไม่ใช่ใน CLAUDE.md ที่เดียว

**บัญชีที่ไม่ได้ดูแลหลักสูตรใดเลย เห็นประโยคของตัวเอง — แต่ไม่มีอะไรทดสอบมัน**
เมื่อตัวกรองเป็นสิ่งจำเป็น บัญชีที่ไม่มีหลักสูตรในสิทธิ์จะไม่มีคำขอใดส่งออกไปเลย หน้าจึงต้อง
หยุดหมุนเอง ไม่งั้น *กำลังโหลด…* ค้างไว้ตลอดไป ซึ่งคือข้อบกพร่องที่ #43 เจอ
แต่ทุกบัญชีใน seed เข้าถึงอย่างน้อยหนึ่งหลักสูตร (#102 ใส่ `0101` ให้ `01` ไปแล้ว) จึง**ไม่มีแถว
ใน seam ไหนที่เดินมาถึงข้อความนี้ได้** คำตอบที่ซื่อตรงคือ *ยังไม่มีเทสต์* ไม่ใช่การเงียบ

**หน้านี้ไม่มีการนำเข้าไฟล์** ตั๋วไม่ได้ขอ และรูปทรงต้นไม้ทำให้ไฟล์แบนต้องอ้างข้อหลักด้วยรหัสที่ยังไม่มีตัวตน
ถ้าจะทำควรเป็นตั๋วของตัวเอง

## ตั้งต้น

```
docker start deep-core-postgres
cd db      && npm run reset && npm run migrate && npm run seed
cd backend && npm start          # http://localhost:3000
cd frontend && npm start         # http://localhost:5000
```

ทุกบัญชีใน seed ใช้รหัสผ่านเดียวกัน คือค่า `PASSWORD` ใน `db/seed.js:58`
อีเมลต้องพิมพ์เต็มรวมโดเมน `@kmitl.ac.th` — อีเมลหรือรหัสผ่านที่ผิดทำให้ server ตอบ 401
และหน้าจอขึ้นกล่อง *Session หมดอายุ* ทับประโยคจริงไว้ หลงทางง่ายมาก ([#97](https://github.com/khthana/Deep-QA/issues/97))
เมนูที่ใช้ตลอดเอกสารนี้คือ *ผลการเรียนรู้ → กำหนดผลการเรียนรู้ระดับหลักสูตร PLO* (`/main/plos`)

**ต้อง restart backend ก่อนเดิน** — process ที่ค้างจากตั๋วก่อนจะตอบ *ไม่พบเส้นทางที่เรียกบนเซิร์ฟเวอร์นี้*
ให้ทุก route ของ #19 (นั่นคือสิ่งที่ #95 ทำให้มันบอกตรง ๆ แทนที่จะโทษการเชื่อมต่อ)

**seed เปลี่ยนไปในตั๋วนี้ ต้อง `npm run seed` ใหม่ก่อนเดิน** หลักสูตร `0503` เพิ่งได้ต้นไม้ PLO ของตัวเอง
สองข้อ (`PLO-1` กับ `PLO-2` พร้อมข้อย่อยสองข้อของ `PLO-2`) และมันมีอยู่เพื่อสองแถวในใบนี้โดยเฉพาะ

- **`0503` มี `PLO-1` เป็นของตัวเอง คนละข้อกับ `PLO-1` ของ `0501`** — นี่คือเกณฑ์ข้อ 5 ทั้งข้อ
  schema เดิมบังคับให้ `outcome_code` ไม่ซ้ำทั้งระบบ ซึ่งห้ามสิ่งนี้ไว้พอดี
- **`PLO-2` ของ `0503` ถูกตั้งลำดับให้อยู่ *เหนือ* `PLO-1`** — บทเรียนจาก
  [#96](https://github.com/khthana/Deep-QA/issues/96) คือ `0501` ตั้ง `sequence_order` เท่ากับเลขในรหัสทุกข้อ
  เรียงตามลำดับกับเรียงตามรหัสจึงให้ผลเหมือนกันเป๊ะ และ assertion ไหนก็ผ่านทั้งคู่
  สองแถวนี้เป็นที่เดียวใน seed ที่การเรียงสองแบบไม่ตรงกัน จึงเป็นที่เดียวที่เกณฑ์ข้อ 4 พิสูจน์ได้

`0501` มี `PLO-1` ถึง `PLO-13` พร้อมข้อย่อย และข้อ 1, 2, 3, 5, 6, 7, 8, 12 ถูกรายวิชา `01076105` map ไว้
— แปดข้อนี้คือข้อที่ลบไม่ออกและจะถูกปิดการใช้งานแทน

**`PLO-14` ไม่มีอยู่ใน seed** — แถวที่เอ่ยถึงมันเป็นแถว ⚙ ซึ่ง `19a-plos.spec.js`
สร้างมันเองใน schema ของ e2e และคนเดินด้วยมือไม่ต้องเดิน แถวการซ้อนสองแถวข้างล่าง
จึงชี้ไปที่ `PLO-12` กับข้อย่อยสองข้อของมันที่ seed ให้มาแล้วแทน

หลายข้อข้างล่างเขียนข้อมูลจริงลงฐานข้อมูล — **`npm run seed` ใหม่หลังเดินจบ** ก่อนเริ่มตั๋วถัดไป

## รายการที่ต้องเดิน

ช่องสุดท้ายมีสี่ค่า

- **☑** เดินจบแล้ว
- **◐** ครึ่งของ server เดินแล้วและผ่าน ส่วนที่เหลือคือสิ่งที่หน้าจอวาด (แถบสี ตาราง dropdown) ยังต้องมีคนดู
- **☐** ยังไม่ได้เดิน
- **⚙** ครอบคลุมด้วย browser seam แล้ว ไม่ต้องเดินด้วยมือซ้ำ ถ้าแถวนั้นพูดถึงสิ่งที่วาดด้วย ครึ่งนั้นยังเป็นงานของคนเดิน และแถวจะเขียนกำกับไว้

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| 1 | กรรมการหลักสูตรเพิ่ม PLO ของหลักสูตรตัวเองได้ | เข้าด้วย `prog.manager@kmitl.ac.th` → *กำหนดผลการเรียนรู้ระดับหลักสูตร PLO* → *เพิ่มผลการเรียนรู้* → รหัส `PLO-14` ชื่ออะไรก็ได้ ประเภท *ทักษะ* ลำดับ `14` → บันทึก | แถบเขียว *บันทึกข้อมูลเรียบร้อยแล้ว* และแถวใหม่โผล่ท้ายรายการ — ครอบคลุมโดย `19a-plos.spec.js` ซึ่งเพิ่มผ่านฟอร์มจริงแล้ว assert ว่าแถวโผล่ | ⚙ |
| 1 | ช่องหลักสูตรไม่ต้องเลือกเมื่อมีหลักสูตรเดียว | ดูหัวหน้ารายการด้วยบัญชี `prog.manager@kmitl.ac.th` | เขียนว่า *หลักสูตร `0501` วิศวกรรมคอมพิวเตอร์* เป็นข้อความ **ไม่ใช่ dropdown** — สิ่งที่เลือกได้คือสิ่งที่ server จะรับ ฟอร์มจึงสร้างคำขอที่ถูกปฏิเสธไม่ได้ — ครอบคลุมโดย `19a-plos.spec.js` ซึ่ง assert ว่าไม่มี combobox บนหน้าเลย (เกณฑ์ข้อ 3 ของ [#101](https://github.com/khthana/Deep-QA/issues/101) พูดเรื่องเดียวกัน แต่คนละบัญชีและคนละ assertion จึงเป็นแถวของมันเองข้างล่าง) | ⚙ |
| 1 | เปิดฟอร์มแล้วยกเลิก ไม่สร้างอะไรทิ้งไว้ | *เพิ่มผลการเรียนรู้* → *ยกเลิก* | รายการเหมือนเดิมทุกแถว — ครอบคลุมโดย `19a-plos.spec.js` ซึ่งเทียบรายชื่อรหัสก่อนและหลัง | ⚙ |
| 1 | แก้ไขได้ | *แก้ไข* ที่แถว `PLO-14` → เปลี่ยนชื่อ → บันทึก | ชื่อใหม่ปรากฏในตาราง — ครอบคลุมโดย `19a-plos.spec.js` (แถวเดียวกับการเปลี่ยนลำดับ) | ⚙ |
| 1 | ลบได้จริงเมื่อไม่มีใครอ้างอิง | *ลบ* ที่แถว `PLO-14` → ยืนยัน | แถบเขียว *ลบผลการเรียนรู้เรียบร้อยแล้ว* และแถวหายไป — ครอบคลุมโดย `19a-plos.spec.js` | ⚙ |
| 2 | สร้างข้อย่อยใต้ข้ออื่นได้ และข้อย่อยเรียงอยู่ใต้ข้อหลักของตัวเอง | *เพิ่มผลการเรียนรู้* → รหัส `PLO-14-2` ช่อง *อยู่ใต้ข้อ* เลือก `PLO-14` ลำดับ `2` → บันทึก แล้วทำซ้ำด้วย `PLO-14-1` ลำดับ `1` | ทั้งสองแถวอยู่ **ติดใต้** `PLO-14` และ `PLO-14-1` อยู่ก่อน `PLO-14-2` แม้จะสร้างทีหลัง — ครอบคลุมโดย `19a-plos.spec.js` ซึ่ง assert ตำแหน่งทั้งสองเทียบกับข้อหลัก | ⚙ |
| 2 | **การซ้อนมองเห็นได้จริงบนหน้าจอ** | เข้าด้วย `prog.manager@kmitl.ac.th` → ดูตารางตรง `PLO-12` กับ `PLO-12-1`, `PLO-12-2` ที่ seed ให้มาแล้ว | แถวข้อย่อย **เยื้องเข้าไป** จากข้อหลัก มี **เส้นแนวตั้งทางซ้าย** ของมัน และมีคำว่า *ข้อย่อย* กำกับ ส่วนข้อหลักเป็นตัวหนา (`font-bold`) — ต้นไม้ที่ *อ่านออกว่าเป็นต้นไม้* เป็นสิ่งที่หน้าจอวาด browser seam รับไปได้แค่ *ลำดับ* ของแถว | ☑ |
| 2 | ข้อย่อยของข้อย่อยเยื้องอีกชั้น | *เพิ่มผลการเรียนรู้* → รหัส `PLO-12-1-1` ช่อง *อยู่ใต้ข้อ* เลือก `PLO-12-1` → บันทึก | แถวใหม่เยื้องลึกกว่า `PLO-12-1` อีกหนึ่งขั้น — ความลึกมาจาก server (ข้อหลักบวกหนึ่ง) ไม่ได้มาจากฟอร์ม | ☑ |
| 3 | ตัวเลือกประเภทอ่านเป็นภาษาคน | เปิดฟอร์ม *เพิ่มผลการเรียนรู้* → กดที่ช่อง *ประเภท* | มีสี่ตัวเลือกพอดี คือ *ความรู้* *ทักษะ* *จริยธรรม* *ลักษณะบุคคล* ไม่มีคำภาษาอังกฤษโผล่ และไม่มีตัวเลือกที่ห้า — ค่าที่ห้าถูกปฏิเสธที่ server แล้ว (`plos.test.js`) แถวนี้คือถ้อยคำบนจอ | ☑ |
| 3 | ประเภทแสดงเป็นภาษาคนในตารางด้วย | ดูคอลัมน์ *ประเภท* ของ `PLO-10` (จริยธรรม) กับ `PLO-12` (ลักษณะบุคคล) | ป้ายอ่านว่า *จริยธรรม* และ *ลักษณะบุคคล* ไม่ใช่ `ethics` / `character` | ☑ |
| 4 | รายการเรียงตามลำดับที่ตั้งไว้ ไม่ใช่ตามรหัส | เข้าด้วย `prog.manager.0503@kmitl.ac.th` → หน้านี้ | `PLO-2` อยู่ **เหนือ** `PLO-1` เพราะลำดับของมันคือ 1 ส่วนของ `PLO-1` คือ 2 และข้อย่อยของ `PLO-2` ตามมาติด ๆ ก่อนจะถึง `PLO-1` — ครอบคลุมโดย `19a-plos.spec.js` | ⚙ |
| 4 | เปลี่ยนลำดับแล้วแถวขยับจริง | ด้วย `prog.manager@kmitl.ac.th` → *แก้ไข* ที่ `PLO-14-1` → ช่อง *ลำดับการแสดงผล* เป็น `3` → บันทึก | `PLO-14-1` ย้ายไปอยู่ **ใต้** `PLO-14-2` และยังอยู่ใต้ `PLO-14` ทั้งคู่ — ครอบคลุมโดย `19a-plos.spec.js` ซึ่ง assert เป็นการ *ย้าย* ไม่ใช่การอ่านตัวเลข | ⚙ |
| 5 | สองหลักสูตรมีรหัสเดียวกันได้ | เข้าด้วย `dept.admin.05@kmitl.ac.th` → เลือกหลักสูตร `0501` ดู `PLO-1` แล้วสลับไป `0503` ดู `PLO-1` | เป็น **คนละข้อ** ชื่อคนละชื่อ (ของ `0501` คือเรื่องคณิตศาสตร์และวิทยาศาสตร์ ของ `0503` คือเรื่องบริบทสากล) และแต่ละหลักสูตรเห็นของตัวเองข้อเดียว — schema เดิมห้ามสิ่งนี้ไว้ — ครอบคลุมโดย `19a-plos.spec.js` ทั้งจากมุมกรรมการหลักสูตรและมุมผู้ดูแลภาควิชา | ⚙ |
| 5 | **ข้อความรหัสซ้ำบอกว่าซ้ำ *ในหลักสูตรนี้*** | ด้วย `prog.manager@kmitl.ac.th` → *เพิ่มผลการเรียนรู้* → รหัส `PLO-1` → บันทึก | แถบแดง *รหัสผลการเรียนรู้นี้มีอยู่ในหลักสูตรนี้แล้ว* — คำว่า *ในหลักสูตรนี้* คือทั้งตั๋ว ถ้าประโยคอ่านเหมือนการห้ามซ้ำทั้งระบบ คนจะไปเปลี่ยนรหัสที่ไม่ต้องเปลี่ยน | ☑ |
| 6 | PLO ที่รายวิชาอ้างอิงอยู่ ถูกปิดการใช้งานแทนการลบ | ด้วย `prog.manager@kmitl.ac.th` → *ลบ* ที่แถว `PLO-3` (ข้อที่ `01076105` map ไว้ และยังมีข้อย่อย) → ยืนยัน | ได้แถบแดง *ผลการเรียนรู้ข้อนี้ยังมีข้อย่อยอยู่ กรุณาลบข้อย่อยก่อน* ก่อน จากนั้นลบ `PLO-3-1` ถึง `PLO-3-4` ให้หมดแล้วลบ `PLO-3` อีกครั้ง คราวนี้ได้แถบเขียวที่บอกว่า *ระบบจึงปิดการใช้งานแทนการลบ* และแถวยัง**อยู่** สถานะ *ปิดใช้งาน* — ครอบคลุมโดย `19a-plos.spec.js` ซึ่งเดินลำดับเดียวกันนี้บน `PLO-12` | ⚙ |
| 6 | เปิดกลับได้จากหน้าเดิม | *แก้ไข* ที่แถวที่เพิ่งถูกปิด → ช่อง *สถานะ* → *ใช้งานอยู่* → บันทึก | สถานะกลับเป็น *ใช้งานอยู่* — นี่คือทางกลับทางเดียว เพราะรหัสถูกแถวเดิมถือไว้แล้ว เพิ่มใหม่จะชนกัน — ครอบคลุมโดย `19a-plos.spec.js` | ⚙ |
| 6 | ข้อหลักที่ยังมีข้อย่อย ลบไม่ได้ | *ลบ* ที่แถว `PLO-5` (ยังมีข้อย่อยครบ) → ยืนยัน | แถบแดง *ผลการเรียนรู้ข้อนี้ยังมีข้อย่อยอยู่ กรุณาลบข้อย่อยก่อน* และแถวยังอยู่ **สถานะไม่เปลี่ยน** — ถ้ามันกลายเป็น *ปิดใช้งาน* แปลว่า guard หายไปและ `deleteOrDeactivate` อ่านข้อย่อยเป็นการอ้างอิงธรรมดา — ครอบคลุมโดย `19a-plos.spec.js` | ⚙ |
| 7 | ถามยืนยันก่อนลบ และกดยกเลิกแล้วไม่มีอะไรเปลี่ยน | *ลบ* ที่แถวใดก็ได้ → *ยกเลิก* | กล่องยืนยันขึ้นมาก่อน และหลังกดยกเลิกแถวยังอยู่ครบ — ครอบคลุมโดย `19a-plos.spec.js` | ⚙ |
| 7 | **ข้อความในกล่องยืนยันบอกครบสามอย่าง** | ดูกล่องยืนยันตอนที่มันเปิดอยู่ | ประโยคมี **รหัส** และ **ชื่อ** ของข้อที่จะลบ และบอกทั้งสองเงื่อนไข คือถ้ามีรายวิชาหรือ CLO อ้างอิงจะปิดการใช้งานแทน และถ้ายังมีข้อย่อยจะลบไม่ได้ | ☑ |
| 8 | ผู้ดูแลภาควิชาเข้าถึงทั้งสองหลักสูตร | เข้าด้วย `dept.admin.05@kmitl.ac.th` | เห็น dropdown *หลักสูตร* ที่มีทั้ง `0501` และ `0503` (คนละความหมายกับป้ายของกรรมการ) และเลือกแล้วรายการเปลี่ยนตาม — ครอบคลุมโดย `19a-plos.spec.js` | ⚙ |
| 8 | กรรมการหลักสูตรเห็นเฉพาะหลักสูตรตัวเอง | เข้าด้วย `prog.manager.0503@kmitl.ac.th` | เห็นเฉพาะต้นไม้ของ `0503` ไม่มีแถวของ `0501` เลย และไม่มี dropdown ให้สลับ — ครอบคลุมโดย `19a-plos.spec.js` ส่วนการปฏิเสธทุกกริยาที่ server พิสูจน์ใน `plos.test.js` | ⚙ |
| 8 | บัญชีที่ไม่ควรเข้าถึงเลย — ครึ่งที่เป็นกฎ | เข้าด้วย `faculty.admin@kmitl.ac.th` แล้ว `admin@kmitl.ac.th` แล้ว `teacher.one@kmitl.ac.th` → พิมพ์ `/main/plos` ตรง ๆ | ได้แถบแดง *บัญชีนี้ไม่มีสิทธิ์ใช้งานส่วนนี้* และตารางว่างเปล่า ไม่มีปุ่ม *เพิ่มผลการเรียนรู้* — ครอบคลุมโดย `19a-plos.spec.js` ซึ่ง assert สถานะ 403 ของคำขอด้วย ไม่ใช่แค่แถบ | ⚙ |
| 8 | **บัญชีที่ไม่ควรเข้าถึงเลย — ครึ่งที่เป็นเมนู** | ด้วยสามบัญชีเดียวกัน ดูแถบเมนูซ้าย | ไม่มีรายการ *กำหนดผลการเรียนรู้ระดับหลักสูตร PLO* เลย และสำหรับ `faculty.admin@kmitl.ac.th` ไม่มีกลุ่ม *ผลการเรียนรู้* ทั้งกลุ่ม ([#79](https://github.com/khthana/Deep-QA/issues/79)) — รายการในเมนูเป็นสิ่งที่วาด browser seam ไม่รับ | ☑ |
| — | **หัวเรื่องและคำอธิบายใต้หัวเรื่องอ่านรู้เรื่อง** | ดูหัวหน้าจอและย่อหน้าใต้ปุ่ม | หัวข้อว่า *ผลการเรียนรู้ระดับหลักสูตร* และย่อหน้าใต้มันบอกสองเรื่อง คือรหัสเป็นของหลักสูตรใดหลักสูตรหนึ่ง และข้อย่อยแสดงเยื้องตามลำดับที่ตั้งไว้ — **ตรวจช่องว่างกลางคำไทยด้วย** JSX ต่อสองบรรทัดด้วยช่องว่างหนึ่งตัวเสมอ | ☑ |
| — | **breadcrumb สะกดถูก** | ดูแถบ breadcrumb เหนือหัวเรื่อง | อ่านว่า *การกำหนดผลการเรียนรู้ระดับหลักสูตร PLO* — เดิมสะกดว่า *ระกับ* แก้ในตั๋วนี้ | ☑ |
| — | รหัสที่เป็นตัวเลขล้วนแต่กว้างเกินคอลัมน์ ตอบ*ไม่พบ* ไม่ใช่ *เกิดข้อผิดพลาดในระบบ* | อ่าน `backend/test/plos.test.js` ข้อ *an id too large for the column is ไม่พบ, not a 500* | ทั้ง `2147483648` และ `99999999999999999999` ได้ 404 พร้อมประโยค *ไม่พบผลการเรียนรู้ที่ระบุ* เดิมของโมดูล — ก่อน [#107](https://github.com/khthana/Deep-QA/issues/107) ทั้งคู่เป็น 22003 → 500 · `107:shapeonly` ค้ำไว้ (`107:nobound` ค้ำเฉพาะครึ่งที่เป็นเพดานของ `integer` คือ `2147483648`) | ☑ |
| — | ตัวกรองหลักสูตรไม่มี *ทุกหลักสูตร* อีกต่อไป ([#101](https://github.com/khthana/Deep-QA/issues/101)) | เข้าด้วย `dept.admin.05@kmitl.ac.th` → กดที่ dropdown *หลักสูตร* | มีสองตัวเลือกพอดี คือ `0501` กับ `0503` **ไม่มีตัวที่สาม** — รายการที่ปนสองหลักสูตรไม่ใช่มุมมองของสิ่งใด เพราะรหัส PLO ซ้ำกันข้ามหลักสูตรได้ (`UNIQUE (program_id, outcome_code)`) — ครอบคลุมโดย `19a-plos.spec.js` แถว *the curriculum filter offers the curricula in reach and nothing wider* | ⚙ |
| — | เข้าหน้ามาแล้วอยู่ที่หลักสูตรแรกในสิทธิ์ที่ยังใช้งานอยู่ ไม่ใช่หน้าว่าง ([#101](https://github.com/khthana/Deep-QA/issues/101)) | เข้าด้วย `dept.admin.05@kmitl.ac.th` → หน้านี้ โดยยังไม่แตะตัวกรอง | dropdown ชี้ที่ `0501` อยู่แล้ว และตารางมีแถวของ `0501` อยู่แล้ว — ครอบคลุมโดย `19a-plos.spec.js` แถว *the screen lands on the first curriculum in reach* · คำว่า *ที่ยังใช้งานอยู่* เป็นครึ่งที่**ไม่มีอะไรพิสูจน์ได้** เพราะหลักสูตรทุกหลักสูตรใน seed ยังใช้งานอยู่ทั้งหมด จึงเป็นคำตอบที่สามของ #107 — ไม่ใช่*พิสูจน์แล้ว* และไม่ใช่*ไปไม่ถึง* แต่*ยังไม่มีเทสต์* | ⚙ |
| — | ทุกแถวบนจอเป็นของหลักสูตรที่ตัวกรองบอก ([#101](https://github.com/khthana/Deep-QA/issues/101)) | ด้วยบัญชีเดิม → สลับ dropdown ไป `0503` แล้วอ่านคอลัมน์ *หลักสูตร* ทั้งคอลัมน์ | ทุกแถวเขียนชื่อหลักสูตรเดียวกัน ไม่มีแถวของอีกหลักสูตรปนอยู่ ทั้งสองค่าของตัวกรอง — ครอบคลุมโดย `19a-plos.spec.js` แถว *every row on screen is of the curriculum the filter names* | ⚙ |
| — | คำขอที่ไม่ระบุหลักสูตร ถูกปฏิเสธที่ server ไม่ใช่แค่หน้าจอสัญญาว่าจะไม่ส่ง ([#101](https://github.com/khthana/Deep-QA/issues/101)) | อ่าน `backend/test/plos.test.js` ข้อ *the list refuses to answer without a curriculum, so no answer can mix two* | `GET /api/plos` ที่ไม่มี `program_id` (รวมทั้งค่าว่าง ช่องว่าง และ `?program_id=0501&program_id=0503` ที่ระบุสองหลักสูตรตรงๆ) ตอบ 400 พร้อม `REFUSALS.ploProgramRequired` ไม่มี `plos` ในคำตอบ — คำขอสุดท้ายนี้เคยตอบ 200 พร้อมรายการว่าง เพราะ query string ซ้ำ key ได้ และค่าที่มาเป็น array ซึ่งไม่ว่างและไม่ขาด (`/code-review` เจอ) — เกณฑ์ข้อ 4 จึงเป็นโครงสร้าง ไม่ใช่คำสัญญาที่หน้าจอรักษาไว้ · `programoptional` ค้ำไว้ (ตายที่ `node --test` แถวนี้จึงเป็น ☑ ไม่ใช่ ⚙) | ☑ |
| — | ช่อง *อยู่ใต้ข้อ* เสนอข้อของหลักสูตรที่ฟอร์มอยู่ ไม่ใช่ของตาราง ([#101](https://github.com/khthana/Deep-QA/issues/101)) | ด้วย `dept.admin.05@kmitl.ac.th` ที่ตารางเป็น `0501` → *เพิ่มผลการเรียนรู้* → เปลี่ยนช่อง *หลักสูตร* ในฟอร์มเป็น `0503` → กดช่อง *อยู่ใต้ข้อ* | มีสี่ข้อของ `0503` ให้เลือก ไม่ใช่ห้าสิบสองข้อของ `0501` และไม่ใช่ช่องว่าง — ก่อน [#101](https://github.com/khthana/Deep-QA/issues/101) ช่องนี้เติมด้วยคำขอที่ไม่ระบุหลักสูตร นี่คือ caller ที่ตั๋วบอกว่า *ถ้ามี* ค่อยคิดใหม่ — ครอบคลุมโดย `19a-plos.spec.js` แถว *the add form moved to the other curriculum* | ⚙ |
| — | กรรมการหลักสูตรที่ดูแลหลักสูตรเดียว ถูกบอกว่าเป็นหลักสูตรไหน ไม่ใช่ถูกถาม ([#101](https://github.com/khthana/Deep-QA/issues/101)) | เข้าด้วย `prog.manager.0503@kmitl.ac.th` → หน้านี้ | เขียนว่า *`0503` วิศวกรรมคอมพิวเตอร์ (หลักสูตรนานาชาติ)* เป็นข้อความ และ**ไม่มี dropdown หลักสูตรเลย** — การเอาตัวเลือกออกจาก dropdown ห่างจากการวาด dropdown ทั้งที่ไม่มีอะไรให้เลือกอยู่แค่การแก้เดียว เกณฑ์ข้อนี้จึงต้องมีแถวของตัวเอง — ครอบคลุมโดย `19a-plos.spec.js` แถว *a committee member reaching one curriculum is told which, and is given no filter* · `alwaysadropdown` ค้ำไว้ | ⚙ |

## บันทึกการเดินด้วยมือ

เดินครบเมื่อ 23 ส.ค. 2569 ผลรวมของเอกสารนี้คือ **11 ☑ · 21 ⚙ · 0 ◐ · 0 ☐**
(ก่อน [#101](https://github.com/khthana/Deep-QA/issues/101) คือ 10 ☑ · 16 ⚙ — ตัวเลขนี้นับจากตาราง ไม่ใช่หิ้วจากบรรทัดนี้)

แถว ⚙ ทั้งยี่สิบเอ็ดถูกครอบด้วย `19a-plos.spec.js` และมัตแตนต์ของมัน อ่านผลจากชื่อเทสต์ที่ตาย
อีกเก้าแถวเดินด้วยมือทีละแถว ติ๊กจากสิ่งที่ผู้เดินเห็นและรายงานกลับมาเท่านั้น ส่วนอีกสองแถวที่เหลือเป็นของ [#107](https://github.com/khthana/Deep-QA/issues/107) กับ [#101](https://github.com/khthana/Deep-QA/issues/101) พิสูจน์ที่ seam ของ HTTP ไม่ใช่ด้วยการเดิน

**สิ่งที่การเดินจับได้ และเทสต์สองชั้นจับไม่ได้**

ข้อหลักในคอลัมน์ *รหัส* ใช้ `font-medium` (น้ำหนัก 500) ส่วนข้อย่อยเป็นค่าปกติของ `td` (400)
บนฟอนต์ไทยขนาดเล็กสองค่านี้แยกด้วยตาไม่ออก ผู้เดินรายงานว่า *เหมือนจะเท่ากัน*
ขณะที่แถวในใบนี้อ้างว่าข้อหลักเป็น *ตัวหนา* ซึ่งคือ `font-bold` คนละ token กัน
แก้ `frontend/src/pages/Plos.js` เป็น `font-bold` แล้วให้ผู้เดินดูซ้ำ คราวนี้เห็นชัด
และเขียนชื่อ token กำกับลงในแถวนั้นแล้ว เพื่อไม่ให้คนอ่านรอบหน้าต้องตีความคำว่า *ตัวหนา* เอง

> บทเรียนเดียวกับ `สีเหลือง` ที่จริง ๆ เป็น `amber-700`:
> **เขียนใบเดินงานด้วยชื่อ token ไม่ใช่ด้วยคำที่ตาคนใช้**

browser seam ยืนยันได้แค่ว่าแถวข้อย่อยอยู่ *ถัดจาก* แถวข้อหลัก คำถามว่าคนอ่านแล้วเห็นเป็นต้นไม้ไหม
มีแต่ตาคนตอบได้ และคำตอบแรกคือ *ไม่*

**สองอย่างที่พบระหว่างเตรียมเดิน เปิดเป็น [#97](https://github.com/khthana/Deep-QA/issues/97) แล้ว**

- `frontend/src/api/client.js:80` ยกกล่อง *Session หมดอายุ* เมื่อได้ 401 ทุกกรณีโดยไม่ดู `reason`
  ทั้งที่ server แยก `anonymous` / `expired` / `invalid` มาให้แล้ว และ `AuthContext.js:45` ก็อ่าน `reason` ถูก
  ผิดเกณฑ์ข้อ 6 ของ #10 ตรง ๆ
- `frontend/src/pages/Login.js:88` เรียก `post('/api/auth/login', …)` โดยไม่ทำเครื่องหมาย anonymous
  จึงเป็นเหยื่อรายแรกของข้อบน — อีเมลหรือรหัสผ่านผิดได้กล่อง *Session หมดอายุ* ทับประโยคจริง
  ซึ่งกินเวลาการเดินรอบนี้ไปหลายรอบก่อนจะรู้ว่าไม่ใช่เรื่อง session เลย

ตอนเขียนตั๋วเจอที่ที่สาม `backend/routes/auth.js:124` ส่งออกแค่ `message` ทิ้ง `reason`
ของ `refuse(401, 'credentials')` ไป ฝั่ง client จึงแยกสองกรณีนี้ไม่ได้แม้จะอยากแยก

**ที่ต้องทำหลังเดินจบ** หลายแถวเขียนข้อมูลจริงลงฐาน — `PLO-12-1-1` ถูกสร้างขึ้นและยังอยู่
`npm run seed` ใน `db/` ล้างได้ และควรล้างก่อนเริ่มตั๋วถัดไป


## มัตแตนต์

`mutation/19-plos.py` — สิบหกตัว ส่วนใหญ่คู่กับแถวเดียว แต่สามตัวไม่ใช่ — `alwaysadropdown` สองแถว `nofilter` สามแถว `landsonthelast` สองแถว และแถวการลบมีสามครึ่งที่พังแยกกันได้ (จำนวนในคอลัมน์ขวาคือของจริง บรรทัดนี้เคยเขียนว่า *แต่ละตัวคู่กับแถวหนึ่งแถว* อยู่เหนือตารางที่ขัดกันเอง)
(สิบสองของ #19 บวกอีกสี่ตัวของ [#101](https://github.com/khthana/Deep-QA/issues/101) หนึ่งในนั้นตายที่ `node --test` จึงค้ำแถว ☑)
ดู `mutation/README.md` ก่อนเชื่อหรือเขียนใหม่

`19a-plos.spec.js` รันแบบ `serial` แถวแรกที่ตายจะหยุดแถวที่เหลือทั้งหมด ผลจึงอ่านจาก
**ชื่อเทสต์ที่ตาย** เท่านั้น นับจำนวน *1 failed* ไม่ใช่หลักฐาน — มัตแตนต์ที่พลาดไปฆ่าแถวก่อนหน้านี้
ก็นับหนึ่งเหมือนกัน ตารางข้างล่างถูกตรวจด้วยวิธีนี้ ครบทั้งสิบหกตัว โดยมี baseline ที่ผ่านครบ 17 ข้อคั่นอยู่
แถวของ [#101](https://github.com/khthana/Deep-QA/issues/101) ถูก *ต่อท้าย* ไม่ใช่แทรกกลาง เลขแถวของ `19a` จึงไม่ขยับ
ตารางนี้จึงอ้างด้วย *ชื่อ* เทสต์เสมอ ไม่ใช่หมายเลข

| มัตแตนต์ | ทำอะไรพัง | แถวที่ตาย |
|---|---|---|
| `alwaysadropdown` | หน้ารายการวาด dropdown แม้มีหลักสูตรเดียว | สองแถว — *ช่องหลักสูตรไม่ต้องเลือกเมื่อมีหลักสูตรเดียว* ของ #19 บวกแถวกรรมการหลักสูตรของ [#101](https://github.com/khthana/Deep-QA/issues/101) — ตัวที่สองเพิ่งนับได้หลังจาก locator ของตัวกรองเลิกดูเฉพาะ `0501` |
| `codeorder` | เรียงต้นไม้ตาม `outcome_code` แทน path ของลำดับ | รายการเรียงตามลำดับที่ตั้งไว้ ไม่ใช่ตามรหัส |
| `childpath` | path ของกิ่งเวียนไม่อ่าน `sequence_order` (ข้อหลักคงเดิม) | ข้อย่อยเรียงอยู่ใต้ข้อหลักของตัวเอง |
| `ordernotsaved` | การแก้ไขไม่เขียนคอลัมน์ `sequence_order` | เปลี่ยนลำดับแล้วแถวขยับจริง |
| `pickeroffersall` | ช่อง *อยู่ใต้ข้อ* เสนอทุกข้อ รวมทั้งตัวเองและลูกหลาน | ตัวเลือกข้อหลักไม่เสนอสิ่งที่ server จะปฏิเสธ |
| `hardcancel` | ปุ่ม *ยกเลิก* ในกล่องยืนยันทำสิ่งที่ปุ่มยืนยันทำ | ถามยืนยันก่อนลบ และกดยกเลิกแล้วไม่มีอะไรเปลี่ยน |
| `nochildguard` | ทิ้งการถามเรื่องข้อย่อย ปล่อยให้ `deleteOrDeactivate` จัดการ | ข้อหลักที่ยังมีข้อย่อย ลบไม่ได้ |
| `saysdeleted` | ตอบ `deactivated: false` ทั้งที่ปิดการใช้งาน | PLO ที่ถูกอ้างอิง ถูกปิดการใช้งานแทนการลบ (ครึ่งแถบข้อความ) |
| `hideinactive` | รายการซ่อนข้อที่ปิดการใช้งาน | ครึ่งที่แถวยังอยู่ในตาราง |
| `statusignored` | `PUT` ไม่เขียน `is_active` | เปิดกลับได้จากหน้าเดิม |
| `nofilter` | `?program_id=` ไม่ถูกใช้ (เล็งใหม่ใน [#101](https://github.com/khthana/Deep-QA/issues/101) หลังจาก `IS NULL OR` หายไป) | สามแถว — *ผู้ดูแลภาควิชาเลือกหลักสูตรแล้วรายการเปลี่ยนตาม* ของ #19 บวกสองแถวของ [#101](https://github.com/khthana/Deep-QA/issues/101) วัดทีละแถวด้วย `--grep` |
| `alwaysallprograms` | ใส่ตัวเลือก *ทุกหลักสูตร* กลับเข้าตัวกรอง | ตัวกรองหลักสูตรไม่มี *ทุกหลักสูตร* อีกต่อไป (แถวเดียว) |
| `landsonthelast` | หน้าจอเลือกหลักสูตรสุดท้ายในสิทธิ์ แทนที่จะเลือกตัวแรก | เข้าหน้ามาแล้วอยู่ที่หลักสูตรแรก และแถวช่อง *อยู่ใต้ข้อ* ซึ่งตั้งต้นด้วยข้อเดียวกัน |
| `formkeepsthetable` | ช่อง *อยู่ใต้ข้อ* อ่านจากหลักสูตรที่ตารางกรองไว้ ไม่ใช่ของหลักสูตรที่ฟอร์มอยู่ | ช่อง *อยู่ใต้ข้อ* เสนอข้อของหลักสูตรที่ฟอร์มอยู่ (แถวเดียว) |
| `programoptional` | `GET /plos` เลิกปฏิเสธคำขอที่ไม่ระบุหลักสูตร | ที่ seam ของ HTTP: *the list refuses to answer without a curriculum* — ตายแถวเดียวใน 717 แถว |
| `facultyadmin` | ใส่ `FACULTY_ADMIN` กลับเข้า `MAINTAINERS` | บัญชีที่ไม่ควรเข้าถึงเลย — ครึ่งที่เป็นกฎ |
| `102:anydepartmentreachesall` | grant ที่ผูกกับภาควิชา เอื้อมถึงทุกหลักสูตร | ที่ seam ของ HTTP: `a department administrator reaches both curricula under their department` — แถวนี้เคยอ่านด้วย `includes` สองครั้ง รายการที่กว้างเกินจึงผ่าน [#102](https://github.com/khthana/Deep-QA/issues/102) เปลี่ยนเป็นทั้งรายการ |
