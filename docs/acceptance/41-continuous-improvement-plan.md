# ตรวจรับด้วยมือ — #41 Continuous improvement plan

`docs/06-implementation-plan.md` §Acceptance กำหนดว่า ticket หนึ่งจะเสร็จเมื่อ automated test ผ่าน
**และ** checklist ที่ทำด้วยมือถูกเดินจนครบ เอกสารนี้คือครึ่งหลังของ #41

ครึ่งแรกอยู่ที่ `backend/test/improvement-plan.test.js` (18 subtests) — ทุกเกณฑ์ที่พิสูจน์ได้ที่ seam ของ
HTTP surface ถูกพิสูจน์ที่นั่นแล้ว รวมทั้งสิ่งที่หน้าจอไปไม่ถึง: **การอ่านไม่สร้างรอบปี** ·
ปีที่ถูกหยิบมาอ้างอิงเมื่อปีก่อนหน้าว่างเปล่าหรือไม่มีการเปิดสอน · `reference_academic_year`
ที่เซิร์ฟเวอร์เขียนเองและ body ที่อ้างปีอื่นถูกเพิกเฉย · หัวข้อนอกสี่ค่า · และรอบปีที่ยังอยู่หลังบันทึกสุดท้ายถูกลบ

seam ที่สองคือเบราว์เซอร์ — `e2e/tests/41a-improvement-plan.spec.js` (9 แถว) ดูหัวข้อ
*มัตแตนต์ที่พิสูจน์แถว ⚙* ท้ายเอกสาร

## สิ่งที่ต้องเข้าใจก่อนเดิน

**บันทึกเป็นของรายวิชาที่เปิดสอน ไม่ใช่ของตอนเรียน** ADR-0003 และตารางที่รับมาก็อยู่ที่ grain นี้
มาก่อนการรื้อสร้างแล้ว (`clo_course_cycle_cloplan` มี key เป็น subject + program + ปีการศึกษา)
ผู้สอนสองคนของสองตอนเรียนจึงเขียนบันทึกชุดเดียวกัน คนหนึ่งบันทึกอีกคนเห็น และแก้ของกันได้
เป็นสิ่งที่ต่างจาก **แผนการสอน** ของ #31 ซึ่งเป็นของตอนเรียน — หัวจอจึงเขียนบอกไว้

**บันทึกของปีก่อนจับคู่ด้วย *เลขข้อ* ไม่ใช่ `clo_id`** เรื่องนี้ผิดได้ง่ายและมองไม่เห็นเมื่อผิด
ADR-0003 ให้ CLO ของแต่ละปีเป็นแถวของตัวเอง CLO-4 ของปีที่แล้วกับของปีนี้จึงเป็นคนละ `clo_id`
ถ้า join ด้วย id แผงอ้างอิงจะว่างเปล่าทุกรายวิชาตลอดไป — และแผงว่างหน้าตาเหมือนปีที่ไม่มีใครเขียนอะไร

**ปีที่ถูกหยิบมาอ้างอิงคือปีล่าสุดที่มีใครเขียนไว้ ไม่ใช่ปีนี้ลบหนึ่ง** รายวิชาไม่ได้เปิดสอนทุกปี
และรอบปีที่ถูกเปิดแล้วปล่อยว่างก็มีได้ ทั้งสองกรณีจะทำให้แผงว่างถ้ากติกาคือ "ปีก่อนหน้า"

**`reference_academic_year` เซิร์ฟเวอร์เขียนเอง และเขียนเฉพาะ `IMPROVEMENT`** docs/02 นิยาม
หัวข้อนี้ว่า *การปรับปรุงจากรอบก่อนหน้า* จึงเป็นหัวข้อเดียวในสี่ที่ชี้ไปปีอื่น ปีที่ชี้คือปีที่แผงอ้างอิง
กำลังแสดงอยู่ การอ่านค่านี้จาก body คือ ADR-0002 ในหน่วยที่เล็กที่สุดที่มันปรากฏในระบบ
**เกณฑ์ทั้งหกข้อไม่ได้เอ่ยถึงคอลัมน์นี้ตรง ๆ** — ที่เก็บไว้เพราะ *what was changed following last
year's reflection* ในหัวตั๋วต้องการบอกได้ว่า *ตามการสะท้อนคิดของปีไหน* และคอลัมน์มีอยู่แล้วในตาราง
ถ้าจะตัดออก ให้ตัดทั้งคอลัมน์ ไม่ใช่ปล่อยให้ว่างไว้เฉย ๆ

**แผงอ้างอิงมีสถานะว่างสองแบบ และจงใจทำไม่เหมือนกัน** ไม่มีปีก่อนที่เขียนไว้เลย → **ไม่วาดแผง**
มีปีก่อนแต่ข้อที่เลือกไม่ได้ถูกเขียนถึง → **วาด และบอกว่าไม่มี** ข้อบกพร่องของ #40 คือ *ตัวควบคุม*
ที่กดแล้วเปิดออกมาว่าง ส่วนอันนี้เป็น *ประโยคบอกข่าว* และเป็นครึ่งที่มีประโยชน์ของคำตอบ —
*ปีที่แล้วไม่มีใครเขียนถึงข้อนี้* กับ *ไม่มีปีที่แล้ว* เป็นคนละข่าว แผงที่หายไปจะรายงานทั้งสองเหมือนกัน
และจะกะพริบเข้าออกทุกครั้งที่เลื่อน dropdown

**รอบปีถูกสร้างตอนบันทึกครั้งแรก ไม่ใช่ตอนเปิดหน้าจอ** เกณฑ์ข้อ 3 ขอแบบนั้นตรง ๆ และเหตุผลที่ต้อง
อยู่ฝั่งเขียนคือ ผู้สอนที่เปิดดูแล้วปิดไปยังไม่ได้เริ่มรอบ แถวที่บอกว่าเริ่มแล้วคือปีที่อยู่ในระเบียน
โดยไม่มีอะไรอยู่ในนั้น ซึ่งคณะกรรมการประเมินอ่านว่าเป็นรอบที่ถูกทิ้งกลางคัน

**ตารางนี้ไม่มีคอลัมน์บอกว่าใครเขียน** `clo_course_cycle_detail_cloplan` มีแต่ `created_at`
ไม่มี `created_by` และไม่มี `updated_at` ต่างจาก `subject_clo` ของ #27 ที่มี `updated_by`
เกณฑ์ของ #41 ไม่ได้ขอ จึงไม่ได้เพิ่ม migration ให้ — และหน้าจอก็**ไม่แสดงเวลา**เลย เพราะ
`created_at` ของแถวที่ถูกแก้แล้วยังเป็นเวลาที่เขียนครั้งแรก การพิมพ์มันข้างข้อความที่เพิ่งแก้
คือการบอกเวลาที่ผิด ถ้าอยากได้ว่าใครแก้ล่าสุด ต้องเปิดตั๋วใหม่พร้อม migration

## ตั้งต้น

```
docker start deep-core-postgres
cd db      && npm run reset && npm run migrate && npm run seed
cd backend && npm start          # http://localhost:3000
cd frontend && npm start         # http://localhost:5000
```

**ต้อง reset + seed ใหม่ก่อนเดินใบนี้ และต้อง kill แล้ว start backend ใหม่** — `node server.js`
ไม่มี reload ทุกบัญชีใน seed ใช้รหัสผ่าน `deep-core-local`

ทางเข้า: เลือกตอนเรียนจากหน้า *รายวิชา* → เมนู **การปรับปรุงอย่างต่อเนื่อง** ที่อยู่คือ
`/teacher/teacherDashboard/<section_id>/ContinuousImprove`

**seed ไม่มีรอบปีเลย** — `db/seed.js` เขียนไว้ตรง ๆ ว่าเว้นไว้ให้ตั๋วที่สร้างหน้าจอนี้ ดังนั้นหน้าจอ
ที่เปิดครั้งแรกจะไม่มีบันทึกและไม่มีแผงอ้างอิง ซึ่งเป็นสถานะตั้งต้นที่ถูกต้อง

| `section_id` | ปีการศึกษา | ผู้สอน | ใช้เดินอะไร |
|---|---|---|---|
| `1` | 2569 (ปัจจุบัน) | `teacher.one@` | เกือบทุกข้อ |
| `2` | 2569 (ปัจจุบัน) | `multi.role@` | ข้อ 5 — สองตอนเรียนของรายวิชาที่เปิดสอนเดียวกัน |
| `3` | **2568 (ปีก่อน)** | `teacher.one@` | ข้อ 4 — เขียนของปีก่อนไว้แล้วกลับมาดูที่ปีนี้ |

## รายการที่ต้องเดิน

ช่องสุดท้ายมีสี่ค่า — **☑** เดินจบแล้ว · **◐** ครึ่งของ server เดินแล้วและผ่าน ส่วนที่เหลือคือสิ่งที่
หน้าจอวาด · **☐** ยังไม่ได้เดิน · **⚙** ครอบคลุมด้วย browser seam แล้ว (แถวบอกไฟล์)

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| 1 | บันทึกได้ครบทั้งสี่หัวข้อ สำหรับ **CLO ที่เลือก** | — | ครอบคลุมโดย `41a` แถว 1 · 2 · 5 (`onlythreesections` ค้ำว่ามีสี่หัวข้อ · `savesthelabel` ค้ำว่าสิ่งที่พิมพ์คือสิ่งที่บันทึก · `writesagainstthefirstoutcome` ค้ำว่าบันทึกลง CLO ที่เลือก · `everyoutcomeshowseverything` ค้ำว่าอ่านกลับมาตาม CLO ที่เลือก) — **ไม่รวมครึ่งที่ว่าด้วยปี** ดูแถวถัดไป | ⚙ |
| 1 | …และของ **ปีการศึกษา** ที่ถูกต้อง | — | **พิสูจน์ที่ seam HTTP เท่านั้น** (`improvement-plan.test.js` — สองปีของรายวิชาเดียวกันมีบันทึกคนละชุด) ปีมาจากตอนเรียนในที่อยู่ตาม ADR-0004 **ไม่มีตัวควบคุมบนจอให้เลือกปี** คนที่จะเขียนของปีอื่นต้องเปิดตอนเรียนของปีนั้น เบราว์เซอร์จึงสร้างคำขอที่ระบุปีเองไม่ได้ ครึ่งที่เดินได้คือหัวจอบอกปีถูกต้อง ซึ่งอยู่ในแถว *ประโยคบอก grain* — ◐ ถาวรโดยโครงสร้าง | ◐ |
| 1 | ถ้อยคำของสี่หัวข้อ และคำอธิบายใต้หัวข้อ | เปิดหน้าจอ | *สรุปผลการดำเนินงาน* · *การสะท้อนคิด* · *การปรับปรุงจากรอบก่อนหน้า* · *แนวทางพัฒนาครั้งถัดไป* เรียงตามนี้ ตรงกับ docs/02 §detail_type และแต่ละหัวข้อมีบรรทัดสีจางบอกว่าช่องนั้นเขียนอะไร | ☑ |
| 1 | ตัวเลือก CLO อ่านออกและเลือกได้ | เปิด dropdown *ผลการเรียนรู้ที่กำลังเขียนถึง* | เก้าข้อ CLO-1..CLO-9 แต่ละบรรทัดเป็น `CLO-n · <รายละเอียด>` — และรายละเอียดยาว ๆ ไม่ล้นกรอบ | ☑ |
| 2 | แก้ไขได้ และลบได้โดยถามก่อน | — | ครอบคลุมโดย `41a` แถว 3 · 4 (`editkeepstheoldwords` ค้ำว่าบันทึกซ้ำคือการแก้จริง · `editorstartsempty` ค้ำว่าเปิดแก้แล้วได้ข้อความเดิม · `cancelalsoremoves` ค้ำว่า *ยกเลิก* ไม่ลบ) | ⚙ |
| 2 | ประโยคในกล่องยืนยัน | กดถังขยะบนหัวข้อใดก็ได้ | *ต้องการลบ&lt;หัวข้อ&gt;ของ CLO-n ในปีการศึกษา 2569 หรือไม่ ทุกตอนเรียนของรายวิชานี้จะไม่เห็นข้อความนี้อีก* — ประโยคเอ่ยทั้งหัวข้อ ข้อ ปี และผลที่ตามมา | ☑ |
| 3 | รอบปีถูกสร้างตอนบันทึก ไม่ต้องตั้งค่าก่อน | หลัง reset + seed ใหม่ เปิดหน้าจอแล้วเขียนหัวข้อแรก | **ครึ่ง server พิสูจน์แล้ว** (การอ่านไม่สร้างแถว และบันทึกสี่ครั้งได้รอบปีเดียว) ครึ่งหน้าจอเดินมือแล้ว: ไม่มีขั้นตอนตั้งค่าใด ๆ ก่อนเขียน ไม่มีปุ่ม *สร้างรอบปี* กด *บันทึก* แล้วผ่านทันที | ☑ |
| 4 | เห็นบันทึกของปีก่อนขณะเขียนปีนี้ | — | ครอบคลุมโดย `41a` แถว 7 (`referencejoinedbyid` ค้ำการจับคู่ด้วยเลขข้อ · `panelignorestheoutcome` ค้ำว่าแผงแสดงเฉพาะข้อที่เลือก และค้ำประโยค *ไม่มีบันทึกของ CLO-n* · `improvementforgetstheyear` ค้ำบรรทัดอ้างปี) | ⚙ |
| 4 | แผงอ้างอิง **ไม่ถูกวาด** เมื่อไม่มีปีก่อนที่เขียนไว้ | — | ครอบคลุมโดย `41a` แถว 8 (`panelopensontonothing`) — บทเรียนของ #40 เขียนเป็น assertion ไว้ก่อนที่จะได้ทำซ้ำ | ⚙ |
| 4 | ถ้อยคำของแผงอ้างอิง ทั้งตอนมีบันทึกและตอนไม่มี | เลือก CLO ที่ปีก่อนเขียนไว้ แล้วเลือกข้อที่ไม่ได้เขียน | หัวแผง *ปีการศึกษา 2568* กับบรรทัดสีจาง *บันทึกของรอบก่อนหน้า สำหรับอ้างอิงขณะเขียนรอบนี้* · เมื่อข้อนั้นไม่มีบันทึก อ่านว่า *ปีการศึกษา 2568 ไม่มีบันทึกของ CLO-n* — **ต้องอ่านออกว่าเป็นการบอกข่าว ไม่ใช่กล่องที่โหลดไม่ขึ้น** | ☑ |
| 4 | การจัดวางของแผงอ้างอิง และการอ่านคู่กัน | เปิดหน้าจอที่มีบันทึกของปีก่อน | แผงอยู่ **ข้าง** สี่หัวข้อบนจอกว้าง (`lg:grid-cols-3` — สองส่วนซ้าย หนึ่งส่วนขวา) และตกลงมาอยู่ **ใต้** เมื่อจอแคบกว่า `lg` — ต้องอ่านคู่กันได้จริงโดยไม่ต้องเลื่อนไปมา | ☑ |
| 5 | ใช้ร่วมกันทุกตอนเรียนของรายวิชาที่เปิดสอน | — | ครอบคลุมโดย `41a` แถว 6 (`onesectionperoffering`) — `teacher.one@` เขียน `multi.role@` เห็นและแก้ได้ แล้วอีกฝั่งเห็นของที่แก้ | ⚙ |
| 5 | ประโยคบอก grain บนหัวจอ | ดูหัวจอ | *ปีการศึกษา 2569 · ทุกตอนเรียนของรายวิชานี้ในปีการศึกษาเดียวกันใช้แผนเดียวกันนี้ และปีการศึกษาอื่นมีแผนของตัวเอง* — วรรคตกก่อน และ ไม่กลางคำ | ☑ |
| 6 | ปฏิเสธรายวิชาที่ไม่ได้สอน ที่เซิร์ฟเวอร์ | — | ครอบคลุมโดย `41a` แถว 9 (`registerignored`) — พิมพ์ที่อยู่ของตอนเรียนคนอื่นแล้วได้ 404 พร้อมประโยค *ไม่พบตอนเรียนที่ระบุ* | ⚙ |
| — | รายวิชาที่ยังไม่มี CLO ไม่ถูกเสนอฟอร์ม | ย้ายปีการศึกษาของ CLO ทั้งหมดออกชั่วคราว แล้วเปิดหน้าจอ | *รายวิชานี้ยังไม่ได้กำหนดผลการเรียนรู้ จึงยังไม่มีอะไรให้ปรับปรุง* และ **ไม่มี** dropdown ไม่มีสี่หัวข้อ ไม่มีแผงอ้างอิง — คืนค่าให้ครบหลังเดิน | ☑ |
| — | เมนูพามาที่หน้านี้ | กดเมนู *การปรับปรุงอย่างต่อเนื่อง* | หน้าจอนี้เปิดขึ้น และ breadcrumb อ่านว่า *การปรับปรุงอย่างต่อเนื่อง* | ☑ |
| — | ข้อความยาวและขึ้นบรรทัดใหม่ | บันทึกข้อความสามย่อหน้า | ย่อหน้าที่พิมพ์ไว้ยังเป็นย่อหน้า (`whitespace-pre-wrap`) ไม่ถูกยุบเป็นก้อนเดียว | ☑ |
| — | แถบแจ้งผลหลังบันทึกและหลังลบ | บันทึกหนึ่งครั้ง ลบหนึ่งครั้ง | *บันทึก&lt;หัวข้อ&gt;แล้ว* และ *ลบ&lt;หัวข้อ&gt;แล้ว* — เอ่ยชื่อหัวข้อ ไม่ใช่ *บันทึกแล้ว* เฉย ๆ | ☑ |
| — | รหัสที่เป็นตัวเลขล้วนแต่กว้างเกินคอลัมน์ ตอบ*ไม่พบ* ไม่ใช่ *เกิดข้อผิดพลาดในระบบ* | อ่าน `backend/test/improvement-plan.test.js` ข้อ *an id too large for the column is ไม่พบ, not a 500* | ทั้ง `2147483648` และ `99999999999999999999` ได้ 404 พร้อมประโยค *ไม่พบรายการของแผนการปรับปรุงที่ระบุ* เดิมของโมดูล — ก่อน [#107](https://github.com/khthana/Deep-QA/issues/107) ทั้งคู่เป็น 22003 → 500 · `107:shapeonly` ค้ำไว้ · ที่นี่ `107:nobound` **ไม่ค้ำ** เพราะ `clo_course_cycle_detail_id` เป็น `bigint` ไม่ใช่ `integer` `2147483648` จึงเป็นค่าที่คอลัมน์รับได้ — ครึ่งที่ยาวเท่านั้นที่พูดแทนแถวนี้ได้ และ `clo_id` ที่มาจาก body ตอบ 400 ด้วยประโยคของร่างที่ไม่สมบูรณ์ | ☑ |

**เก็บกวาดหลังเดิน** ใบนี้เขียนของจริงลงฐานข้อมูล จบแล้ว reset + seed ใหม่เร็วที่สุด ถ้าไม่ reset
ให้**นับแถวใน `clo_course_cycle_detail_cloplan` ก่อนและหลัง** และถ้าเดินแถว *รายวิชาที่ยังไม่มี CLO*
ให้ยืนยันว่า CLO ทั้งเก้าข้อของปี 2569 กลับมาครบ พร้อมพฤติกรรมและเกณฑ์การบรรลุผลของแต่ละข้อ

## บันทึกการเดินด้วยมือ

เดินเมื่อ **5 ก.ย. 2569** โดย Claude ขับ Chrome ผ่าน extension (ผู้ใช้ดูอยู่) บนสแตกเดินมือ
(backend 3000 · frontend 5000 · `deep_core` ที่ reset + seed ใหม่ก่อนเดิน) บัญชี `teacher.one@`
ตอนเรียน 1 ปีการศึกษา 2569 และตอนเรียน 3 ปีการศึกษา 2568 — ทุกแถวติ๊กจากสิ่งที่**เรนเดอร์ออกมาจริง**

**สภาพข้อมูลก่อนเดิน** CLO 2569 เก้าข้อ · CLO 2568 เก้าข้อ · รอบปี 0 · บันทึก 0 · คะแนน 2249 ·
เกณฑ์การบรรลุผล 72 · พฤติกรรมบ่งชี้ 36 · การผูกกิจกรรมกับ CLO 39

| แถว | เห็นอะไร |
|---|---|
| เมนูพามาที่หน้านี้ | เมนู *การปรับปรุงอย่างต่อเนื่อง* เป็นรายการสุดท้ายใต้ *ข้อมูลรายวิชา* กดแล้วเปิดหน้านี้ breadcrumb อ่านว่า *รายวิชาที่รับผิดชอบ › 1 › การปรับปรุงอย่างต่อเนื่อง* |
| ถ้อยคำของสี่หัวข้อ | ครบสี่ เรียงตาม docs/02 และแต่ละหัวข้อมีบรรทัดสีจางบอกว่าเขียนอะไร — **ชัด แยกกันได้** |
| ตัวเลือก CLO | เก้าข้อ รูปแบบ `CLO-n · <รายละเอียด>` ตรงตามใบ · ข้อที่ยาวที่สุดคือ CLO-3 (68 อักขระ) |
| รอบปีสร้างตอนบันทึก | หลัง seed ใหม่ หน้าจอเปิดมาไม่มีขั้นตอนตั้งค่า ไม่มีปุ่ม *สร้างรอบปี* เขียนแล้วกด *บันทึก* ผ่านทันที |
| ข้อความยาวและย่อหน้า | เขียนสามย่อหน้าคั่นด้วยบรรทัดว่าง บันทึกแล้วการ์ดยังแสดงเป็นสามย่อหน้าเท่าเดิม |
| แถบแจ้งผล | บันทึก → *บันทึกสรุปผลการดำเนินงานแล้ว* · ลบ → *ลบสรุปผลการดำเนินงานแล้ว* ทั้งคู่เอ่ยชื่อหัวข้อ |
| ประโยคในกล่องยืนยัน | *ต้องการลบสรุปผลการดำเนินงานของ CLO-3 ในปีการศึกษา 2569 หรือไม่ ทุกตอนเรียนของรายวิชานี้จะไม่เห็นข้อความนี้อีก* — **ครบ รู้ว่าจะเสียอะไร** |
| ถ้อยคำของแผงอ้างอิง | เขียนของปี 2568 ไว้สองหัวข้อบน CLO-3 แล้วกลับมาที่ปี 2569 · หัวแผง *ปีการศึกษา 2568* · เลือก CLO-1 แล้วอ่านว่า *ปีการศึกษา 2568 ไม่มีบันทึกของ CLO-1* — **รู้ว่าเป็นการบอกข่าว** |
| การจับคู่ข้ามปี | CLO-3 ของ 2568 เป็น `clo_id` 12 ส่วนของ 2569 เป็น 3 — คนละแถวจริง และแผงยังจับคู่ถูกด้วยเลขข้อ |
| การจัดวาง | 1510px แผงอยู่ข้างขวา **อ่านคู่กันได้** · 806px และ 620px แผงตกลงมาอยู่ใต้สี่หัวข้อเต็มความกว้าง **รับได้ เป็นธรรมดาของจอแคบ** |
| ประโยคบอก grain | ที่ 1510px อยู่บรรทัดเดียว · ที่ 620px ตัดที่ *เดียวกัน / นี้* ซึ่งเป็นขอบคำจริง |
| รายวิชาที่ยังไม่มี CLO | ย้ายปีของ CLO ทั้งเก้าออกชั่วคราว → *รายวิชานี้ยังไม่ได้กำหนดผลการเรียนรู้ จึงยังไม่มีอะไรให้ปรับปรุง* กับ *กำหนดผลการเรียนรู้ที่หน้า ผลการเรียนรู้รายวิชา ก่อน* และ**ไม่มี dropdown ไม่มีสี่หัวข้อ ไม่มีแผงอ้างอิง** — **รู้ว่าต้องทำอะไร และแยกออกได้** |

**สภาพข้อมูลหลังเดิน** CLO 2569 เก้าข้อ · CLO 2568 เก้าข้อ · คะแนน 2249 · เกณฑ์ 72 · พฤติกรรม 36 ·
การผูกกิจกรรม 39 — **เท่ากันทุกตัว** ส่วนรอบปีและบันทึกที่การเดินเขียนไว้ (2 รอบปี · 3 บันทึก)
ถูกลบออกจนกลับเป็น 0 ซึ่งเป็นสภาพที่ seed ทิ้งไว้

## สี่อย่างที่การเดินหาเจอ และแก้ในตั๋วนี้

ทั้งสี่ข้อไม่มี assertion ไหนพูดได้ ล้วนเป็นการตัดสินว่า *สิ่งที่วาดออกมามันใช้งานได้จริงไหม*

**หนึ่ง — การ์ดกินความกว้างแค่สองในสาม ทั้งที่ไม่มีแผงอ้างอิง** `lg:grid-cols-3` ถูกประกาศไว้เสมอ
ไม่ว่าจะมีแผงหรือไม่ พอไม่มีแผง ที่ว่างหนึ่งส่วนทางขวาอ่านเหมือนแผงที่โหลดไม่ขึ้น ไม่ใช่แผงที่ไม่มี
แก้ด้วยการผูกทั้ง `lg:grid-cols-3` และ `lg:col-span-2` เข้ากับ `data.previous`
**นี่คือบทเรียนของ #40 ในรูปที่ไม่คาดคิด** — ตัวควบคุมไม่ได้ถูกวาดผิด แต่ *ที่ว่างที่มันเคยอยู่* ยังอยู่

**สอง — ช่องเลือก CLO แคบเกินไป** `sm:max-w-md` (448px) ตัด CLO-3 ที่ *…ในการเขียนโปรแกร*
และ native `<select>` ตัดโดยไม่มีจุดไข่ปลา ตัวเลือกที่ถูกตัดจึงอ่านเหมือนพิมพ์ตกมากกว่าล้นกรอบ
ขยายเป็น `sm:max-w-3xl`

**สาม — การ์ดทั้งสี่ไม่เอ่ยชื่อ CLO เลย** ช่องเลือกอยู่บนสุดและเลื่อนพ้นจอ พอเลื่อนลงมาเขียนหัวข้อ
ท้าย ๆ ก็ไม่มีอะไรบนจอบอกว่ากำลังเขียนถึงข้อไหน เติม `CLO-n` เป็นบรรทัดเล็กเหนือหัวข้อของทุกการ์ด
วางไว้นอก `aria-label` เพื่อไม่ให้ชื่อที่เข้าถึงได้ของ section เปลี่ยน

**สี่ — บรรทัดอ้างปีเล็กและจางเกินไป** *ต่อเนื่องจากการสะท้อนคิดของปีการศึกษา 2568* เป็น
`text-xs text-slate-400` อ่านเหมือนคำบรรยายภาพ ทั้งที่มันคือ**หลักฐานที่คณะกรรมการประเมินอ่าน** —
สิ่งที่ทำให้การเปลี่ยนแปลงเป็นการตอบรอบก่อน ไม่ใช่เรื่องบังเอิญ เปลี่ยนเป็น `text-sm text-slate-600`
บนแถบพื้น `bg-slate-50`

ทั้งสี่แก้แล้วและตรวจซ้ำบนจอในการเดินรอบเดียวกัน — **รับได้ทั้งสาม** และ **เด่นพอแล้ว**

## หนึ่งอย่างที่เจอ และไม่ได้แก้ที่นี่

กล่องยืนยันตัดคำว่า **ปีการศึกษา** เป็น *ในปีการ / ศึกษา* ซึ่งไม่ใช่ขอบคำ `ConfirmDialog`
เป็นกล่องเดียวที่ทุกการลบในระบบใช้ตั้งแต่ #14 ประโยคของมันเป็นภาษาไทยที่เอ่ยชื่อสิ่งที่กำลังจะหาย
ทุกใบ เรื่องนี้จึงเป็นของทั้งระบบ ไม่ใช่ของหน้าเดียว เปิดเป็น
[#118](https://github.com/khthana/Deep-QA/issues/118) ชนิดเดียวกับ #117 คนละตัวเรนเดอร์

## มัตแตนต์ที่พิสูจน์แถว ⚙

`mutation/41-improvement-plan.py` สิบสามตัว อ่านวิธีใช้ที่ `mutation/README.md` ก่อน
กวาดจริงเมื่อ 5 ก.ย. 2569 ด้วย `npx playwright test 41a` (9 tests) ทีละตัว log อยู่ที่
`_local/mutation-41/`

**สองตัวอยู่ในไฟล์ของตั๋วอื่น** — `onesectionperoffering` กับ `registerignored` แตะ
`backend/routes/clos.js` ซึ่ง #27 เขียนและหน้าจอนี้เรียกใช้ทั้งก้อน เกณฑ์ข้อ 5 กับข้อ 6 เป็นคำกล่าวถึง
`offeringOf` โดยตรง และไม่มีทางทำให้ทั้งสองพังจากไฟล์ของ #41 เองได้ เพราะระเบียนนี้ไม่มีคอลัมน์
ตอนเรียนให้กรอง และไม่มีเจ้าของให้เทียบ **อย่ากวาดใบนี้พร้อมกับใบ `27`**

ตารางบันทึก**สิ่งที่มัตแตนต์ฆ่าได้จริง** คอลัมน์สุดท้ายคือแถวอื่นที่ตายไปด้วย

| มัตแตนต์ | แตะอะไร | ทำอะไร | แถวที่มันค้ำ · ตายที่ | แถวอื่นที่ตายไปด้วย |
|---|---|---|---|---|
| `onlythreesections` | screen | ตัด *แนวทางพัฒนาครั้งถัดไป* ออกจากฟอร์ม | **แถว 1** · หัวข้อที่สี่ต้องมองเห็นได้ | 4 (ปนเปื้อน — แถว 4 เขียนลงหัวข้อที่หายไป) |
| `savesthelabel` | screen | บันทึกชื่อหัวข้อแทนสิ่งที่พิมพ์ | **แถว 2** · ข้อความบนการ์ดต้องเป็นประโยคที่พิมพ์ | 3 · 5 · 6 (ปนเปื้อน — ทุกแถวที่อ่านข้อความกลับมา) |
| `writesagainstthefirstoutcome` | screen | เขียนลง CLO ตัวแรกเสมอ ไม่ใช่ที่เลือก | **แถว 2** · `entries[0].clo_id` ต้องเป็นของ CLO-9 | 3 · 4 · 5 · 6 · 7 (ปนเปื้อน — ทุกแถวที่เขียนลง CLO อื่นที่ไม่ใช่ตัวแรก) |
| `editkeepstheoldwords` | route | upsert เก็บข้อความเดิมไว้ | **แถว 3** · บันทึกครั้งที่สองต้องได้ *ฉบับที่สอง* | 6 (ปนเปื้อน — การแก้ของอีกฝั่งไม่ถึงจอแรก) |
| `editorstartsempty` | form | เปิดแก้ไขแล้วได้กล่องเปล่า | **แถว 3** · `toHaveValue('ฉบับแรก')` | — |
| `cancelalsoremoves` | screen | *ยกเลิก* บนกล่องยืนยันลบทิ้งด้วย | **แถว 4** · รายการ DELETE ระหว่างที่กล่องเปิดต้องว่าง | — |
| `everyoutcomeshowseverything` | screen | สี่หัวข้อเลิกกรองตาม CLO | **แถว 5** · CLO-5 ต้องไม่มีข้อความของ CLO-4 | — |
| `onesectionperoffering` | register | ทะเบียนรับเฉพาะตอนเรียนที่ 1 | **แถว 6** · `multi.role@` ต้องเปิดแผนของตัวเองได้ | — |
| `referencejoinedbyid` | screen | แผงอ้างอิง join ด้วย `clo_id` | **แถว 7** · แผงต้องมีข้อความของปีก่อน | — |
| `panelignorestheoutcome` | screen | แผงอ้างอิงเลิกกรองตามเลขข้อ | **แถว 7** · CLO-8 ต้องไม่เห็นบันทึกของ CLO-7 | — |
| `improvementforgetstheyear` | route | การปรับปรุงไม่บันทึกปีที่อ้างถึง | **แถว 7** · การ์ดต้องมีปี 2568 อยู่ในนั้น | — |
| `panelopensontonothing` | route | ส่ง `previous` เสมอ แผงจึงเปิดบนความว่างเปล่า | **แถว 8** · `toHaveCount(0)` | — |
| `registerignored` | register | ทะเบียนเลิกถามว่าใครเรียก | **แถว 9** · `teacher.two@` ต้องได้ 404 | — |

**ทั้งสิบสามตัวฆ่าแถวที่ตัวเองอ้างได้จริง** และเก้าตัวฆ่าได้แถวเดียว การปนเปื้อนที่เหลือมาจากสองแบบ:
มัตแตนต์ที่ทำให้ *ทุก* การบันทึกผิด (`savesthelabel` · `writesagainstthefirstoutcome`) และมัตแตนต์ที่
ตัดสิ่งที่แถวหลังใช้เป็นทางผ่าน (`onlythreesections` ตัดหัวข้อที่แถว 4 เขียนลง)

**สิ่งที่การกวาดยืนยัน และเป็นเหตุผลที่ต้องกวาด** แถว 8 คือแถวที่มีไว้เพราะการเดินของ #40 —
`panelopensontonothing` ทำให้แผงอ้างอิงถูกวาดบนความว่างเปล่า และ **อีกแปดแถวผ่านหมด**
นั่นคือรูปร่างของข้อบกพร่องที่ #40 เจอ พูดเป็นตัวเลข: assertion ทุกตัวที่ถามว่าตัวควบคุมทำงานไหม
ไม่มีตัวไหนสังเกตเห็นเลย มีแต่แถวที่ถามว่ามันควรมีอยู่ไหมที่เห็น

## คำตอบที่ถูกแทนที่ — #133 (16 ก.ย. 2569)

`load` ของหน้าจอนี้รับธง `isCurrent` แล้ว และวาดคำตอบก็ต่อเมื่อคำขอที่ทำให้มันเกิด ยังเป็นคำขอ
ที่หน้าจอต้องการอยู่ — กฎเดียวกับ #68 ตัวอักษรต่อตัวอักษร ซึ่งเขียนไว้บน `frontend/src/pages/Students.js`

**และหน้านี้ไม่ได้อยู่ในรายการของตั๋วด้วยซ้ำ** grep ที่ #133 เผยแพร่ไว้มองหารูป
`set…(await …)` ส่วนหน้านี้รับคำตอบลง `const` ก่อนแล้วค่อยวาด มันจึงไม่เคยถูกจับได้ —
*grep เป็นหลักฐานของแพตเทิร์นที่พิมพ์ ไม่ใช่ของข้ออ้างที่ต้องการ* (#111) และการตรวจของ #133
คืนคำตอบว่ายี่สิบสองจุด ไม่ใช่สิบหก

**แต่ตรงนี้ยังไม่ได้ทดสอบ ไม่ใช่ไม่ต้องมี** `sectionId` เป็นพารามิเตอร์ตัวเดียวของหน้านี้
และมาจาก `useParams` (ADR-0004) สิ่งเดียวที่แทนคำขอได้คือการเดินจากตอนเรียนหนึ่ง
ไปอีกตอนบนเส้นทางเดิม โดยคอมโพเนนต์ไม่ถูกถอด ซึ่งทุกลิงก์บนจอนี้เดิน*ขึ้น*ไปหารายการ
ที่ถอดหน้านี้ทิ้ง — วัดเมื่อ 16 ก.ย. 2569 และเป็นคำกล่าว**ที่มีวันหมดอายุ** ตั๋วที่
เพิ่มทางเดินระหว่างพี่น้องบนเส้นทางนี้ ทำให้มันเข้าถึงได้ทันที และต้องมีแถวของตัวเองในวันนั้น
รายการทั้งยี่สิบสองจุดพร้อมสถานะอยู่ที่ `docs/acceptance/57-pager.md` §ครึ่งหลังปิดแล้ว — #133
และตัวติดตามของสิบหกจุดที่ยังไม่มีแถวของตัวเองคือ #141

## รายการที่โหลดใหม่ถูกแทนที่ — #140 (17 ก.ย. 2569)

`frontend/src/pages/ContinuousImprovement.js` โหลดใหม่หลังการบันทึกและการลบ ก่อนตั๋วนี้ทุกจุดเรียก `load()` เฉย ๆ แล้วรับค่าตั้งต้นที่ตอบว่า
*ยังเป็นคำขอปัจจุบัน* เสมอ ตอนนี้ทั้งสองจุดส่ง `() => onScreen.current === load` และค่าตั้งต้นถูกเอาออก
ทุกจอ — รูปเดียวกับอีกเก้าจอที่มีแถวของตัวเอง เหตุผลอยู่บน `frontend/src/pages/Students.js`

**แต่ตรงนี้ยังไม่ได้ทดสอบ ไม่ใช่ไม่ต้องมี** คำตอบที่ถูกแทนต้องมีอะไรสักอย่างเปลี่ยนสิ่งที่ `load` ขอ
ระหว่างที่การโหลดยังไม่กลับ และสิ่งที่ `load` ของจอนี้ขอมาจาก `useParams` ทั้งหมด
จึงไม่มีแถวและไม่มีมัตแตนต์ — วัดเมื่อ 17 ก.ย. 2569 ด้วยเหตุผลเดียวกับส่วน #133 ข้างบน และ**หมดอายุ
วันเดียวกัน**: ทางเดินระหว่างพี่น้องบนเส้นทางนี้ หรือตัวควบคุมบนจอที่ป้อนเข้า `load` ทำให้จุดเหล่านี้
ต้องมีแถวของตัวเองทันที ตัวติดตามคือ #141

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| — | รายการที่ handler สั่งโหลดใหม่ ไม่วาดทับสิ่งที่หน้าจอย้ายไปแล้ว ([#140](https://github.com/khthana/Deep-QA/issues/140)) | ยังไม่มี — ไม่มีอะไรบนจอนี้เปลี่ยนสิ่งที่ `load` ขอระหว่างที่การโหลดใหม่ยังไม่กลับ | **ยังไม่ได้ทดสอบ** (17 ก.ย. 2569) ไม่ใช่*ไม่ต้องมี* — เหตุผลและวันหมดอายุอยู่ในย่อหน้าข้างบน ตัวติดตาม #141 | ☐ |

## คำตอบของการบันทึก ปิดฟอร์มที่ส่งมันไป ไม่ใช่ฟอร์มที่มันเจอ — #146 (20 ก.ย. 2569)

`save` ของ `frontend/src/pages/ContinuousImprovement.js` เคยจบด้วย `setEditing(null)`
กับประโยคสำเร็จ โดยไม่ถามอะไรเลย
การเขียนไม่มีอะไรมารื้อทิ้ง คำตอบของมันจึงมาถึงหน้าจอที่ขยับไปแล้วได้ —
สี่หัวข้อคือสี่การเปิดของฟอร์มเดียว *เขียน…* ของหัวข้ออื่นมาแทนได้ทันที
และจอนี้ยังมีตัวควบคุมที่อีกสิบสองจอไม่มี คือตัวเลือกผลการเรียนรู้
ซึ่งปิดฟอร์มโดยไม่ได้เป็นตัวควบคุมของฟอร์มเลย
ฟอร์มที่สองจึงถูกปิดไปพร้อมสิ่งที่พิมพ์ค้างไว้
และประโยคของการบันทึกครั้งก่อนมาขึ้นในที่ที่อ่านได้ว่า
เป็นเรื่องของรายการที่คนกำลังมองอยู่ ตอนนี้ `showing` จำ **สิ่งล่าสุดที่เปิดขึ้นบนจอนี้**
ทุกตัวควบคุมที่เปิดอะไรขึ้นมาเขียนมัน และคำตอบที่เจอการเปิดอื่นไม่ปิดอะไรและไม่พูดอะไร ทั้งประโยคสำเร็จ
และคำปฏิเสธ — คำถามเดียวกับที่ #139 ให้การอ่านถาม ที่ปลายของการเขียน (#133) เหตุผลทั้งหมดอยู่บน
`frontend/src/pages/Departments.js`

สิ่งที่ไม่ได้เปลี่ยนคือสิ่งที่หน้าจอปิดการใช้งาน ทั้ง *เขียน…*
ของทุกหัวข้อและตัวเลือกผลการเรียนรู้ยังกดได้ระหว่างการบันทึก เพราะนั่นเป็นการเปลี่ยน UI
ซึ่งเป็นคำถาม
ไม่ใช่การแก้ (`docs/06` §Out of Scope) และการโหลดรายการใหม่ทำเสมอ ไม่ว่าการเทียบจะออกทางไหน (#131)

**ฟอร์มอยู่ต่อ แต่สิ่งที่พิมพ์ในนั้นไม่อยู่**
([#149](https://github.com/khthana/Deep-QA/issues/149)) จอนี้วาดทั้งรายการและฟอร์มไว้ใน
`{!loading && data && (` อันเดียวกัน และการโหลดทุกครั้งขึ้นต้นด้วย `setLoading(true)`
การโหลดที่ตามหลังการบันทึกจึง **ถอดฟอร์มทิ้ง** ตลอดช่วงที่คำขอยังไม่กลับ แล้วสร้างใหม่โดยอ่านค่า
จากระเบียน #146 กันไม่ให้คำตอบที่มาช้า *ปิด* ฟอร์มที่คนย้ายไปแล้ว แต่ไม่ได้กันสิ่งที่พิมพ์ค้างไว้
ในฟอร์มนั้น แถวข้างล่างไม่ได้อ้างถึงเนื้อในฟอร์ม (มันอ่านว่าฟอร์มไหนเปิดอยู่ กับการมีหรือไม่มีประโยค)
จึงไม่มีการอ้างเกิน แต่ก็ยังไม่มีแถวไหนถือเรื่องนี้ไว้ หกจอที่เป็นรายการไม่มีรูปนี้ — `!loading`
ของมันครอบแค่แถวในตาราง ฟอร์มอยู่นอกนั้นและรอดการโหลด ซึ่งเป็นเหตุผลที่แถวของจอเหล่านั้น
อ่านค่าที่พิมพ์ไว้ในช่องได้หลังการโหลด (`inputValue()`) และแถวของจอนี้อ่านไม่ได้

**ย่อหน้านี้คือสภาพของวันที่ 20 ก.ย. 2569** — แก้แล้วเมื่อ 21 ก.ย. 2569 ตามคำตอบของเจ้าของ
จอนี้เก็บสิ่งที่วาดไว้ระหว่างการโหลด ฟอร์มจึงรอด และแถวของ `149a` อ่าน `inputValue()` ได้แล้ว
ดูหัวข้อ #149 ท้ายใบนี้

#148 อธิบายอาการเดียวกันบนจอนี้ด้วยกลไกอื่น (effect ที่ผูกกับ object `entry`)
ย่อหน้านี้เคยเขียนว่าวัดแล้วว่ากลไกนั้นไม่ใช่ตัวที่ทำงาน ซึ่งจริงแค่ครึ่งเดียว — **การวัดเมื่อ
21 ก.ย. 2569 เจอว่าทั้งสองกลไกเป็นของจริง และเป็นสองชั้น** ประตู `loading` ถอดฟอร์มทิ้งก่อน
effect จะได้ทำงาน จึงบังมันไว้ พอยกประตูออก หัวข้อที่*เขียนแล้ว*ยังถูกตั้งค่าใหม่บน node เดิม
ผลการวัดอยู่ในคอมเมนต์บน #148 และคำตอบของเจ้าของอยู่ในหัวข้อ #149 ท้ายใบนี้

ทุกแถวข้างล่าง **เดินด้วยมือไม่ได้** มันต้องการคำขอที่ถูกกักไว้จนกว่าแถวจะปล่อย ซึ่ง
`e2e/support/gate.js` ทำให้
การเขียนที่ถูกกักถูกตอบด้วย 200 หรือ 409 ที่แต่งขึ้น จึงไม่มีแถวไหนเขียนฐานข้อมูล เข้าด้วย
`teacher.one@kmitl.ac.th`
ครอบคลุมโดย describe *แผนพัฒนาต่อเนื่อง (ContinuousImprovement.js)* ใน
`146a-save-closes-a-later-form.spec.js` จุดรอของแถวที่การบันทึกสำเร็จคือการโหลดรายการ
ที่ตามมา — การปิดฟอร์มที่ข้อบกพร่องจะวาด commit ไปก่อนคำขอนั้นถูกส่ง — แล้วอ่านครั้งเดียวหลังจากนั้น
250 ms ส่วนแถวคำปฏิเสธไม่มีจุดแบบนั้น (คำปฏิเสธไม่โหลดอะไรใหม่
และประโยคที่ถูกต้องคือประโยคที่ไม่ถูกวาด)
จึงนับจากตอนที่คำตอบถึงหน้า ตัวเลขนั้นตัดสินผลได้ (#52) สิ่งที่ค้ำมันคือการกวาดข้างล่าง

**คำถามที่ตั๋วฝากไว้ ถูกตอบแล้ว (20 ก.ย. 2569)** ร่างแรกให้ *ยกเลิก* เขียน `null` ลง `showing`
การบันทึกที่ถูก *ยกเลิก* แซงโดยไม่มีฟอร์มที่สองมาแทน จึงเงียบทั้งประโยคสำเร็จและคำปฏิเสธ —
คนที่กด *บันทึก* ไม่ได้ยินอะไรเลยเกี่ยวกับการเขียนที่ตัวเองสั่ง ตั๋วฝากเรื่องนี้ไว้เองว่าเป็น
*คำถามเรื่องสิ่งที่คนเห็น แบบเดียวกับ #143* ซึ่ง `docs/06` §Out of Scope บอกว่าเป็นคำถาม
ไม่ใช่การแก้ คำตอบของอาจารย์คือ **ให้ขึ้นเหมือนเดิมเมื่อไม่มีใครมาแทน**

`showing` จึงไม่ได้แปลว่า *ฟอร์มไหนเปิดอยู่* อีกต่อไป แต่แปลว่า **สิ่งล่าสุดที่เปิดขึ้นบนจอนี้**
— *ยกเลิก* ไม่เขียนมัน และการบันทึกที่ปิดฟอร์มของตัวเองก็ไม่เขียน `null` จึงเหลือความหมายเดียว
คือ *ยังไม่มีอะไรเปิดขึ้นเลย* บรรทัดที่เทียบ (`showing.current === sent`) ไม่ถูกแตะเลย
สิ่งที่เปลี่ยนคือใครเขียน ref บ้าง

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| — | คำตอบของการบันทึกที่หน้าจอย้ายไปแล้ว ไม่ปิดช่องเขียนของหัวข้ออื่น และไม่พูดอะไร ([#146](https://github.com/khthana/Deep-QA/issues/146)) | *เขียนสรุปผลการดำเนินงาน* → พิมพ์ → *บันทึก* (กักการเขียนไว้) → *เขียนการสะท้อนคิด* → พิมพ์ → ปล่อยการเขียน (ตอบ 200 ที่แต่งขึ้น) → รอการโหลดรายการ → อ่านครั้งเดียว | ช่องเขียนของ *การสะท้อนคิด* ยังอยู่ · ไม่มีประโยค *บันทึกสรุปผลการดำเนินงานแล้ว* · แถว *a save that lands after another section's เขียน leaves that form open* — แถวอ่านว่าช่องยังเปิดอยู่ไหม ไม่ใช่ว่าในช่องมีอะไร เพราะการโหลดรายการใหม่ล้าง draft ที่พิมพ์ค้างไว้ ซึ่งเป็นข้อบกพร่องของตัวเองที่การแก้ของ #146 ทำให้ไปถึงได้ ([#148](https://github.com/khthana/Deep-QA/issues/148)) — แก้แล้วเมื่อ 21 ก.ย. 2569 และสิ่งที่พิมพ์ถูกถือไว้โดยแถวของ `149a` ในหัวข้อ #149 ท้ายใบ แถวนี้ยังอ่านแค่ว่าช่องเปิดอยู่ เพราะนั่นคือสิ่งที่ #146 อ้าง · `146:improvementclosewins` ค้ำไว้ — ตั้งแต่ [#149](https://github.com/khthana/Deep-QA/issues/149) (21 ก.ย. 2569) ที่ฟอร์มเท่านั้น ครึ่ง*ประโยค*ถามเงื่อนไขของตัวเองหลังการโหลด และ `149:improvementsavebannerunguarded` ค้ำมัน | ⚙ |
| — | ประโยคของหัวข้อที่เพิ่งเขียน ไม่มาขึ้นใต้ผลการเรียนรู้ที่คนย้ายไปแล้ว ([#146](https://github.com/khthana/Deep-QA/issues/146)) | *เขียนสรุปผลการดำเนินงาน* → พิมพ์ → *บันทึก* (กักการเขียนไว้) → เปลี่ยนตัวเลือกผลการเรียนรู้ไปข้อที่สอง → ปล่อยการเขียน → รอการโหลดรายการ → อ่านครั้งเดียว | ไม่มีประโยค *บันทึกสรุปผลการดำเนินงานแล้ว* บนจอ · แถว *a save that lands after the outcome picker moved says nothing about the one now shown* · `146:improvementpickerkeepsshowing` ค้ำไว้ | ⚙ |
| — | คำปฏิเสธของการบันทึกที่ถูกแซง ไม่มาขึ้นเหนือช่องเขียนใหม่ ([#146](https://github.com/khthana/Deep-QA/issues/146)) | *เขียนสรุปผลการดำเนินงาน* → พิมพ์ → *บันทึก* (กักไว้ ตอบ 409 ที่แต่งขึ้น) → *เขียนการสะท้อนคิด* → พิมพ์ → ปล่อยการเขียน → อ่านครั้งเดียว | ไม่มีประโยคของคำปฏิเสธนั้นบนจอ · ช่องเขียนของ *การสะท้อนคิด* ยังอยู่ · แถว *a save refused after the screen moved on says nothing over the new form* — จอนี้ไม่โหลดรายการใหม่เมื่อถูกปฏิเสธ ทั้งการปิดฟอร์มและการโหลดอยู่บนทางที่สำเร็จ · `146:improvementrefusalwins` ค้ำไว้ | ⚙ |
| — | การบันทึกที่ไม่มีอะไรมาแทนฟอร์มที่มันถูกส่งมาจาก ยังบอกว่ามันทำอะไรไป ([#146](https://github.com/khthana/Deep-QA/issues/146)) | *เขียนบทสรุป* → พิมพ์ลงกล่อง → *บันทึก* (กักการเขียนไว้) → *ยกเลิก* โดยไม่เปิดฟอร์มอื่น → ปล่อยการเขียน (ตอบ 200 ที่แต่งขึ้น) → รอการโหลดรายการ | ประโยค *บันทึกบทสรุปแล้ว* ขึ้นบนจอ · แถว *a save that nothing took the place of says what it did* · `146:improvementcancelclears` ค้ำไว้ | ⚙ |
| — | คำปฏิเสธของการบันทึกที่ไม่มีใครมาแทนฟอร์ม ยังถูกพูด ([#146](https://github.com/khthana/Deep-QA/issues/146)) | *เขียนบทสรุป* → พิมพ์ลงกล่อง → *บันทึก* (กักไว้ ตอบ 409 ที่แต่งขึ้น) → *ยกเลิก* โดยไม่เปิดฟอร์มอื่น → ปล่อยการเขียน | ประโยคของคำปฏิเสธนั้นขึ้นบนจอ · แถว *a refusal that nothing took the place of is still said* · `146:improvementcancelclears` ค้ำไว้ | ⚙ |

### มัตแตนต์

`146:improvementclosewins` `146:improvementrefusalwins` และ
`146:improvementpickerkeepsshowing` ใน `mutation/146-save-closes-a-later-form.py`
เปลี่ยนการเทียบ `showing.current === sent` เป็น `showing.current !== null` — guard ที่จำว่า
*มีฟอร์มเปิดอยู่ไหม* แทนที่จะจำว่า *ฟอร์มไหน* ซึ่งเป็นความผิดที่คนเขียนจริง ๆ จะเขียน ไม่ใช่การ
ถอดเงื่อนไขทิ้ง (#102)

กวาดเมื่อ 20 ก.ย. 2569 ด้วย `146a` ทั้งไฟล์ ทีละตัว — baseline **28 ผ่านทั้ง 28** ซึ่งเป็น
จำนวนแถวของไฟล์ในวันนั้น ก่อนแถวคำปฏิเสธของแผนพัฒนาต่อเนื่องและสองแถวของทะเบียนนักศึกษา
จะถูกเขียน สามแถวนั้นกวาดทีหลังบนฐาน 31 — แถวคำปฏิเสธของจอนี้อยู่ข้างล่าง
อีกสองแถวอยู่ใน `17-students.md`

`146:improvementclosewins` **ล้ม 1 ผ่าน 27** และ `146:improvementpickerkeepsshowing`
**ล้ม 1 ผ่าน 27** ตัวหลังถอด `showing.current = {}` ออกจากตัวเลือกผลการเรียนรู้ ซึ่งเป็น
ตัวควบคุมที่จออื่นไม่มี — มันเปลี่ยนฟอร์มโดยไม่เปลี่ยนหัวข้อ

**`146:improvementrefusalwins` รอดในการกวาดรอบนั้น** ยี่สิบแปดจากยี่สิบแปด จอนี้เป็นจอเดียว
ในสิบสองจอของการกวาดรอบนั้นที่ยังไม่มีแถวคำปฏิเสธ มัตแตนต์จึงไม่มีอะไรให้ฆ่า นั่นคือ *ตัวที่รอดบนแถวที่ไม่มีใครสงสัย
คือการอ้างเกิน* (#97) ในรูปที่ตรงที่สุด — ใบนี้กำลังจะอ้างว่าคำปฏิเสธของจอนี้ถูกพิสูจน์แล้ว และมีแต่
การกวาดเท่านั้นที่บอกได้ว่าไม่ แถวถูกเขียนเพิ่ม แล้ว **วัดซ้ำเมื่อ 20 ก.ย. 2569 บนฐาน 31 —
ล้ม 1 ผ่าน 30** ที่แถวคำปฏิเสธนั้น การเขียนแถวไม่ใช่หลักฐานว่ามัตแตนต์ฆ่ามันได้ การรันเป็นหลักฐาน

ทุกตัวล้มที่ assertion ของแถว ไม่ใช่ที่ timeout และตัวที่ล้มคือแถวที่ชื่อของมันบอก อ่านจากชื่อเทสต์ที่ตาย
ไม่ใช่จากจำนวน

### วัดซ้ำทั้งใบ หลังคำตอบของอาจารย์ที่ปรึกษา (20 ก.ย. 2569)

คำตอบเปลี่ยน*ความหมาย*ของ `showing` ไม่ใช่รูปของการเทียบ มัตแตนต์เดิมยี่สิบหกตัวจึงเกาะที่เดิม
ทั้งหมด แต่ *สมอที่ยังอยู่ไม่ได้แปลว่ายังพิสูจน์อะไรได้ มีแต่การกวาดที่บอกได้* (#107) และไฟล์
`146a` โตจาก 31 เป็น 49 แถว ตัวเลขทุกตัวข้างบนจึงหมดอายุพร้อมกัน กวาดใหม่ทั้งใบ 42 ตัว
ทีละตัว — baseline **49 ผ่านทั้ง 49** ทุกรันใช้เวลา 2.6–3.0 นาทีเท่า baseline และทุกรัน
ล้ม + ผ่าน = 49 พอดี ไม่มีตัวไหนหยุดแอปพลิเคชัน และไม่มีแถวไหนหายไปจากการรัน

- `146:improvementclosewins` **ล้ม 2 ผ่าน 47** ที่แถวฟอร์มของอีกหัวข้อหนึ่ง *a save that lands
  after another section’s เขียน leaves that form open* และแถวตัวเลือกผลการเรียนรู้
- `146:improvementrefusalwins` **ล้ม 1 ผ่าน 48** ที่แถว *a save refused after the screen moved on
  says nothing over the new form*
- `146:improvementcancelclears` **ล้ม 2 ผ่าน 47** ที่สองแถวที่คำตอบเพิ่มเข้ามา — *a save that
  nothing took the place of says what it did* และ *a refusal that nothing took the place of is
  still said* ซึ่งใบนี้ถือแถวคำปฏิเสธของทรงนี้ไว้ให้ทั้งกลุ่ม
- `146:improvementpickerkeepsshowing` **ล้ม 1 ผ่าน 48** ที่แถว *a save that lands after the
  outcome picker moved says nothing about the one now shown* แถวเดียว
  ตัวเลือกนี้เป็นตัวควบคุมที่จออื่นไม่มี
  และเป็นจอเดียวที่*ไม่มีอะไรเปิดอยู่*ไม่ได้แปลว่าไม่มีใครมาแทน

ทุกตัวล้มที่ assertion ของแถว ไม่ใช่ที่ timeout และตัวที่ล้มคือแถวที่ชื่อของมันบอก อ่านจากชื่อเทสต์
ที่ตาย ไม่ใช่จากจำนวน

## การโหลดใหม่ไม่ถอดช่องเขียนที่คนกำลังพิมพ์ — #149 และ #148 (21 ก.ย. 2569)

การวัดเมื่อ 21 ก.ย. 2569 (คอมเมนต์บน [#148](https://github.com/khthana/Deep-QA/issues/148)) เจอว่า
อาการของจอนี้มีสองกลไก ซ้อนกันเป็นสองชั้น ไม่ใช่ทางเลือกของกันและกัน:

| | effect ผูกกับ object `entry` | effect ผูกกับ `entry?.entry_id` |
|---|---|---|
| **ประตู `!loading && data`** | ยังไม่เขียน: หาย · เขียนแล้ว: หาย — สร้างใหม่ทั้งคู่ | เหมือนช่องซ้าย ทุกอย่าง |
| **ยกประตูออก (`data &&`)** | ยังไม่เขียน: อยู่ · เขียนแล้ว: **หาย บน node เดิม** | **อยู่ทั้งคู่** |

ประตูบังกลไกที่สองไว้ — ช่องเขียนไม่ถูก mount อยู่เลยตอนการโหลดยังไม่กลับ effect จึงไม่มีโอกาส
ทำงาน พอยกประตูออก หัวข้อที่*เขียนแล้ว*ยังถูกตั้งค่าใหม่ เพราะ `entries` ถูกสร้างใหม่จาก `data`
ที่โหลดมา และส่ง object ใหม่ที่ `entry_id` เดิมให้ `EntrySection` หัวข้อที่ยังไม่เขียนรอดเพราะ
`entry` เป็น `undefined` ทั้งก่อนและหลัง

คำตอบของเจ้าของคือ **ทั้งสองชั้น** — จอเก็บสิ่งที่วาดไว้ระหว่างการโหลด บรรทัด *กำลังโหลดข้อมูล…*
ขึ้นเฉพาะตอนที่ยังไม่มีอะไรให้วาด (#149) และ `EntrySection` ตั้ง draft ตาม `entry_id` (#148)
จอนี้เป็นจอเดียวในเจ็ดที่ต้องมีชั้นที่สอง อีกหกจอส่งแถวที่ถืออยู่ใน `editing` ให้ฟอร์ม ซึ่งไม่มีการโหลด
ไหนแตะตัวตนของมัน **แถวที่แยกสองชั้นออกจากกันได้มีแถวเดียว คือหัวข้อที่เขียนแล้ว**

การยกประตูเปิดประตูที่ #146 นับไว้ว่าปิด: ระหว่างการโหลด *เขียน…* ของหัวข้ออื่นกดได้แล้ว และทั้ง `save`
กับ `remove` ตั้งประโยคของตัวเอง*หลัง*การโหลด ประโยคของการบันทึกจึงถาม `showing.current === sent`
ตอนที่มันตัดสิน แทนที่จะถือคำตอบเก่าไว้ใน `mine` และ `remove` จับ `sent` ไว้ตอนถูกกด

ทุกแถวข้างล่าง **เดินด้วยมือไม่ได้** มันต้องการคำขอที่ถูกกักไว้ที่ route จนกว่าแถวจะปล่อย
(`e2e/support/gate.js`) การเขียนที่ถูกกักถูกตอบด้วย 200 ที่แต่งขึ้น สิ่งที่แถวเขียนจริงคือบันทึก
ที่แถวต้องใช้เป็นสถานการณ์ (หัวข้อที่เขียนแล้วของสองแถว) ซึ่ง `hold()` เอาออกเมื่อจบไฟล์ เข้าด้วย
`teacher.one@kmitl.ac.th` ครอบคลุมโดย describe *แผนพัฒนาต่อเนื่อง (ContinuousImprovement.js)* ใน
`149a-reload-keeps-the-open-form.spec.js` จุดอ่านคือหลังคำตอบที่ถูกกักถึงหน้า แล้ว 250 ms อ่านครั้งเดียว
ตัวเลขนั้นตัดสินผลได้ (#52) สิ่งที่ค้ำมันคือการกวาดข้างล่าง

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| — | สิ่งที่พิมพ์ค้างไว้ในหัวข้อที่*ยังไม่เขียน* รอดการโหลดที่การบันทึกของอีกหัวข้อจบด้วย ([#149](https://github.com/khthana/Deep-QA/issues/149)) | *เขียนสรุปผลการดำเนินงาน* → พิมพ์ → *บันทึก* (กักการเขียนไว้) → *เขียนการสะท้อนคิด* → พิมพ์ → ปล่อยการเขียน (ตอบ 200 ที่แต่งขึ้น) → รอการโหลดรายการ → อ่านครั้งเดียว | ช่องของ *การสะท้อนคิด* ยังเปิด และในนั้นคือสิ่งที่พิมพ์ · แถว *what was typed into an unwritten section survives the reload a save ends with* · `149:improvementgateback` ค้ำไว้ที่ค่าในช่อง — ครึ่ง *ยังเปิดอยู่* อ่านเป็นฐานของแถว **ไม่ได้อ้างด้วยตัวนี้**: บนประตูเก่าฟอร์มถูกถอดแล้ววาดกลับโดย `editing` ยังตั้งอยู่ มันจึงเปิดอยู่ทั้งสองทาง การไม่ปิดฟอร์มที่เปิดทีหลังเป็นข้อของ [#146](https://github.com/khthana/Deep-QA/issues/146) · `improvemententrybyobject` **ไม่**ฆ่าแถวนี้ ซึ่งคือสิ่งที่ตารางข้างบนทำนาย | ⚙ |
| — | สิ่งที่พิมพ์ค้างไว้ในหัวข้อที่*เขียนแล้ว* รอดการโหลดเดียวกัน ([#148](https://github.com/khthana/Deep-QA/issues/148)) | แถวเขียน *แนวทางพัฒนาครั้งถัดไป* ไว้ก่อน → *เขียนสรุปผลการดำเนินงาน* → พิมพ์ → *บันทึก* (กักไว้) → *แก้ไขแนวทางพัฒนาครั้งถัดไป* → พิมพ์ทับ → ปล่อยการเขียน → รอการโหลดรายการ → อ่านครั้งเดียว | ในช่องคือสิ่งที่พิมพ์ ไม่ใช่ข้อความที่บันทึกไว้ · แถว *what was typed into a written section survives the reload a save ends with* · `149:improvemententrybyobject` ค้ำชั้นที่สอง `149:improvementgateback` ค้ำชั้นแรก — **แถวเดียวที่แยกสองชั้นออกจากกัน** | ⚙ |
| — | ระหว่างการโหลดใหม่ ไม่มี *กำลังโหลดข้อมูล…* ขึ้นทับหัวข้อที่วาดอยู่แล้ว ([#149](https://github.com/khthana/Deep-QA/issues/149)) | *เขียนสรุปผลการดำเนินงาน* → พิมพ์ → *บันทึก* (ตอบทันที กักการโหลดที่ตามมา) → รอ 250 ms → อ่านครั้งเดียว | ไม่มี *กำลังโหลดข้อมูล…* บนจอ · แถว *a save whose reload another section's เขียน overtook says nothing over it* ที่ assertion *the loading line over a list already drawn* ซึ่งอ่านก่อนการกด · `149:improvementloadingline` ค้ำไว้ — **ถ้อยคำและตำแหน่งของบรรทัดนั้นไม่ได้อ้าง** | ⚙ |
| — | ประโยคของการบันทึกที่การโหลดของมันถูก *เขียน…* แซง ไม่ขึ้นเหนือช่องที่เพิ่งเปิด (#149 ประตูของ [#146](https://github.com/khthana/Deep-QA/issues/146)) | *เขียนสรุปผลการดำเนินงาน* → พิมพ์ → *บันทึก* (ตอบทันที กักการโหลด) → *เขียนการสะท้อนคิด* ระหว่างการโหลดยังถูกกัก → ปล่อยการโหลด → อ่านครั้งเดียว | ไม่มี *บันทึกสรุปผลการดำเนินงานแล้ว* · ช่องของ *การสะท้อนคิด* ยังเปิด · แถว *a save whose reload another section's เขียน overtook says nothing over it* · `149:improvementsavebannerunasked` ค้ำไว้ที่ประโยค — ครึ่ง *ยังอยู่/ยังเปิด* อ่านเป็นฐานของแถว **ไม่ได้อ้าง**: หลังการโหลดไม่มีโค้ดไหนปิดฟอร์ม มัตแตนต์จึงไม่มีทางแยกมันออก | ⚙ |
| — | ประโยคของการลบที่การโหลดของมันถูก *เขียน…* แซง ไม่ขึ้นเหนือช่องที่เพิ่งเปิด (#149 ประตูของ [#146](https://github.com/khthana/Deep-QA/issues/146)) | แถวเขียน *การปรับปรุงจากรอบก่อนหน้า* ไว้ก่อน → ถังขยะของหัวข้อนั้น → *ลบ* ในกล่องยืนยัน (ตอบทันที กักการโหลด) → *เขียนการสะท้อนคิด* → ปล่อยการโหลด → อ่านครั้งเดียว | ไม่มี *ลบการปรับปรุงจากรอบก่อนหน้าแล้ว* · ช่องของ *การสะท้อนคิด* ยังเปิด · แถว *a removal whose reload another section's เขียน overtook says nothing over it* · `149:improvementremovalsentatthelanding` ค้ำไว้ที่ประโยค — ครึ่ง *ยังอยู่/ยังเปิด* อ่านเป็นฐานของแถว **ไม่ได้อ้าง**: หลังการโหลดไม่มีโค้ดไหนปิดฟอร์ม มัตแตนต์จึงไม่มีทางแยกมันออก | ⚙ |
| — | การลบที่ไม่มีอะไรมาแทน ยังบอกว่ามันลบไปแล้ว ([#149](https://github.com/khthana/Deep-QA/issues/149) ครึ่งที่ประโยคต้องขึ้น) | แถวเขียน *การสะท้อนคิด* ไว้ก่อน → ถังขยะของหัวข้อนั้น → *ลบ* ในกล่องยืนยัน (ตอบทันที กักการโหลด) → ปล่อยการโหลด โดยไม่เปิดอะไร → อ่านครั้งเดียว | *ลบการสะท้อนคิดแล้ว* ขึ้นหนึ่งครั้ง · แถว *a removal nothing took the place of says what it did* · `149:improvementremovalbannerunsaid` ค้ำไว้ | ⚙ |

### มัตแตนต์

ห้าตัวของจอนี้อยู่ใน `mutation/149-reload-keeps-the-open-form.py` กวาดเมื่อ 21 ก.ย. 2569 ด้วย `149a`
ทั้งไฟล์ ทีละตัว — baseline **22 ผ่านทั้ง 22** (1.2 นาที)

- `149:improvementgateback` **ล้ม 4 ผ่าน 18** (1.5 นาที) — ที่ assertion ของค่าในช่องของ*ทั้งสอง*แถว
  สิ่งที่พิมพ์ และที่ timeout ของการกด *เขียน…* ในแถวประโยคสองแถว
- `149:improvemententrybyobject` **ล้ม 1 ผ่าน 21** (1.3 นาที) — ที่ assertion ของค่าในช่องของแถว
  หัวข้อที่เขียนแล้ว **แถวเดียว** แถวหัวข้อที่ยังไม่เขียนผ่าน
- `149:improvementloadingline` **ล้ม 1 ผ่าน 21** (1.3 นาที) — ที่ assertion *the loading line over a
  list already drawn* แถวเดียว
- `149:improvementsavebannerunasked` **ล้ม 1 ผ่าน 21** (1.3 นาที) — ที่ assertion *the banner of the
  save before* แถวเดียว
- `149:improvementremovalsentatthelanding` **ล้ม 1 ผ่าน 21** (1.3 นาที) — ที่ assertion *the banner of
  the removal* แถวเดียว

`improvementgateback` ฆ่าแถวของประโยคสองแถวด้วย **แต่ที่การรอ ไม่ใช่ที่ assertion** — *เขียน…* ที่แถวกด
ไม่ถูกวาดระหว่างการโหลด และการกดมี timeout ห้าวินาที *การอ่านสิ่งที่มัตแตนต์ทำให้ไม่มีอยู่คือการรอ*
(#139) สองแถวนั้นจึงไม่ถูกอ้างด้วยตัวนี้

### สิ่งที่การตรวจเจอ และการกวาดที่มันขอ (21 ก.ย. 2569)

การตรวจโค้ดอ่านสามข้อที่การกวาดครั้งแรกไม่เห็น

- **ไม่มีแถวไหนถามว่าประโยคของการลบยังขึ้น** ทุกแถวถามแค่ให้มันเงียบ ตัวที่ทำให้มันไม่พูดอีกเลยจะรอด ทุกไฟล์ แถว *a removal nothing took the place of says what it did* กับ `improvementremovalbannerunsaid` (`false &&` หน้าเงื่อนไข) เพิ่มเข้ามา — *สองตัวที่แปรคุณสมบัติเดียวกันไม่เห็นอันที่สาม* (#96)
- **`removalsentatthelanding` คือการถอดเงื่อนไขทิ้งในผล** `sent` ที่จับข้างการเปรียบเทียบเท่ากับ ref ตัวเอง ใบมัตแตนต์เคยเขียนว่าไม่ใช่ แก้คำบรรยายแล้ว การฆ่ายังเป็นการฆ่า
- **คำตอบนี้แยกประโยคของการบันทึกออกจาก `mine`** ก่อนหน้านั้น `146:improvementclosewins` ฆ่าทั้งการปิดและประโยคด้วยตัวเดียว ตอนนี้มันเห็นแค่การปิด ครึ่ง *ไม่มีประโยคสำเร็จ* ของแถว [#146](https://github.com/khthana/Deep-QA/issues/146) จึงได้ `improvementsavebannerunguarded` (`true ||` หน้าเงื่อนไข) — การกวาดซ้ำของ `146` เป็นที่ที่เห็นมัน *สมอที่ยังอยู่ไม่ได้แปลว่ายังพิสูจน์อะไรได้* (#107)

กวาดทีละตัว — baseline ของ `149a` **29 ผ่านทั้ง 29** (1.6 นาที) และของ `146a` **49 ผ่านทั้ง 49** (2.5–2.6 นาที) ทุกรันเวลาเท่า baseline ล้ม + ผ่าน = จำนวนแถวพอดี อ่านจากชื่อ describe *แผนพัฒนาต่อเนื่อง (ContinuousImprovement.js)*:

- `149:improvementremovalbannerunsaid` กับ `149a` **ล้ม 1 ผ่าน 28** ที่ *the banner of the removal* ของแถวใหม่ แถวเดียว
- `149:improvementsavebannerunguarded` กับ `149a` **ล้ม 1 ผ่าน 28** ที่ *the banner of the save before* และกับ `146a` **ล้ม 2 ผ่าน 47** ที่ *the first save's banner* และที่แถวตัวเลือกผลการเรียนรู้ *a sentence about the outcome that was left*
- `146:improvementclosewins` **ล้ม 1 ผ่าน 48** ที่ assertion ของฟอร์ม — **เคยล้ม 2** (20 ก.ย.) แถวที่สองคือแถวตัวเลือกผลการเรียนรู้ ซึ่งเคยตายที่ประโยคเพราะประโยคถาม `mine` ร่วมกับการปิด ตอนนี้ครึ่งนั้นเป็นของ `pickerkeepsshowing` กับ `savebannerunguarded`
- `146:improvementcancelclears` **ล้ม 2 ผ่าน 47** — เหมือน 20 ก.ย.
- `146:improvementpickerkeepsshowing` **ล้ม 1 ผ่าน 48** — เหมือน 20 ก.ย.

เจ็ดตัวของจอนี้ใน `149` ตอนนี้ และ baseline ของ `149a` คือ 29 ไม่ใช่ 22 ที่ตัวเลขข้างบนอ่าน

## การโหลดใหม่ที่ล้มเหลวเก็บสิ่งที่วาดไว้ — #151 (22 ก.ย. 2569)

ตั้งแต่ #149 จอเก็บรายการและฟอร์มไว้ระหว่างการโหลดใหม่ แต่การโหลดที่**ล้มเหลว**ยังล้างทั้งคู่: `catch` ของ `load` ตั้ง `data` เป็น `null` ฟอร์มจึงถูกถอดพร้อมสิ่งที่พิมพ์ไว้ คำตอบ (22 ก.ย. 2569) คือคำตอบของ #149 ต่อไปอีกขั้น — **การโหลดที่ล้มเหลวเก็บสิ่งที่จอวาดไว้ล่าสุด** และคำปฏิเสธขึ้นเหนือมัน การโหลดครั้งแรกที่ล้มไม่มีอะไรให้เก็บ `loading && !data` วาดเหมือนเดิม ซึ่งตั๋วนี้ไม่เปลี่ยน

ทุกแถว **เดินด้วยมือไม่ได้** การเขียนถูกตอบด้วย 200 ที่แต่งขึ้น และการโหลดที่ตามมาด้วย 409 ที่แต่งขึ้น (`e2e/support/gate.js`) จึงไม่มีแถวไหนเขียนฐานข้อมูลผ่านการบันทึก เข้าด้วย `teacher.one@kmitl.ac.th` ครอบคลุมโดย describe *แผนพัฒนาต่อเนื่อง (ContinuousImprovement.js)* ใน `151a-failed-reload-keeps-the-list.spec.js` จุดอ่านคือหลังคำปฏิเสธถึงหน้า แล้ว 250 ms อ่านครั้งเดียว (#52) สิ่งที่ค้ำมันคือการกวาดข้างล่าง

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| — | สิ่งที่พิมพ์ค้างไว้ในฟอร์มของส่วนอื่น รอดการโหลดที่ล้มเหลว ([#151](https://github.com/khthana/Deep-QA/issues/151)) | เปิดส่วนแรก → *บันทึก* (ตอบ 200 ที่แต่งขึ้น) → ระหว่างนั้นเปิดส่วนที่สอง → พิมพ์ → การโหลดถูกตอบ 409 → อ่านครั้งเดียว | ฟอร์มของส่วนที่สองยังเปิดอยู่ และในกล่องคือสิ่งที่พิมพ์ · แถว *what was typed into another section survives a reload that fails* ที่ *the second section's editor* ซึ่ง `151:improvementblanks` ค้ำไว้ และที่ *what was typed* ซึ่ง `151:improvementblinks` ค้ำไว้ — คำปฏิเสธที่แถวอ่านก่อน (*the failed reload's refusal*) **ไม่ได้อ้าง**: มันขึ้นทั้งก่อนและหลัง #151 มัตแตนต์จึงแยกมันไม่ได้ | ⚙ |
| — | การโหลดที่ล้มเหลวเก็บรายการที่วาดไว้ ([#151](https://github.com/khthana/Deep-QA/issues/151)) | เปิดส่วนแรก → *บันทึก* → การโหลดถูกตอบ 409 → อ่านครั้งเดียว | ปุ่ม *เขียน* ของส่วนอื่นยังอยู่ · แถว *a reload that fails keeps the plan it last drew* ที่ *a section drawn before the reload* · `151:improvementblanks` ค้ำไว้ | ⚙ |

### มัตแตนต์

`151:improvementblanks` ใส่ `setData(null)` กลับเข้า `catch` ของจอนี้ — โค้ดก่อน #151 — และ `151:improvementblinks` ล้าง `data` แล้วคืนมันในจังหวะถัดไป ฟอร์มจึงถูกถอดแล้ววาดใหม่ เปิดอยู่แต่สิ่งที่พิมพ์หายไป `blinks` เพิ่มหลังการตรวจ: `blanks` ฆ่าแถวฟอร์มที่ *the second section's editor* ซึ่งอ่านก่อน *what was typed* ครึ่งหลังนั้นจึงไม่มีมัตแตนต์ไปถึง กวาดทั้งสิบสี่ตัวของใบ `151` เมื่อ 22 ก.ย. 2569 ด้วย `151a` ทั้งไฟล์บนข้อความสุดท้ายของมัน — baseline **14 ผ่านทั้ง 14** (0.8 นาที) `improvementblanks` **ล้ม 2 ผ่าน 12** (0.9 นาที) ที่ *the second section's editor* และที่ *a section drawn before the reload* `improvementblinks` **ล้ม 1 ผ่าน 13** (0.9 นาที) ที่ *what was typed* แถวเดียว ไม่ใช่ที่ timeout และไม่มีแถวของจออื่นตาย อ่านจากชื่อ describe ไม่ใช่จากจำนวน

### สิ่งที่วัดได้แต่ไม่ใช่ของตั๋วนี้

ตั๋วเขียนว่า *แบนเนอร์คำปฏิเสธบอกว่าการโหลดล้มเหลว* วัดแล้วจริงแค่เมื่อมีฟอร์มอื่นเปิดทีหลัง: ถ้าไม่มีอะไรเปิด `save` ตั้งประโยคสำเร็จ*หลัง*การโหลด ประโยคนั้นจึงทับคำปฏิเสธ (วัดด้วย probe ที่ลบทิ้งแล้ว: บนจอ ผลการเรียนรู้รายวิชา คำปฏิเสธ 0 ประโยคสำเร็จ 1 และบนแถวรายการของ `151a` ทั้งเจ็ดจอ คำปฏิเสธ 0 ทุกจอ) — ตอนนี้คือ *บันทึก…แล้ว* เหนือรายการ ที่ยังไม่มีสิ่งที่บันทึก ซึ่งเป็นคำถามของเจ้าของ ไม่ได้แก้ในตั๋วนี้ — เปิดเป็น [#152](https://github.com/khthana/Deep-QA/issues/152)
