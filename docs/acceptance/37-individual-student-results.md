# ใบตรวจรับ #37 — ผลลัพธ์การเรียนรู้รายบุคคล

[#37](https://github.com/khthana/Deep-QA/issues/37) · หน้าจอ `/teacher/teacherDashboard/:sectionId/studentResults`

## สิ่งที่ต้องเข้าใจก่อนเดิน

**ตั๋วนี้ไม่มี route เป็นของตัวเอง และนั่นคือสาระของมัน** เกณฑ์ข้อ 5 ขอให้คะแนนใช้มาตราส่วนห้าคะแนน
ชุดเดียวกับผลลัพธ์รายวิชา วิธีที่ถูกที่สุดคือเขียนเทสต์มายืนยัน วิธีที่ซื่อสัตย์คือทำให้มันผิดไม่ได้ —
และการอ่าน `GET /teaching/sections/:id/learning-details` ของ #38 คือวิธีหลัง

grain ที่ #37 ต้องการ — นักศึกษาทุกคนของตอนเรียนนี้ ต่อผลการเรียนรู้ทุกข้อของมัน บวกค่าเฉลี่ยของ
ตอนเรียนรายข้อ — คือสิ่งที่ heatmap ของ #38 คำนวณอยู่แล้ว หลังด่านเดียวกันเป๊ะ และ
`backend/lib/attainment.js` เขียนกฎไว้ตรง ๆ ว่า *query อยู่ใน route เพราะสิ่งที่ต่างกันระหว่างหน้าจอ
คือคะแนนชุดไหนที่นับ* — ตรงนี้มันไม่ต่างกันเลยแม้แต่นิดเดียว route ของตัวเองจึงจะเป็นสำเนาที่สองของ
query ที่มีอนาคตเดียวคือขัดแย้งกับสำเนาแรก **ผู้ใช้เป็นคนเลือกข้อนี้เมื่อ 3 กันยายน 2569 ก่อนเขียนโค้ด**

ราคาที่จ่ายคือการผูกกัน และควรพูดออกมาให้ชัด: ถ้ารูปคำตอบของ #38 ขยับ หน้าจอนี้ขยับตาม
นั่นคือราคาปกติของการมีคำตอบเดียวแทนที่จะมีสอง และแถวใน `37a` จะพังเสียงดังถ้าวันหนึ่งจ่ายมันแบบไม่ระวัง

**ผลพลอยได้ที่ไม่ใช่ของแถม: ทะเบียนมาพร้อมกราฟ** endpoint รายชื่อชั้นเรียนแบ่งหน้าละสิบ ตอนเรียนห้าสิบเจ็ดคน
หลังตัวเลือกที่แบ่งหน้าคือคนกด *ถัดไป* ห้าครั้งเพื่อหานักศึกษาหนึ่งคน การอ่านคำตอบของ heatmap แทน
แปลว่าทะเบียนทั้งห้องอยู่บนหน้าจอแล้ว ช่องค้นหาจึงกรองสิ่งที่อยู่ในหน่วยความจำ การติ๊กชื่อไม่ยิง request เลย
และกราฟวาดใหม่ในเฟรมเดียวกับที่ติ๊ก

**สี่คนบนกราฟเดียว และค่าเฉลี่ยไม่กินสีของใคร** จานสีของ `RadarChart` มีสี่ชุดที่แยกออกจากกันได้
(สี + ลายเส้น) ถ้าค่าเฉลี่ยกินไปหนึ่งชุด การ *ดูการกระจาย* จะจบที่สามคน ค่าเฉลี่ยจึงได้เส้นของตัวเอง —
เทา ประถี่ มีพื้นอ่อน อยู่นอกจานสี — เพราะมันเป็นพื้นหลังที่คนอื่นถูกอ่านเทียบกับมัน ไม่ใช่คู่แข่งของใคร
**ผู้ใช้เป็นคนเลือกข้อนี้เมื่อ 3 กันยายน 2569 ก่อนเขียนโค้ดเช่นกัน** และเส้นของนักศึกษาถูกถอดพื้นออก
เพราะรูปหลายเหลี่ยมสี่รูปที่ถมทับกันคือโคลน — ค่าเฉลี่ยเป็นเส้นเดียวบนกราฟนี้ที่มีพื้น

**เพดานสิบแกนเป็นของกราฟ ไม่ใช่ของข้อมูล** ป้ายแกนวางบนวงรัศมี 154 แล้วยื่นออกนอก พอเกินสิบข้อ
ป้ายจะเริ่มชนกันที่หัวและท้ายวง ซึ่งเป็นที่ที่มุมระหว่างสองแกนแคบที่สุด ผลการเรียนรู้ทุกข้อยังมีแถวของมัน
ในตารางข้างล่างครบถ้วน เพราะตารางคือที่ที่ตัวเลขถูกอ่านจริงมาตั้งแต่ต้น

## Checklist

- **☑** เดินด้วยมือจบแล้ว
- **◐** เดินไปครึ่งเดียว
- **☐** ยังไม่ได้เดิน
- **⚙** ครอบคลุมด้วย browser seam แล้ว (#65) ไม่ต้องเดินด้วยมือซ้ำ แถวบอกไฟล์และมัตแตนต์ที่ค้ำไว้

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| 1 | เลือกนักศึกษาแล้วเห็นผลสัมฤทธิ์ของเขาบน CLO ของตอนเรียน | ติ๊กชื่อหนึ่งคน | เส้นที่สองปรากฏบนแกนชุดเดิม จุดที่วาดอยู่ห่างจากจุดศูนย์กลางตามคะแนนของคนนั้น และตารางบอกตัวเลขเดียวกัน — **ชุดทดสอบหลังบ้านเห็นข้อนี้ไม่ได้** ที่ seam HTTP ไม่มีพิกัด — ครอบคลุมโดย `e2e/tests/37a-student-results.spec.js` แถว 1 (`studentlinedrawnfromtheaverage`) · มัตแตนต์ตัวนั้นวาดเส้นของนักศึกษาจากค่าเฉลี่ย ซึ่งเป็นคำตอบผิดตัวเดียวของหน้าจอนี้ที่ดูน่าเชื่อสนิท เพราะรูปที่วาดออกมาเป็นรูปของข้อมูลจริง | ⚙ |
| 2 | ค่าเฉลี่ยของตอนเรียนแสดงคู่กันเพื่อใช้เทียบ | เปิดหน้าจอ | เส้นค่าเฉลี่ยอยู่บนกราฟตั้งแต่ยังไม่เลือกใคร และจุดของมันตรงกับ `clos[].mean` — ครอบคลุมโดย `37a` แถว 1 (`averagedrawnasfullmarks`) · มัตแตนต์แยกต่างหากจากข้อ 1 เพราะหน้าจอที่วาดเส้นนักศึกษาถูกแต่วาดพื้นหลังผิด จะผ่านทุกแถวที่เหลือในไฟล์ | ⚙ |
| 3 | เลือกหลายคนมาเทียบกันบนกราฟเดียวได้ | ติ๊กสี่ชื่อ | สี่เส้นบวกค่าเฉลี่ยเป็นห้าเส้นบนแกนชุดเดียว ทุกเส้นวาดจากคะแนนของเจ้าของเส้น — ครอบคลุมโดย `37a` แถว 3 (`onlythefirststudentreachesthechart`) · **รอบแรกแถวนี้อ้าง `fifthstudentisallowedon` ซึ่งผิด** มัตแตนต์ตัวนั้นยกเพดาน การติ๊กสี่ชื่อก็ยังได้สี่เส้นเหมือนเดิม assertion ของแถวนี้จึงไม่ตาย สิ่งที่มันฆ่าคือ assertion ของแถวถัดไป — รีวิวเป็นคนจับได้ | ⚙ |
| 3 | เพดานสี่คน และบอกเหตุผลเมื่อเต็ม | ติ๊กชื่อที่ห้า | ช่องที่ห้ากดไม่ได้ **และมีประโยคบอกว่าทำไม** · เอาคนออกหนึ่งคนแล้วช่องกลับมากดได้ — ครอบคลุมโดย `37a` แถว 3 (`fifthstudentisallowedon` · `studentcapnotsaid`) · คนที่ห้าจะได้ `seriesStyle(4)` ซึ่งวนกลับไปชุดแรก: สีเดียวกันและลายเดียวกันกับคนที่หนึ่ง โดยไม่มีอะไรพัง | ⚙ |
| 4 | กราฟแสดง CLO ไม่เกินสิบข้อ | สร้างวิชาที่มีสิบเอ็ดข้อแล้วเปิดหน้าจอ | นับป้ายแกนได้สิบ — ครอบคลุมโดย `37a` แถว 6 (`nocaponhowmanyaxes`) · **แถวนี้สร้างสถานการณ์เอง** seed มีเก้าข้อ จึงเพิ่ม CLO-10 กับ CLO-11 เข้า Offering แล้วลบทิ้งหลังจบ | ⚙ |
| 4 | และบอกเมื่อมีมากกว่านั้น | เปิดหน้าจอเดียวกัน | เห็นประโยคที่มีทั้งสองตัวเลข *กราฟแสดงผลการเรียนรู้ 10 ข้อแรก จากทั้งหมด 11 ข้อ* และทุกข้อยังมีแถวในตาราง — ครอบคลุมโดย `37a` แถว 6 (`axiscapnotsaid`) · ครึ่งนี้ของเกณฑ์ไม่ใช่เรื่องเพดาน แต่เป็นเรื่องที่คนอ่านรูปต้องรู้ว่ากำลังอ่านแค่ส่วนหนึ่ง | ⚙ |
| 5 | คะแนนใช้มาตราส่วนห้าคะแนนชุดเดียวกับผลลัพธ์รายวิชา | — | หน้าจอนี้อ่าน endpoint เดียวกับ #38 ซึ่ง `backend/test/learning-details.test.js` พิสูจน์เศษส่วนไว้ทีละหลัก และ #36 กับ #38 ใช้ `lib/attainment.js` ตัวเดียวกัน — **พิสูจน์เชิงโครงสร้างและที่ seam HTTP** · **ไม่อ้าง ⚙** เพราะไม่มีมัตแตนต์ตัวไหนฆ่า assertion ของแถวนี้เอง แถวนี้เป็นจริงเพราะไม่มีที่ให้มันเป็นเท็จ ไม่ใช่เพราะมีใครไปตรวจ | ☑ |
| 6 | นักศึกษาที่ยังไม่มีคะแนนได้สถานะที่อธิบายได้ ไม่ใช่กราฟที่พัง | ล้างคะแนนของคนหนึ่งแล้วติ๊กชื่อเขา | ไม่มีเส้น ไม่มีจุด และ**ไม่มีรูปหลายเหลี่ยมที่ยุบลงจุดศูนย์กลาง** ซึ่งเป็นสิ่งที่กราฟห้าคะแนนวาดให้คนที่ถูกอ่านเป็นแถวของศูนย์ · แทนที่ด้วยประโยคใน live region ที่บอกว่าเส้นของใครหายไปและทำไม · ค่าเฉลี่ยยังวาดอยู่ เพราะนักศึกษาหนึ่งคนที่ไม่มีคะแนนไม่ใช่ห้องที่ไม่มีคะแนน — ครอบคลุมโดย `37a` แถว 4 (`unmarkedstudentnotnamed`) · **แถวนี้สร้างสถานการณ์เอง** seed ให้คะแนนทุกคนครบทุกข้อ | ⚙ |
| 6 | ตัวเลือกบอกไว้ก่อนติ๊กว่าคนไหนยังไม่มีคะแนน | เปิดหน้าจอหลังล้างคะแนน | เห็น *ยังไม่มีคะแนน* ข้างชื่อคนนั้นในรายการ — คนที่เลือกสี่ชื่อมาเทียบกันเลือกจากรายการ ไม่ใช่จากประโยคที่ได้หลังเลือกผิด — ครอบคลุมโดย `37a` แถว 4 (`pickerdoesnotflagunmarkedstudents`) · **รอบแรกแถวนี้ไม่มีมัตแตนต์เลย** ตัวที่อ้างไว้แตะแค่ live region ไม่ได้แตะป้ายในรายการ — รีวิวเป็นคนจับได้ | ⚙ |
| 6 | นักศึกษาที่มีคะแนนเฉพาะข้อที่เพดานตัดทิ้งก็ต้องได้รับคำอธิบาย | สร้างวิชาสิบเอ็ดข้อ ล้างคะแนนของคนหนึ่งทุกข้อยกเว้น CLO-9 แล้วติ๊กชื่อเขา | ไม่มีเส้น — ซึ่งถูก ไม่มีอะไรบนแกนชุดนี้ให้วาด — **แต่เหตุผลที่บอกต้องเป็นเหตุผลที่จริง** คือ *มีคะแนนเฉพาะข้อที่ไม่ได้อยู่บนกราฟ* ไม่ใช่ *ยังไม่มีคะแนน* ซึ่งจะเป็นคำโกหกเรื่องการให้คะแนนของเขา · คะแนนของเขายังอยู่ในตาราง — ครอบคลุมโดย `37a` แถว 5 (`markedpastthecapreadsasdrawn`) · **เกณฑ์ข้อ 4 กับข้อ 6 ชนกันตรงนี้ และรีวิวเป็นคนเจอ** โค้ดชุดแรกวัด *มีคะแนนไหม* จากข้อทั้งหมด ไม่ใช่จากสิบข้อที่วาด คนที่ถูกเพดานตัดจึงนับเป็นคนมีคะแนน ไม่มีเส้น และไม่มีประโยค | ⚙ |
| 7 | รายชื่อนักศึกษาโหลดได้ | เปิดหน้าจอ | ช่องติ๊กในตัวเลือกเท่ากับทะเบียน**ทั้งห้อง** ไม่ใช่หน้าแรกสิบคน — ครอบคลุมโดย `37a` แถว 1 (`pickeroffersonlyafirstpage`) · มัตแตนต์ตัวนั้นตัดเหลือสิบ ซึ่งเป็นค่าตั้งต้นของ `lib/paging.js` พอดี คือรูปร่างที่หน้าจอนี้จะเป็นถ้าไปอ่านรายชื่อชั้นเรียนแบบแบ่งหน้า · **รีบิลด์ไม่เคยมี hook ที่พังตัวนั้น** ถ้อยคำของเกณฑ์เป็นของตั๋วเดิม สิ่งที่พิสูจน์คือเนื้อความ | ⚙ |
| 7 | ค้นหาแล้วคนที่เลือกไว้ไม่หลุดออกจากรายการ | ติ๊กคนหนึ่ง แล้วพิมพ์รหัสของอีกคน | ทั้งสองคนยังอยู่ในรายการ คนที่ติ๊กไว้ยังติ๊กอยู่ และเส้นของเขายังอยู่บนกราฟ — ครอบคลุมโดย `37a` แถว 2 (`searchhidesastudentwhoisonthechart`) · ตัวกรองที่ซ่อนคนที่ถูกติ๊กจะทิ้งเส้นไว้บนกราฟโดยไม่มีตัวควบคุมเหลือให้เอาออก ซึ่งเป็นรูปร่างเดียวกับที่รีวิวของ #36 เจอในทางปฏิเสธของมัน | ⚙ |
| 8 | ผู้สอนถูกปฏิเสธตอนเรียนที่ไม่ได้สอน บังคับที่เซิร์ฟเวอร์ | — | ด่านเป็นของ #38 ทั้งหมด `sectionOf` + `offeringOf` อ่านจาก session ไม่ใช่จาก request body (ADR-0002) — พิสูจน์ที่ seam HTTP ใน `learning-details.test.js` · **ไม่อ้าง ⚙** เพราะไม่มีมัตแตนต์ของตั๋วนี้ฆ่า assertion นั้น มันเป็น assertion ของ #38 | ☑ |
| 8 | และผู้สอนที่พิมพ์ที่อยู่เองไม่ถูกทิ้งให้รอใต้คำปฏิเสธ | พิมพ์ที่อยู่ของตอนเรียนอื่นที่แถบที่อยู่ | เห็นคำปฏิเสธ ไม่มีกราฟ ไม่มีตัวเลือก และ **ไม่มี *กำลังโหลดข้อมูล…* ค้างอยู่ใต้คำปฏิเสธ** — ครอบคลุมโดย `37a` แถว 7 (`refusalkeepsloading`) · นี่คือข้อบกพร่องที่การเดิน #43 เจอ และ #36 มีมัตแตนต์ชื่อเดียวกัน **ไม่ใช่มัตแตนต์ตัวเดียวครอบสองหน้าจอ** — การแก้คือหนึ่งบรรทัดใน `finally` ของแต่ละหน้า และการเดินครั้งนั้นเจอมันบนสองหน้าจอ*แยกกัน* | ⚙ |
| — | ห้าเส้นแยกออกจากกันด้วยอย่างอื่นนอกจากสี | ติ๊กสี่ชื่อแล้วดูคำอธิบายสัญลักษณ์ | ค่าเฉลี่ยเป็น `#64748B` ลาย `1 3` กำกับว่า *(เส้นประถี่)* และเป็นเส้นเดียวที่มีพื้น ส่วนนักศึกษาสี่คนได้ `#0F2A60` ทึบ · `#B45309` ลาย `6 4` · `#0F766E` ลาย `2 3` · `#7E22CE` ลาย `8 3 2 3` ทั้งสี่ไม่มีพื้น — **เป็นสิ่งที่คนต้องดู** เพราะเป็นเรื่องของรูปลักษณ์ · เรียกสีด้วย token ไม่ใช่ด้วยคำว่าเทาหรือน้ำเงิน · **เดินแล้ว 4 ก.ย. 2569** อ่าน token จาก DOM จริงได้ครบทั้งห้า และสัญลักษณ์ในคำอธิบายตรงกับเส้นบนกราฟทีละคู่ · `filledCount` อ่านได้ **1** คือค่าเฉลี่ยเป็นเส้นเดียวที่มีพื้น ตามที่รีวิวให้แก้ · ผู้ใช้ตอบว่า **แยกออกทั้งห้า** รวมสองเส้นที่ทับกันแถว CLO-2 ถึง CLO-3 · seed แถมของจริงมาให้: นักศึกษาสองคนที่เลือกชื่อ **กมลชนก** เหมือนกัน คำอธิบายจึงต้องขึ้นรหัสก่อนชื่อ ไม่งั้นจะมีสองบรรทัดที่อ่านเหมือนกัน | ☑ |
| — | สีของช่องในตารางอ่านเป็นช่วงคะแนนได้ | ติ๊กคนที่คะแนนกระจาย แล้วดูตาราง | ช่องของนักศึกษาใช้สีตาม `BANDS` ชุดเดียวกับ heatmap ของ #38 และช่องค่าเฉลี่ยไม่มีสี — **เดินแล้ว 4 ก.ย. 2569** โผล่ครบทั้งห้าช่วงบนหน้าเดียว: `bg-red-100` (2.88 มี *ต่ำกว่าเกณฑ์* ต่อท้ายป้ายเสียง) · `bg-amber-100` (3.37) · `bg-yellow-50` (3.55) · `bg-lime-100` (4.06) · `bg-emerald-100` (4.71) คอลัมน์ค่าเฉลี่ยไม่มีสีเลย ผู้ใช้ตอบว่า *อ่านออกครับ* — **แต่ทักด้วยว่า *สีมันออกไปทางอ่อนไปหน่อย*** และสั่งให้เข้มขึ้นทุกช่วง วัดจากหน้าจอจริงได้ว่าพื้นของทุกช่วงต่างจากสีขาวแค่ **1.03–1.22 เท่า** (`bg-yellow-50` แย่สุด) ขณะที่ตัวอักษรบนพื้นมี contrast 6.6–8.6 คือเหลือที่ให้เข้มขึ้นอีกมาก — เปิด [#113](https://github.com/khthana/Deep-QA/issues/113) แทนที่จะแก้ที่นี่ เพราะพาเลตนี้ใช้ร่วมสี่หน้าจอ และ `38a` ยืนยัน token ไว้ตรง ๆ | ☑ |
| — | ป้ายแกนและตัวเลือกไม่ทับกันที่จอแคบ | เปิดหน้าจอที่ครึ่งจอ | **เดินแล้ว 4 ก.ย. 2569** ที่ `innerWidth` **714** — ป้ายแกนเก้าอัน **ไม่ทับกันสักคู่** ไม่มีป้ายล้นขอบทั้งซ้ายและขวา `scrollWidth` เท่ากับ `clientWidth` คือหน้าไม่เลื่อนแนวนอน ผังสองคอลัมน์ยุบเหลือคอลัมน์เดียว (620px) ตัวเลือกเลื่อนไปอยู่**เหนือ**กราฟ และคำอธิบายสัญลักษณ์กลับมาเป็นบรรทัดละ 20px ทั้งห้าแถว ผู้ใช้ตอบว่า *ไม่ทับ ไม่ล้น ตัวเลือกย้ายไปอยู่บนกราฟ ไม่มีแถบเลื่อนแนวนอน* · **แต่ที่จอกว้างพร้อมเมนูซ้ายเปิด คำอธิบายตัดเป็นสามบรรทัดต่อแถว** อ่านออกและไม่ล้น แต่รุงรัง — ผู้ใช้สั่งให้เปิดตั๋วไว้ ดู [#114](https://github.com/khthana/Deep-QA/issues/114) | ☑ |
| — | ถ้อยคำตอนที่ยังไม่ได้เลือกใคร | เปิดหน้าจอแล้วไม่ต้องติ๊กอะไร | เห็นคำชวนให้เลือกคน พร้อมเพดานสี่คน ไม่ใช่หน้าที่ดูเหมือนโหลดไม่เสร็จ — **เดินแล้ว 4 ก.ย. 2569 และการเดินเป็นคนสั่งแก้ถ้อยคำนี้** ของเดิมคือ *ยังไม่ได้เลือกนักศึกษา — กราฟกำลังแสดงเฉพาะค่าเฉลี่ยของทั้งตอนเรียน* ซึ่งรายงานความบกพร่องมากกว่าจะชี้ขั้นต่อไป และซ้ำกับประโยคเหนือกราฟที่บอกวิธีทำอยู่แล้ว · เปลี่ยนเป็น *เลือกนักศึกษาจากรายการ ครั้งละไม่เกิน 4 คน เพื่อวางเส้นของแต่ละคนทับบนค่าเฉลี่ยของตอนเรียน* สีจาก `text-slate-500` เป็น `text-slate-900` (ผู้ใช้บอกว่าอ่อนไปสองรอบ จึงขยับสองขั้น) · **คำว่า *ทางซ้าย* ถูกตัดออกระหว่างเดิน** เพราะต่ำกว่า `lg` ตัวเลือกไปอยู่เหนือกราฟ คำนั้นจะชี้ไปที่ที่ไม่มีอะไร — จับได้เพราะเพิ่งเดินแถวจอแคบมาก่อนหน้า | ☑ |

## บันทึกการเดินด้วยมือ

**เดินจบเมื่อ 4 กันยายน 2569** ผู้ใช้เป็นคนเข้าระบบเอง ผมขับเบราว์เซอร์ผ่านส่วนขยาย Chrome
เซิร์ฟเวอร์เดินที่ 3000/5000 schema `deep_core` — **การเดินนี้อ่านอย่างเดียว ไม่ได้เขียนอะไรลงฐานข้อมูล**
จึงไม่ต้องนับแถวก่อนหลัง สิ่งที่เปลี่ยนคือโค้ด ไม่ใช่ข้อมูล

### การเดินเปลี่ยนหน้าจอไปสองอย่าง

ทั้งสองอย่างเป็นเรื่องถ้อยคำและรูปลักษณ์ ซึ่งเป็นสิ่งที่ไม่มี seam ไหนพิสูจน์แทนได้ และเป็นเหตุผลที่
สี่แถวนี้ถูกเขียนเป็นแถวที่ต้องใช้ตาตั้งแต่แรกแทนที่จะปล่อยให้ชุดทดสอบตัดสิน

**ประโยคตอนยังไม่ได้เลือกใคร ถูกเขียนใหม่ทั้งประโยค** ของเดิม *ยังไม่ได้เลือกนักศึกษา —
กราฟกำลังแสดงเฉพาะค่าเฉลี่ยของทั้งตอนเรียน* รายงานความบกพร่องมากกว่าจะชี้ขั้นต่อไป และซ้ำกับ
ประโยคเหนือกราฟที่บอกวิธีทำอยู่แล้ว ผู้ใช้สั่งให้เป็นคำชวนที่บอกว่าเลือกจากไหนและเลือกได้กี่คน

**และคำว่า *ทางซ้าย* ถูกตัดออกในนาทีสุดท้าย** ผู้ใช้เลือกถ้อยคำที่มีคำนั้นอยู่ แต่แถวจอแคบเพิ่งเดิน
ไปก่อนหน้าและวัดไว้เองว่า ต่ำกว่า `lg` ผังยุบเหลือคอลัมน์เดียวและตัวเลือกเลื่อนไปอยู่**เหนือ**กราฟ
คำว่าซ้ายจะชี้ไปที่ที่ไม่มีอะไรอยู่ประมาณครึ่งหนึ่งของเวลา — บอกผู้ใช้แล้วผู้ใช้สั่งให้ตัด
**นี่คือลำดับการเดินที่ทำให้จับได้** ถ้าเดินแถวถ้อยคำก่อนแถวจอแคบ ประโยคนั้นคงผ่านไปแล้ว

### สองอย่างที่การเดินเจอแล้วไม่ได้แก้ในตั๋วนี้

**[#113](https://github.com/khthana/Deep-QA/issues/113) — ห้าช่วงคะแนนอ่อนเกินไปทุกช่วง**
ผู้ใช้ทักว่า *สีมันออกไปทางอ่อนไปหน่อย* ตอนตอบแถวสีในตาราง วัดจากหน้าจอจริงได้ว่าพื้นของทุกช่วง
ต่างจากสีขาวแค่ 1.03–1.22 เท่า ขณะที่ตัวอักษรบนพื้นมี contrast 6.6–8.6 คือเหลือที่ให้เข้มขึ้นอีกมาก
ไม่แก้ที่นี่เพราะพาเลตอยู่ใน `lib/bands.js` ใช้ร่วมสี่หน้าจอ และ `38a` กับใบตรวจรับของ #38 และ #42
เขียน token ไว้เป็นหลักฐานของแถวที่เดินไปแล้ว การเปลี่ยนสีจึงบังคับให้ต้อง**เดินซ้ำ** ไม่ใช่แก้แต่ตัวหนังสือ

**[#114](https://github.com/khthana/Deep-QA/issues/114) — คำอธิบายสัญลักษณ์ตัดเป็นสามบรรทัด**
ที่จอกว้างพร้อมเมนูซ้ายเปิด ซึ่งเป็นสถานะตั้งต้น คำอธิบายเหลือที่ราว 200px ทั้งชื่อและคำกำกับลายเส้น
จึงถูกหั่นเป็นสามบรรทัด ห้าบรรทัดกลายเป็นสิบห้า พอยุบเมนูซ้ายกลับเป็นบรรทัดเดียวทันที
อ่านออก ไม่ล้น หน้าไม่เลื่อน — เป็นความรุงรังล้วน ๆ ผู้ใช้ตัดสินว่าเปิดตั๋วไว้ก่อน

### สิ่งที่ seed แถมมาให้เอง

นักศึกษาสองคนที่เลือกมาเทียบกันชื่อ **กมลชนก** เหมือนกันทั้งคู่ — 66010031 ทองคำ กับ
66010067 สุวรรณชาติ ซึ่งเป็นเหตุผลที่ป้ายทุกที่บนหน้าจอนี้ขึ้นรหัสก่อนชื่อ ถ้าขึ้นชื่ออย่างเดียว
คำอธิบายสัญลักษณ์จะมีสองบรรทัดที่อ่านเหมือนกันโดยไม่มีอะไรบอกว่าเป็นคนละคน

และคะแนนของ seed เกาะกลุ่มกันมาก — ค่าเฉลี่ยรายคนอยู่ระหว่าง 3.54 ถึง 4.29 เท่านั้น
เส้นทั้งสี่จึงทับกันเกือบตลอดวง โดยเฉพาะช่วง CLO-2 ถึง CLO-3 **ซึ่งเป็นสถานการณ์ที่ทำให้ลายเส้น
มีความหมายจริง ๆ ไม่ใช่ของประดับ** เป็นเหตุผลเดียวกับที่ #36 เจอตอนเดิน คือ 2568 กับ 2569
คะแนนใกล้กันจนถ้ามีแต่สีจะอ่านเป็นเส้นเดียว

### กับดักที่เจอซ้ำระหว่างเดิน

**เซิร์ฟเวอร์เดินจอตายไปทั้งคู่ก่อนเริ่ม** `curl --max-time` เป็นสิ่งแรกที่ทำ และทั้ง 3000 และ 5000
ไม่ตอบ ไม่มีอะไร listen อยู่เลย สตาร์ทใหม่ทั้งคู่แล้วรอจนตอบ 200 จริงจึงเริ่ม — ถ้าไม่ curl ก่อน
รอบนี้จะเสียเวลาไปกับการโทษส่วนขยายเหมือนรอบ #36

**`document.hidden` เป็น true ทุกครั้งที่ผู้ใช้หันมาพิมพ์ตอบ** ซึ่งทำให้ถ่ายภาพไม่ได้เลย —
`Page.captureScreenshot` timeout เพราะ renderer ถูก throttle ส่วน `javascript_tool` ยังทำงานปกติ
**การวัดจึงทำผ่าน JS ได้ทั้งหมด แต่การให้ผู้ใช้ *ดู* ต้องให้เขาหันไปดูหน้าจอเอง** ซึ่งเร็วกว่าถ่ายภาพส่งด้วย

**`resize_window` ตอบว่าสำเร็จโดยไม่ทำอะไร** ตอนหน้าต่างไม่ได้โฟกัส `innerWidth` ไม่ขยับและ
`outerWidth` อ่านได้ 0 หรือ 160 สุดท้ายให้ผู้ใช้ย่อหน้าต่างเองด้วย `Win + ←` แล้วผมอ่านค่าจริง

**เซสชันหมดอายุกลางทาง** ระหว่างคุยเรื่องสีกับถ้อยคำ ([#99](https://github.com/khthana/Deep-QA/issues/99)
— 30 นาทีนับจาก sign-in ไม่ต่ออายุเมื่อใช้งาน) ผู้ใช้ต้องพิมพ์รหัสผ่านใหม่หนึ่งรอบ

## มัตแตนต์ที่พิสูจน์แถว ⚙

`mutation/37-student-results.py` สิบสามตัว อ่านวิธีใช้ที่ `mutation/README.md` ก่อน
**ทุกตัวอยู่ในเบราว์เซอร์** ซึ่งไม่ใช่ช่องโหว่ของการกวาด แต่เป็นรูปร่างของตั๋ว: #37 ไม่มี route
กฎทุกข้อที่มันใช้เป็นของ #38 การทำให้ข้อไหนพังจะไปทำให้ `38a` และชุดทดสอบ HTTP พัง ไม่ใช่ `37a`

ตารางนี้บันทึก **สิ่งที่มัตแตนต์ฆ่าได้จริง** ไม่ใช่สิ่งที่ควรจะฆ่าได้

| มัตแตนต์ | แถวที่มันค้ำ | ตายที่ |
|---|---|---|
| `pickeroffersonlyafirstpage` | 7 (ทะเบียนทั้งห้อง) | แถว 1 ตัวเดียว — ตัดรายชื่อเหลือสิบ ซึ่งเป็นค่าตั้งต้นของ `lib/paging.js` พอดี |
| `averagedrawnasfullmarks` | 2 (ค่าเฉลี่ยเป็นพื้นหลัง) | แถว 1 ตัวเดียว — ตาราง คำอธิบายสัญลักษณ์ และเส้นของนักศึกษายังถูกหมด ผิดแค่พื้นที่ทุกคนถูกอ่านเทียบกับมัน |
| `studentlinedrawnfromtheaverage` | 1 (เส้นเป็นของคนนั้นจริง) | แถว 1 **และแถว 4 และแถว 5** — พอเส้นของนักศึกษามาจากค่าเฉลี่ย คนที่ไม่มีคะแนนก็ถูกวาด ทั้งสองแถวจึงล้มด้วย |
| `onlythefirststudentreachesthechart` | 3 (หลายคนบนกราฟเดียว) | แถว 3 ตัวเดียว — ตัวเลือกยังติ๊กได้สี่ คำอธิบายยังมีสี่บรรทัด ตารางยังงอกสี่คอลัมน์ กราฟมีเส้นเดียว |
| `searchhidesastudentwhoisonthechart` | 7 (คนที่เลือกไว้ไม่หลุด) | แถว 2 ตัวเดียว |
| `fifthstudentisallowedon` | 3 (เพดานสี่คน) | แถว 3 — ตรงที่ช่องที่ห้ากดไม่ได้ **ไม่ใช่**ตรงที่สี่คนได้สี่เส้น ซึ่งเป็นความเข้าใจผิดรอบแรกของใบนี้ |
| `studentcapnotsaid` | 3 (บอกเหตุผลเมื่อเต็ม) | แถว 3 — ตรงประโยค |
| `pickerdoesnotflagunmarkedstudents` | 6 (ตัวเลือกบอกก่อนติ๊ก) | แถว 4 ตัวเดียว — live region ยังทำงาน ขาดแต่ป้ายในรายการ ซึ่งเป็นจุดที่การรู้มีประโยชน์ |
| `unmarkedstudentnotnamed` | 6 (คนไม่มีคะแนนได้คำอธิบาย) | แถว 4 ตัวเดียว |
| `markedpastthecapreadsasdrawn` | 6 (คนที่มีคะแนนเฉพาะข้อที่ถูกตัด) | แถว 5 ตัวเดียว — เป็นโค้ดชุดแรกของบรรทัดนั้นเป๊ะ ๆ คือข้อบกพร่องที่รีวิวเจอ เอามาเก็บไว้เป็นมัตแตนต์ |
| `nocaponhowmanyaxes` | 4 (เพดานสิบแกน) | แถว 5 **และแถว 6** — พอไม่มีเพดาน CLO-9 กลับขึ้นกราฟ สถานการณ์ของแถว 5 จึงหายไปด้วย |
| `axiscapnotsaid` | 4 (บอกเมื่อมีเกินสิบ) | แถว 6 — ตรงประโยค ตัวเลขในตารางยังครบ ไม่มีอะไร*หาย* แต่คนอ่านรูปไม่รู้ว่าอ่านแค่ส่วนหนึ่ง |
| `refusalkeepsloading` | 8 (คำปฏิเสธไม่ทิ้งใครไว้รอ) | แถว 7 ตัวเดียว |

### สองตัวที่ MISS ตอนกวาดรอบสอง

`refusalkeepsloading` กับ `unmarkedstudentnotnamed` ตอบ MISS หลังแก้ตามรีวิว เพราะบรรทัดที่มัน
ยึดไว้ถูกแก้ไปเอง — `finally` ได้ ticket guard เพิ่ม และ `unmarked` กลายเป็นสามรายการที่มีชื่อ
ชี้ใหม่ตามที่โค้ดเขียนอยู่จริง แล้วกวาดซ้ำจึงฆ่า ซึ่งเป็นสิ่งที่ `mutation/README.md` บอกให้ทำเมื่อ MISS
**MISS ไม่ใช่ความล้มเหลวของโค้ด แต่เป็นสัญญาณว่าข้อความที่มัตแตนต์ยึดไม่มีอยู่แล้ว**

## สิ่งที่รีวิวสองแกนเจอ และแก้ไปแล้ว

รีวิวรอบนี้เจอของจริงเจ็ดอย่าง หนึ่งอย่างเป็นข้อบกพร่องบนหน้าจอ สองอย่างเป็นเครื่องหมายที่ไม่ได้มาจริง

1. **เกณฑ์ข้อ 4 ทำให้เกณฑ์ข้อ 6 พังเงียบ ๆ** — วัด *มีคะแนนไหม* จากข้อทั้งหมดแทนที่จะเป็นสิบข้อที่วาด
   คนที่มีคะแนนเฉพาะข้อที่เพดานตัดทิ้งจึงได้ช่องที่ติ๊กแล้วกับกราฟที่ไม่ขยับ ซึ่งเป็นอาการที่ประโยคของ
   ข้อ 6 มีไว้กันพอดี แก้เป็นสามรายการที่มีชื่อ (`silent` · `neverMarked` · `offChartOnly`)
   และเพิ่มแถว 5 ของ `37a` มาปักไว้
2. **⚙ ของแถว 3 ไม่ได้มาจริง** — อ้าง `fifthstudentisallowedon` ซึ่งไม่ได้ฆ่า assertion ของแถวนั้น
   เขียน `onlythefirststudentreachesthechart` ขึ้นมาใหม่
3. **⚙ ของแถว 6 (ป้ายในตัวเลือก) ไม่มีมัตแตนต์เลย** — เขียน `pickerdoesnotflagunmarkedstudents`
4. **คอมเมนต์กับโค้ดขัดกัน** — `AVERAGE_STYLE` เขียนว่าเป็นเส้นเดียวที่มีพื้น แต่ทุกชุดใน `SERIES`
   มีพื้นและรูปที่ปิดวงจะถูกถม แก้ให้เป็นจริง โดยถอดพื้นออกจากเส้นของนักศึกษา
5. **โค้ดตายกับสำเนาซ้ำใน e2e** — `legendLines` มีสองสำเนาและไม่มีใครเรียก ลบทั้งคู่ · `tableCell`
   เหมือนกันทุกไบต์ในสองไฟล์ ย้ายไป `radar-chart.js` ที่เดียว
6. **การอ่านสองครั้งไม่มีตัวกันของเก่าทับของใหม่** — เปลี่ยนตอนเรียนเร็ว ๆ ทำให้คำตอบเก่าชนะได้
   ใส่ ticket guard แบบเดียวกับ #36 · และ `notice` ไม่เคยถูกล้าง คำปฏิเสธเก่าจึงค้างข้ามหน้าจอ
7. **การเข้าถึง** — ช่องค้นหามีแต่ `placeholder` ซึ่งหายไปเมื่อพิมพ์ ใส่ `aria-label` · ช่องที่ถูก
   ปิดไม่ได้ชี้ไปที่เหตุผล ใส่ `aria-describedby`

## หมายเหตุที่ไม่ใช่เกณฑ์

**#96 โผล่มาระหว่างทำแถวเพดานสิบแกน** ลำดับของ CLO เรียงตาม `clo_number` แบบข้อความ พอเพิ่ม CLO-10
กับ CLO-11 เข้าไป มันจึงไปแทรกอยู่ระหว่าง CLO-1 กับ CLO-2 และข้อที่ตกขอบสิบแกนคือ CLO-9
แถวที่ 6 ของ `37a` จึงยืนยันแค่*จำนวน*แกน ไม่ยืนยันว่าเป็นข้อไหน — การปักลำดับไว้ตรงนี้จะต้องมาแก้อีกที
ตอน [#96](https://github.com/khthana/Deep-QA/issues/96) ถูกซ่อม และจะทำให้แถวนี้กลายเป็นตัวค้ำข้อบกพร่อง
แถวที่ 5 กลับ*อาศัย*ลำดับนี้โดยตั้งใจ เพราะมันต้องการข้อที่อยู่นอกกราฟและยังมีคะแนน

**`MAX_AXES` อยู่ใน `RadarChart` แต่มีหน้าจอเดียวที่บังคับใช้** #36 ยังวาด CLO ทุกข้อ วิชาที่มีสิบห้าข้อ
จะเลอะที่นั่น ไม่ใช่ที่นี่ ไม่ได้แก้ในตั๋วนี้เพราะการใส่เพดานให้ #36 จะทำให้แถวที่ 1 ของใบตรวจรับ #36
(*แกนของกราฟคือ CLO ทุกข้อ*) เป็นเท็จ ซึ่งเป็นการเปลี่ยนเกณฑ์ของตั๋วที่ปิดไปแล้ว — เปิดไว้เป็น [#112](https://github.com/khthana/Deep-QA/issues/112)

**`pointsOf` `axesOf` กับ `tableCell` ย้ายบ้าน** จาก `e2e/support/section-results-screen.js` ไปอยู่
`e2e/support/radar-chart.js` เพราะตอนนี้ `RadarChart` วาดสองหน้าจอ และสำเนาที่สองของตัวอ่านกราฟ
คือของที่ต้องแก้สองที่ทุกครั้งที่กราฟขยับ `36a` ชี้ไปที่บ้านใหม่แล้ว
