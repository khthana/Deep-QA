# ใบตรวจรับ #42 — ผลการเรียนรู้ระดับหลักสูตรตามปีรับเข้า

[#42](https://github.com/khthana/Deep-QA/issues/42) · หน้าจอ `/main/programLevelByIntake`

## สิ่งที่ต้องเข้าใจก่อนเดิน

**หน้านี้ไม่เก็บอะไรเลย** ทุกตัวเลขบนจอเป็นความเห็นเกี่ยวกับคะแนนที่ #34 บันทึกไว้ เหมือน #38
ต่างกันที่ระดับ — #38 ถามว่า *ตอนเรียนหนึ่งทำได้แค่ไหนกับผลการเรียนรู้รายวิชาข้อหนึ่ง* ส่วนหน้านี้ถามว่า
*นักศึกษารุ่นหนึ่งทำได้แค่ไหนกับผลการเรียนรู้ระดับหลักสูตรข้อหนึ่ง จากทุกรายวิชาที่รุ่นนั้นเรียนมา*

**การรวบยอดมีสองขั้น และสองขั้นนี้สลับกันไม่ได้**

1. คะแนนของนักศึกษาหนึ่งคนต่อ CLO หนึ่งข้อ — กฎของ #38 ไม่เปลี่ยนเลย: ได้เท่าไรหารด้วยที่มีให้ทำ
   คูณห้า ช่องที่ยังไม่มีคะแนนถูกตัดออกจากทั้งเศษและส่วน
2. คะแนนของคนคนนั้นต่อ PLO หนึ่งข้อ — **ค่าเฉลี่ยของคะแนน CLO** ทุกข้อที่ชี้มาที่ PLO นั้น
   โดยแต่ละ CLO นับหนึ่งเสียงเท่ากัน

ขั้นที่สองเป็นการตัดสินใจ ไม่ใช่สิ่งที่กฎข้อไหนเขียนไว้ **ผู้ใช้เลือกไว้ก่อนสร้าง** และเป็นกฎเดียวกับ
ที่ระบบเดิมใช้ (`DEEP-QA-BACKEND/services/ploScoreService.js`) ทางเลือกอีกทางคือรวมคะแนนดิบทั้งกอง
ซึ่งเป็นกฎของ #38 ยกขึ้นมาอีกชั้น — แต่จะทำให้ CLO ที่บังเอิญมีงานวัดเยอะพูดแทน PLO ของมันดังกว่า
CLO ที่มีงานวัดน้อย โดยไม่มีอะไรในข้อกำหนดบอกว่าควรเป็นเช่นนั้น และกรรมการที่อ่านว่า
*PLO-2 คือค่าเฉลี่ยของ CLO สามข้อที่รับใช้มัน* ตรวจเลขตามได้ ส่วนน้ำหนักที่มองไม่เห็น ตรวจไม่ได้

**กฎทั้งหมดย้ายไปอยู่ที่ `backend/lib/attainment.js` แล้ว** ตอน #38 มันอยู่ในไฟล์ route ของ #38 เอง
ซึ่งเป็นเหตุผลทั้งหมดที่ไฟล์นั้นมีอยู่ พอ #42 ต้องใช้ทุกข้อเหมือนกันเป๊ะ สำเนาที่สองของการ normalise
คือหนี้ชนิดที่ไม่ส่งเสียงตอนมันเพี้ยน — สองหน้าจอจะยังวาดตัวเลขที่ดูสมเหตุสมผลต่อไป และมีแต่คนที่เอา
สองใบมาวางข้างกันเท่านั้นที่จะเห็นว่ามันไม่ตรงกัน

**เกณฑ์ข้อ 5 ทำได้ครึ่งเดียวโดยตั้งใจ** หน้าจอ *เอ่ยชื่อ* หลักฐานของกิจกรรมที่เป็นที่มาของตัวเลข แต่
*เปิดไม่ได้* — [#35](https://github.com/khthana/Deep-QA/issues/35) เป็นเจ้าของทั้งการอัปโหลดและการ
ดาวน์โหลดที่ตรวจสิทธิ์ก่อน และหนึ่งในสองข้อบกพร่องที่ตั๋วนั้นมีไว้แก้คือระบบเดิมเสิร์ฟโฟลเดอร์หลักฐาน
เป็นไฟล์สถิตโดยไม่ตรวจอะไรเลย ปุ่มดาวน์โหลดที่นี่จึงเท่ากับเขียนเกณฑ์ของ #35 ซ้ำอีกรอบโดยไม่มีด่าน
**ผู้ใช้ตัดสินใจข้อนี้เมื่อ 3 ก.ย. 2569**

**ผู้ประเมินภายนอกอ่านหน้านี้ได้** ไม่ใช่เพราะข้อกำหนดบอก แต่เพราะเปลือกหน้าจอส่งเขามาที่นี่อยู่แล้ว
และเขียนเหตุผลไว้ที่ `frontend/src/components/SidebarItem/ExtAssessor.js` — เมนูของบทบาทนี้มีรายการ
เดียวและชี้มาที่นี่ ถ้า route ปฏิเสธเขา เมนูนั้นจะพาไปสู่คำปฏิเสธ ชุดหน้าจอที่ถูกต้องของบทบาทนี้ยังเป็น
[#49](https://github.com/khthana/Deep-QA/issues/49) และรายชื่อ `READERS` คือที่ที่มันจะเปลี่ยน

## ข้อมูลที่ใช้เดิน

seed มีสองรุ่น — **2566** (113 คน สองตอนเรียนของการเปิดสอนปี 2569) และ **2565** (60 คน ตอนเรียนเดียว
ของปี 2568) ทั้งสองรุ่นเรียนรายวิชาเดียวกันคนละปี หลักสูตร 0501 มีผลการเรียนรู้ข้อหลักสิบสามข้อ และ
**เจ็ดข้อเท่านั้นที่มี CLO ชี้มา** — อีกหกข้อจึงเป็นแถวที่ไม่มีใครถูกวัด ซึ่งเป็นกรณีที่เกณฑ์ข้อ 1 พูดถึง

**รุ่น 2566 กินสองตอนเรียนที่สอนโดยคนละคน** จึงเป็นข้อมูลที่ทำให้เกณฑ์ข้อ 2 ตรวจได้จริง — การรวบยอด
ที่หยุดอยู่ที่ตอนเรียนเดียวจะรายงานครึ่งรุ่นโดยไม่บอกใคร

## Checklist

- **☑** เดินด้วยมือจบแล้ว
- **◐** เดินไปครึ่งเดียว
- **☐** ยังไม่ได้เดิน
- **⚙** ครอบคลุมด้วย browser seam แล้ว (#65) ไม่ต้องเดินด้วยมือซ้ำ แถวบอกไฟล์ที่ครอบคลุมไว้

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| — | ทางเข้าอยู่ในเมนูของกรรมการหลักสูตร | เข้าด้วย `prog.manager@` → *การประเมินผลการเรียนรู้* → *ระดับหลักสูตรตามรุ่นปีรับเข้า* | เมนูกลุ่มกางออกและรายการพาไปที่ `/main/programLevelByIntake` — ครอบคลุมโดย `e2e/tests/42a-program-level-by-intake.spec.js` (แถว 1) | ⚙ |
| 1 | เลือกหลักสูตรและปีรับเข้าแล้วเห็นผลของ PLO ทุกข้อ | — | ตารางมีแถวเท่ากับจำนวนผลการเรียนรู้ข้อหลักของหลักสูตรนั้น **ไม่ใช่เฉพาะข้อที่มีการวัด** — ครอบคลุมโดย `42a` แถว 1 (`outcomesonlymeasured`) ซึ่งนับแถวจากฐานข้อมูลขณะนั้น ไม่ใช่จากเลขที่เขียนไว้ | ⚙ |
| 1 | ตัวเลือกทั้งสองขับรายงานจริง ไม่ใช่ป้ายประดับ | เปลี่ยนปีรับเข้าจาก 2566 เป็น 2565 | บรรทัดหัวเปลี่ยนทั้งปีและจำนวนคน (113 → 60) — ครอบคลุมโดย `42a` แถว 2 (`intakefrozen`) ซึ่งรอคำตอบที่มี `admission_year` ใหม่จริงก่อนอ่านจอ | ⚙ |
| 1 | รายการปีรับเข้ามาจากทะเบียน ไม่ใช่ช่วงปีที่เดาไว้ | ดู dropdown *ปีรับเข้า* | dropdown มีสองตัวเลือกพอดี **2566 (113 คน)** และ **2565 (60 คน)** ปีใหม่ขึ้นก่อน แต่ละตัวบอกจำนวนคน และไม่มีปีที่หลักสูตรนี้ไม่มีนักศึกษา · เดินมือ 3 ก.ย. 2569 (อ่านจาก `<option>` ที่วาดจริง) | ☑ |
| 2 | รวบยอดจากคะแนน CLO ข้ามทุกรายวิชาที่รุ่นนั้นเรียน | ดูคอลัมน์ *จำนวนที่วัดได้* ของ PLO-2 แล้วกด *ดูที่มา* | PLO-2 อ่านว่า **จำนวนที่วัดได้ 113** ไม่ใช่ 57 และแผงที่มามีกิจกรรมของ **ตอนเรียน 1 กับตอนเรียน 2** ปนกันอยู่ในรายวิชาเดียว — การรวบยอดที่หยุดที่ตอนเรียนเดียววาดออกมาไม่เหมือนนี้ **ใบนี้เคยเขียนว่าเบราว์เซอร์แยกไม่ออก ซึ่งผิด** · เดินมือ 3 ก.ย. 2569 | ☑ |
| 2 | สองขั้นของการรวบยอดให้เลขที่คิดตามได้บนกระดาษ | อ่าน `backend/test/program-results.test.js` | ชุดทดสอบปลูกรุ่นของตัวเองที่คะแนน 80/60/100 กับ 40/50/90 บนงานที่เต็ม 100 ต่อข้อ แล้วยืนยันว่า PLO-2 ของสองคนคือ 4.00 กับ 3.00 และของรุ่นคือ 3.50 — เลขทุกตัวคิดมือได้ ไม่ได้ให้โค้ดคิดให้ | ☑ |
| 3 | ใช้การ normalise ห้าคะแนนชุดเดียวกับหน้าจอระดับรายวิชา | อ่าน `backend/lib/attainment.js` และดูว่าใครเรียกมัน | ค่าคงที่และสูตรทั้งหมดมีสำเนาเดียว และทั้ง `learningDetails.js` (#38) กับ `programResults.js` (#42) นำเข้าจากที่เดียวกัน ไม่มีตัวเลข 5, 3, 60 หรือขอบแถบเขียนซ้ำในสองไฟล์นั้น | ☑ |
| 3 | ช่องที่ยังไม่มีคะแนนไม่ถูกนับเป็นศูนย์ ที่ระดับนี้ด้วย | — | คนที่ถูกวัดสองข้อจากสามข้อได้ 3.50 ไม่ใช่ 2.33 — พิสูจน์ที่ seam HTTP (*an unmarked piece of work is left out of the roll-up*) | ◐ |
| 4 | เลือก PLO แล้วเห็นรายวิชาและกิจกรรมที่เป็นที่มา | กด *ดูที่มา* ที่แถวใดแถวหนึ่ง แล้วกดซ้ำ | แผงกางออกพร้อมชื่อรายวิชาและกิจกรรม แล้วกดซ้ำหุบกลับ — ครอบคลุมโดย `42a` แถว 3 (`drilldownwontclose`) | ⚙ |
| 4 | แผงบอกด้วยว่ากิจกรรมนั้นเชื่อมกับ CLO ข้อไหน | ดูใต้ชื่อกิจกรรม | ใต้ชื่อกิจกรรมอ่านว่า *ตอนเรียน 2 · คะแนนเต็ม 100.00 · CLO-1 · CLO-3* ครบทั้งตอนเรียน คะแนนเต็ม และรายการ CLO · เดินมือ 3 ก.ย. 2569 | ☑ |
| 5 | เปิดไฟล์หลักฐานของกิจกรรมที่เป็นที่มาได้ | กด *ดูที่มา* แล้วดูใต้กิจกรรม | **ครึ่งที่ทำแล้ว** ชื่อไฟล์ ประเภท และคำบรรยายขึ้นบนจอ และไฟล์ที่ถูกลบแล้วไม่ขึ้น — พิสูจน์ที่ seam HTTP (*the drill-down names the evidence a figure rests on*) · **ครึ่งที่ยังไม่ได้ทำ** การเปิดไฟล์ ซึ่งเป็นของ [#35](https://github.com/khthana/Deep-QA/issues/35) ทั้งการอัปโหลดและการตรวจสิทธิ์ก่อนดาวน์โหลด หน้าจอเขียนบอกไว้ตรง ๆ ว่ายังเปิดไม่ได้และรออะไรอยู่ | ◐ |
| 6 | รุ่นที่ยังไม่มีคะแนนได้ข้อความอธิบาย ไม่ใช่ศูนย์ | สร้างรุ่นที่ลงทะเบียนแล้วแต่ยังไม่มีใครให้คะแนน แล้วเลือกปีนั้น | อ่านว่า *ยังไม่มีคะแนนของนักศึกษารุ่นนี้* และ **ไม่มีตาราง** ไม่ใช่สิบสามแถวที่เป็นขีดทั้งหมด — ครอบคลุมโดย `42a` แถว 5 (`emptydrawsthetable`) ซึ่งลงทะเบียนรุ่นของตัวเองแล้วเก็บกวาดเอง | ⚙ |
| 7 | ชื่อ route ไฟล์และคอมโพเนนต์เป็นระดับหลักสูตร ไม่เหลือชื่อระดับรายวิชา | `grep -rn courseLevel` นอก `DEEP-QA-*/` | เหลือแต่ **ร้อยแก้ว** — สามแห่งที่พูดถึงการเปลี่ยนชื่อ (`AppRoutes.js` คอมเมนต์ · `CONTEXT.md` คำที่ให้เลี่ยง · `docs/07` ตารางตั๋ว) และอีกยี่สิบสี่แห่งใน `docs/05-screen-api-mapping.md` ซึ่งรวมแถวตาราง route ของ `/main/courseLevelByIntake` ด้วย — **เอกสารชุด `docs/01`–`05` บรรยายระบบที่ส่งมอบ ไม่ได้สั่งว่าจะสร้างอะไร** (CLAUDE.md) ชื่อเก่าที่นั่นจึงเป็นบันทึกว่าของเดิมชื่ออะไร ไม่ใช่ชื่อที่ยังใช้อยู่ ไม่มี route ไฟล์ หรือคอมโพเนนต์ใดในต้นไม้ที่กำลังสร้างชื่อระดับรายวิชา — **การเปลี่ยนชื่อทำไปแล้วตอนสร้างเปลือกหน้าจอ #10 ตั๋วนี้ไม่ได้เปลี่ยนอะไรเพิ่ม** เพียงยืนยันว่ายังเป็นเช่นนั้น | ☑ |
| 8 | กรรมการหลักสูตรถูกปฏิเสธหลักสูตรอื่น บังคับที่เซิร์ฟเวอร์ | — | `prog.manager@` (0501) ขอ 0503 ได้ *ไม่พบหลักสูตรที่ระบุ* เท่ากับที่ขอหลักสูตรที่ไม่มีอยู่ และ `prog.manager.0503@` ได้ผลกลับกัน — พิสูจน์ที่ seam HTTP ทั้งสองทาง · **เบราว์เซอร์ยิงคำขอนี้ไม่ได้** เพราะหลักสูตรเป็น query ที่หน้าจอตั้งจากรายการที่เซิร์ฟเวอร์ส่งมา ไม่ได้อยู่ใน URL ที่คนพิมพ์ได้ | ◐ |
| 8 | อาจารย์ผู้สอนไม่ถึงหน้านี้เลย | เข้าด้วย `teacher.one@` แล้วพิมพ์ `/main/programLevelByIntake` ที่แถบที่อยู่ | `teacher.one@` ได้ 403 — พิสูจน์ที่ seam HTTP · **แถวนี้เดินได้ ยังไม่ได้เดิน** ช่องที่อยู่ก็เป็นตัวควบคุมอันหนึ่ง ไม่เหมือนแถวข้างบนที่ไม่มีทางสร้างคำขอนั้นจากหน้าจอเลย ผู้ใช้เลือกไม่เดินเมื่อ 3 ก.ย. 2569 เพราะไม่ได้อยู่ในใบเดินตอนนั้น | ◐ |
| 9 | ชุดทดสอบปลูกรุ่นที่มีคะแนนรู้ค่า แล้วยืนยันเลขที่รวบยอดได้ | อ่าน `backend/test/program-results.test.js` | สิบเจ็ดข้อ ปลูกรุ่นของตัวเองสามรุ่น (มีคะแนน · ลงทะเบียนแต่ไม่มีคะแนน · มีช่องว่าง) และยืนยันทั้งค่าเฉลี่ย อัตราผ่าน คำตัดสิน และแถบ | ☑ |
| — | ผู้ประเมินภายนอกไปถึงหน้าเดียวที่เมนูของเขามี | เข้าด้วย `external.assessor@` แล้วกดเมนูเดียวที่มี | ได้รายงานจริง ไม่ใช่ *หน้านี้กำลังพัฒนา* — ครอบคลุมโดย `42a` แถว 6 (`assessorlosesthescreen`) · **นี่คือสิ่งที่ปิดครึ่งหนึ่งของแถว ☐ ใน `10-application-shell.md`** ส่วนที่เหลือคือชุดเมนูที่ถูกต้อง ซึ่งยังเป็น #49 | ⚙ |
| — | ข้อที่ยังไม่มีใครถูกวัดไม่ถูกวาดเป็นผ่าน | ดูชิปของ PLO-4 | ชิปเป็น `bg-slate-100` ขีดกลาง ไม่ใช่ `bg-emerald-100` ของ Y — สามสถานะ ไม่ใช่สอง · **นี่คือความพังที่ #38 ส่งมอบไปแล้วและถูกจับได้ตอนเดินมือ** หน้านี้สร้างโดยรู้เรื่องนั้นแต่แรก — ครอบคลุมโดย `42a` แถว 4 (`unmeasuredreadsaspass`) | ⚙ |
| — | ชิปคำตัดสินพูดได้ ไม่ใช่มีแต่สีกับตัวอักษรเดียว | — | ป้ายอ่านว่า *PLO-2 ผ่าน — ผ่านเกณฑ์ 96.5% ของนักศึกษาที่ถูกวัด* หรือ *PLO-4 ยังไม่มีการวัด* — ครอบคลุมโดย `42a` แถว 4 (`labelsaysonlythecode`) | ⚙ |
| — | คำอธิบายแถบสีอ่านรู้เรื่อง และตัวเลขมาจากกฎ | ดูแถบคำอธิบายเหนือตาราง | ห้าช่วงพร้อมสี — ต่ำกว่า 3.0 (แดง) · 3.0 – 3.4 (ส้ม) · 3.5 – 3.9 (เหลือง) · 4.0 – 4.4 (เขียวอ่อน) · 4.5 ขึ้นไป (เขียว) ไล่เป็นสเกลเดียว ตรงกับ BR-20 และมาจาก `band_floors` · เดินมือ 3 ก.ย. 2569 | ☑ |
| — | คอลัมน์ *ประเภท* อ่านเป็นคำ ไม่ใช่รหัสอังกฤษ | ดูคอลัมน์ที่สอง | อ่านว่า *ความรู้* · *ทักษะ* · *จริยธรรม* · *ลักษณะบุคคล* ครบทั้งสี่ประเภทที่หลักสูตรนี้มี ไม่มี `knowledge` หลุดมา · เดินมือ 3 ก.ย. 2569 | ☑ |
| — | ประโยคที่บอกว่าหลักฐานยังเปิดไม่ได้ อ่านแล้วเข้าใจว่ารออะไร | กด *ดูที่มา* แล้วอ่านบรรทัดล่างสุดของแผง | ผู้ใช้อ่านแล้วเข้าใจว่ารออะไร แต่รอบแรกตอบว่า **ต้องสังเกต เพราะสีจาง** — `text-xs text-slate-400` บนพื้น `bg-gray-50` ได้คอนทราสต์ราว 2.5:1 ต่ำกว่าเกณฑ์ AA แก้เป็น `text-slate-600` (~7:1) แล้วให้ดูอีกรอบ ได้คำตอบว่า *อ่านได้ชัดเจน* · เดินมือ 3 ก.ย. 2569 **ผู้ใช้เป็นคนติ๊ก ไม่ใช่คนเขียน** | ☑ |

## บันทึกการเดินด้วยมือ

เดินเมื่อ **3 ก.ย. 2569** โดย Claude ขับผ่านส่วนขยาย Chrome ผู้ใช้นั่งดูอยู่ · บัญชี `prog.manager@`
(กรรมการหลักสูตร 0501) · ข้อมูล `deep_core` รุ่น 2566

สี่แถว ☐ ติ๊กครบ และอีกสองแถว ◐ กลายเป็น ☑ เพราะสิ่งที่วาดออกมาบอกได้มากกว่าที่ใบนี้เดาไว้ —
แถว *รวบยอดข้ามทุกรายวิชา* เคยเขียนว่าเบราว์เซอร์เห็นแค่ตัวเลขสุดท้าย แต่คอลัมน์ *จำนวนที่วัดได้*
อ่านว่า 113 และแผงที่มามีสองตอนเรียนอยู่ในรายวิชาเดียว ซึ่งเป็นสองอย่างที่การรวบยอดผิดวาดไม่ได้

**การเดินหาข้อบกพร่องเจอหนึ่งข้อ และแก้ระหว่างเดิน** ประโยคที่บอกว่าหลักฐานยังเปิดไม่ได้ใช้
`text-slate-400` ผู้ใช้ตอบว่าเห็น *แต่ต้องสังเกต* — โน้ตที่ไม่มีใครอ่านก็เท่ากับไม่มี และประโยคนี้คือ
คำตอบของคำถาม *ทำไมเปิดไฟล์ไม่ได้* เปลี่ยนเป็น `text-slate-600` แล้วให้ดูซ้ำ ได้คำตอบว่าอ่านได้ชัดเจน

**สิ่งที่ไม่ได้เดิน และเพราะอะไร** ผู้ใช้ตัดสินใจไม่สร้างสถานการณ์นอกใบเดิน สองแถวที่เหลือจึงคง ◐ ไว้
ตามที่ใบนี้เขียนไว้แต่แรก — ครึ่งที่เหลือของแถวหลักฐานเป็น *การเปิดไฟล์* ซึ่งเป็นของ #35 ทั้งก้อน
และแถวอาจารย์ผู้สอนถูกปฏิเสธพิสูจน์ที่ seam HTTP อยู่แล้ว

**สิ่งที่เห็นโดยไม่ได้ตั้งใจ** กดปุ่มพลาดไปโดน PLO-1 ซึ่งไม่มี CLO ชี้มาเลย แผงกางออกแล้วอ่านว่า
*ยังไม่มีรายวิชาใดของรุ่นนี้ที่บันทึกคะแนนไว้กับผลการเรียนรู้ข้อนี้* ไม่ใช่แผงเปล่าที่ปล่อยให้คนเดาว่า
โหลดค้างหรือไม่มีข้อมูล ไม่มีแถวไหนขอไว้ แต่บันทึกไว้เพราะเป็นสิ่งที่คนจะเจอ

## สิ่งที่ #43 เปลี่ยนในหน้าจอนี้ (3 ก.ย. 2569)

**ตัวเลือกหลักสูตรกับปีรับเข้าย้ายออกไปเป็นของกลาง** `frontend/src/components/results/CohortPickers.js`
เพราะ #43 เป็นผู้เรียกรายที่สอง และถ้าปล่อยไว้ #44 กับ #45 จะเป็นสำเนาที่สามและสี่ หน้าจอนี้วาดเหมือนเดิม
ทุกจุด มัตแตนต์ `intakefrozen` ย้ายตามไปอยู่ที่ไฟล์นั้น และกวาดใหม่แล้วยังฆ่าแถว 2 ได้เหมือนเดิม
คำอธิบายแถบสีก็ย้ายไปเป็น `components/results/BandLegend.js` ด้วยเหตุผลเดียวกัน

**`empty` เปลี่ยนวิธีนับ และเป็นการแก้ข้อบกพร่อง** เดิมนับจากจำนวนแถวที่ query คะแนนส่งกลับมา
(`marks.length === 0`) ซึ่งตอบ *ไม่ว่าง* ได้ทั้งที่ไม่มีคะแนนไปถึงคอลัมน์ไหนเลย — `subject_clo.plo_id`
เป็น NULL ได้ CLO ที่ไม่ชี้ไปที่ผลการเรียนรู้ข้อใดจึงมีคะแนนที่ไม่เป็นของแถวไหนในรายงานนี้ ตอนนี้นับจาก
คอลัมน์ที่รวบยอดออกมาแล้ว **การรีวิวของ #43 เป็นคนเจอ** และแก้พร้อมกันทั้งสองหน้าจอ ชุดทดสอบของ #43
มีข้อยืนยันของกรณีนี้

## มัตแตนต์ที่พิสูจน์แถว ⚙

`mutation/42-program-level-by-intake.py` แปดตัว อ่านวิธีใช้ที่ `mutation/README.md` ก่อน

ตารางนี้บันทึก **สิ่งที่มัตแตนต์ฆ่าได้จริง** ไม่ใช่สิ่งที่ควรจะฆ่าได้

| มัตแตนต์ | แถวที่มันค้ำ | ตายที่ |
|---|---|---|
| `committeemenumisleads` | ทางเข้าในเมนูกรรมการ | แถว 1 ที่ `waitForURL` — รายการเมนูเปิดหน้าจออื่น |
| `outcomesonlymeasured` | 1 (ข้อ 1) | แถว 1 ที่ `toHaveCount` — ตารางเหลือเจ็ดแถวจากสิบสาม · แถว 4 ล้มตามไปด้วย เพราะ PLO-4 ที่มันตรวจหายไปทั้งแถว |
| `intakefrozen` | 2 (ข้อ 1) | แถว 2 ที่ `waitForResponse` — ไม่มีคำขอที่มีปีใหม่ออกไปเลย · แถว 5 ล้มตามไปด้วย เพราะมันก็เลือกปีเหมือนกัน |
| `drilldownwontclose` | 3 (ข้อ 4) | แถว 3 ที่ `toHaveText('ดูที่มา')` — ปุ่มยังเขียนว่า *ซ่อนที่มา* หลังกดครั้งที่สอง |
| `unmeasuredreadsaspass` | 4 (สามสถานะ) | แถว 4 ที่ `toHaveClass(/bg-slate-100/)` — ชิปของ PLO-4 เป็นเขียว |
| `labelsaysonlythecode` | 4 (ป้ายที่พูดได้) | แถว 4 ที่ `toHaveAttribute('aria-label', ...)` — ป้ายเหลือแค่ `PLO-4` |
| `emptydrawsthetable` | 5 (ข้อ 6) | แถว 5 ที่ `toBeVisible` ของประโยค — ได้ตารางขีดสิบสามแถวแทน |
| `assessorlosesthescreen` | 6 (ผู้ประเมินภายนอก) | แถว 6 ที่ `toBeVisible` ของบรรทัดรุ่น — เมนูเดียวที่บทบาทนี้มีพาไปสู่คำปฏิเสธ |

### สองตัวที่ต้องเขียนใหม่ก่อนจะเชื่อได้

กวาดรอบแรกไม่ผ่านสองตัว และทั้งสองสอนคนละเรื่อง

**`drilldownwontclose` ไม่ฆ่าใครเลย** แถว 3 ถามแค่ว่าหัวข้อของแผงหายไปหรือยัง และการกดซ้ำที่
*ดึงข้อมูลใหม่* แทนที่จะหุบ ก็ทำให้แผงว่างไปชั่วขณะระหว่างรอคำตอบ — คำยืนยันจึงผ่านบนช่องว่างไม่กี่
ร้อยมิลลิวินาที ระหว่างทางไปสู่แผงเดิมที่กลับมา แถวนี้ตอนนี้อ่าน **ปุ่ม** ซึ่งเขียนว่า *ซ่อนที่มา* ตลอดเวลา
ที่ข้อนั้นเปิดอยู่ และไม่ได้เป็นอย่างอื่นชั่วขณะ

**`intakefrozen` ฆ่าทั้งหกแถว** ตัวเดิมตัดปีออกจาก dependency array ของ effect ซึ่งทำให้รายงาน
*แรก* ไม่โหลดด้วย — หน้าจอหายไปทั้งหน้า ไม่ใช่แค่ตัวเลือกที่ขับอะไรไม่ได้ มัตแตนต์ที่เอาหน้าจอไปทั้งหน้า
ไม่ได้พิสูจน์อะไรเกี่ยวกับแถวที่มันถูกเขียนมาเพื่อ ตัวใหม่แก้ที่ตัวจัดการ `onChange` ให้เมินตัวเลือกที่ถูกเลือก
แล้วใส่ปีล่าสุดกลับไป — รายงานยังวาด ตัวเลขยังถูกต้องสำหรับรุ่นที่มันแสดง และสิ่งเดียวที่ผิดคือมันเป็น
รุ่นเดิมเสมอ

## บันทึกของ e2e ที่ผ่านตอนรันเดี่ยวแต่ล้มตอนรันทั้งชุด

`42a` ผ่านหกจากหกตอนรันเดี่ยว แล้วล้มห้าจากหกตอนรันทั้งชุด สาเหตุไม่ใช่ลำดับหรือการแย่งข้อมูล แต่เป็น
**สิ่งที่หน้าจอเปิดขึ้นมาแล้วเลือกให้เอง** — รายการปีรับเข้าเรียงปีใหม่ขึ้นก่อนและหน้าจอเลือกตัวแรก ส่วน
แถวนำเข้านักศึกษาของ `17b` ที่รันไปก่อนหน้า ใส่นักศึกษารหัส `68…` ลงทะเบียนหลักสูตร 0501 ไว้ ซึ่งเป็น
รุ่น **2568** ใหม่กว่า 2566 ของ seed ทั้งชุด รายงานจึงเปิดขึ้นมาที่รุ่นที่ยังไม่มีใครให้คะแนน และวาด
ประโยค *ยังไม่มีคะแนน* แทนตาราง

นี่ไม่ใช่ข้อบกพร่องของหน้าจอ — รุ่นที่เพิ่งนำเข้าและยังไม่มีคะแนนก็ควรอ่านอย่างนั้นจริง ๆ สิ่งที่ผิดคือแถว
ที่อ่าน *อะไรก็ตามที่รายงานเปิดขึ้นมา* แทนที่จะบอกว่าตัวเองหมายถึงรุ่นไหน ทุกแถวจึงเรียก
`showIntake(page, ปี)` ก่อนอ่านจอ และ `npx playwright test 17b 42a` — ลำดับที่ทำให้มันล้ม — ผ่านสิบสอง
จากสิบสอง
