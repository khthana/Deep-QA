# ตรวจรับด้วยมือ — #22 Rubric criteria

`docs/06-implementation-plan.md` §Acceptance กำหนดว่า ticket หนึ่งจะเสร็จเมื่อ automated test ผ่าน
**และ** checklist ที่ทำด้วยมือถูกเดินจนครบ เอกสารนี้คือครึ่งหลังของ #22

ครึ่งแรกอยู่ที่ `backend/test/rubric-criteria.test.js` (13 subtests) — ทุกเกณฑ์ที่พิสูจน์ได้ที่ seam ของ
HTTP surface ถูกพิสูจน์ที่นั่นแล้ว รวมทั้งข้อที่ **ไม่มีหน้าจอไหนแสดงได้** คือเกณฑ์ที่มีอยู่จริงแต่อยู่ใต้
Rubric คนละตัวกับที่อยู่ใน address

seam ที่สองคือเบราว์เซอร์ — `e2e/tests/22a-rubric-criteria.spec.js` 10 ข้อทดสอบ
ดูหัวข้อ *มัตแตนต์* ท้ายเอกสารว่าแต่ละแถว ⚙ ถูกพิสูจน์ด้วยการทำอะไรพัง

## สิ่งที่ต้องเข้าใจก่อนเดิน — เกณฑ์ไม่มีหลักสูตรเป็นของตัวเอง

`rubric_details` **ไม่มีคอลัมน์ `program_id`** สิ่งเดียวที่บอกว่าใครเขียนเกณฑ์ได้คือ Rubric ที่อยู่เหนือมัน
ซึ่ง address เป็นคนระบุ ทุก verb ของหน้านี้จึงตรวจ Rubric ก่อนเสมอ และตรวจ **สองครึ่งที่แยกกัน**

1. Rubric นั้นอยู่ในความรับผิดชอบของบัญชีนี้หรือไม่ — ใช้ `reachableRubric` ตัวเดียวกับ #21
   ไม่ได้ถามฐานข้อมูลเองซ้ำ เพราะสองคำตอบต่อคำถามเดียวจะเพี้ยนออกจากกันสักวัน
2. **เกณฑ์นั้นอยู่ใต้ Rubric ตัวที่ address ระบุหรือไม่** — ทุกคำสั่งเป็น `WHERE id = $1 AND rubric_id = $2`
   ไม่ใช่ `WHERE id = $1` เดี่ยว ๆ ถ้าขาดครึ่งหลัง บัญชีที่เข้าถึง Rubric A ได้จะแก้เกณฑ์ของ Rubric B
   ได้ด้วยการเรียก `/rubrics/A/criteria/<id ของ B>` การตรวจแม่ผ่าน แถวถูกหาเจอด้วย id ของตัวเอง
   และไม่มีอะไรบังคับให้สองอย่างตรงกันเลย นั่นคือเกณฑ์ข้อ 6 ของตั๋วที่พังทั้งที่ route ดูถูกต้อง

`rubric-criteria.test.js` จึงทดสอบครึ่งที่สองด้วย Rubric **สองตัวของบัญชีเดียวกัน** ที่ซึ่ง reach
ไม่ได้ทำงานอะไรเลย สิ่งที่ปฏิเสธคือการจับคู่

## route เข้มกว่า schema โดยตั้งใจ — คำอธิบายทั้งสี่ระดับ

`level_1_description` ถึง `level_4_description` เป็น `text` ที่ **ไม่มี `NOT NULL`** แต่เกณฑ์ข้อ 3
ของตั๋วบอกว่าเกณฑ์หนึ่งข้อต้องมีคำอธิบายครบทั้งสี่ระดับ route จึงเข้มกว่า schema ของตัวเอง
เหมือนที่ #21 เข้มกว่า `rubrics.program_id` ที่เป็น nullable

เหตุผลไม่ใช่ความเรียบร้อย — Rubric ที่เขียนคำอธิบายไว้ครึ่งเดียวแย่กว่าไม่มี Rubric เลย
มันดูเหมือนแนวทางแต่ไม่ได้ให้แนวทาง และผู้ตรวจที่อ่านจะเติมช่องว่างด้วยดุลพินิจของตัวเอง
ซึ่งคือสิ่งเดียวที่ Rubric มีไว้เพื่อกำจัด

เทสต์จึงลองไม่ส่ง **ทีละระดับครบทั้งสี่** ไม่ใช่ระดับเดียว — validator ที่ตรวจแค่ `level_4_description`
จะผ่านชุดทดสอบที่ลองไม่ส่งแค่ระดับนั้น

## น้ำหนักเป็นตัวเลข ไม่ใช่สตริง

`weight` เป็น `numeric(5,2)` และ **node-postgres อ่านคอลัมน์ `numeric` กลับมาเป็นสตริง** —
`'1.00'` ไม่ใช่ `1` เพราะชนิดนี้กว้างกว่า double ของ JavaScript และ driver ไม่ยอมย่อให้เงียบ ๆ
ที่นี่มันล้น double ไม่ได้ (ห้าหลัก ทศนิยมสองตำแหน่ง แทนได้ตรง ๆ ในฐานสอง) route จึง cast เป็น
`float8` ขาออก JSON จึงพา *ตัวเลข* ถ้าปล่อยเป็นสตริง หน้าจอจะเขียน `12.50` ตรงที่คนพิมพ์ `12.5`
และอะไรก็ตามที่บวกน้ำหนักสองค่าจะได้การต่อสตริง

**ทศนิยมเกินสองตำแหน่งถูกปฏิเสธ** เพราะ PostgreSQL ไม่ได้ปฏิเสธ — มันปัดให้เงียบ ๆ
แถวที่กลับมาจึงไม่ใช่แถวที่ส่งไป และไม่มีใครบอกคนกรอกเลย การตรวจเขียนด้วยค่าความคลาดเคลื่อน
ไม่ใช่ `Math.round(w * 100) !== w * 100` ซึ่งเป็นรูปที่เขียนไว้ตอนแรกและ **ปฏิเสธ 1.1, 8.2 และ 0.07**
เพราะหนึ่งในสิบไม่ลงตัวในฐานสอง เทสต์มีแถวที่ยิงค่าเหล่านั้นเข้าไปโดยเฉพาะ

**น้ำหนัก 0 ถูกปฏิเสธเป็นการตัดสินใจ ไม่ใช่เลขคณิต** เกณฑ์ที่มีน้ำหนักศูนย์ถูกตรวจ ถูกแสดง
และไม่มีผลอะไรเลย ซึ่งเป็นความผิดพลาดที่หน้าตาเหมือนการตั้งค่า วิธีเอาเกณฑ์ออกคือลบมัน

## สิ่งที่ตั๋วเขียนไว้แล้วโค้ดไม่ทำตาม และทำไม

**`FACULTY_ADMIN` เข้าหน้านี้ไม่ได้** ตั๋วไม่ได้พูดถึงคณะโดยตรง แต่ `MAINTAINERS` ของหน้านี้
คือตัวเดียวกับของ #21 ซึ่ง [#79](https://github.com/khthana/Deep-QA/issues/79) ระบุ **A04** ไว้ตรง ๆ
และเหตุผลของ #79 ใช้กับหน้านี้ยิ่งกว่าหน้าแม่ของมัน — คณะถือ *รายชื่อ* ภาควิชาและหลักสูตร
ส่วนสิ่งที่อยู่ *ข้างใน* หลักสูตร ซึ่งเกณฑ์การให้คะแนนของ Rubric อยู่ลึกที่สุดในนั้น เป็นของภาควิชากับกรรมการหลักสูตร

**หน้านี้ไม่แบ่งหน้า** ต่างจากทุกหน้าข้อมูลหลักในระบบ Rubric คือหน้ากระดาษที่ผู้ตรวจอ่านระหว่างตรวจงาน
แนวทางที่ถูกตัดครึ่งไปอยู่คนละหน้าแย่กว่าแนวทางที่ยาว หน้าจอบอกจำนวนแทน

**หน้านี้ไม่มีตัวเลือกหลักสูตรและไม่มีตัวกรอง** ไม่มีอะไรให้เลือก — เกณฑ์อยู่ใต้ Rubric เดียว
และ Rubric อยู่ใต้หลักสูตรเดียว address คือคำตอบทั้งหมด (ADR-0004 รูปเดิม ต่ำลงมาหนึ่งชั้น)

## ข้อจำกัดที่รู้อยู่ ไม่ใช่บั๊กของตั๋วนี้

**คำอธิบายสี่ระดับที่ยาวมากทำให้ตารางกว้างเกินจอ** ตารางเลื่อนแนวนอนได้ (`overflow-x-auto`)
ซึ่งเป็นสิ่งเดียวกับที่ทุกตารางกว้างในระบบนี้ทำ ถ้าจะทำให้ดีกว่านั้นควรเป็นตั๋วของตัวเอง

## ตั้งต้น

```
docker start deep-core-postgres
cd db      && npm run reset && npm run migrate && npm run seed
cd backend && npm start          # http://localhost:3000
cd frontend && npm start         # http://localhost:5000
```

ทุกบัญชีใน seed ใช้รหัสผ่านเดียวกัน คือค่า `PASSWORD` ใน `db/seed.js:58`
อีเมลต้องพิมพ์เต็มรวมโดเมน `@kmitl.ac.th` — อีเมลหรือรหัสผ่านที่ผิดทำให้หน้าจอขึ้นกล่อง
*Session หมดอายุ* ทับประโยคจริงไว้ หลงทางง่ายมาก ([#97](https://github.com/khthana/Deep-QA/issues/97) ยังไม่แก้)

**ต้อง restart backend ก่อนเดิน** — process ที่ค้างจากตั๋วก่อนจะตอบ *ไม่พบเส้นทางที่เรียกบนเซิร์ฟเวอร์นี้*
ให้ทุก route ของ #22 และ **`Server running` ใน log ไม่ใช่หลักฐานว่า route ใหม่อยู่ในนั้น**

**seed ไม่ได้เปลี่ยนในตั๋วนี้** ถ้าเพิ่งเดิน #21 จบ ให้ `npm run seed` ใหม่ก่อน เพราะแถวของ #21
ลบ `RUB-01` ทิ้งไปหนึ่งแถว ข้อมูลที่แถวข้างล่างอ้างถึงคือ

- **`RUB-01` มีเกณฑ์ย่อย 3 ข้อ และ `RUB-02` มี 2 ข้อ** ที่เหลือของ `0501` ยังว่าง
- เกณฑ์ที่ seed สร้างมีคำอธิบายครบทั้งสี่ระดับทุกข้อ ชื่อไทยอ่านว่า *`<ชื่อ Rubric>` — เกณฑ์ที่ `<n>`*
  และ **น้ำหนักเป็น 1 ทุกข้อ** แถวที่ต้องเห็นน้ำหนักต่างกันจึงต้องพิมพ์เอง
- **ลำดับของเกณฑ์ที่ seed สร้างคือ 1, 2, 3 เรียงตรงกับเลขในชื่อ** แถวที่พิสูจน์เรื่องการเรียง
  จึงต้องสร้างเกณฑ์เองอย่างน้อยสองข้อที่ลำดับสวนทางกับชื่อ ([#96](https://github.com/khthana/Deep-QA/issues/96))
- `0503` มี `RUB-51` กับ `RUB-52` ซึ่ง **ไม่มีเกณฑ์ย่อยเลย** ใช้สำหรับแถวที่ต้องเห็น Rubric ว่าง

หลายข้อข้างล่างเขียนข้อมูลจริงลงฐานข้อมูล — **`npm run seed` ใหม่หลังเดินจบ** ก่อนเริ่มตั๋วถัดไป

## รายการที่ต้องเดิน

ช่องสุดท้ายมีสี่ค่า

- **☑** เดินจบแล้ว
- **◐** ครึ่งของ server เดินแล้วและผ่าน ส่วนที่เหลือคือสิ่งที่หน้าจอวาด (แถบสี ตาราง dropdown) ยังต้องมีคนดู
- **☐** ยังไม่ได้เดิน
- **⚙** ครอบคลุมด้วย browser seam แล้ว ไม่ต้องเดินด้วยมือซ้ำ ถ้าแถวนั้นพูดถึงสิ่งที่วาดด้วย ครึ่งนั้นยังเป็นงานของคนเดิน และแถวจะเขียนกำกับไว้

| # | เกณฑ์ | ทำอะไร | ต้องเห็นอะไร | ✓ |
|---|---|---|---|---|
| 1 | ทางเข้าจากหน้า Rubric พาไปที่ Rubric ตัวนั้น และ Rubric ที่ยังว่างบอกว่าว่าง | เข้าด้วย `prog.manager@kmitl.ac.th` → *ข้อมูล Rubric กลาง* → กดลิงก์ *ยังไม่มีเกณฑ์ — กำหนดเกณฑ์* ที่แถว `RUB-03` | หัวเรื่องอ่านว่า *เกณฑ์การให้คะแนนของ Rubric `RUB-03`* พร้อมชื่อไทยของ Rubric นั้นใต้หัวเรื่อง และตารางว่างอ่านว่า *ยังไม่มีเกณฑ์การให้คะแนนใน Rubric นี้* — ครอบคลุมโดย `22a-rubric-criteria.spec.js` | ⚙ |
| 1 · 2 · 3 | เพิ่มเกณฑ์ได้ พร้อมน้ำหนักและคำอธิบายครบสี่ระดับ | *เพิ่มเกณฑ์* → ชื่อไทย/อังกฤษ น้ำหนัก `40` ลำดับ `10` และคำอธิบายสี่ช่อง → บันทึก | แถบเขียว *บันทึกข้อมูลเรียบร้อยแล้ว* และแถวใหม่แสดง **ชื่อทั้งสองภาษา น้ำหนัก และคำอธิบายทั้งสี่ระดับ** ในแถวเดียว และบรรทัดใต้หัวเรื่องเปลี่ยนเป็น *Rubric นี้มีเกณฑ์การให้คะแนน 1 ข้อ* — ครอบคลุมโดย `22a-rubric-criteria.spec.js` | ⚙ |
| 1 | แก้ไขได้ | *แก้ไข* ที่เกณฑ์ที่เพิ่งสร้าง → เปลี่ยนชื่อไทยและน้ำหนักเป็น `15.5` → บันทึก | ชื่อใหม่และน้ำหนัก `15.5` ปรากฏในตาราง และแถวเดิมไม่เหลืออยู่ — ครอบคลุมโดย `22a-rubric-criteria.spec.js` | ⚙ |
| 4 | เรียงตามลำดับที่ตั้งไว้ และลำดับเท่ากันก็ยังเรียงนิ่ง | สร้างเกณฑ์สามข้อ ลำดับ `20`, `20`, `5` โดยตั้งชื่อให้เรียงสวนทางกับลำดับ | ข้อที่ลำดับ `5` อยู่บนสุด และสองข้อที่ลำดับ `20` เท่ากันอยู่ **ตามลำดับที่เพิ่ม** ทุกครั้งที่โหลด ไม่ใช่เรียงตามชื่อ — ครอบคลุมโดย `22a-rubric-criteria.spec.js` | ⚙ |
| 1 · 5 | จำนวนเกณฑ์ที่หน้า Rubric นับ เปลี่ยนตามที่หน้านี้เขียน | กลับไป *ข้อมูล Rubric กลาง* แล้วดูแถวของ Rubric ที่เพิ่งเติมเกณฑ์ | ลิงก์ที่เคยอ่านว่า *ยังไม่มีเกณฑ์* อ่านว่า *ดูเกณฑ์ `<n>` ข้อ* ตรงกับจำนวนที่เพิ่งเขียน — ครอบคลุมโดย `22a-rubric-criteria.spec.js` | ⚙ |
| 5 | ถามยืนยันก่อนลบ กดยกเลิกแล้วไม่มีอะไรเปลี่ยน และลบแล้วบอกว่าลบอันไหน | *ลบ* ที่เกณฑ์หนึ่งข้อ → *ยกเลิก* → *ลบ* อีกครั้ง → *ลบเกณฑ์* | หลังยกเลิกแถวยังอยู่ครบ หลังยืนยันได้แถบเขียว *ลบเกณฑ์ `<ชื่อไทย>` เรียบร้อยแล้ว* แถวนั้นหายไป **แถวอื่นอยู่ครบ** และจำนวนใต้หัวเรื่องลดลงหนึ่ง — ครอบคลุมโดย `22a-rubric-criteria.spec.js` | ⚙ |
| 5 | ลบ Rubric แล้วเกณฑ์ที่เขียนไว้ไปด้วย และบอกว่าไปกี่ข้อ | กลับไป *ข้อมูล Rubric กลาง* → *ลบ* ที่ Rubric ที่เพิ่งเติมเกณฑ์ → ยืนยัน | แถบเขียวบอกจำนวนเกณฑ์ที่ไปด้วย ตรงกับจำนวนที่เขียนไว้จริง — ครอบคลุมโดย `22a-rubric-criteria.spec.js` (แถวนี้พิสูจน์กับเกณฑ์ที่เพิ่งพิมพ์เอง ส่วน #21 พิสูจน์กับของ seed) | ⚙ |
| 6 | Rubric ของหลักสูตรอื่นถูกปฏิเสธ **เป็นคำพูด** ไม่ใช่ตารางว่าง | จำ `<id>` ของ Rubric ของ `0501` ไว้จาก URL → ออกจากระบบ → เข้าด้วย `prog.manager.0503@kmitl.ac.th` → พิมพ์ `/main/rubrics/<id>/criteria` ตรง ๆ | เห็นประโยค *ไม่พบ Rubric ที่ระบุ* และ **ไม่มีตารางเลย** — ตารางว่างจะแปลว่า *Rubric นี้ยังไม่มีเกณฑ์* ซึ่งเป็นคนละเรื่องและเป็นเรื่องเท็จ ทั้งยังบอกคนที่เดา id ว่า id นั้นมีอยู่จริง — ครอบคลุมโดย `22a-rubric-criteria.spec.js` (รวมถึง id ที่ไม่เคยมีอยู่ ซึ่งตอบประโยคเดียวกัน) | ⚙ |
| 6 | บัญชีที่ไม่ควรเข้าถึงเลย — ครึ่งที่เป็นกฎ | ด้วย `faculty.admin@`, `admin@` และ `teacher.one@` พิมพ์ `/main/rubrics/<id>/criteria` ตรง ๆ | ได้ประโยค *บัญชีนี้ไม่มีสิทธิ์ใช้งานส่วนนี้* และไม่มีตาราง — ครอบคลุมโดย `22a-rubric-criteria.spec.js` ซึ่ง assert สถานะ 403 ของคำขอด้วย ไม่ใช่แค่ข้อความ | ⚙ |
| 3 | **คอลัมน์สี่ระดับมีป้ายบอกว่าแต่ละระดับแปลว่าอะไร** | ดูหัวตาราง | หัวคอลัมน์อ่านว่า *4 — ดีเยี่ยม*, *3 — ดีมาก*, *2 — ปานกลาง*, *1 — ต้องปรับปรุง** เรียงจากสูงลงต่ำ — คำเหล่านี้ยกมาจากหน้าจอที่นักศึกษาส่งมอบโดยตั้งใจ คนที่จะอ่านมันเคยอ่านคำชุดนี้มาแล้ว คอลัมน์ที่เขียนแค่ *4* ทิ้งให้คนเขียนตัดสินเองเงียบ ๆ ว่าเลขมากคือดีหรือแย่ | ☑ |
| 3 | **ฟอร์มบังคับคำอธิบายครบทั้งสี่ช่อง และบอกว่าทำไม** | *เพิ่มเกณฑ์* → กรอกทุกช่องยกเว้นระดับ 2 → กด *บันทึก* | มีช่องคำอธิบายสี่ช่องแยกกัน เบราว์เซอร์ไม่ยอมให้ส่งจนกว่าจะกรอกครบ (ทุกช่องเป็น `required`) และมีย่อหน้าเหนือช่องเหล่านั้นบอกว่าต้องกรอกครบเพราะผู้ตรวจใช้ข้อความนี้แยกงานแต่ละระดับ — ฝั่ง server ปฏิเสธแล้วใน `rubric-criteria.test.js` แถวนี้คือสิ่งที่ฟอร์มทำ | ☑ |
| 2 | **น้ำหนักแสดงเป็นเลขที่คนพิมพ์ ไม่ใช่ `12.50`** | ดูคอลัมน์ *น้ำหนัก* ของเกณฑ์ที่ตั้งน้ำหนัก `12.5` และของเกณฑ์ที่ seed สร้าง (น้ำหนัก 1) | อ่านว่า `12.5` และ `1` ไม่ใช่ `12.50` และ `1.00` — คอลัมน์เป็น `numeric(5,2)` และ driver อ่านกลับมาเป็นสตริง ถ้าเห็นเลขทศนิยมสองตำแหน่งเสมอแปลว่า cast ขาออกหายไป |☑ |
| 2 | **ช่องน้ำหนักบอกขอบเขตของตัวเอง** | ดูข้อความใต้ช่อง *น้ำหนัก* ในฟอร์ม แล้วลองพิมพ์ `1.005` | ข้อความอ่านว่า *มากกว่า 0 และไม่เกิน 999.99 ทศนิยมไม่เกินสองตำแหน่ง* และ `step="0.01"` ทำให้เบราว์เซอร์ค้านก่อนส่ง — ถ้าส่งไปถึง server จะได้แถบแดง เพราะ PostgreSQL ปัดทศนิยมตำแหน่งที่สามทิ้งเงียบ ๆ | ☑ |
| 5 | **ข้อความในกล่องยืนยันบอกครบสามอย่าง** | *ลบ* ที่เกณฑ์หนึ่งข้อ แล้วอ่านกล่องก่อนกดอะไร | ประโยคมี **ชื่อไทยของเกณฑ์** บอกว่า **คำอธิบายทั้งสี่ระดับจะถูกลบไปด้วย** และบอกว่า **ย้อนกลับไม่ได้** |☑ |
| — | **ทางกลับไปหน้า Rubric อยู่บนหน้าจอ** | ดูมุมขวาบนของหน้า และดู breadcrumb | มีลิงก์ *กลับไปหน้าข้อมูล Rubric กลาง* และ breadcrumb อ่านว่า *ข้อมูล Rubric กลาง › `<id>` › เกณฑ์การให้คะแนนของ Rubric* — หน้านี้ไม่มีรายการเมนูของตัวเอง เข้าได้ทางเดียวคือจากหน้า Rubric ทางกลับจึงเป็นส่วนหนึ่งของหน้า ไม่ใช่ของเบราว์เซอร์ | ☑ |
| — | **หัวเรื่องและคำอธิบายใต้หัวเรื่องอ่านรู้เรื่อง** | ดูหัวหน้าจอและย่อหน้าใต้ปุ่ม | หัวเรื่องมีรหัส Rubric ต่อท้าย ใต้มันเป็นชื่อไทย · ชื่ออังกฤษ และย่อหน้าถัดมาบอกจำนวนเกณฑ์ การเรียง และว่าทุกข้อต้องมีคำอธิบายครบสี่ระดับ — **ตรวจช่องว่างกลางคำไทยด้วย** JSX ต่อสองบรรทัดด้วยช่องว่างหนึ่งตัวเสมอ | ☑ |
| — | **ตารางเลื่อนแนวนอนได้เมื่อคำอธิบายยาว** | สร้างเกณฑ์ที่คำอธิบายระดับ 4 ยาวสักสองบรรทัด แล้วดูตารางบนจอแคบ | ตารางเลื่อนซ้ายขวาได้ในกรอบของตัวเอง **หน้าเว็บทั้งหน้าไม่เลื่อนตาม** และคอลัมน์ *จัดการ* ยังกดถึง | ☐ |

## บันทึกการเดินด้วยมือ

**เดินครบทั้งแปดแถวแล้ว 2026-08-24 ถึง 2026-08-25** โดยเจ้าของงาน ผ่านเจ็ดแถว
**ตกหนึ่งแถว** คือตารางเลื่อนแนวนอน ซึ่งยังเป็น ☐ และเป็นเหตุที่ตั๋วนี้ยังปิดไม่ได้

**แถวที่ตก — ตารางเลื่อนแนวนอนไม่ได้ และคอลัมน์ จัดการ กดไม่ถึงบนจอแคบ** ย่อหน้าต่างลงมา
ราวครึ่งจอแล้วคอลัมน์ขวาสุดหายไปโดยไม่มีแถบเลื่อนให้ตามไปหา ต้นเหตุไม่ได้อยู่ที่หน้านี้ —
`frontend/src/pages/Mainpage.js:63` วาง `<main>` เป็น `flex-1` **โดยไม่มี `min-w-0`**
flex item จึงย่อลงต่ำกว่าความกว้างขั้นต่ำของเนื้อหาไม่ได้ ตารางที่ต้องการราว 1,100px
(สี่คอลัมน์คำอธิบายที่ `min-w-48` เท่ากับ 192px ต่อคอลัมน์ บวกคอลัมน์ที่เหลือ) จึงดัน `<main>`
ให้กว้างเกินจอ แล้ว `Mainpage.js:92` ที่เป็น `overflow-x-hidden` ตัดส่วนที่ล้นทิ้ง
`overflow-x-auto` ที่กรอบตารางไม่เคยได้ทำงาน เพราะกรอบเองก็กว้างตามเนื้อหาไปแล้ว
เรื่องนี้กระทบทุกหน้าที่มีตารางกว้าง ไม่ใช่ของตั๋วนี้ตั๋วเดียว แต่หน้านี้เจอหนักที่สุด
เพราะคอลัมน์ที่หายไปคือคอลัมน์ที่มีปุ่ม ลบ

สามอย่างที่ต้องบันทึกไว้สำหรับรอบหน้า

- **แถวน้ำหนักเคยถูกติ๊กแล้วถอนคืน** คำตอบที่ได้มาบอกว่าบันทึกสำเร็จและน้ำหนักอ่านว่า `12.5`
  แต่การตรวจฐานข้อมูลหลังติ๊กพบว่า `rubric_details` ยังมีห้าแถวของ seed เท่านั้น และ `created_at`
  ล่าสุดคือเวลาที่ seed ไม่ใช่เวลาที่เดิน แถวถูกคืนเป็น ☐ แล้วเดินใหม่ในวันถัดมาจนผ่าน
  **การถามหลายข้อในคำสั่งเดียวคือต้นเหตุ** เจ้าของงานบอกไว้ตั้งแต่ต้นว่าให้ถามทีละข้อ
  คำสั่งนั้นถามสี่ข้อพร้อมกันและระบุไว้ด้วยว่าควรเห็นอะไร ซึ่งเป็นรูปแบบที่ทำให้คำตอบ
  สะท้อนใบเดินแทนที่จะสะท้อนหน้าจอ
- **ตรวจฐานข้อมูลหลังทุกแถวที่เขียนข้อมูล** ไม่ใช่แค่แถวที่น่าสงสัย แถบเขียวเป็นสิ่งที่หน้าจอวาด
  ไม่ใช่หลักฐานว่ามีแถวเกิดขึ้นจริง
- **session หมดอายุกลางการเดิน** `backend/auth/session.js:33` ตั้งอายุ token ไว้ 30 นาทีและ
  **ไม่ต่ออายุเมื่อมีการใช้งาน** คนที่กำลังกรอกฟอร์มยาว ๆ อยู่จึงถูกเด้งออกพร้อมของที่กรอกค้างไว้
  และกล่องที่ขึ้นเป็นใบเดียวกับกล่องรหัสผ่านผิดของ #97 จึงแยกไม่ออกว่าเกิดอะไรขึ้น
  ใบเดินรอบหน้าควรเรียงแถวที่ต้องกรอกฟอร์มไว้ต้น ๆ หรือเตรียมข้อมูลไว้ล่วงหน้า

เกณฑ์ `การตอบคำถามจากผู้ฟัง` ใต้ `RUB-01` น้ำหนัก `12.5` ลำดับ `4` คำอธิบายครบสี่ระดับ
โดยระดับ 4 ยาว 156 ตัวอักษร ถูกใส่ด้วย SQL ตรง ๆ ไม่ผ่านฟอร์ม หลัง session เด้งคนเดินออก
กลางฟอร์ม ซึ่งไม่เสียความหมายของสามแถวสุดท้าย เพราะทั้งสามถามว่า **หน้าจอแสดงข้อมูลอย่างไร**
ไม่ได้ถามว่าข้อมูลเข้าไปได้อย่างไร ส่วนการสร้างผ่านฟอร์มมีแถว ⚙ คุมอยู่ และครึ่งที่เป็นพฤติกรรม
ของฟอร์ม (บังคับครบสี่ช่อง กับขอบเขตของน้ำหนัก) เดินด้วยมือสดไปแล้วก่อนหน้านั้น
ถ้า seed ใหม่ ข้อมูลชุดนี้จะหายและต้องใส่กลับก่อนเดินแถวที่ตกซ้ำ

## มัตแตนต์

`mutation/22-rubric-criteria.py` — สิบสี่ตัว หนึ่งตัวต่อ *ข้ออ้าง* หนึ่งข้อ ไม่ใช่หนึ่งตัวต่อแถว
เพราะสามแถวพูดหลายเรื่องที่พังแยกกันได้ ดู `mutation/README.md` ก่อนเชื่อหรือเขียนใหม่

`22a-rubric-criteria.spec.js` รันแบบ `serial` แถวแรกที่ตายจะหยุดแถวที่เหลือทั้งหมด ผลจึงอ่านจาก
**ชื่อเทสต์ที่ตาย** เท่านั้น นับจำนวน *1 failed* ไม่ใช่หลักฐาน

**ไม่มีมัตแตนต์ตัวไหนในไฟล์นี้แก้ตัวบท SQL ที่ผูกพารามิเตอร์** นั่นคือกับดักที่ #21 เสียเวลาไปสอง
ครั้ง — คำสั่งที่เลิกอ้างถึง `$n` ขณะที่ยังส่งค่ามาเท่าเดิมทำให้ PostgreSQL ตอบ 42P18 route ตอบ 500
และ **ทุกแถวตาย** เริ่มจากแถวแรก ซึ่งไม่ได้พูดอะไรเลยเกี่ยวกับสิ่งที่มัตแตนต์อ้าง `editnotsaved`
ที่นี่จึงแก้ *ค่าที่ผูกเข้า* `$3` ไม่ใช่คำสั่งที่ผูกมัน

| มัตแตนต์ | ทำอะไรพัง | แถวที่ตาย |
|---|---|---|
| `headingnotnamed` | หัวเรื่องไม่บอกว่าเป็น Rubric ตัวไหน | ทางเข้าจากหน้า Rubric พาไปที่ Rubric ตัวนั้น |
| `emptynotsaid` | Rubric ที่ยังว่างไม่บอกว่าว่าง | ทางเข้าจากหน้า Rubric (ครึ่งที่เป็น Rubric ว่าง) |
| `weightnotshown` | ตารางไม่วาดคอลัมน์น้ำหนัก (ค่ายังถูกบันทึกและส่งกลับมา) | เพิ่มเกณฑ์ได้ พร้อมน้ำหนักและคำอธิบายครบสี่ระดับ |
| `bandsnotshown` | ตารางวาดคำอธิบายแค่ระดับบนสุด | เพิ่มเกณฑ์ได้ (ครึ่งที่เป็นสี่ระดับ) |
| `countnotsaid` | คำตอบของรายการส่ง `total: 0` เสมอ บรรทัดใต้หัวเรื่องจึงนับไม่ขึ้น (Rubric ว่างยังอ่านว่า `0` แถวที่หนึ่งจึงไม่ตาย) | เพิ่มเกณฑ์ได้ (ครึ่งที่เป็นจำนวนใต้หัวเรื่อง) |
| `nameorder` | เรียงตามชื่อไทยแทนลำดับที่ตั้งไว้ | เรียงตามลำดับที่ตั้งไว้ |
| `tiebreakreversed` | ตัวตัดสินลำดับซ้ำกลับทาง (`d.id DESC`) | เรียงตามลำดับที่ตั้งไว้ (ครึ่งที่เป็นลำดับซ้ำ) |
| `editnotsaved` | `PUT` ผูก `$3` ด้วยชื่อเดิมแทนชื่อที่ส่งมา (ตอบ 200 ตามเดิม) | แก้ไขได้ |
| `countoffbyone` | subquery `criteria_count` ของ #21 นับขาดไปหนึ่ง (Rubric ว่างยังอ่านว่ายังไม่มีเกณฑ์ แถวที่หนึ่งจึงไม่ตาย) | จำนวนเกณฑ์ที่หน้า Rubric นับ |
| `hardcancel` | ปุ่ม *ยกเลิก* ในกล่องยืนยันทำสิ่งที่ปุ่มยืนยันทำ | ถามยืนยันก่อนลบ |
| `removalnotnamed` | คำตอบของการลบไม่พาชื่อกลับมา แถบจึงไม่บอกว่าลบอันไหน | ถามยืนยันก่อนลบ (ครึ่งที่เป็นแถบหลังลบ) |
| `refusalasemptytable` | คำปฏิเสธถูกกลืน หน้าจอวาดตารางว่างแทน | Rubric ของหลักสูตรอื่นถูกปฏิเสธเป็นคำพูด |
| `facultyadmin` | ใส่ `FACULTY_ADMIN` เข้า route ของรายการ | บัญชีที่ไม่ควรเข้าถึงเลย — ครึ่งที่เป็นกฎ |
| `cascadenotcounted` | ตอบ `criteria_removed: 0` ทั้งที่เกณฑ์ถูกลบไปจริง | ลบ Rubric แล้วเกณฑ์ที่เขียนไว้ไปด้วย |
