# การส่งมอบ — 6 ก.ย. 2569

แทนที่ `2026-09-05-the-last-of-the-original-forty-four.md` สำหรับสถานะ ใบนั้นยังใช้อ่านเรื่อง
สี่ข้อที่ควรอ่านก่อนทำอะไรต่อได้อยู่ ใบนี้เล่าเฉพาะสิ่งที่เปลี่ยนหลังจากนั้น

**รอบนี้ปิดสองตั๋ว — [#50](https://github.com/khthana/Deep-QA/issues/50) หน้าจอลงชื่อเข้าใช้
และ [#66](https://github.com/khthana/Deep-QA/issues/66) ลงชื่อเข้าใช้แล้วไปจบที่ไหน**
ทั้งคู่เป็นตั๋วของหน้างานแบบใหม่: ไม่ใช่หน้าจอถัดไป แต่เป็นรูในคลังตรวจรับ กับข้อบกพร่องที่การรื้อเจอเอง

## สถานะ

ปิดถึง **#45 ต่อเนื่องไม่มีข้อยกเว้น** บวก **#50** และ **#66**

หน้างานคือเลขที่เปิดค้างเหนือ #45 กับแถว ◐ ที่ใบตรวจรับยังถืออยู่สิบเอ็ดแถว ไม่มีหน้าจอถัดไปรออยู่
รอบนี้เปิดเพิ่มสองใบ — [#119](https://github.com/khthana/Deep-QA/issues/119) กับ
[#120](https://github.com/khthana/Deep-QA/issues/120) — ทั้งคู่มาจากการตรวจของ #66 เอง

| seam | จำนวน |
|---|---:|
| backend HTTP | **697** |
| db | **117** |
| browser (`e2e/`) | **347** |
| มัตแตนต์ | **457** (40 ไฟล์) |

| ใบตรวจรับ | 38 ใบ · 856 แถว |
|---|---:|
| ☑ | **443** |
| ⚙ | **399** |
| ◐ | **11** |
| ☐ | **3** |

ตัวเลขสองชุดนี้ให้เอาจากสคริปต์ที่ไล่ใบจริงทุกครั้ง **อย่ายกตารางนี้ไปใช้ในรอบหน้า** เคยค้างมาแล้ว
สองรอบ:

```bash
cd mutation && python -c "import ast,io,glob; fs=[f for f in glob.glob('*.py') if f!='harness.py']; print(sum(len(n.value.keys) for f in fs for n in ast.parse(io.open(f,encoding='utf-8').read()).body if isinstance(n,ast.Assign) and getattr(n.targets[0],'id','')=='MUTANTS'))"
```

**#50 ไม่ได้ปิดโดยไม่เหลือ ◐ และ ☐ อย่างสองใบก่อนหน้า** และสองแถวที่มันเพิ่มเป็นแบบที่ตรงไปตรงมา:
◐ หนึ่งแถวสำหรับคำปฏิเสธที่มีแต่เซิร์ฟเวอร์โหมด production เท่านั้นที่สร้างได้ (`passwordNotAllowed`)
และ ☐ หนึ่งแถวสำหรับเกณฑ์ที่ **วันนี้ยังไม่จริง** — รหัสผ่านผิดทำให้กล่อง *Session หมดอายุ* ขึ้นทับ
หน้าลงชื่อเข้าใช้ ([#97](https://github.com/khthana/Deep-QA/issues/97)) ☐ จึงเป็นวิธีที่ใบตรวจรับ
พูดว่า *ข้อนี้เป็นของตั๋วที่เปิดอยู่ใบอื่น* ไม่ใช่แค่ *ยังไม่มีใครไปดู*

แถว ☐ อีกใบยังเป็นครึ่งของ #49 เหมือนเดิม และ **#66 เพิ่มใบที่สาม** — ครึ่งเส้นทาง Google
ของการลงชื่อเข้าใช้ ซึ่งไม่มี seam ใดยืนยันได้เลยบนเครื่องนี้ ([#119](https://github.com/khthana/Deep-QA/issues/119))

**ตัวเลข ⚙ ขึ้นแต่ ☑ ลง** ทั้งที่รอบนี้ไม่มีใครถอนการเดินสักแถว เพราะ #66 ลบสองแถวที่ #50
เพิ่งเดินเมื่อวานออกจากตาราง — หน้าเลือกแอปกับรายการ *ไปที่ Deep Portfolio* ถูกลบทั้งคู่
รายละเอียดอยู่ในข้อ 5 ข้างล่าง

## ห้าข้อที่ #66 ทิ้งไว้

### 1. ตั๋วบางใบมี *คำถาม* ก่อนมี *งาน* และคำถามนั้นไม่ใช่ของเราตอบ

#66 ถามว่าหน้าเลือกแอปควรมีอยู่ไหม ก่อนจะขอให้แก้อะไรเลย และ `docs/06` §Out of Scope
บอกไว้ชัดว่าใครตอบ — *"UI ทำซ้ำตามของเดิม ข้อเสนอให้เปลี่ยนให้ยกเป็นคำถาม ไม่ใช่ลงมือทำ"*
เจ้าของข้อกำหนดตอบเมื่อ 6 ก.ย. 2569 ว่า **ต่อไปมีแอปเดียว** หน้าเลือกแอปกับรายการ
*ไปที่ Deep Portfolio* จึงถูกลบ ไม่ใช่ถูกซ่อมให้กลับเข้าเส้นทาง

**อ่านตั๋วข้อบกพร่องหาคำถามก่อนอ่านหาคำสั่ง** การตอบเองคือวิธีที่การรื้อระบบจะสะสมความเห็น
ที่ไม่มีใครขอ

### 2. *สิ่งที่ผิด* ในตั๋ว คือคำกล่าวของวันที่มันถูกเขียน ให้วัดก่อนแก้

#66 เขียนว่าเส้นทางรหัสผ่านไปไม่ถึงหน้าเลือกแอปเลย การบันทึกทุกการเปลี่ยนหน้าบอกอีกอย่าง:

    /  →  /select-app  →  /main  →  รายการแรกของเมนู

มัน **ไปถึง** ถูกวาดขึ้น แล้วถูกดึงออกไป ซึ่งแย่กว่าที่ตั๋วเขียน ไม่ใช่เบากว่า —
หน้าจอที่ไม่มีใครไปถึงคือโค้ดตาย ส่วนหน้าจอที่โผล่ขึ้นแล้วถูกกระชากออกไปคือหน้าจอที่ใช้งานไม่ได้
และมันคือเกณฑ์ข้อ 2 ของตั๋วเองพอดี: มีคอมโพเนนต์หนึ่งพาไปยัง route ที่อีกตัวเปลี่ยนเส้นทางออกไปแล้ว

**ราคาของการเชื่อคำอธิบายในตั๋วโดยไม่วัด** คือชุดเทสต์ที่เขียนไว้จับสิ่งที่ไม่ได้เกิด
สามแถวแรกของ `66a` เขียนขึ้นตามคำอธิบายในตั๋วและ *ทั้งสามผิด* จนกระทั่งวัดจริง

### 3. เกณฑ์ที่ *ผ่านตามตัวอักษร แต่คาใจ* มักวัดได้ และการวัดถูกกว่าทั้งการเถียงและการรื้อ

เกณฑ์ข้อ 2 ของ #66 อ่านว่า *ไม่มีคอมโพเนนต์ใดพาไปยัง route ที่อีกตัวเปลี่ยนเส้นทางออกไปแล้ว*
หลังแก้เสร็จยังเหลือสองตัวที่มีรูปร่างแบบนั้น — `GuestRoute` พาไป `/main` และ `SidebarItem`
เปลี่ยนเส้นทางออกจาก `/main` ทางเลือกที่ยั่วใจคือประกาศว่าผ่าน หรือไม่ก็ลงมือแยก `MENUS`
ออกมาเป็นโมดูลกลางกลางตั๋ว **ที่ทำจริงคือวัด** — เก็บตัวอย่างหนึ่งครั้งต่อหนึ่งเฟรมที่ถูก composite
ตลอดเส้นทางลงชื่อเข้าใช้จริง:

    เส้นทาง:                 /  →  /main  →  /main/rubrics
    เฟรมที่วาดตอนอยู่ /main:  1 จาก 45 และข้อความยังไม่ถูก layout

หนึ่งเฟรม ไม่มีสภาพไหนที่คนเห็น shell ที่วาดแล้วพร้อมเนื้อหาว่าง เพราะไม่มีอะไรในช่วงนั้นรอเครือข่าย
มันจึงเป็น [#120](https://github.com/khthana/Deep-QA/issues/120) พร้อมตัวเลข ไม่ใช่การรื้อในตั๋ว
ที่ขอบเขตคือลบหน้าเลือกแอป — และตั๋วนั้นเขียนไว้ด้วยว่าขอบเขตหนึ่งเฟรมเป็น *อุบัติเหตุของ
การไหลข้อมูลวันนี้* ไม่ใช่การรับประกัน วันที่เมนูของบทบาทหนึ่งต้องไปดึงมาจากเซิร์ฟเวอร์
(ซึ่งคือสิ่งที่ #49 ต้องตัดสินให้ `EXT_ASSESSOR`) หน้าว่างนั้นจะอยู่บนจอนานเท่าที่คำขอใช้เวลา

### 4. ⚙ ที่อ้างถึง *สองทางเข้า* ต้องมี seam ที่เข้าไปทั้งสองทาง

แถวลงชื่อเข้าใช้บน `10-application-shell.md` เขียนว่า *ทั้งทางรหัสผ่านและทาง Google ส่งต่อที่จุดเดียวกัน*
แล้วติด ⚙ ชี้ไปที่ `66a` rows 1–2 ซึ่งขับ **เฉพาะฟอร์มรหัสผ่าน** /code-review ของตั๋วนี้จับได้
`CLAUDE.md` เตือนเรื่อง ⚙ ที่กินความกว้างกว่าที่ assertion ครอบคลุมมาเก้าตั๋วติดกันแล้ว
ใบนี้เป็นใบที่สิบ และต่างจากเก้าใบก่อนตรงที่ครึ่งที่ขาดไม่ใช่ *มัตแตนต์ไม่ครอบคลุม*
แต่เป็น **ทางเข้าที่ seam ไม่เคยเดินเข้าไปเลย**

ที่สำคัญกว่าคือครึ่งที่แยกออกมาไม่ได้กลายเป็น ☑ ที่ชี้ไป seam อื่น: เส้นทาง Google ตอนสำเร็จ
ขับด้วยชุดทดสอบไม่ได้ (หน้ายินยอมของ Google ไม่ใช่สิ่งที่ script ได้) และทั้ง stack ของ e2e
กับเครื่องนี้ตั้ง credentials ว่างไว้โดยตั้งใจ มันจึงเป็น **☐ ที่มีตั๋ว** ไม่ใช่ ☑ ที่ไม่มีใครพิสูจน์

**อ่านทุกคำว่า *ทั้ง… และ…* ในใบตรวจรับเป็นข้ออ้างสองข้อ** แล้วตรวจว่า spec เข้าไปทั้งสองทางจริง

### 5. อย่าประดิษฐ์เครื่องหมายที่ห้าเพื่อเก็บแถวที่ถูกลบ

#50 เดินหน้าเลือกแอปกับรายการ *ไปที่ Deep Portfolio* ผ่านเมื่อ 6 ก.ย. แล้ว #66 ลบทั้งสองในวันเดียวกัน
ร่างแรกของการบันทึกเรื่องนี้ขีดฆ่าสองแถวไว้ในตารางแล้วให้เครื่องหมาย `—` แปลว่า *ปลดแล้ว*
ซึ่งเป็นเครื่องหมายที่ห้าในคลังที่นิยามไว้สี่ตัว **เครื่องหมายที่ไม่มีนิยามแย่กว่าการไม่มีแถว**
เพราะคนอ่านทีหลังต้องเดา ตอนนี้สองแถวถูกถอดออกจากตาราง และการปลดถูกเล่าเป็นข้อความ
ในบันทึกการเดินแทน

**ถ้าตั๋วบนหน้างานกำลังจะตัดสินชะตาของหน้าจอไหน ให้เดินหน้าจอนั้นทีหลัง ไม่ใช่ก่อน**

## สามข้อที่ #50 ทิ้งไว้

### 1. การเดินไม่ใช่ตาข่ายเดียว และ #50 คือใบที่แสดงว่าทำไม

#50 เดินสามแถวและ **ติ๊กครบทั้งสาม ไม่เจออะไรเลย** — เป็นใบที่สองในคลังต่อจาก #36
แต่สิ่งที่ทำให้ใบนี้ต่างจาก #36 คือมันไม่ได้ *ไม่มีของ* ตั๋วนี้เจอข้อบกพร่องสี่อย่าง
และทั้งสี่อย่างถูกจับด้วยการ **อ่านโค้ดเทียบกับข้อกำหนด** ไม่ใช่ด้วยตา

เหตุผลตรงไปตรงมา: ทั้งสี่อย่างมองไม่เห็นบนหน้าจอที่ทำงานถูกต้องอยู่แล้วในสภาพปกติ

- กุญแจ `outsideValidity` ที่หายไปจากตารางคำ — เห็นได้ก็ต่อเมื่อมีผู้ประเมินภายนอกที่รอบปิดไปแล้ว
  มาลงชื่อเข้าใช้ด้วย Google ซึ่งเป็นสภาพที่ seed ไม่มีและการเดินสร้างไม่ได้
- คำปฏิเสธที่เป็นหน้า JSON — เห็นได้ก็ต่อเมื่อกดปุ่ม Google บนเครื่องที่ไม่ได้ตั้งค่า OAuth
  ซึ่งเป็นเครื่องของทุกคน แต่ไม่มีใครกดเพราะไม่มีใครใช้เส้นทางนั้นตอนพัฒนา
- ป้ายกำกับที่ชี้ไปที่ id ที่ไม่มีอยู่ — หน้าตาเหมือนกันทุกพิกเซล เห็นได้ด้วยการ *คลิกที่ตัวหนังสือ*
  ซึ่งไม่มีใครทำ หรือด้วยโปรแกรมอ่านหน้าจอ
- หน้าที่ไม่มีใครพาไปถึง — ตามนิยามแล้วไม่มีใครไปถึง

**การเดินตอบคำถามว่า *สิ่งที่วาดออกมาอ่านรู้เรื่องไหม* การอ่านโค้ดตอบคำถามว่า *สิ่งที่ควรมี
มีครบไหม*** สองคำถามนี้ไม่ทับกัน และตั๋วที่เจอของด้วยวิธีหนึ่งไม่ได้แปลว่าอีกวิธีเสียเปล่า

### 2. คำที่เดินทางในที่อยู่ ต้องมีปลายทางทั้งสองข้างที่ตรวจกันได้

`GOOGLE_REFUSALS` ในหน้าจอกับ `refuse()` ใน `admit` เป็น contract ข้ามแพ็กเกจที่ไม่มีตัวแปลไหน
บังคับให้ตรงกัน redirect ไม่มี body มีแต่ `?error=<key>` ฝั่งหนึ่งจึงตามหลังอีกฝั่งได้เงียบ ๆ
และตามหลังไปหกเดือน

ทางที่เลือกคือ **สองด้าน จับคนละครึ่ง**:

- `backend/auth/accounts.js` ประกาศ `GOOGLE_REFUSAL_REASONS` และเทสต์ฝั่ง backend
  **อ่าน `refuse()` ออกมาจากซอร์สของ `admit` เอง** มาเทียบกับรายการ — รายการจึงตามหลังกฎไม่ได้
- `e2e/tests/50a-sign-in.spec.js` วนรายการนั้น เปิด `/login?error=<key>` ทีละตัว
  แล้ว assert ว่าหน้าจอแสดง `REFUSALS[key]` — หน้าจอจึงตามหลังรายการไม่ได้

**ไม่มี seam ไหนย้ายไปครอบอีกครึ่งได้** การลบกุญแจออกจากรายการไม่ฆ่าแถวฝั่ง browser
เพราะลูปก็สั้นลงตาม เขียนไว้ตรง ๆ ใน `mutation/50-sign-in-screens.py` แล้ว
เพราะเป็นสิ่งที่คนอ่านชุดมัตแตนต์แล้วจะเข้าใจผิดได้ว่าครอบคลุมครบ

### 3. รหัสสถานะที่ไม่มีใครอ่าน คือคำตอบที่ไม่ได้ตอบ

`/api/auth/google-login` ตอบ `503` พร้อม JSON ซึ่งถูกต้องตามหลัก HTTP ทุกประการ
และผิดทั้งหมดในทางปฏิบัติ เพราะผู้เรียกคือเบราว์เซอร์ที่ทำ top-level navigation
สิ่งที่คนได้จริงคือหน้า `{"message":"..."}` บน origin ของ API อยู่นอกแอปพลิเคชัน
ไม่มีทางกลับนอกจากปุ่ม back

**ถามว่าใครเป็นคนอ่านคำตอบ ก่อนจะเลือกรูปแบบของคำตอบ** ถ้าเป็นโค้ด รหัสสถานะคือคำตอบ
ถ้าเป็นเบราว์เซอร์ที่กำลังนำทาง คำตอบคือหน้าจอที่มีคำอธิบาย — ซึ่ง callback ของเส้นทาง
เดียวกันทำถูกอยู่ก่อนแล้ว และเหตุผลก็เขียนอยู่ในนั้น ทั้งสองทางใช้ `refuseToBrowser` ตัวเดียวกันแล้ว

## เรื่องเดินสะดุด

**ส่วนขยาย Chrome ที่ต่ออยู่ไม่ได้อยู่บนเครื่องนี้** `localhost:5000` ที่นั่นตอบ
`ERR_CONNECTION_REFUSED` ทั้งที่ curl จากเครื่องนี้ได้ 200 — การเดินรอบนี้ผู้ใช้จึงขับเอง
โหลดหน้าแล้วดูก่อนโทษ stack เสมอ

## สิ่งที่ยังเปิดอยู่และเกี่ยวกับใบนี้

- [#119](https://github.com/khthana/Deep-QA/issues/119) ไม่มี seam ใดยืนยันว่าเส้นทาง Google
  ไปจบที่ไหน — ☐ ใบที่สามของคลัง แก้แบบถูกที่สุดคือเทสต์ฝั่ง backend ที่ stub การแลก OAuth
  แล้วยืนยันว่า redirect ไป `${FRONTEND_URL}/main` ส่วนการเดินจริงต้องมี Google project จริง
- [#120](https://github.com/khthana/Deep-QA/issues/120) `/main` เป็น route ที่มีไว้ให้ออก
  — วัดแล้วว่าราคาวันนี้คือหนึ่งเฟรม จึงไม่ใช่ของด่วน แต่ถ้าวันไหนแตะ `SidebarItem` หรือ
  `GuestRoute` อยู่แล้ว ให้ปิดไปพร้อมกัน ตั๋วเขียนวิธีไว้ครบ
- [#85](https://github.com/khthana/Deep-QA/issues/85) แถบคำปฏิเสธหายใน 3 วินาที
- [#111](https://github.com/khthana/Deep-QA/issues/111) ไม่มี live region ที่ไหนเลย —
  `refusalBanner` ใน `e2e/support/sign-in-screen.js` จึงหาแถบด้วย class ของ Tailwind
  ซึ่งไม่ใช่ที่ที่ locator ควรอยู่ วันที่ #111 ลง มันจะกลายเป็น `getByRole('alert')`
- [#97](https://github.com/khthana/Deep-QA/issues/97) รหัสผ่านผิดขึ้นกล่อง Session หมดอายุทับ
  — อยู่บนหน้าจอชุดเดียวกัน และใบตรวจรับของ #50 มีแถวที่จะรับมันได้ทันทีเมื่อแก้

## Gotchas ที่ยังต้องพก

- `git clone` ที่ทำไว้ก่อนเปลี่ยนชื่อ branch ยังชี้ `master` ที่ตายแล้ว แก้ด้วย
  `git remote set-head origin -a && git branch -m master main && git branch -u origin/main`
- e2e ใช้พอร์ต 5100 ซึ่ง Windows จองไว้เป็นช่วง ๆ ถ้าเจอ *already running* ทั้งที่ไม่มีอะไรฟัง
  ให้รันด้วย `E2E_FRONTEND_PORT=5300`
- `node server.js` ไม่มี reload — บันซ์ backend ก่อนเดินทุกครั้ง
- TaskStop ปิด shell แต่ไม่ปิดลูกของมัน เซิร์ฟเวอร์ที่ค้างจะทำให้การกวาดรอบถัดไปรายงานว่า
  *ไม่มีอะไรล้ม* ซึ่งหน้าตาเหมือนชุดมัตแตนต์ที่ฆ่าอะไรไม่ได้เลย
- **เดินด้วยมือก่อนกวาดมัตแตนต์และก่อนรันชุดเต็ม** การกวาดเขียนโค้ดที่พังลงดิสก์จริง
  หน้าจอที่เดินอยู่จะเสิร์ฟของที่พังโดยตั้งใจ
