# การส่งมอบ — 9 ก.ย. 2569

แทนที่ `2026-09-08-run-the-explanation-not-only-the-symptom.md` สำหรับสถานะ ใบนั้นยังใช้อ่านเรื่อง
#102 #67 และ #101 ได้ครบ และใบก่อนหน้ามันยังใช้อ่านเรื่อง #123 #122 #96 #107 #50 #66 #97 #85
#111 #121 และ #119 ใบนี้เล่าเฉพาะสิ่งที่เปลี่ยนหลังจากนั้น

**รอบนี้ปิดสามตั๋ว — [#124](https://github.com/khthana/Deep-QA/issues/124)
[#126](https://github.com/khthana/Deep-QA/issues/126) และ
[#125](https://github.com/khthana/Deep-QA/issues/125)** ตัวกลางไม่ใช่ตั๋วของระบบ แต่เป็นตั๋วของ
**เครื่องมือ** และเป็นข้อบกพร่องที่อันตรายที่สุดที่คลังนี้เคยมี — `mutation/harness.py` เขียนทับไฟล์
ที่มันอธิบายไม่ได้ ในความเงียบ บนต้นไม้ที่สะอาดอยู่ก่อนแล้ว **ถ้าจะอ่านหัวข้อเดียวจากใบนี้ ให้อ่าน
หัวข้อของ #126**

## สถานะ

ปิดถึง **#45 ต่อเนื่องไม่มีข้อยกเว้น** บวก **#50** **#66** **#97** **#85** **#111** **#121**
**#119** **#123** **#122** **#96** **#107** **#102** **#67** **#101** **#124** **#126** และ
**#125**

| seam | จำนวน |
|---|---:|
| backend HTTP | **726** (37 ไฟล์) |
| db | **117** |
| browser (`e2e/`) | **369** (53 ไฟล์) |
| มัตแตนต์ | **499** (52 ใบ) |
| `mutation/harness_test.py` | **17** |

| ใบตรวจรับ | 38 ใบ · 900 แถว |
|---|---:|
| ☑ | **460** |
| ⚙ | **422** |
| ◐ | **17** |
| ☐ | **1** |

ตัวเลขทั้งสองตารางนับด้วยสคริปต์ในรอบนี้เอง มัตแตนต์นับจาก `python mutation/anchors.py`
(ตอบ `mutants 499 | anchors checked 500 | superseded on purpose 1 | unreadable 0 | problems 0`)
— **อย่ายกตารางจากใบส่งมอบเก่ามาใช้ต่อ**

สคริปต์นับเครื่องหมายรอบนี้อ่านช่องสุดท้ายของทุกแถวตาราง ไม่ใช่ช่องที่ห้า จึงไม่พลาดตารางหกช่อง
เหมือนรอบก่อน และมันถูกตรวจด้วยวิธีที่ใบก่อนหน้าบอกไว้ — เอาไปนับที่ `694a3ef` แล้วได้
**38 ใบ · 897 แถว · ☑ 458 · ⚙ 420 · ◐ 17 · ☐ 2** ตรงกับที่ใบนั้นเขียนไว้ทุกตัว **วิธีตรวจเครื่องมือนับ
คือให้มันนับสิ่งที่มีคนนับไว้แล้ว**

**☐ ลดจาก 2 เหลือ 1** ตัวที่หายคือครึ่งแถวดาวน์โหลดใน `11-user-accounts.md` ที่ #124 ปิด — มันเป็น
☐ อยู่วันเดียว ซึ่งคือวงจรชีวิตทั้งหมดที่ ☐ มีไว้ทำ ตัวที่เหลือยังเป็นชุดเมนูของ `external.assessor@`
ที่ [#49](https://github.com/khthana/Deep-QA/issues/49) ถืออยู่ ◐ ยังเป็น 17 เท่าเดิม

## สามตั๋วที่ปิดรอบนี้

### #124 — ก่อนเอาอะไรออก ให้ถามว่ามันสอนอะไรอยู่

แถวตัวอย่างในแบบฟอร์มผู้ใช้ตั้งชื่อคนจริง อัปโหลดกลับโดยไม่แก้แล้วได้บัญชีที่ `users.js` ไม่มี
`DELETE` ให้ลบ เจ้าของข้อกำหนดเลือกข้อเดียวกับ #67 คือ **เอาแถวออกทั้งแถว เหลือแต่หัวคอลัมน์**

สิ่งที่ตั๋วไม่ได้ตีราคาไว้คือครึ่งหลัง `ImportPanel` ไม่ได้วาดคำอธิบายคอลัมน์อะไรเลย มีแต่หัวข้อ
คำโปรย และปุ่มสองปุ่ม พอเอาแถวตัวอย่างออก **ไฟล์ CSV จะกลายเป็นครูคนเดียวและมันว่างเปล่า**
คำตอบไม่ใช่ชื่อคอลัมน์ — หัวคอลัมน์ทั้งสิบห้าช่องยังอยู่ครบ — แต่เป็น**กฎที่หัวคอลัมน์พูดไม่ได้**
สองในเจ็ดข้อคือเหตุผลที่หัวคอลัมน์ทำงานนี้ไม่ได้เลย: ชื่อเป็น *อย่างใดอย่างหนึ่ง* ข้ามสองคอลัมน์
และรหัสผ่านจำเป็นสำหรับสองบทบาทและไร้ประโยชน์กับที่เหลือ

**และการเขียนเจ็ดข้อนั้นทีละข้อจาก `readAccount` คือสิ่งที่เจอ #125** ข้อที่สามเขียนไม่ได้เลยถ้า
ไม่ตัดสินก่อนว่าปีที่กรอกเป็นศักราชไหน

### #126 — ผู้คุ้มกันปกป้องต้นไม้จากสำเนาสำรอง แต่ไม่มีอะไรปกป้องสำเนาสำรองจากต้นไม้

`apply` ของ `mutation/harness.py` คืนไฟล์ทุกไฟล์ใน `FILES` ก่อนเสมอ เพื่อไม่ให้มัตแตนต์ซ้อนกัน —
คืนจาก `mutation/.backup/` ซึ่งเป็น**คลังเดียว คีย์ด้วย path** และไม่เคยตรวจว่าสำเนานั้นเป็นสำเนาของ
ต้นไม้จริงหรือเปล่า รัน `python mutation/18-program-subjects.py nobom` โดยไม่ `save` ก่อน แล้วมันคืน
`backend/lib/importer.js` กลับไปก่อน #67 และ `frontend/src/pages/ProgramSubjects.js` กลับไป
**21 สิงหาคม** พร้อมแบนเนอร์ที่ #55 ลบไปแล้ว — สองไฟล์ที่ไม่เกี่ยวกับมัตแตนต์ตัวนั้นเลย

`mutation/README.md` บอกให้ `save` ก่อนไว้สองที่ และบันทึกไว้ด้วยว่า #33 เสียไปสองรอบเพราะเรื่องนี้
**ตั๋วนี้จึงไม่ใช่เรื่องที่ไม่มีใครรู้ แต่เป็นเรื่องที่เครื่องมือไม่บังคับอะไรเลย** และเหตุผลที่ README ให้
ไว้ก็ผิดด้วย — *แต่ละใบมีสำเนาของตัวเอง* ไม่จริง `_backup()` ใช้ไดเรกทอรีเดียวคีย์ด้วย path
**ไม่ต้องมี race และไม่ต้องกวาดสองใบพร้อมกัน สำเนาที่เก่าอยู่ก่อนแล้วก็พอ**

ตัวคุ้มกันที่ใส่เข้าไปถามคำถามที่**แคบกว่า** *คุณ `save` หรือยัง* เพราะถ้าปฏิเสธทุกครั้งที่ต้นไม้ต่างจาก
สำเนา มันจะพังกรณีเดียวที่ restore-first มีไว้ทำ คือ `apply` ทับมัตแตนต์ที่ `apply` วางไว้เอง
harness จึงจดสิ่งที่ตัวเองเขียน (`mutation/.backup/applied.json` คีย์ด้วย path เหมือนกัน) และปฏิเสธ
เฉพาะเมื่อสิ่งที่อยู่บนดิสก์**ไม่ใช่ทั้งสำเนาและไม่ใช่มัตแตนต์ของตัวเอง** — นั่นคือความต่างทั้งหมด
ระหว่างผู้คุ้มกันที่คนเก็บไว้ กับผู้คุ้มกันที่คน `--force` ผ่านตั้งแต่วันที่สอง

`mutation/harness_test.py` คือสิบเจ็ดข้อเรื่องนี้ (Python ชุดแรกของคลัง `unittest` ยิงที่ tree ชั่วคราว)
ข้อที่สำคัญที่สุดคือข้อที่บอกว่า **ไม่มีอะไรถูกเขียนบน path ที่มันปฏิเสธ** เครื่องมือที่รายงานการปฏิเสธ
หลังจากสร้างความเสียหายไปแล้ว คือข้อบกพร่องที่มีข้อความประกอบ

**ห้าในสิบเจ็ดข้อมีเพราะ `/code-review` พบว่าการแก้ได้สร้างข้อบกพร่องที่มันกำลังปิด** `apply` เขียน
ทีละ edit แล้วค่อยจดตอนจบ มัตแตนต์สอง edit ที่ edit ที่**สอง** MISS จึงทิ้ง edit แรกไว้บนดิสก์โดย
ไม่มีบันทึก — restore ครั้งถัดไปเจอไฟล์ที่อธิบายไม่ได้ ปฏิเสธ แล้วขึ้นข้อความเรื่องสำเนาเก่าของ #126
**โดยที่ไฟล์ยังเป็นมัตแตนต์อยู่** ในคลังมีมัตแตนต์ยี่สิบห้าตัวใน 4 ใบที่เขียนเป็น list อีกสี่ข้อเป็น
สภาพโลกอื่น ๆ: บันทึกที่ parse ได้เป็น `[]` แทน `{}` ทำให้ฟังก์ชันที่มีหน้าที่ทำงานต่อโดยไม่มีบันทึกพัง,
ไฟล์ที่หายไปจากต้นไม้กลายเป็น traceback ทั้งที่ `restore` เคยเขียนมันกลับมาเฉย ๆ, และ `save` ทับ
มัตแตนต์ที่ apply ค้างอยู่ก็ยังอบมัตแตนต์ลงสำเนา **ผู้คุ้มกันที่อ่านโลกก่อนเขียน ต้องรอดทุกสภาพที่โลก
เป็นอยู่จริง** — สภาพที่ต้องลองคือ ว่างเปล่า, รูปร่างผิด, ไม่มีไฟล์ และสภาพที่เครื่องมือของคุณเพิ่งทำเอง

และการแก้เกือบทำให้ตัวเลขที่มันมีไว้ปกป้องพัง `anchors.py` กับ README เลือกใบมัตแตนต์ด้วยการ
**ตั้งชื่อไฟล์ที่ไม่ใช่ใบ** พอ `harness_test.py` (ซึ่งมี fixture ชื่อ `MUTANTS`) ลงมา ยอดของคลังก็
กระโดดเป็น **497** ทันที กฎตอนนี้เป็น `^\d+-` **เครื่องมือที่ข้ามของด้วยชื่อ จะโกหกทันทีที่มีไฟล์ใหม่**

### #125 — ด่านที่เข้มงวดเรื่องรูปแบบ มองไม่เห็นความผิดพลาดเรื่องความหมาย

`readDate` ปฏิเสธ `01/03/2026` โดยตั้งใจ พร้อม docstring อธิบายว่ากรุงเทพฯ กับบอสตันอ่านคนละแบบ
แต่รับ `2569-09-30` เงียบ ๆ แล้วเก็บช่วงเวลาของผู้ประเมินไว้ในปี 2569 **ค.ศ.** บนบัญชีที่สร้างเป็น
`active` เข้าระบบไม่ได้ และลบไม่ได้เพราะไฟล์นี้ไม่มี `DELETE` เจ้าของข้อกำหนดเลือกข้อสองจากสามข้อ
เมื่อ 9 ก.ย. 2569 — **ปฏิเสธ แล้วทดเลขให้ในประโยค**

**ด่านคือสี่บรรทัด งานอยู่ที่อื่น** ประโยคที่เอ่ยถึงค่าที่คนพิมพ์มา เป็นคนละชนิดกับประโยคที่เอ่ยถึงเหตุผล
และจุดที่อ่านสองอย่างนี้ไม่ใช่จุดเดียวกัน `REFUSALS` มีสมาชิกที่เป็นฟังก์ชันมาตั้งแต่ #26 (นับได้
**ยี่สิบก่อน ยี่สิบสองหลัง** เมื่อ 9 ก.ย. 2569) แต่มีแต่การนำเข้าที่เคยอ่านมัน ผ่าน `sentenceOf` ส่วน
route สองเส้นเขียนว่า `REFUSALS[draft.reason]` ซึ่งยื่น**ฟังก์ชัน**ให้ `res.json` แล้ว `JSON.stringify`
ทิ้งมันไป **คนกรอกฟอร์มถูกปฏิเสธโดยไม่มีข้อความอะไรเลย** `sentenceOf` จึงย้ายไป
`auth/refusals.js` **ก่อนเพิ่มคำปฏิเสธที่รับพารามิเตอร์ ให้ไล่หาทุกจุดที่แปลง reason เป็นประโยค**
grep คือ `REFUSALS[`

**เส้นแก้ไขไม่เคยมีแถวของตัวเอง** ทั้งที่การต่อรอบให้ผู้ประเมินคือสิ่งที่เกิดกับช่วงเวลาจริง ๆ
`editsentenceisakey` ฆ่าได้ข้อเดียว และข้อนั้นเขียนขึ้นชั่วโมงเดียวกัน **เมื่อการแก้ครั้งเดียวลงสองจุด
ให้เขียนมัตแตนต์ต่อจุด ก่อนจะเชื่อว่าเทสต์เดียวครอบคลุมทั้งสอง**

**และแถวที่ยืนยันรหัสสถานะคือ near-miss ของรอบนี้เอง** แถวขอบช่วงเคยยืนยันแค่ `400` สำหรับ
1899 กับ 2201 — ผู้ประเมินภายนอกที่สร้างโดยไม่ใส่รหัสผ่านได้ `400` อยู่แล้วจากด่านที่อยู่ต่ำลงไป
สี่สิบบรรทัด **มันผ่านบนต้นไม้ที่มันถูกเขียนขึ้นเพื่อให้แดง** สิ่งที่จับได้คือการอ่านว่ารอบแดง*แถวไหน*
แดงบ้าง **รหัสสถานะไม่ใช่คำยืนยันเรื่องด่านของคุณ บน route ที่ตอบรหัสนั้นได้หลายทาง**

## /code-review ของ #125 เจอหกข้อ และข้อที่ผิดคือข้อที่ต้องตรวจก่อน

- **ตัวเลขที่เขียนจากความจำ** *สิบแปดฟังก์ชันใน `REFUSALS`* อยู่ในสามไฟล์ นับจริงได้ยี่สิบก่อน
  ยี่สิบสองหลัง — #96 เขียนกฎนี้ไว้แล้วและรอบนี้ก็ยังพลาด
- **คอมเมนต์ในใบมัตแตนต์บอก 3 ข้อ ที่กวาดจริงได้ 4** docstring กับตารางแก้แล้ว คอมเมนต์บรรทัด
  เดียวไม่ได้แก้ — **แก้ตัวเลขเฉพาะที่ที่จำได้ว่าเขียน ไม่ใช่การแก้ตัวเลข**
- **รายการ *ห้ามกวาดคู่กับ* ถูกสร้างจากการอ่าน ไม่ใช่จาก `FILES`** จึงมีชื่อที่สี่เกินมา —
  `67-import-template-sample.py` ถือ `backend/routes/students.js` และเอ่ยถึง `users.js` ใน
  docstring เท่านั้น **กฎที่รายการนี้มีไว้บังคับ คือกฎที่จับมันเอง**
- **ประโยคใน CLAUDE.md ที่ตรวจไม่ได้** *ด่านสองอันอยู่ห่างกันแปดบรรทัด* — มันอยู่คนละไฟล์
  สิ่งที่จริงคือ **ชื่อเดียวกัน** ซึ่งเป็นเหตุผลทั้งหมดที่กฎของ #107 ยังจับคนได้
- **เลขบรรทัดในคำอ้าง** diff นี้ลบคำอ้างแบบเลขบรรทัดไปหนึ่ง แล้วเพิ่มมาสอง — เปลี่ยนเป็นชื่อ
  ฟังก์ชันแล้วทั้งคู่ (#123)
- **#125 ไม่มีแถวในตารางเกณฑ์** มันลงบนใบ `11-user-accounts.md` เป็นย่อหน้า ซึ่งคือ
  *เกณฑ์ที่พับเข้าไปในแถวข้าง ๆ คือเกณฑ์ที่ไม่มีแถว* ของ #101 — ตอนนี้เป็นแถว ☑ ของข้อ 4 แล้ว

**ข้อที่หกที่รีวิวเสนอนั้นผิด และการตรวจก่อนแก้คือสิ่งที่กันไว้** agent ฝั่ง Standards บอกว่าสาขา
`typeof refusal === 'string'` ของ `sentenceOf` ไม่มีใครเรียก — `backend/routes/users.js:556`
คืนสตริงเปล่า `'scopeNotYours'` จาก verify hook ของการนำเข้า และ `importer.js:147` ส่งมันเข้า
`sentenceOf` ตรง ๆ **agent ผิดได้ ตรวจก่อนลงมือ**

## สิ่งที่ยังเปิดอยู่ และควรอ่านก่อน

- [#127](https://github.com/khthana/Deep-QA/issues/127) **`activities.js` มี `readDate` ของตัวเอง
  และรับปีพุทธศักราช** — เปิดโดย #125 วัดแล้ว: `201` เก็บเป็น `2569-09-30` และมันยังรับ
  `2026-02-31` ด้วยเพราะไม่มีการตรวจปฏิทิน **grep เหมือนกันไม่ได้แปลว่าเป็นด่านเดียวกัน** สิ่งที่
  ต้องตัดสินคือช่วงปีควรอยู่ที่ไหนให้ทั้งสองอ่านได้ ซึ่งคำตอบไม่ใช่ *แยกตัว parser ออกมา* เพราะ
  สัญญาต่างกันโดยตั้งใจ
- [#88](https://github.com/khthana/Deep-QA/issues/88) คือครึ่งฝั่งหน้าจอของ #125 — ช่องวันที่ไม่มี
  `min`/`max` วันนี้ server ตอบเป็นประโยคที่ทดเลขให้แล้ว แต่ยังต้องรอ round trip
- [#110](https://github.com/khthana/Deep-QA/issues/110) และ
  [#112](https://github.com/khthana/Deep-QA/issues/112) ยังเป็นรูปของ #97 (สองที่ถือความเห็นเดียว)
- [#104](https://github.com/khthana/Deep-QA/issues/104) การ join ทะเบียนสอนเขียนสามครั้ง — ถ้าหยิบ
  ให้ดู `24:nonumericguard` ด้วย
- [#120](https://github.com/khthana/Deep-QA/issues/120) `/main` เป็น route ที่มีไว้ให้ออก วัดแล้วว่าราคา
  วันนี้คือหนึ่งเฟรม
- [#49](https://github.com/khthana/Deep-QA/issues/49) ถือ ☐ ตัวสุดท้ายของคลังไว้ รอเจ้าของข้อกำหนด
- แถวของ #119 **ยังไม่ใช่แถวที่เดินแล้ว** ต้องมี credentials จริงกับ Google project สำหรับทดสอบ

## Gotchas ที่ยังต้องพก

ยกมาทั้งชุดจากใบก่อนหน้า ยังใช้ได้ทุกข้อ และเพิ่มสี่ข้อจากรอบนี้

- `git clone` ที่ทำไว้ก่อนเปลี่ยนชื่อ branch ยังชี้ `master` ที่ตายแล้ว แก้ด้วย
  `git remote set-head origin -a && git branch -m master main && git branch -u origin/main`
- **`python mutation/<ใบ>.py save` ก่อนทุกมัตแตนต์ และอ่าน `git status` หลัง `apply` ไม่ใช่แค่หลัง
  `restore`** ตั้งแต่ #126 harness ปฏิเสธแทนที่จะเขียนทับไฟล์ที่มันอธิบายไม่ได้ แต่การถูกปฏิเสธ
  กลางการกวาด ก็ยังคือการกวาดที่ต้องเริ่มใหม่
- **รายการ *ห้ามกวาดคู่กับ* ต้องสร้างจาก `grep -l` บน `FILES` ไม่ใช่จากการอ่านว่าใบไหนพูดถึงอะไร**
- **อย่าพิมพ์ภาษาไทยหรือ ☑ ⚙ ◐ ☐ ผ่าน Bash ด้วย Python** console ที่นี่เป็น cp874 และจะได้
  `UnicodeEncodeError` — เขียนไฟล์ด้วย `io.open(..., encoding='utf-8')` แล้วอ่านด้วยเครื่องมืออ่านไฟล์
- **`.gitattributes` เป็น `* text=auto eol=lf` — ต้นไม้ต้องเป็น LF** สคริปต์แก้ไฟล์ที่เขียนด้วย
  `io.open(...)` ต้องส่ง `newline=''` ทั้งตอนอ่านและตอนเขียน ไม่งั้นไฟล์กลายเป็น CRLF เงียบ ๆ
  ตรวจด้วย `git ls-files --eol`
- e2e ใช้พอร์ต 5100 ซึ่ง Windows จองไว้เป็นช่วง ๆ ถ้าเจอ *already running* ทั้งที่ไม่มีอะไรฟัง
  ให้รันด้วย `E2E_FRONTEND_PORT=5300` ชุดเต็มใช้เวลาราว **18 นาที** (รอบนี้ 18.1 นาที 369 แถว)
- `node server.js` ไม่มี reload — บันซ์ backend ก่อนเดินทุกครั้ง
- TaskStop ปิด shell แต่ไม่ปิดลูกของมัน เซิร์ฟเวอร์ที่ค้างจะทำให้การกวาดรอบถัดไปรายงานว่า
  *ไม่มีอะไรล้ม*
- **อย่ากวาดมัตแตนต์ระหว่างที่ชุด e2e กำลังรัน** และ **เดินด้วยมือก่อนกวาดและก่อนรันชุดเต็ม**
  รอบนี้ sub-agent ของรีวิวรัน suite เดียวกันบน schema เดียวกันพร้อมกัน แล้วได้ `anchors.py`
  รายงาน MISS กับ users 49/51 ซึ่งหายไปเองเมื่อรันใหม่คนเดียว
- **การแก้ไฟล์ใน `frontend/src` ระหว่างที่ชุด e2e กำลังรัน ทำให้ CRA คอมไพล์ใหม่กลางคัน**
  เอกสารกับ `backend/` แก้ได้ปลอดภัย
- **docstring ในไฟล์มัตแตนต์ที่อ้าง regexp ต้องเป็น raw string** (`r"""`)
- **สคริปต์นับเครื่องหมายต้องอ่านช่องสุดท้ายของแถว ไม่ใช่ช่องที่ห้า** ไม่งั้นหายเจ็ดแถว ☑ เงียบ ๆ
  วิธีตรวจคือให้มันนับที่ commit ของใบส่งมอบก่อนหน้าแล้วเทียบกับตัวเลขที่ใบนั้นเขียนไว้
- **spec ที่รันแบบ `serial` รายงาน kill ต่ำกว่าจริง** แถวแรกที่ตายหยุดแถวที่เหลือ — วัดทีละแถวด้วย
  `--grep` ก่อนเขียนจำนวนลงใบตรวจรับ
- **`npx eslint` ใน `backend/` และ `e2e/` ออก exit 2 เพราะไม่มี config ไม่ใช่เพราะ lint ไม่ผ่าน** ใช้
  `node --check` ที่นั่น ส่วน `frontend/` มี config และใช้ `npx eslint` ได้จริง
