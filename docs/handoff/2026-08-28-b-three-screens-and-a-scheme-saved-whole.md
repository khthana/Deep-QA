# Handoff — 28 ส.ค. 2569 (b) — สามหน้าจอในเซสชันเดียว และชุดที่บันทึกทั้งชุด

แทน `2026-08-28-moving-the-work-to-another-machine.md` ในเรื่องสถานะ (ฉบับนั้นชี้กลับไปที่
`2026-08-25-c-...` ซึ่งบัดนี้เก่าแล้วทั้งคู่ในเรื่องสถานะ) หัวข้อ *กับดัก* ของฉบับย้ายเครื่อง
ยังใช้ได้ทั้งหมดและถูกยกส่วนที่ยังเป็นจริงมาไว้ท้ายฉบับนี้แล้ว

## 1. เกิดอะไรขึ้นในเซสชันนี้

เซสชันเดียวบนเครื่องใหม่ หลังยืนยันการย้ายเครื่องสำเร็จ (backend 411/411 · browser 181/181
· Playwright ต้อง `npx playwright install chromium` ก่อนหนึ่งครั้ง):

| commit | ตั๋ว | คืออะไร |
|---|---|---|
| `9fe9b2b` | **#28 Measurable Behaviors** | พฤติกรรมบ่งชี้ใต้ CLO — ยก `offeringOf`/`cloOf` ของ `clos.js` ขึ้น module level ให้ import ได้ (เปิด [#104](https://github.com/khthana/Deep-QA/issues/104) เรื่องสำเนา join ที่เหลือ) |
| `a5bacee` | **#29 Achievement Criteria** | เกณฑ์การบรรลุผลสี่ระดับใต้ CLO — ฝาแฝดของ #28 หนึ่งตารางถัดไป ระดับเป็น CHECK คำไทย ไม่มีตารางป้าย |
| `3c745de` | **#30 Weighting scheme** | สัดส่วนคะแนน บันทึกทั้งชุดผ่าน PUT เดียว (กติการวมร้อยเป็นของทั้งชุด) reconcile รักษา id ของแถวเพราะ Activities ชี้อยู่ · import ผ่าน `lib/importer` ที่ได้ hook ใหม่สองตัว (`whole`, `onCommit` ที่ปฏิเสธได้ — BR-11 ของ #33/#34 คือผู้ใช้รายถัดไป) · `refusals.js` มีประโยคแบบ parameterised ครั้งแรก |
| ก่อนหน้านั้น | #25 | เปิดตั๋ว #104 และเตรียมใบเดิน `_local/walk-25/walk-sheet.md` ไว้แล้ว (ยังไม่ได้เดิน) |

ทุกตั๋วผ่านครบทั้งสาม seam: backend test → e2e → mutation sweep (ฆ่าที่บรรทัดของแถวตัวเอง
log สะอาด) → code-review สองแกน → แก้ findings → commit review ของ #30 จับ**บั๊กจริง**ได้หนึ่งตัว
(double-claim — เปลี่ยนชื่อแถวด้วย id พร้อมแถวใหม่ที่ใช้ชื่อเดิม ทำให้ชุดที่ commit รวมไม่ถึงร้อย
ทั้งที่ผ่านด่านตรวจ) ตอนนี้มีเทสต์ regression ค้ำอยู่

## 2. ตัวเลขปัจจุบัน (ยืนยันจากการรันจริงในเซสชันนี้)

- backend `npm test` — **467/467** (~14 วินาที)
- e2e `npx playwright test` — **207/207** (~10 นาที) *รันก่อนแก้ review-findings ของ #30
  สองจุดสุดท้าย ซึ่งแตะเฉพาะ weights และถูกยืนยันซ้ำด้วย 30a (10/10) + backend เต็มหลังแก้*
- mutation — **247 ตัว, 246 ใช้ได้กับโค้ดปัจจุบัน** (ประวัติ MISS อยู่ใน `mutation/README.md`)
  log การกวาดของสามตั๋วใหม่อยู่ที่ `_local/mutation-28/` `-29/` `-30/`

## 3. งานที่ค้าง — คิวเดินมือสี่ใบ (ผู้ใช้สั่งพักไว้เอง)

ผู้ใช้บอกว่า *เดินมือเก็บไว้ก่อน ทำอย่างอื่นก่อน* — สี่ตั๋วนี้จึง**เปิดค้างรอเดินอย่างเดียว**
โค้ดกับเทสต์จบแล้วทั้งหมด:

| ตั๋ว | ใบตรวจ | แถวที่รอมือ |
|---|---|---|
| #25 | `docs/acceptance/25-section-enrolment.md` + ใบเดิน `_local/walk-25/walk-sheet.md` | 12 ☐ + 2 ◐ |
| #28 | `docs/acceptance/28-measurable-behaviors.md` | 9 ☐ + 3 ◐ |
| #29 | `docs/acceptance/29-achievement-criteria.md` | 8 ☐ + 3 ◐ |
| #30 | `docs/acceptance/30-weighting-scheme.md` | 7 ☐ + ◐ (แถวสถานะว่าง**เดินกับ seed ไม่ได้** — ต้องสร้างการเปิดสอนใหม่ผ่าน #23 ก่อน ใบตรวจบอกไว้แล้ว) |

ก่อนเดิน: reset+seed ใหม่ · รีสตาร์ต backend · ทำตามข้อตกลง walk-sheet เดิม (คนเดินคือผู้ใช้
Claude เขียนใบและติ๊กตามรายงาน · เรียกสี/น้ำหนักฟอนต์ด้วยชื่อ token · นับแถวก่อน-หลังข้อที่มีการลบ)

## 4. งานต่อไปที่ไม่ใช่เดินมือ — frontier ณ วันนี้

- **#31 Teaching plan** — ตัวบล็อกที่เหลือของสาย Activity (#32–#34, critical path) ตอนนี้ #30 ปิดไปหนึ่งแล้ว
- #41, #47, #48 — blockers ว่างตั้งแต่ก่อนเซสชันนี้
- [#104](https://github.com/khthana/Deep-QA/issues/104) — ยุบสำเนา teaching-register join ใน
  `teaching.js` / `enrolment.js` ให้เหลือ seam เดียว (acceptance = backend เต็มไม่เปลี่ยน)
- ตั๋วบั๊กเล็ก (#66, #77, #81, #85, #97, …) — ส่วนใหญ่พ่วงแถวเดินมือ ระวังพอกคิวข้อ 3

การตัดสินใจที่ยังเปิดรอผู้ใช้ (ยกมาจากฉบับก่อน ไม่เปลี่ยน): ลบ `_local/machine-move/`
(สำเนาคริเดนเชียลชุดที่สอง — ยืนยันเครื่องใหม่ใช้ได้แล้ว เหลือแค่กดลบ) · แบบแผน id ล้น int4
ตอบ 500 (จะเปิดตั๋วไหม) · waitForResponse body-race ใน `clos-screen.js`/`24a` ·
`SubjectStudents.js` โชว์ `program_id` ดิบใต้คอลัมน์หลักสูตร

## 5. สิ่งที่คนต่อควรรู้จากสามตั๋วนี้ (สั้น — ฉบับเต็มอยู่ในใบตรวจ/หัวไฟล์)

- **แบบแผนลูกของ CLO** (#28/#29): ทุก lookup จับคู่ `id AND clo_id` · เลขลำดับเป็นของ server
  (MAX+1 ใต้ `FOR UPDATE`, ลบแล้ว renumber จากน้อยไปมาก) · คำถามสิทธิ์ import จาก `clos.js`
  ห้ามถามซ้ำ
- **`lib/importer` โต** (#30): hook `whole(drafts)` คืน*ประโยค*ปฏิเสธระดับไฟล์ และ `onCommit`
  คืน `{ ok:false, message }` เพื่อ rollback ได้ — สัญญาอยู่ในคอมเมนต์หัว `importRows`
  ผู้ใช้เดิมทุกรายไม่กระทบ (ตรวจแล้ว)
- **`GradingWeights` ไม่ blank ระหว่าง reload โดยตั้งใจ** — ต่างจากหน้าพี่น้อง เพื่อรักษารายงานของ
  ImportPanel ถ้าแก้กลับ 30a แถว 8 จะจับ
- **ชื่อชั่วคราวตอน rename ขึ้นต้นด้วยช่องว่าง** (`parkedName` ใน `weights.js`) ผูกกับการที่
  `text()` trim ทุกอย่างขาเข้า — เลิก trim เมื่อไหร่ต้องหาที่ซ่อนใหม่
- **มัตแตนต์ที่แก้ไฟล์แล้ว MISS**: หลังแก้ไฟล์ที่มัตแตนต์ผูกอยู่ ให้ `save` ใหม่แล้ว dry-apply
  ทั้งชุดก่อน commit (ทำแล้วทั้งสามตั๋ว — `27:anyclo` เคย MISS จากการยก `offeringOf` และถูกเขียนใหม่
  พิสูจน์ซ้ำแล้วใน commit ของ #28)

## 6. กับดัก (ยกมาจากฉบับก่อน เฉพาะที่ยังเป็นจริง)

**clone อื่นที่ยังชี้ `master`** — สาขาหลักถูกเปลี่ยนชื่อ `master` → `main` ฝั่ง server เมื่อ
17 ส.ค. 2569 clone เก่าบนเครื่องอื่นซ่อมด้วย:

```bash
git branch -m master main && git fetch origin --prune && git branch -u origin/main main
```

เก็บข้อนี้ไว้ในทุก handoff จนกว่าจะยืนยันว่าทุก clone ถูกซ่อมหมดแล้ว

**อย่า `git add` อะไรใน `DEEP-QA-BACKEND/` หรือ `_local/`** — `.env` มีคริเดนเชียลจริง
`uploads/` เป็นงานของนักศึกษาจริง และ repository นี้เป็น public

**อย่าพิมพ์ภาษาไทยผ่าน Bash** (cp874 `UnicodeEncodeError`) — ใช้ Write tool หรือสคริปต์ Python
`io.open(..., encoding='utf-8', newline='\n')` และระวัง NUL/อักขระควบคุมหลุดลง string
(เจอจริงรอบนี้: NUL ใน SQL parameter ทำ Postgres ปฏิเสธ `invalid byte sequence`)

**รีสตาร์ต backend ก่อนเดินใบตรวจด้วยมือ** — `node server.js` ไม่มี reload

**สภาพเครื่อง ณ จบเซสชัน** — backend (3000) กับ frontend (5000) สำหรับใบเดิน #25 ถูกเปิดค้างแบบ
detached ไว้ก่อนหน้าในเซสชัน ถ้าจะใช้ให้ตรวจก่อนว่ายังอยู่ (แก้ route ไปหลายรอบแล้ว — **ต้อง bounce
ก่อนเดินแน่นอน**) · `grader-app-1` ที่เคยนั่งทับพอร์ต 3000 ถูก stop ไว้ คืนได้ด้วย
`docker start grader-app-1`

## 7. ข้อตกลงยืนพื้น

ไม่เปลี่ยน — ดูฉบับ `2026-08-25-b-...` และ `2026-08-25-c-...`: สองแกนของ code-review ·
เกณฑ์ตัดสินมัตแตนต์สามข้อ · การตายแบบปนเปื้อนบันทึกเป็นการปนเปื้อน · แถวที่ schema
ไม่เหลืออะไรให้กรองบันทึกไว้ตรง ๆ แทนการทำเนียน
