# การส่งมอบ — 2 ก.ย. 2569 (ค)

แทนที่ `2026-09-02-b-two-blanks-that-were-never-anybodys-answer.md` สำหรับสถานะ
เอกสารนั้นยังใช้อ่านเรื่อง #34 และกับดักที่บันทึกไว้ได้ ใบนี้เล่าเฉพาะสิ่งที่เปลี่ยนหลังจากนั้น

## สถานะ

ปิดถึง **#38** แล้ว — #26 (กลุ่มงานนักศึกษา) · #34 (คะแนนกิจกรรม) · #38 (รายละเอียดผลการเรียนรู้)

**ไม่มีแถวไหนในใบตรวจรับใบใดที่ยังไม่ได้เดิน** ทุกใบเดินครบ แถวที่ไม่ได้ติ๊ก ☑ ติดค้างเพราะ
เหตุผลอื่นที่ไม่ใช่ "ยังไม่มีคนดู" — เกือบทั้งหมดเป็นข้ออ้างเชิงเลขที่ seam พิสูจน์ได้ดีกว่าตาคน
ใบของ #38 ปิดที่ 6 ☑ · 8 ◐ · 10 ⚙ · 0 ☐

| seam | ผล ณ วันส่งมอบ |
|---|---|
| backend | 573 |
| db | 117 |
| e2e | 266 |
| มัตแตนต์ | 334 |

## #38 คืออะไร

หน้าจอแรกของการสร้างใหม่ที่ **คำนวณ** แทนที่จะ **บันทึก** ไม่มีอะไรถูกเก็บเลย ทุกตัวเลขบนจอ
เป็นความเห็นเกี่ยวกับคะแนนที่ #34 เขียนไว้ ทั้งไฟล์ `backend/routes/learningDetails.js` คือกฎสี่ข้อ
ที่แปลงอย่างหนึ่งเป็นอีกอย่าง

กฎที่ตัดสินเกือบทุกอย่างคือกฎที่ #34 ทิ้งไว้ — **ช่องว่างไม่ถูกนับทั้งสองข้างของเศษส่วน**
กิจกรรมที่นักศึกษายังไม่มีคะแนนไม่ใช่ศูนย์ที่เขาทำได้ มันคืองานที่ยังไม่มีใครตรวจ ถ้านับเป็นศูนย์
ทั้งห้องจะอยู่ในแถบแดงตั้งแต่สัปดาห์แรก

เกณฑ์สามเส้น เขียนไว้แล้วสอง — BR-20 แบ่งแถบ BR-17 ให้ข้อผ่านเมื่อ**เกิน**หกสิบเปอร์เซ็นต์
อย่างเคร่งครัด (TC-EVAL-004) เส้นที่สามคือ *นักศึกษาคนหนึ่งผ่านข้อหนึ่งเมื่อไร* ซึ่งไม่มีในกฎไหนเลย
ตกลงกับผู้ใช้ไว้ที่ 3.0 จาก 5 ก่อนสร้าง และกฎที่เขียนไว้แล้วสองข้อชี้ไปที่เลขเดียวกัน — หกสิบ
เปอร์เซ็นต์ของห้าคือสามพอดี และสามคือจุดที่ BR-20 เลิกขึ้นธง สีกับเลขจึงลากเส้นเดียวกัน

## สิ่งที่ต้องรู้ถ้าจะทำต่อ

### เศษส่วนที่ไม่มีกฎรองรับ

การ์ด *ผ่านเกณฑ์รายคนรายข้อ* นับสัดส่วนของ **ช่อง** ที่ถึง 3.0 ไม่ใช่สัดส่วนของนักศึกษา
เพราะ**ไม่มีกฎข้อไหนบอกว่านักศึกษาคนหนึ่งผ่านทั้งตอนเรียนเมื่อไร** BR-17 พูดถึงข้อหนึ่งเทียบทั้งห้อง
BR-20 พูดถึงคนหนึ่งกับข้อหนึ่ง ไม่มีข้อที่สาม ถ้าใครจะทำสัดส่วนรายนักศึกษา **ต้องตกลงกฎใหม่ก่อน**
ว่าผ่านกี่ข้อถึงเรียกว่าผ่าน แล้วกฎนั้นจะเป็นกฎของระบบ ไม่ใช่ของตั๋วนี้

การ์ดจึงแสดงเศษส่วนกำกับ (`479 จาก 513 ช่องที่มีคะแนน`) เพราะ**เปอร์เซ็นต์ที่วางข้างการ์ดซึ่ง
เขียนว่า *57 คน* จะถูกอ่านว่าเป็นสัดส่วนของนักศึกษาไม่ว่าจะตั้งชื่อว่าอะไร** ตัวส่วนที่ไม่ใช่
จำนวนคนคือสิ่งเดียวที่ปิดการอ่านผิดนั้นได้

ผู้ใช้อ่านถ้อยคำใหม่บนหน้าจอเองแล้วและตอบว่าอ่านรู้เรื่อง แถวจึงติ๊ก ☑ — **คนที่เขียนถ้อยคำ
ไม่ใช่คนที่ควรติ๊กว่าถ้อยคำนั้นอ่านรู้เรื่อง** ถ้อยคำที่ผู้ใช้ยังไม่ได้อ่านต้องค้างไว้ที่ ◐ เสมอ

### คอลัมน์คือผลการเรียนรู้ของ Offering ไม่ใช่ของตารางการเชื่อมโยง

ข้อที่ไม่มีกิจกรรมใดวัดต้องเป็น**คอลัมน์ว่าง** ไม่ใช่คอลัมน์ที่หายไป เพราะช่องว่างในแผนการสอน
คือสิ่งที่อาจารย์ต้องเห็น ถ้าใครแก้ให้อ่านผ่าน `activity_clo_mapping` เพื่อความเร็ว จะซ่อนกรณีนั้น
พอดี — และ seed จะไม่ฟ้อง เพราะ seed วัดครบทุกข้อ

## กับดักที่บันทึกใหม่รอบนี้

### MISS ที่ apply ได้แต่ไม่ตัดสินอะไร

มัตแตนต์สองตัวของ #38 ต้องเขียนสองรอบ **ทั้งคู่ apply ผ่าน ข้อความที่ผูกไว้ยังอยู่ครบ
แต่สิ่งที่มันแก้ไม่เปลี่ยนพฤติกรรมเลย** — ตัวหนึ่งลบความกว้างขั้นต่ำของตารางที่กว้างเกินจออยู่แล้ว
อีกตัวแคบคอลัมน์บน seed ที่วัดครบทุกข้อ

**MISS แบบนี้อ่านไม่ออกจากผลของคำสั่ง apply อ่านออกจากการที่ไม่มีแถวไหนตายเท่านั้น**
ซึ่งเป็นเหตุผลที่ทุกตัวต้องถูกกวาดจริง ไม่ใช่แค่ตรวจว่า apply ผ่าน

การไล่หาสาเหตุตัวแรกยังเปิดว่าแถวนั้นยืนยันสิ่งที่เป็นจริงเสมอ — เปลือกของทุกหน้าจอมี
`overflow-x-hidden` อยู่แล้ว **หน้าจึงเลื่อนข้างไม่ได้ไม่ว่าจะเขียนโค้ดอย่างไร** ข้ออ้างที่เป็นจริง
เสมอคือข้ออ้างที่ไม่ได้พูดอะไร

### สิ่งที่เทสต์ทุกชุดผ่านแต่การเดินจับได้

กล่อง *ผลการเรียนรู้ที่ควรปรับปรุง* เขียนว่า *ทุกผลการเรียนรู้ผ่านเกณฑ์* บนหน้าที่คอลัมน์สุดท้าย
แสดงขีดตรงคำตัดสินนั้นพอดี — **รายการว่างถูกอ่านเป็นข่าวดี แทนที่จะเป็นคำถามที่ยังไม่ได้ถาม**

265 เทสต์กับรีวิวสองแกนมองไม่เห็น เพราะทุกแถวในเบราว์เซอร์ทำงานกับข้อที่มีคะแนนอยู่แล้ว
กรณีนี้เกิดต่อเมื่อมีข้อที่ยังไม่ถูกวัด และไม่เคยมีแถวไหนสร้างมันแล้วอ่านกล่องนั้น
`38:attentionclaimsallpassed` จึงเป็นมัตแตนต์ตัวเดียวใน 334 ตัวที่เขียนขึ้น **หลัง** คนมองหน้าจอ

### walk backend ต้องรีสตาร์ต *หลัง* แก้ route ครั้งสุดท้าย

รู้อยู่แล้วว่า `node server.js` ไม่ reload แต่รอบนี้ยังพลาด — รีสตาร์ตตอนต้นการเดิน แล้วแก้ route
ระหว่างเดิน หน้าจอขึ้น `undefined จาก undefined` **เทสต์ทุกชุดผ่านหมด เพราะแต่ละชุดปั่นเซิร์ฟเวอร์
ของตัวเองขึ้นใหม่ทุกครั้ง** มีแต่การเดินบนเซิร์ฟเวอร์ที่ค้างอยู่จริงเท่านั้นที่เห็น

### เซสชันรอดการรีสตาร์ต backend

`session` เก็บในฐานข้อมูล ไม่ใช่หน่วยความจำ **การรีสตาร์ต backend ไม่ทำให้หลุด** — รอบแรกที่หลุด
เป็นเพราะอายุเซสชันเอง ไม่ใช่เพราะรีสตาร์ต อย่ารบกวนผู้ใช้ให้ล็อกอินใหม่โดยไม่ตรวจก่อน

### พิกัดของเครื่องมือคลิกไม่ใช่ CSS pixel

`getBoundingClientRect()` ให้พิกัดใน viewport (1360 กว้าง) แต่เครื่องมือคลิกใช้พิกัดของภาพ
ที่ถ่ายมา (1509 กว้าง) คลิกจึงไปตกผิดที่โดยไม่มีข้อผิดพลาดใด ๆ **ถ่ายภาพแล้วอ่านพิกัดจากภาพ**
และถ้า `document.visibilityState` เป็น `hidden` คลิกจะไม่ถึงหน้าเลย ต้องให้ผู้ใช้สลับแท็บมาก่อน

## ที่ค้างอยู่

- **#109** — `24a` แถว 3 flake ประมาณ 1 ใน 4 ไม่เกี่ยวกับงานล่าสุด อย่าด่วนสรุปว่าเป็นเพราะงานตัวเอง

## `_local/machine-move/` ถูกลบแล้ว

ลบเมื่อ 2 ก.ย. 2569 ตามที่ผู้ใช้สั่ง — 531 ไฟล์ 3.7 MB ตรวจก่อนลบว่าสำเนา `.env` ทั้งสี่
เหมือนต้นฉบับที่ยังอยู่ทุกไบต์ memory 12 ไฟล์ย้ายมาอยู่ในที่ใช้งานจริงครบแล้ว และไม่มีอะไร
นอก `_local/` อ้างถึงโฟลเดอร์นั้น

**สำเนา credential ชุดที่สองที่ sync ขึ้น OneDrive จึงหมดไป** ตอนนี้ `.env` สี่ไฟล์ที่
`.env` · `DEEP-QA-BACKEND/.env` · `DEEP-QA-FRONTEND/.env` · `frontend/.env`
**เป็นสำเนาเดียวที่มี** และ repository เป็น public — **ห้าม `git add` ไฟล์เหล่านี้เด็ดขาด**
`_local/` อยู่ใน `.gitignore` บรรทัด 19 การลบจึงไม่กระทบ git

## clone ที่ยังชี้ `master`

สาขาหลักชื่อ `main` clone เก่าที่ยังมี `master` ตายค้างอยู่ ใช้คำสั่งนี้แก้

```bash
git branch -m master main && git fetch origin && git branch -u origin/main main
```
