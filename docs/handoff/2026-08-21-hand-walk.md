# Handoff — DEEP-Core rebuild

**Workspace:** `C:\Users\khtha\OneDrive\Desktop\Code\Deep-QA`
**Written:** 2026-08-21 · updated after #72 closed
**State:** No new screen was built this session. The whole session was spent walking acceptance
rows by hand with the advisor ruling on each one, and following one ruling into the server. Four
tickets closed — **#18, #63, #72, #73, #75** — and three were opened — **#79, #80, #81**.

Supersedes `2026-08-20-two-seams.md` for state. That document is still the authority on **how the
browser seam works** (its section 2) and on **running the two stacks** (its section 3); nothing
there changed. Its census in section 4 is now out of date — the one below replaces it.

`2026-08-16-planning-session.md` is still the authority on how the plan was arrived at.

---

## 1. Where the acceptance documents stand

Row-anchored count, `grep -c '^|.*| ☐ |' docs/acceptance/NN.md` and its siblings:

| document | ☑ walked | ⚙ seam | ☐ left |
|---|---|---|---|
| `10-application-shell.md` | 2 | 8 | **1** |
| `11-user-accounts.md` | 1 | 13 | **20** |
| `12-role-grants.md` | 6 | 9 | 0 |
| `13-user-activity-history.md` | 5 | 9 | 0 |
| `14-departments.md` | 1 | 13 | **19** |
| `15-programs.md` | 41 | 3 | 0 |
| `16-subjects.md` | 25 | 9 | 0 |
| `17-students.md` | 0 | 12 | **10** |
| `18-program-subjects.md` | 1 | 18 | 0 |
| `57-pager.md` | 1 | 8 | 0 |
| | **83** | **102** | **50** |

The one row left in `10` is the external assessor's shell and is **blocked on #49** — it cannot be
walked until that account can sign in at all. The other 49 are workable.

**The agreed order for what remains** is #71 (20) · #74 (19) · #64 (10). All three write real rows
into `deep_core`. `npm run seed` alone does **not** undo a
write - it upserts against natural keys and leaves anything extra in place. The clean sequence is
`cd db && npm run reset && npm run migrate && npm run seed`.

## 2. What was walked, and what it cost

- **#75 / #18** — the last row of `18-program-subjects.md`. Walking it is what turned up #79.
- **#73** — the three rows of `13-user-activity-history.md`. The row-3 breadcrumb text in the
  document was stale and was corrected to what the screen draws.
- **#63** — the three rows of `16-subjects.md`.
- **#70** — rows 2 and 5 of `10-application-shell.md`. Row 5's expectation was **rewritten** on the
  advisor's ruling: `PROG_MANAGER` seeing two sidebar items and no account menus is correct, so the
  document was what was wrong. Row 2 turned up #80.

## 3. #79 — the faculty does not reach inside a curriculum

Commit `38059e9`. `FACULTY_ADMIN` left `MAINTAINERS` in `backend/routes/programSubjects.js`, which
reverses a note #61 had left there, and left the faculty sidebar with it. Hiding a menu is not what
keeps anyone out (ADR-0002, and criterion 8 of `10-application-shell.md`), so the change is at the
server and the menu follows it, not the other way round.

`docs/05-screen-api-mapping.md` was corrected with it: A04, A06, A09 and A10 lose `FACULTY_ADMIN`;
A03 and A07 read `DEPT_ADMIN`; A11, A12 and A13 name their three roles instead of saying "every
admin role". That last one was a **documentation error rather than a divergence** — the student
sidebar for `PROG_MANAGER` has no `/main/users` at all, so the table never described the delivered
code either.

`#19`, `#20` and `#21` were commented with the constraint, because their screens are still
`NotBuiltYet` and will otherwise be built to the old rule.

**The part that is easy to forget:** two mutants were written against the `MAINTAINERS` line #79
rewrote, so the proof behind eighteen of #18's ⚙ marks and one of #10's could no longer be
reproduced. Both were rewritten against what the file says now and **re-run**: `18:maintainerall`
dies at `18c-program-subjects-refusals.spec.js:152`, `10:maintainer` at `10a-shell.spec.js:104`,
each exactly where its own table already said it would. A census of all six scripts against the
tree gives **79 mutants, one MISS**, still `13:N6` — the same figure `mutation/README.md` carries.

Whenever a route or an assertion an ⚙ row rests on is edited, run that census before believing the
row. It is a short script: import each `mutation/*.py`, and for every mutant assert its `old`
string appears exactly once in the file it names.

## 4. #72 — walked and closed

All six rows of `12-role-grants.md` were walked on 21 Aug 2026 and ticked by the advisor from the
evidence. The document records what was seen in its own section; the ticket comment carries the
same table. Two things about it are worth carrying forward.

**Two rows were proved from the DOM, not from a screenshot.** Criterion 5 asks what is *absent*
from a dropdown — no `FULL_ADMIN`, no `01` — and a picture cannot prove an absence. Reading
`select.options` is the honest form of that evidence, and the same will be true of any future row
phrased as "must not offer".

**The row-4 setup wrote to the database, and `npm run seed` did not undo it.** Walking row 4 needed
`teach01` to hold a second grant, so one was made through the screen. `npm run seed` upserts
against natural keys; it re-created what the seed defines and left the extra grant untouched. The
sequence that actually restores the documented state is `npm run reset && npm run migrate &&
npm run seed`, and that is what was run — `teach01` is back to `TEACHER/05` alone.

## 5. Opened this session

- **#79** — above. Closed by `38059e9`, kept open only if the advisor wants the note trail.
- **#80** — *a session that died leaves the expiry dialog with no way out.* `POST /api/auth/logout`
  sits behind `requireSession`, so a request whose token no longer verifies never reaches
  `clearCookie`, and *เข้าสู่ระบบใหม่* only reloads. The sign-in form is behind the dialog and
  unreachable; the only escape is deleting the cookie in DevTools. Root cause at
  `backend/routes/auth.js:130`, where the comment's second half is the wrong half. Both existing
  e2e rows that touch this dialog clear cookies first, so neither ever enters the state.
- **#81** — *switching the acting grant leaves the person on the old grant's route.* The sidebar
  changes at once, which is correct and is what row 4 asks for, but the page does not: switching to
  the teacher hat while on `/main/rubrics` leaves them there, and the breadcrumb then reads
  *ข้อมูลหลัก / รายวิชาที่รับผิดชอบ*. Harmless today because both destinations are `NotBuiltYet`;
  once #21 and #24 are built it becomes a `403` the person did nothing to earn.

## 6. Gotchas that cost time

- **A fresh clone lands on a dead `master`.** Run, verbatim:
  `git branch -m master main && git fetch origin --prune && git branch -u origin/main main`
- **Never run the e2e stack while the dev stack is up.** CRA starves it, the `webServer` times out
  at 240 s, and the run reports green having tested nothing. Stop both servers first — and note
  that stopping the background task is not enough on Windows: the `node` processes survive it.
  Check with `netstat -ano | grep -E ":(3000|5000)\s.*LISTENING"` and `taskkill //PID n //T //F`.
- **Sign-in through the browser tools needs a real click.** `form_input` on the two fields followed
  by Enter does not submit. Fill both fields, then click *Login to your account* by coordinate —
  it sat at ≈(1098, 558) — and wait eight seconds.
- **A logout click can land mid-fade and not take.** Confirm with `/api/me` rather than the screen.
  The first screenshot after each sign-in shows the sidebar text overlapping; that is the submenu
  animation in flight, not a defect. Wait four seconds.
- Everything in section 6 of `2026-08-20-two-seams.md` still applies: cp874, `rev`, `gh --body-file`,
  U+FEFF, the mutation backup keying, and `toBeDisabled()` on an `<option>`.

## 7. Standing agreements (unchanged, still binding)

Section 7 of `2026-08-20-two-seams.md`, in full and without amendment. The two that were leaned on
hardest this session: **do not tick a row from reasoning about the code**, and **ask before
pushing**.
