# -*- coding: utf-8 -*-
"""#48 - ช่วงเวลาใช้งานของบัญชีผู้ประเมินภายนอก

ตั๋วเขียนไว้ว่า *"Nothing in the schema can express it"* และ *"The window
belongs on `user_roles`, not on `users`"* ทั้งสองประโยคหมดอายุแล้ว
`db/migrations/0005_external_assessor_validity.sql` วางช่วงเวลาไว้บน `users`
ตั้งแต่ #11 และคอมเมนต์ของไมเกรชันนั้น **เถียงข้อเสนอของตั๋วตรง ๆ**: R005 กับ
ROLE-6 พูดถึง*ตัวบัญชี* ไม่ใช่*สิทธิ์* และคอลัมน์บน `user_roles` จะทำให้ #11 กับ
#12 เขียนแถวเดียวกันด้วยเหตุผลคนละอย่าง

สิ่งที่ยังขาดจริงจึงไม่ใช่คอลัมน์ แต่เป็น **ฟิกซ์เจอร์** และตั๋วก็ขอไว้ในเกณฑ์ข้อ 7

**บัญชีที่ช่วงเวลามีไว้เพื่อมัน คือบัญชีเดียวที่ไม่มีช่วงเวลา** ก่อนตั๋วนี้ `db/seed.js`
ปล่อยทั้งสองปลายเป็น null บนผู้ประเมินทั้งสองบัญชี แปลว่าทุกสวีทที่เอา `U_EXT` เข้าระบบ
เดินอยู่บนเส้นทาง *ไม่มีช่วงเวลาเลย* ซึ่งเป็นเส้นทางเดียวกับบัญชีพนักงานทั่วไป R005 บอกว่า
บัญชีถูกสร้าง "พร้อมกำหนดช่วงเวลาการใช้งาน" และ ROLE-6 เรียกมันว่า *บัญชีชั่วคราว*
ชุดข้อมูลจึงไม่เคยมีบัญชีที่เป็นแบบนั้นเลยสักบัญชี

ร่างแรกของย่อหน้านี้เขียนว่า **สิบไฟล์** โดยไม่มี grep ค้ำ ของจริงคือ **เจ็ดไฟล์ถือ alias นี้**
หกไฟล์ลงชื่อเข้าใช้ผ่านฟอร์ม (`authorise` `evidence` `offerings` `program-results`
`program-results-students` `shell`) ส่วน `auth.test.js` รับมันเข้าทาง
`resolveGoogleAccount` ไม่ได้ลงชื่อเข้าใช้ **ตัวเลขที่เขียนไว้ในไฟล์มัตแตนต์ ก็เป็นข้ออ้าง
เหมือนตัวเลขในใบตรวจรับ** และอันนี้ผิดตั้งแต่บรรทัดที่ประกาศว่าตัวเลขในไฟล์นี้วัดก่อนเขียน

## เกณฑ์ข้อ 7 ถูกเขียนไว้ถูกในวันที่เขียน และผิดในวันนี้

ตั๋วขอให้ `U_NONKMITL` เป็นฝั่งที่ช่วงเวลาปิดไปแล้ว ซึ่งถูกเมื่อ 17 ส.ค. 2569 และ
ผิดตอนนี้ — สองสวีทให้งานที่สองแก่บัญชีนั้นไปแล้ว และงานนั้นทำได้ก็ต่อเมื่อลงชื่อเข้าใช้
ได้ (`authorise.test.js` ใช้มันพิสูจน์ว่ากฎโดเมนเป็นของ sign-in และ
`shell.test.js` เลือกมันเพราะไม่มีเทสต์อื่นลงชื่อเข้าใช้ด้วยบัญชีนี้) บัญชีที่เข้าไม่ได้
ถือสองงานนั้นไม่ได้ ฝั่งที่ปิดแล้วจึงได้แถวของตัวเอง — `U_EXT_CLOSED` — แทนที่จะ
ไปทับแถวที่คนอื่นใช้อยู่

**นี่คือบทเรียนของ #102 กลับด้าน** #102 ย้ายสถานการณ์ที่สร้างด้วยมือในสวีทเดียว
เข้าไปไว้ใน seed ตั๋วนี้ก็ทำอย่างเดียวกัน แต่สิ่งที่ต้องระวังคนละอย่าง: ฟิกซ์เจอร์ที่ย้าย
เข้า seed แล้ว **ทุกเทสต์ที่เคยตั้งสมมติฐานว่าคอลัมน์เป็น null ต้องถูกตามหา** สอง
ซับเทสต์ใน `auth.test.js` คืนค่าเป็น `NULL` ใน `finally` ซึ่งถูกต้องเฉพาะตอนที่ seed
ปล่อยว่าง ตอนนี้คืนผ่าน `resetValidity` ของ seed เอง — กฎเดียวสำหรับเขียนหน้าต่าง
ลงไป และสำหรับเอากลับคืน

## สิ่งที่วัดแล้วไม่เป็นอย่างที่คิด

การเอาช่วงเวลาใส่ seed **ไม่ได้ทำให้มัตแตนต์ตัวไหนของ #89 กัดแรงขึ้นเลย**
`endsareswapped` ฆ่าห้าข้อทั้งก่อนและหลัง เพราะมันเป็นมัตแตนต์เรื่อง *ประโยคไหน
เป็นของปลายไหน* ส่วนสิบสวีทที่ลงชื่อเข้าใช้ด้วย `U_EXT` ไม่ได้ยืนยันประโยคอะไรเลย
มันแค่เข้าให้ได้ และหน้าต่างที่เปิดอยู่ก็ให้เข้าได้ทั้งสองแบบ

ตัวเลขนี้วัดก่อนเขียน ไม่ใช่ทำนายแล้วเขียน — และมันเป็นเหตุผลที่มัตแตนต์ข้างล่างต้อง
ถูกเขียนขึ้นใหม่ ไม่ใช่ยืมของ #89 มาอ้าง **สิ่งที่ฟิกซ์เจอร์ใน seed ทำให้พิสูจน์ได้ ต้องมี
มัตแตนต์ของมันเอง มิฉะนั้นมันคือแถวข้อมูลที่ไม่มีใครค้ำ**

## การกวาด

| มัตแตนต์ | ฆ่า | ก่อนมีฟิกซ์เจอร์ใน seed |
|---|---|---|
| `insidewindowisrefused` | 17 ข้อ ใน 4 สวีท | 6 ข้อ ใน 2 สวีท |
| `sessionignoresthewindow` | 1 ข้อ | 1 ข้อ |

ตัวที่สองไม่ขยับ และนั่นถูกแล้ว — แถวที่มันฆ่าสร้างบัญชีของตัวเองแล้วเลื่อนหน้าต่าง
ข้ามวันสิ้นสุด ไม่ได้พึ่ง seed **ฟิกซ์เจอร์ทำให้ข้อเรียกร้องที่ *ยังไม่มีใครค้ำ* แข็งขึ้น
ไม่ได้ทำให้ทุกอย่างแข็งขึ้น** และการเขียนตัวเลขที่ไม่ขยับลงไปด้วย คือสิ่งที่ทำให้อีก
ตัวหนึ่งเชื่อถือได้

## สองข้อที่ยังเป็นของเจ้าของข้อกำหนด

เกณฑ์ข้อ 1 กับข้อ 6 ของตั๋ว (ช่วงเวลาอยู่บน `user_roles` และสิทธิ์ที่หมดอายุไม่กระทบ
สิทธิ์อื่นของบัญชีเดียวกัน) เป็น**คำถามเดียวกันถามสองครั้ง** — หมดอายุแล้วสิ่งที่หมดคือ
*ตัวคน* หรือ *ใบมอบหมาย* วันนี้คือตัวคน และไม่มีบัญชีไหนใน seed ที่ถือ
`EXT_ASSESSOR` ควบกับสิทธิ์อื่น (`U_MULTI` คือ PROG_MANAGER + TEACHER) เกณฑ์
ข้อ 6 จึงไม่มีแม้แต่ฟิกซ์เจอร์ให้ตกได้ ยังไม่ทำในตั๋วนี้ รอคำตอบ

    python mutation/48-assessor-validity-window.py save
    python mutation/48-assessor-validity-window.py <mutant>
    python mutation/48-assessor-validity-window.py restore
"""

from harness import main

FILES = {
    "accounts": "backend/auth/accounts.js",
}

MUTANTS = {
    # บัญชีที่อยู่ *ใน* ช่วงเวลากลับถูกปฏิเสธ - เครื่องหมายกลับข้าง ไม่ใช่ด่านหายไป
    # นี่คือข้อเรียกร้องที่ฟิกซ์เจอร์ใน seed ทำให้พิสูจน์ได้ และเป็นเกณฑ์ข้อ 3 ของตั๋ว
    # (TC-AUTH-005) ก่อน #48 ไม่มีบัญชีไหนใน seed มี valid_from เลย ตัวนี้จึงจับได้
    # เฉพาะเทสต์ที่สร้างหน้าต่างขึ้นเอง
    #
    # ไม่ใช่รูป *ฆ่ามากเกินไป* ของ #97 เพราะมันแตะเฉพาะบัญชีที่มีช่วงเวลา ซึ่งมีสอง
    # บัญชี ที่เหลือทั้งระบบทำงานปกติ
    #
    # ฆ่า 17 ข้อ ใน 4 สวีท (auth 4 · authorise 4 · shell 3 · users 6)
    # ก่อน seed มีช่วงเวลา ตัวเดียวกันนี้ฆ่า **6** ข้อ ใน 2 สวีท — วัดด้วยการถอด
    # `validity` ทั้งสองบรรทัดออกแล้วรันมัตแตนต์เดิมซ้ำ ข้อที่เจ็ดในรอบนั้นคือข้อที่
    # ตกเพราะฟิกซ์เจอร์หายไป ไม่ใช่เพราะมัตแตนต์ จึงไม่นับ
    #
    # **ฆ่า 17 การเสริมความแข็งแรงคือ 11** — #102 บันทึกไว้ว่าตัวเลขเดียวใน
    # รายงานซ่อนอันที่ผู้อ่านไม่ได้นึกถึงอยู่
    "insidewindowisrefused": ("accounts",
        "  if (user.valid_from && now < calendarDay(user.valid_from)) {\n",
        "  if (user.valid_from && now >= calendarDay(user.valid_from)) {\n"),
    # เกณฑ์ข้อ 5: สิทธิ์ที่หมดอายุกลางทางต้องหยุดให้สิทธิ์ที่การตรวจครั้งถัดไป ไม่ใช่รอ
    # ให้ cookie หมดอายุ ด่านที่ `admit` ยังอยู่ครบ คนที่ลงชื่อเข้าใช้ใหม่จึงยังถูกปฏิเสธ
    # เหมือนเดิม - เปลี่ยนแค่ session ที่ถืออยู่แล้วข้ามเส้นวันสิ้นสุด
    #
    # สองบรรทัดนี้เขียนเหมือนกันเป๊ะทั้งใน `admit` และ `sessionAdmission` จุดยึดจึง
    # ต้องกินบรรทัดที่ตามมาด้วย ไม่งั้นมันชี้ได้สองที่
    #
    # ฆ่า 1 ข้อ คือข้อที่มันพูดถึงพอดี ไม่มีอะไรอื่นล้ม
    "sessionignoresthewindow": ("accounts",
        "  const outsideWindow = validityRefusal(user);\n"
        "  if (outsideWindow) return outsideWindow;\n"
        "  return null;\n"
        "}\n",
        "  return null;\n"
        "}\n"),
}

main(FILES, MUTANTS)
