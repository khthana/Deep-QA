# -*- coding: utf-8 -*-
"""#48 - ช่วงเวลาใช้งานของบัญชีผู้ประเมินภายนอก

ตั๋วเขียนไว้ว่า *"Nothing in the schema can express it"* และ *"The window
belongs on `user_roles`, not on `users`"* ทั้งสองประโยคหมดอายุแล้ว
`db/migrations/0005_external_assessor_validity.sql` วางช่วงเวลาไว้บน `users`
ตั้งแต่ #11 และคอมเมนต์ของไมเกรชันนั้น **เถียงข้อเสนอของตั๋วตรง ๆ**: R005 กับ
ROLE-6 พูดถึง*ตัวบัญชี* ไม่ใช่*สิทธิ์* และคอลัมน์บน `user_roles` จะทำให้ #11 กับ
#12 เขียนแถวเดียวกันด้วยเหตุผลคนละอย่าง

สิ่งที่ยังขาดจริงจึงไม่ใช่คอลัมน์ แต่เป็น **ฟิกซ์เจอร์** และตั๋วก็ขอไว้ในเกณฑ์ข้อ 7

**บัญชีที่ช่วงเวลามีไว้เพื่อมัน คือบัญชีเดียวที่ไม่มีช่วงเวลา** ก่อนตั๋วนี้ `db/seed.js`
ปล่อยทั้งสองปลายเป็น null บนผู้ประเมินทั้งสองบัญชี แปลว่าทุกสวีทที่เอา `U_EXT` เข้าระบบ
เดินอยู่บนเส้นทาง *ไม่มีช่วงเวลาเลย* ซึ่งเป็นเส้นทางเดียวกับบัญชีพนักงานทั่วไป R005 บอกว่า
บัญชีถูกสร้าง "พร้อมกำหนดช่วงเวลาการใช้งาน" และ ROLE-6 เรียกมันว่า *บัญชีชั่วคราว*
ชุดข้อมูลจึงไม่เคยมีบัญชีที่เป็นแบบนั้นเลยสักบัญชี

ร่างแรกของย่อหน้านี้เขียนว่า **สิบไฟล์** โดยไม่มี grep ค้ำ ของจริงคือ **เจ็ดไฟล์ถือ alias นี้**
หกไฟล์ลงชื่อเข้าใช้ผ่านฟอร์ม (`authorise` `evidence` `offerings` `program-results`
`program-results-students` `shell`) ส่วน `auth.test.js` รับมันเข้าทาง
`resolveGoogleAccount` ไม่ได้ลงชื่อเข้าใช้ **ตัวเลขที่เขียนไว้ในไฟล์มัตแตนต์ ก็เป็นข้ออ้าง
เหมือนตัวเลขในใบตรวจรับ** และอันนี้ผิดตั้งแต่บรรทัดที่ประกาศว่าตัวเลขในไฟล์นี้วัดก่อนเขียน

**แล้วย่อหน้าที่แก้ตัวเลขนี้ ก็ยังทิ้ง *สิบ* ไว้อีกสามที่** — `db/seed.js`,
`db/test/seed.test.js` และ **ยี่สิบหกบรรทัดใต้ย่อหน้านี้เอง** `/code-review` เป็นคนเจอ
นี่คือกฎของ #96 ในรูปที่ชัดที่สุดเท่าที่คลังนี้เคยมี: **การแก้ตัวเลขในที่ที่จำได้ว่าเขียน
ไม่ใช่การแก้ตัวเลข** และย่อหน้าที่ประกาศว่าได้แก้แล้ว คือย่อหน้าที่อ่านเหมือนหลักฐาน
ทั้งที่ยังไม่ได้ grep ซ้ำ ครั้งหน้า grep ค่าที่ผิด **หลัง** แก้ ไม่ใช่ก่อน

## เกณฑ์ข้อ 7 ถูกเขียนไว้ถูกในวันที่เขียน และผิดในวันนี้

ตั๋วขอให้ `U_NONKMITL` เป็นฝั่งที่ช่วงเวลาปิดไปแล้ว ซึ่งถูกเมื่อ 17 ส.ค. 2569 และ
ผิดตอนนี้ — สองสวีทให้งานที่สองแก่บัญชีนั้นไปแล้ว และงานนั้นทำได้ก็ต่อเมื่อลงชื่อเข้าใช้
ได้ (`authorise.test.js` ใช้มันพิสูจน์ว่ากฎโดเมนเป็นของ sign-in และ
`shell.test.js` เลือกมันเพราะไม่มีเทสต์อื่นลงชื่อเข้าใช้ด้วยบัญชีนี้) บัญชีที่เข้าไม่ได้
ถือสองงานนั้นไม่ได้ ฝั่งที่ปิดแล้วจึงได้แถวของตัวเอง — `U_EXT_CLOSED` — แทนที่จะ
ไปทับแถวที่คนอื่นใช้อยู่

**นี่คือบทเรียนของ #102 กลับด้าน** #102 ย้ายสถานการณ์ที่สร้างด้วยมือในสวีทเดียว
เข้าไปไว้ใน seed ตั๋วนี้ก็ทำอย่างเดียวกัน แต่สิ่งที่ต้องระวังคนละอย่าง: ฟิกซ์เจอร์ที่ย้าย
เข้า seed แล้ว **ทุกเทสต์ที่เคยตั้งสมมติฐานว่าคอลัมน์เป็น null ต้องถูกตามหา** สอง
ซับเทสต์ใน `auth.test.js` คืนค่าเป็น `NULL` ใน `finally` ซึ่งถูกต้องเฉพาะตอนที่ seed
ปล่อยว่าง ตอนนี้คืนผ่าน `resetValidity` ของ seed เอง — กฎเดียวสำหรับเขียนหน้าต่าง
ลงไป และสำหรับเอากลับคืน

## สิ่งที่วัดแล้วไม่เป็นอย่างที่คิด

การเอาช่วงเวลาใส่ seed **ไม่ได้ทำให้มัตแตนต์ตัวไหนของ #89 กัดแรงขึ้นเลย**
`endsareswapped` ฆ่าห้าข้อทั้งก่อนและหลัง เพราะมันเป็นมัตแตนต์เรื่อง *ประโยคไหน
เป็นของปลายไหน* ส่วนเจ็ดสวีทที่เอา `U_EXT` เข้าระบบ ไม่ได้ยืนยันประโยคอะไรเลย
มันแค่เข้าให้ได้ และหน้าต่างที่เปิดอยู่ก็ให้เข้าได้ทั้งสองแบบ

ตัวเลขนี้วัดก่อนเขียน ไม่ใช่ทำนายแล้วเขียน — และมันเป็นเหตุผลที่มัตแตนต์ข้างล่างต้อง
ถูกเขียนขึ้นใหม่ ไม่ใช่ยืมของ #89 มาอ้าง **สิ่งที่ฟิกซ์เจอร์ใน seed ทำให้พิสูจน์ได้ ต้องมี
มัตแตนต์ของมันเอง มิฉะนั้นมันคือแถวข้อมูลที่ไม่มีใครค้ำ**

## การกวาด

| มัตแตนต์ | ฆ่า | ก่อนมีฟิกซ์เจอร์ใน seed |
|---|---|---|
| `insidewindowisrefused` | 24 ข้อ ใน 8 สวีท | 6 ข้อ ใน 2 สวีท |
| `sessionignoresthewindow` | 1 ข้อ | 1 ข้อ |
| `windowyieldstoasecondgrant` | 1 ข้อ | เขียนขึ้น 24 ก.ย. 2569 ไม่มีค่าก่อนหน้า |

**21 เป็น 24 เมื่อ 24 ก.ย. 2569** วัดใหม่หลัง `U_NONKMITL` ได้ช่วงเวลาเปิดข้างเดียวตาม
เกณฑ์ข้อ 7 ที่ปิดในวันนั้น สามข้อที่เพิ่มมาอธิบายได้ทั้งหมด: `authorise` 4→5 กับ `shell` 3→4
คือสองสวีทที่ลงชื่อเข้าใช้ด้วยบัญชีนั้น (ใน `shell` เป็นข้อ *takes effect at the next
sign-in* ข้อเดียว อีกสามข้อในกลุ่มเดียวกันใช้ `U_EXT` และตายอยู่แล้ว) ส่วน `users` 6→7 คือ
แถวใหม่ของเกณฑ์ข้อ 6 รายสวีทตอนนี้คือ auth 4 · authorise 5 · shell 4 · users 7 ·
evidence 1 · offerings 1 · program-results 1 · program-results-students 1

**ฟิกซ์เจอร์ที่เพิ่มเข้า seed ทำให้มัตแตนต์ที่มีอยู่แล้วเปลี่ยนตัวเลข** — ตัวเลขในตารางนี้จึง
ต้องกวาดใหม่ทุกครั้งที่ seed เติมบัญชีที่มีช่วงเวลา ไม่ใช่แค่ตอนที่มัตแตนต์เปลี่ยน

**เลขแรกที่เขียนไว้คือ 17 ใน 4 สวีท และมันผิด** `/code-review` จับได้ สาเหตุคือกฎของคลังนี้เอง:
`node --test` นับ suite แม่รวมใน `# fail` คลังจึงวางกฎว่าให้อ่านจำนวนที่ตายจริงจากบรรทัด
`not ok` **ที่ย่อหน้า** — และกฎนั้นมีรู เทสต์ที่เขียนไว้ที่ระดับบนสุดของไฟล์ไม่มี suite แม่
บรรทัด `not ok` ของมัน**จึงไม่ย่อหน้า** และหน้าตาเหมือนหัว suite ทุกประการ สี่ข้อที่หายไป
เป็นรูปนั้นทั้งหมด และอยู่ในสี่สวีทที่ไม่มีใครคิดจะเปิด (`evidence` `offerings`
`program-results` `program-results-students`) — ซึ่งพอดีกับสี่ในหกไฟล์ที่ลงชื่อเข้าใช้
ด้วย `U_EXT` ผ่านฟอร์ม คือรายการที่ย่อหน้าบนของไฟล์นี้เขียนไว้เอง

วิธีที่ถูกคือ **28 = 7 suite แม่ + 17 ใบย่อหน้า + 4 ใบระดับบนสุด** นับหัว suite ออก
อย่าเดาจากการย่อหน้าอย่างเดียว และอย่ารันทีละไฟล์ที่คิดว่าเกี่ยว — รันทั้งชุดแล้วอ่านชื่อ

ตัวที่สองไม่ขยับ และนั่นถูกแล้ว — แถวที่มันฆ่าสร้างบัญชีของตัวเองแล้วเลื่อนหน้าต่าง
ข้ามวันสิ้นสุด ไม่ได้พึ่ง seed **ฟิกซ์เจอร์ทำให้ข้อเรียกร้องที่ *ยังไม่มีใครค้ำ* แข็งขึ้น
ไม่ได้ทำให้ทุกอย่างแข็งขึ้น** และการเขียนตัวเลขที่ไม่ขยับลงไปด้วย คือสิ่งที่ทำให้อีก
ตัวหนึ่งเชื่อถือได้

## สองข้อที่เจ้าของข้อกำหนดตอบแล้ว เมื่อ 24 ก.ย. 2569

เกณฑ์ข้อ 1 กับข้อ 6 ของตั๋ว (ช่วงเวลาอยู่บน `user_roles` และสิทธิ์ที่หมดอายุไม่กระทบ
สิทธิ์อื่นของบัญชีเดียวกัน) เป็น**คำถามเดียวกันถามสองครั้ง** — หมดอายุแล้วสิ่งที่หมดคือ
*ตัวคน* หรือ *ใบมอบหมาย* คำตอบคือ **ตัวคน** ช่วงเวลาอยู่บน `users` ต่อไปตามที่ไมเกรชัน
0005 ตัดสินไว้ และเกณฑ์ข้อ 6 ถูกตอบว่า *ไม่* ไม่ใช่ถูกทิ้งว่าง

**คำตอบว่า *ไม่* ต้องมีแถวเหมือนคำตอบว่า *ใช่*** เดิมเกณฑ์ข้อนี้ไม่มีฟิกซ์เจอร์ให้ตกได้
เพราะไม่มีบัญชีไหนใน seed ถือ `EXT_ASSESSOR` ควบสิทธิ์อื่น และตั้งแต่ #87 ก็ไม่มีวันมี —
ผู้ประเมินภายนอกถูกนิยามด้วยการอยู่นอกสถาบัน ส่วนอาจารย์กับกรรมการอยู่ใน คนที่ถือทั้งสอง
อย่างคือคนที่มีไม่ได้ **รูปเดียวที่กรณีหลายสิทธิ์เป็นได้ที่นี่คือผู้ประเมินคนเดียวถือสองหลักสูตร**
ซึ่ง `users.test.js` สร้างเองในแถว *closes over every grant an account holds* และ
`windowyieldstoasecondgrant` เป็นตัวที่ทำให้แถวนั้นเป็นการพิสูจน์ ไม่ใช่การบรรยาย

ราคาของคำตอบนี้เขียนไว้ตรง ๆ: ผู้ประเมินที่ตรวจสองหลักสูตรซึ่งรอบจบคนละวัน ได้ช่วงเวลา
เดียวสำหรับทั้งสอง คนที่ต้องการสองวันสิ้นสุดออกบัญชีสองใบ ซึ่งเป็นสิ่งที่คำว่า *บัญชีชั่วคราว*
แปลว่าอยู่แล้ว

    python mutation/48-assessor-validity-window.py save
    python mutation/48-assessor-validity-window.py <mutant>
    python mutation/48-assessor-validity-window.py restore
"""

from harness import main

FILES = {
    "accounts": "backend/auth/accounts.js",
}

MUTANTS = {
    # บัญชีที่อยู่ *ใน* ช่วงเวลากลับถูกปฏิเสธ - เครื่องหมายกลับข้าง ไม่ใช่ด่านหายไป
    # นี่คือข้อเรียกร้องที่ฟิกซ์เจอร์ใน seed ทำให้พิสูจน์ได้ และเป็นเกณฑ์ข้อ 3 ของตั๋ว
    # (TC-AUTH-005) ก่อน #48 ไม่มีบัญชีไหนใน seed มี valid_from เลย ตัวนี้จึงจับได้
    # เฉพาะเทสต์ที่สร้างหน้าต่างขึ้นเอง
    #
    # ไม่ใช่รูป *ฆ่ามากเกินไป* ของ #97 เพราะมันแตะเฉพาะบัญชีที่มีช่วงเวลา ซึ่งมีสาม
    # บัญชีตั้งแต่ 24 ก.ย. 2569 ที่เหลือทั้งระบบทำงานปกติ
    #
    # ฆ่า 24 ข้อ ใน 8 สวีท (auth 4 · authorise 5 · shell 4 · users 7 ·
    # evidence 1 · offerings 1 · program-results 1 · program-results-students 1)
    # ก่อน seed มีช่วงเวลา ตัวเดียวกันนี้ฆ่า **6** ข้อ ใน 2 สวีท — วัดด้วยการถอด
    # `validity` ทั้งสองบรรทัดออกแล้วรันมัตแตนต์เดิมซ้ำ ข้อที่เจ็ดในรอบนั้นคือข้อที่
    # ตกเพราะฟิกซ์เจอร์หายไป ไม่ใช่เพราะมัตแตนต์ จึงไม่นับ
    #
    # **ฆ่า 24 การเสริมความแข็งแรงคือ 18** — #102 บันทึกไว้ว่าตัวเลขเดียวใน
    # รายงานซ่อนอันที่ผู้อ่านไม่ได้นึกถึงอยู่ ตัวเลขชุดนี้เป็นของ 24 ก.ย. 2569
    # ชุดของ 9 ก.ย. คือ 21 (authorise 4 · shell 3 · users 6) และหมดอายุเพราะ seed
    # โตขึ้น ไม่ใช่เพราะมัตแตนต์เปลี่ยน — ดูย่อหน้าใต้ตารางข้างบน
    "insidewindowisrefused": ("accounts",
        "  if (user.valid_from && now < calendarDay(user.valid_from)) {\n",
        "  if (user.valid_from && now >= calendarDay(user.valid_from)) {\n"),
    # เกณฑ์ข้อ 5: สิทธิ์ที่หมดอายุกลางทางต้องหยุดให้สิทธิ์ที่การตรวจครั้งถัดไป ไม่ใช่รอ
    # ให้ cookie หมดอายุ ด่านที่ `admit` ยังอยู่ครบ คนที่ลงชื่อเข้าใช้ใหม่จึงยังถูกปฏิเสธ
    # เหมือนเดิม - เปลี่ยนแค่ session ที่ถืออยู่แล้วข้ามเส้นวันสิ้นสุด
    #
    # สองบรรทัดนี้เขียนเหมือนกันเป๊ะทั้งใน `admit` และ `sessionAdmission` จุดยึดจึง
    # ต้องกินบรรทัดที่ตามมาด้วย ไม่งั้นมันชี้ได้สองที่
    #
    # ฆ่า 1 ข้อ คือข้อที่มันพูดถึงพอดี ไม่มีอะไรอื่นล้ม
    "sessionignoresthewindow": ("accounts",
        "  const outsideWindow = validityRefusal(user);\n"
        "  if (outsideWindow) return outsideWindow;\n"
        "  return null;\n"
        "}\n",
        "  return null;\n"
        "}\n"),
    # ปลายที่ *ปิด* ของหน้าต่าง ซึ่งเป็นการเปรียบเทียบที่ไม่เคยมีมัตแตนต์ตัวไหนแตะเลย
    # `insidewindowisrefused` พลิกปลายที่เปิด ส่วน `89:endsareswapped` เปลี่ยนแค่*ประโยค*
    # ของปลายนี้ ไม่ได้เปลี่ยนเงื่อนไข — แถวที่ยืนยันแค่ `403` จึงรอดมันไปได้ทั้งหมด
    # นี่คือกฎ *หนึ่งมัตแตนต์ต่อหนึ่งข้อต่อของด่าน* ของ #96 กับ #107 และเป็นครึ่งหนึ่งของ
    # เกณฑ์ข้อ 4 (TC-AUTH-006) ที่ยังไม่เคยถูกทำให้ล้ม
    #
    # `>` เป็น `<=` กลับข้างทั้งปลาย: บัญชีที่ยังอยู่ในหน้าต่างถูกปฏิเสธว่าหมดอายุ และบัญชีที่
    # หมดอายุแล้วเข้าได้ ไม่ใช่การลบด่าน — จำนวนคำปฏิเสธเท่าเดิม สลับว่าใครได้
    #
    # ฆ่า 21 ข้อ ใน 8 สวีท (auth 3 · authorise 5 · shell 3 · users 6 · evidence 1 ·
    # offerings 1 · program-results 1 · program-results-students 1)
    #
    # **ร่างแรกของบรรทัดนี้ทำนายไว้ 25 และวัดได้ 21** สิ่งที่น่าสนใจคือสามข้อที่มัน *ไม่* ฆ่า
    # ทั้งที่ `insidewindowisrefused` ฆ่า — และทั้งสามอธิบายตัวเอง:
    #
    # * สองข้อที่ลงชื่อเข้าใช้ด้วย `U_NONKMITL` (`auth` *password sign-in succeeds for an
    #   external assessor from outside KMITL* และ `shell` *takes effect at the next
    #   sign-in*) รอด เพราะบัญชีนั้น `valid_until` เป็น NULL เงื่อนไขทั้งก้อนจึงไม่ทำงาน
    # * `users` *is refused before the window opens, not only after it closes* รอด เพราะ
    #   บัญชีของมันถูกปฏิเสธที่ปลายอีกข้างซึ่งอยู่ก่อน โค้ดบรรทัดนี้ไม่เคยถูกเดินถึง
    #
    # **สองมัตแตนต์นี้จึงไม่ใช่สำเนากัน มันแยกปลายสองข้างของหน้าต่างออกจากกันได้จริง** —
    # ซึ่งเป็นสิ่งที่ #96 กับ #107 เรียกว่าหนึ่งมัตแตนต์ต่อหนึ่งข้อต่อ และเป็นเหตุผลที่บัญชี
    # เปิดข้างเดียวของเกณฑ์ข้อ 7 มีค่ามากกว่าการเป็นฟิกซ์เจอร์ของหน้าจอ
    "endcomparisonisreversed": ("accounts",
        "  if (user.valid_until && now > calendarDay(user.valid_until)) {\n",
        "  if (user.valid_until && now <= calendarDay(user.valid_until)) {\n"),
    # เกณฑ์ข้อ 6 ทำแบบตรงตัวที่สุดเท่าที่ทำได้โดยไม่ย้ายคอลัมน์ ช่วงเวลายังปฏิเสธเหมือนเดิม
    # ทุกประการ *จนกว่า* บัญชีจะถือสิทธิ์ใบที่สอง แล้วมันก็ยอมให้ผ่าน นี่คือข้อเสนอของตั๋ว
    # ในรูปที่รันได้ และเป็นตัวที่บอกว่าคำตอบ *ไม่* ทำแบบนั้นถูกค้ำด้วยโค้ด ไม่ใช่ด้วย
    # ร้อยแก้วบนใบรับ
    #
    # ทุกแถวเรื่องช่วงเวลาที่มีอยู่เดิมใช้บัญชีสิทธิ์ใบเดียวทั้งหมด (`U_EXT`
    # `U_EXT_CLOSED` `U_NONKMITL` และทุกบัญชีที่ `users.test.js` สร้างเอง) มัตแตนต์ตัวนี้
    # จึงเห็นแถวเดียว ซึ่งเป็นสิ่งที่ #97 เรียกว่า *ล้มแถวหนึ่งโดยปล่อยที่เหลือยืน*
    #
    # ฆ่า 1 ข้อ คือ *closes over every grant an account holds*
    "windowyieldstoasecondgrant": ("accounts",
        "  const outsideWindow = validityRefusal(user);\n"
        "  if (outsideWindow) return outsideWindow;\n"
        "\n"
        "  const roles = await allRoles(pool, user.user_id);\n"
        "  if (roles.length === 0) return refuse(403, 'noRole');\n",
        "  const outsideWindow = validityRefusal(user);\n"
        "\n"
        "  const roles = await allRoles(pool, user.user_id);\n"
        "  if (roles.length === 0) return refuse(403, 'noRole');\n"
        "  if (outsideWindow && roles.length < 2) return outsideWindow;\n"),
}

main(FILES, MUTANTS)
