# -*- coding: utf-8 -*-
"""#133 - อีกหกจุดที่คำตอบถูกแทนที่ได้ด้วยตัวควบคุมบนจอตัวเอง

#68 ปิดเรื่องนี้บนสิบห้าแผงที่ทำ paging แล้วเลื่อนครึ่งหลังของ grep ที่ตัวเองเขียนไว้ออกไป
ครึ่งที่เหลือคือสิบหกจุดที่ยังอ่านคำตอบด้วยรูป `setSomething(await getSomething(…))` โดยไม่มี
อะไรผูกคำตอบเข้ากับคำขอที่ทำให้มันเกิด — **ใบที่มาถึงทีหลังชนะ ไม่ใช่ใบที่ขอทีหลัง**

**สิบหกคือสิ่งที่ grep นั้นจับได้ ไม่ใช่จำนวนจุด — จำนวนที่วัดได้คือยี่สิบสอง** อีกหกจุด
ตั้งชื่อให้คำตอบก่อนแล้วค่อยวาด (`Plos` · `GradingWeights` · `ContinuousImprovement` ·
`TeacherSection` · `GrantsPanel` และ `loadList` ของ `ActivityScores` เอง) รูป
`set…(await …)` จึงไม่มีทางแตะถึง สิ่งที่หามันเจอคือการไล่ `await` ทุกตัวในต้นไม้แล้ว
อ่านว่าแต่ละตัวทำอะไรกับคำตอบ ไม่ใช่การขัดแพตเทิร์นให้ดีขึ้น — *grep เป็นหลักฐานของ
แพตเทิร์นที่พิมพ์ ไม่ใช่ของข้ออ้างที่ต้องการ* (#111) และ*ตั๋วที่ระบุว่ามันเปลี่ยนกี่ที่ ได้ประกาศ
ข้ออ้างที่ grep หนึ่งนาทีตรวจได้* (#55)

`TeacherDashboard` อยู่ในรายการที่ไล่นั้นและ**ไม่นับ**เป็นหนึ่งในยี่สิบสอง เพราะมันถามโดย
ไม่มีพารามิเตอร์เลย จึงไม่มีคำขอที่สองให้คำขอที่หนึ่งแพ้ได้ — *เขียนสิ่งที่เครื่องมือวัดไม่ได้
เอาไว้* (#132) ถูกกว่าปล่อยให้คนอ่านคนถัดไปวัดใหม่

**ยี่สิบสองจุดไม่ใช่ตระกูลเดียวกัน** สิ่งที่ต่างคือ*อะไรแทนคำขอได้* สิบหกจุดถามด้วย
`useParams` อย่างเดียว หรือด้วย prop ที่จอข้างบนเปลี่ยนได้ด้วยการถอดมันทิ้งเท่านั้น
การแทนจึงต้องเป็นการเดินจาก CLO หรือกิจกรรมหนึ่งไปอีกอันบนเส้นทางเดิม
โดยคอมโพเนนต์ไม่ถูกถอด ซึ่งทุกลิงก์บนจอเหล่านั้นเดิน*ขึ้น*ไปหารายการที่ถอดมันทิ้ง จุดพวกนั้น
จึงเป็น *ยังไม่ได้ทดสอบ* บนใบตรวจรับ พร้อมเหตุผลและวันที่ ไม่ใช่ *ไม่ต้องมี*

**อีกหกจุดมีตัวควบคุมอยู่บนจอเดียวกัน และหกจุดนี้คือแฟ้มนี้** หนึ่งมัตแตนต์ต่อหนึ่งจุด
ตามที่ตั๋วตั้งราคาไว้ว่า *แถวต่อจุด ไม่ใช่บรรทัดต่อจุด*

## ทำไมหกจุด ทั้งที่ตั๋วเขียนว่าสาม

ตั๋วจัด `ActivityScores` ไว้ในกลุ่ม `useParams` ซึ่ง**ผิด** — มีแต่ `sectionId` ที่มาจากเส้นทาง
(ADR-0004) ส่วน `activityId` เป็น state ที่ `<select>` บนจอเป็นคนตั้ง ทั้งสองจุดของไฟล์นั้น
จึงเป็นตระกูลเดียวกับ #68 และจุดที่สองคือ**คำตอบของการบันทึก** ซึ่งวาดเหมือนการอ่านทุกประการ
เพราะ `screenOf` ตอบทั้งจอกลับมา *ตั๋วที่ระบุว่ามันเปลี่ยนกี่ที่ ได้ประกาศข้ออ้างที่ grep
หนึ่งนาทีตรวจได้* (#55) — และการตรวจคืนคำตอบว่าห้า ไม่ใช่สาม

**จุดที่หกคือ `Plos.js` ซึ่ง grep ของตั๋วแตะไม่ถึง** มันอ่าน `const { plos: rows } = await
listPlos(…)` แยกคำตอบออกมาก่อนวาด และมันมีตัวกรองหลักสูตรอยู่เหนือตาราง ซึ่งเป็นตัวควบคุม
เดียวกับที่ `PloMapping` มี วาดให้เฉพาะบัญชีที่ดูแลมากกว่าหนึ่งหลักสูตรเท่านั้น อีกสองจอที่
grep เดียวกันแตะไม่ถึง — `GradingWeights.js` กับ `ContinuousImprovement.js` — อ่าน
`sectionId` จาก `useParams` อย่างเดียว จึงได้ธงและได้ *ยังไม่ได้ทดสอบ* บนใบของตัวเอง
ไม่ได้มัตแตนต์ อีกสามจุดที่การไล่ `await` หาเจอทีหลัง — `TeacherSection.js` ·
`GrantsPanel.js` และ `loadList` ของ `ActivityScores` เอง — ก็อยู่ในกลุ่มหลังด้วย
เหตุผลเดียวกัน *อย่าเลื่อนครึ่งหลังของประโยคที่ตัวเองกำลังปิด* (#119)

## สำมะโนไฟล์

ใบนี้ถือห้าไฟล์ และสี่ในห้าเป็น **path ที่ชนอันใหม่** — `PloMapping.js` (`20` ถืออยู่ใบเดียว)
`Plos.js` (`19`) `ProgramLevelIndividual.js` (`45`) และ `ActivityScores.js` (`34`) ส่วน
`ProgramLevelByIntake.js` ขึ้นจากคู่ (`35` · `42`) เป็นกลุ่มสาม สำมะโนทั้งสโตร์ขยับจาก 34
เป็น 38 path และรูปร่างจาก 17/7/6/3/1 เป็น **20/8/6/3/1** วัดด้วยสคริปต์ใน
`mutation/README.md` เมื่อ 16 ก.ย. 2569 — *ตัวเลขที่ตัดสินคือกลุ่ม ไม่ใช่ยอดรวม* (#125)
**ห้ามกวาดใบนี้พร้อมกับ `19` · `20` · `34` · `35` · `42` · `45`**

**และ #133 ทำให้จุดยึดของใบอื่นหลุดเก้าตัว ซึ่งเป็นราคาที่การแก้แบบนี้ต้องจ่าย**
`28:swallowrefusal` · `29:swallowrefusal` · `30:swallowrefusal` · `31:swallowrefusal` ·
`32:swallowrefusal` ยึดกับบรรทัดใน `catch` ที่ตอนนี้ย้ายเข้าไปอยู่ใน `if (isCurrent())`
ลึกอีกขั้นหนึ่ง (`30` คือใบของ `GradingWeights.js` ซึ่งเป็นหนึ่งในสามจอที่ grep ของตั๋ว
แตะไม่ถึง — ตัวมันเองไม่ได้มัตแตนต์ใหม่ แต่ธงที่ใส่ให้มันขยับจุดยึดของใบ `30`)
`39:refusalkeepsloading` กับ `40:refusalkeepsloading` ยึดกับ `setLoading(false)` ใน `finally`
ที่ตอนนี้อยู่หลังธง และ `42:drilldownwontclose` กับ `45:drilldownwillnotclose` ยึดกับสาขาที่ปิด
แผงใน `toggle` ซึ่ง #133 ย้ายการดึงข้อมูลออกไปเป็น effect แล้วเหลือ `toggle` เป็นการเปลี่ยน
state อย่างเดียว ทั้งเก้าตัวเล็งใหม่ที่**ข้ออ้างเดิม** ไม่ใช่ข้ออ้างใหม่ และต้องกวาดซ้ำกับ
ใบของตัวเองก่อนจึงเชื่อได้ — *การตรวจจุดยึดบอกว่ามัตแตนต์ไม่เข้ากับโค้ดแล้ว ไม่เคยบอกว่า
มันยังพิสูจน์อะไรได้อยู่* (#107) — ผลการกวาดซ้ำนั้นอยู่ท้ายไฟล์นี้
(`40` ได้ของแถมตอนเล็งใหม่: จุดยึดเดิมกินคอมเมนต์สามบรรทัดเข้าไปด้วย
ตอนนี้เหลือเฉพาะบรรทัดที่ข้ออ้างพูดถึง — *anchor to the code a mutant breaks and never to the
comment beside it* (#101))

## เจ็ดตัว หกจุด และคนละแถวกัน

`mappingstalewins` · `plosstalewins` · `intakedrillstalewins` · `studentdrillstalewins` ·
`scoresstalewins` คือข้อบกพร่องเดิมที่จุดละที่
ส่วน `savedrawsafterthemove` เป็นคนละรูป: `save` ไม่มีใครถอด
มันจึงถามคำถามที่ต่างออกไป — *ตัวเลือกยังอยู่ที่เดิมกับตอนที่ผมถูกส่งไปไหม* ซึ่งเป็นคำถามเรื่อง
**ตอนนี้** ที่ ref ตอบได้ และ state ใน closure ตอบไม่ได้ ถอดเงื่อนไขนั้นออกแล้วคำตอบของการบันทึก
จะวาดทับกิจกรรมที่ครูย้ายไปแล้ว พร้อมช่องกรอกที่แก้ได้ทุกช่อง

ตัวที่เจ็ด `ploshandlerstalewins` เป็นรูปเดียวกับ `savedrawsafterthemove` แต่คนละจอ และมันมี
อยู่เพราะ**ผู้เรียกที่สอง** ซึ่งเป็นประโยคสุดท้ายของตั๋วเอง `Plos.js` โหลดรายการใหม่จาก handler
สามตัว (บันทึก · ลบ · แก้ไขที่ถูกปฏิเสธ) และไม่มีอะไรถอด handler ทิ้ง ธงของ effect จึงรับใช้
มันไม่ได้ รอบแรกของตั๋วนี้อ่านผู้เรียกที่สองเพื่อดู**รูปของธง** — ต้องใส่หรือมีค่าตั้งต้น — แล้วเขียน
คอมเมนต์ว่าสำหรับ handler คำตอบที่ซื่อสัตย์ของ *ยังเป็นปัจจุบันอยู่ไหม* คือใช่ ซึ่งบนสองจอที่มี
ตัวควบคุมอยู่ข้าง handler เป็นคำตอบที่ผิด — *ธงที่เขียนให้ผู้เรียกคนเดียว คือข้ออ้างเกี่ยวกับ
ผู้เรียกทุกคน* (#68) และคอมเมนต์นั้นคือข้ออ้าง

**และมันไม่ซ้ำกับ `plosstalewins`** ตัวนั้นถอดการตรวจธงออกจาก `load` ซึ่งเป็นบรรทัดเดียว
ที่รับใช้ผู้เรียกสองคน ตัวนี้ถอดเฉพาะสิ่งที่ handler ส่งเข้าไป effect จึงยังมีธงครบ และสิ่งที่
เสียไปคือคำตอบของการโหลดใหม่หลังบันทึกเท่านั้น ผลที่วัดได้ของทั้งสองตัวอยู่ในตารางข้างล่าง
และมันต่างกันจริง

ด้านที่ไม่ได้เขียนเป็นมัตแตนต์คือ cleanup ของ effect (`current = false`) ถอดออกแล้วได้ผลเท่ากับ
ถอดธงทุกตัวพร้อมกัน ตามเหตุผลเดียวกับ #129 และ #68 อีกสองจุดที่ `/code-review` รอบสองยกมา
ก็ไม่มีมัตแตนต์ ด้วยคนละเหตุผลกัน: `Plos.openEditor` เข้าถึงอันตรายเดียวกันได้ทางที่สอง
แต่เป็นหนึ่งในตระกูลแปดจุดที่เลื่อนไปเป็นตั๋วของตัวเอง **#139** (เขียนไว้ที่คอมเมนต์
ของมันเองและที่ `docs/lessons.md` พร้อมเลขบรรทัด) — **และ #139 วัดแล้วว่าไม่ใช่อันตรายเดียวกัน**
ฟอร์มพูดความจริงเกี่ยวกับหลักสูตรของตัวเอง ทางเข้าจริงของมันคือ แก้ไข เพิ่ม และลบ ไม่ใช่ตัวกรอง
(`mutation/139-superseded-row-detail.py`) ส่วน `PloMapping.choose` **โครงสร้างเข้าไม่ถึง** — เซลล์
ที่ค้างพก `outcome_id` ของหลักสูตรที่จากมา และทุกช่องวาดจาก `grid.outcomes` ของหลักสูตร
ที่อยู่บนจอ จึงไม่มีช่องไหนเปิดหามันเจอ เขียนไว้ที่คอมเมนต์ของมันพร้อมวันหมดอายุ ส่วน `onImported` ของ `ActivityScores`
ได้ธงเดียวกันแต่**ไม่ได้มัตแตนต์** เพราะไม่มีแถวไหนสร้างสถานการณ์ของมันได้โดยไม่เขียนคะแนน
ของทั้งห้อง ซึ่งเป็นสิ่งที่แถวของใบ `34` เป็นเจ้าของ — *ยังไม่ได้ทดสอบ* บนใบนั้น ไม่ใช่ *ไม่ต้องมี*

## การกวาด

`cd e2e && E2E_FRONTEND_PORT=5300 npx playwright test` (389 แถว) กวาดเมื่อ 16 ก.ย. 2569
ตารางนี้บันทึก **สิ่งที่มัตแตนต์ฆ่าได้จริง** ไม่ใช่สิ่งที่ควรจะฆ่าได้

| มัตแตนต์ | แถวที่มันค้ำ | ตายที่ |
|---|---|---|
| `mappingstalewins` | ตารางของ #20 เป็นของหลักสูตรที่ `<select>` เลือกอยู่ | แถวที่หนึ่ง (`133a:160`) ที่ `toEqual` — `chosen: "0503"` แต่ `codes` เป็นสิบสามรหัส `PLO-1`…`PLO-13` ของ `0501` ที่ควรเป็นสองรหัสของ `0503` |
| `plosstalewins` | รายการของ #19 เป็นของหลักสูตรที่ตัวกรองเลือกอยู่ | แถวที่สอง (`133a:222`) ที่ `toEqual` — `chosen: "0503"` แต่ `codes` มีห้าสิบสองรหัสของ `0501` ที่ควรเป็นสี่รหัสของ `0503` |
| `intakedrillstalewins` | แผง *ที่มา* ของรายงานรุ่นเป็นของข้อที่กดล่าสุด | แถวที่สาม (`133a:285`) ที่ `toBe` ของหัวแผง — ได้ *ที่มาของ PLO-2 …* ที่ควรเป็น *ที่มาของ PLO-1 …* |
| `studentdrillstalewins` | แผง *ที่มา* ของรายงานรายคนเป็นของคนที่อยู่บนจอ | แถวที่สี่ (`133a:341`) ที่ `toBe` ของหัวแผง — ได้ `… — 66010001 ธัญญาเรศ พูนผล` ที่ควรเป็น `… — 66010002 ชนาธิป สมบูรณ์` |
| `scoresstalewins` | ตารางคะแนนเป็นของกิจกรรมที่ `<select>` เลือกอยู่ | แถวที่ห้า (`133a:398`) ที่ `toEqual` — `chosen: "3"` แต่ `heading` เป็น *โครงงานย่อยที่ 2 — การสืบทอดและพอลิมอร์ฟิซึม* ที่ควรเป็น *โครงงานปลายภาค* |
| `savedrawsafterthemove` | คำตอบของการบันทึกไม่วาดทับกิจกรรมที่ย้ายไปแล้ว | แถวที่หก (`133a:454`) ที่ `toEqual` — รูปเดียวกัน `{chosen, heading}` โดยคำตอบที่วาดทับมาจาก `saveScores` ไม่ใช่จากการอ่าน |
| `ploshandlerstalewins` | รายการที่การบันทึกสั่งโหลดใหม่ ไม่วาดทับหลักสูตรที่ตัวกรองย้ายไปแล้ว | แถวที่เจ็ด (`133a:511`) ที่ `toEqual` — `chosen: "0503"` แต่ `codes` เป็นห้าสิบสองรหัสของ `0501` ที่ควรเป็นสี่รหัสของ `0503` |

**หกตัวแรกฆ่าแถวของตัวเองแถวเดียว — 1 ล้ม 388 ผ่าน ทุกรอบ** ไม่มีรอบไหนที่แถวของใบอื่นขยับ
ซึ่งเป็นสิ่งที่มัตแตนต์ต้องทำได้ตามกฎ: *มัตแตนต์ต้องทำให้แถวหนึ่งล้มโดยแถวที่เหลือยังยืนอยู่ได้*

รอบสะอาดของหกตัวแรก: **389 ผ่านทั้ง 389 (19.2 นาที)** — ธงที่ใส่ให้ยี่สิบไฟล์
ไม่ทำให้แถวไหนล้ม ซึ่งเป็นสิ่งที่*การกวาดมองไม่เห็น มีแต่รอบสะอาดที่เห็น* (#83)

**รอบแรกของ `scoresstalewins` นับได้สองแถว และแถวที่สองไม่ใช่ของมัน** `32a-activity-list.spec.js:86`
ล้มไปด้วยในรอบนั้น ทั้งที่มัตแตนต์แก้แต่ `ActivityScores.js` ส่วนแถวนั้นอ่าน `LearningActivities.js`
รัน `32a` ทั้งไฟล์พร้อมมัตแตนต์ตัวเดิมได้ **9 ผ่านทั้ง 9 (37.1 วินาที)** กลไกอยู่ที่
`groupsOnScreen` ใน `e2e/support/activities-screen.js` ซึ่งอ่าน `allTextContents()` — ไม่ลองใหม่ —
หลัง `waitForActivities` ซึ่งรอ*คำตอบ* ไม่ใช่*การวาด* ค่าที่ได้คือ `Array []` ตรงตามชนิดที่ #132
ตั้งชื่อไว้ **เหตุผลไม่ใช่การวัด** จึงรันชุดเต็มซ้ำด้วยมัตแตนต์ตัวเดิม แล้วได้ 1 ล้ม 388 ผ่าน
ตารางข้างบนบันทึกรอบที่สอง และบรรทัดนี้บันทึกว่ารอบแรกอ่านได้อีกอย่าง

ไม่มีมัตแตนต์ตัวไหนทำให้เกิด `error:` หรือ unhandled rejection ฝั่งเซิร์ฟเวอร์ — ตรวจแล้ว
ทั้งเจ็ดไฟล์บันทึกการรัน (รอบสะอาดหนึ่ง มัตแตนต์หก)

## ตัวที่เจ็ด และตัวเลขของ `plosstalewins` ที่เปลี่ยนไป (16 ก.ย. 2569)

แถวที่เจ็ดเข้ามาทีหลัง ชุดจึงเป็น 390 แถว และต้องวัดใหม่ทั้งรอบสะอาดและมัตแตนต์ที่เกี่ยวข้อง

| รอบ | ผล | ตัวที่ล้ม |
|---|---|---|
| รอบสะอาด | **390 ผ่านทั้ง 390 (19.4 นาที)** | ไม่มี — และไม่มี `error:` หรือ unhandled rejection ฝั่งเซิร์ฟเวอร์ |
| รอบสะอาด หลังรีวิวรอบสอง | **390 ผ่านทั้ง 390 (19.2 นาที)** | ไม่มี — รอบนี้คือต้นไม้ที่ commit จริง แก้เฉพาะคอมเมนต์กับเอกสาร |
| `ploshandlerstalewins` | **1 ล้ม 389 ผ่าน (19.2 นาที)** | แถวที่เจ็ดตัวเดียว |
| `plosstalewins` วัดใหม่ | **2 ล้ม 388 ผ่าน (19.1 นาที)** | แถวที่สอง**และ**แถวที่เจ็ด |

**เลขสองของ `plosstalewins` ไม่ใช่การฆ่าเกิน แต่คือสิ่งที่วัดได้** มันถอดการตรวจธงออกจาก
`load` ซึ่งเป็นบรรทัดเดียวที่ผู้เรียกทั้งสองคนวิ่งผ่าน ทั้ง effect และ handler จึงเสียธงพร้อมกัน
ส่วนตัวที่เจ็ดถอดเฉพาะสิ่งที่ handler ส่งเข้าไป effect จึงยังมีธงครบ **ตัวเลขที่ต่างกันคือสิ่งที่
บอกว่าสองตัวนี้ไม่ใช่ตัวเดียวกัน** และแถวที่เจ็ดถูกค้ำด้วยตัวหลัง ไม่ใช่ตัวแรก ส่วนเลขเดิมของ
`plosstalewins` (1 ล้ม 388 ผ่าน) เป็นเลขของชุดที่ยังไม่มีแถวที่เจ็ด — *รายงานเลขที่วัดได้
ไม่ใช่เลขของตั๋ว* (#102) ซึ่งกินความถึงเลขของตัวเองเมื่อชั่วโมงก่อนด้วย

ทั้งสองรอบ `restore` แล้วไฟล์ทั้งห้ากลับมาเท่าเดิมทุกไบต์ ตรวจด้วย sha1 ก่อน `save` และหลัง
`restore` เพราะ `git status` บอกอะไรไม่ได้ในเมื่อทั้งห้าไฟล์ถูกแก้ค้างไว้ทั้งตั๋วนี้ — กฎคือ
*อ่าน `git status` หลัง `apply` พอๆ กับหลัง `restore`* (#126) และสิ่งที่อ่านได้ในที่นี้คือ hash

## ผลกวาดซ้ำของเก้าจุดยึดที่เล็งใหม่ (16 ก.ย. 2569)

เล็งใหม่แล้วยังต้องพิสูจน์ว่ามันวัดอะไรได้อยู่ ทั้งเก้าตัวจึงถูกใส่ทีละตัว แล้วรัน**ใบของตัวเอง**
ทีละใบ (`npx playwright test tests/<ใบ>`) แล้ว `restore` พร้อมตรวจ `git status` ทุกครั้ง

| มัตแตนต์ | ใบที่รัน | แถวที่ตาย | ผลรวมของใบ |
|---|---|---|---|
| `28:swallowrefusal` | `28a` | แถว 7 *a section that is somebody else's hides the behaviours too* · แถว 8 | 2 ล้ม 6 ผ่าน |
| `29:swallowrefusal` | `29a` | แถว 7 *… hides the criteria too* · แถว 8 | 2 ล้ม 6 ผ่าน |
| `30:swallowrefusal` | `30a` | แถว 10 *… hides the scheme too* | 1 ล้ม 9 ผ่าน |
| `31:swallowrefusal` | `31a` | แถว 7 *… hides the plan too* | 1 ล้ม 7 ผ่าน |
| `32:swallowrefusal` | `32a` | แถว 8 *… hides the work too* | 1 ล้ม 8 ผ่าน |
| `39:refusalkeepsloading` | `39a` | แถว 9 *the ตอนเรียน of another account is refused rather than drawn* | 1 ล้ม 8 ผ่าน |
| `40:refusalkeepsloading` | `40a` | แถว 8 *… refused rather than reported* | 1 ล้ม 8 ผ่าน |
| `42:drilldownwontclose` | `42a` | *selecting an outcome opens what is behind its figure, and closing puts it away* | 1 ล้ม 5 ผ่าน |
| `45:drilldownwillnotclose` | `45a` | *opening an outcome shows what this student was marked on, and closing puts it away* | 1 ล้ม 6 ผ่าน |

**ทั้งเก้าฆ่าแถวเดิมของตัวเอง ไม่ใช่แถวอื่น** — แถวที่ตายตรงกับที่ไฟล์มัตแตนต์ของแต่ละใบ
เขียนอ้างไว้ทุกตัว (`28`/`29` อ้างว่าฆ่าแถว 7 แล้วแถว 8 ตายตามเพราะเป็นการกลืนคำปฏิเสธ
เดียวกันคนละประโยค ซึ่งเป็นสิ่งที่วัดได้จริงทั้งสองใบ) การเล็งใหม่จึงคืนข้ออ้างเดิม
ไม่ได้ย้ายข้ออ้าง — *การตรวจจุดยึดบอกว่ามัตแตนต์ไม่เข้ากับโค้ดแล้ว ไม่เคยบอกว่ามันยัง
พิสูจน์อะไรได้อยู่* (#107) และคำตอบของรอบนี้คือ ยังพิสูจน์ได้ครบเก้า
"""
from harness import main

FILES = {
    "mapping": "frontend/src/pages/PloMapping.js",
    "plos": "frontend/src/pages/Plos.js",
    "intake": "frontend/src/pages/ProgramLevelByIntake.js",
    "student": "frontend/src/pages/ProgramLevelIndividual.js",
    "scores": "frontend/src/pages/ActivityScores.js",
}

MUTANTS = {
    # ตารางของหลักสูตรที่ตัวเลือกย้ายไปแล้ว วาดทับตารางของหลักสูตรที่กำลังดูอยู่
    "mappingstalewins": ("mapping",
        "      if (isCurrent()) setGrid(answer)\n",
        "      setGrid(answer)\n"),
    # รายการของหลักสูตรที่ตัวกรองย้ายไปแล้ว วาดทับรายการของหลักสูตรที่กำลังดูอยู่
    "plosstalewins": ("plos",
        "      if (isCurrent()) setPlos(rows)\n",
        "      setPlos(rows)\n"),
    # แผงที่มาของ PLO ที่ถูกกดแทนที่ไปแล้ว มาถึงทีหลังแล้วชนะ
    "intakedrillstalewins": ("intake",
        "      if (isCurrent()) setDrill(answer)\n",
        "      setDrill(answer)\n"),
    # เหมือนกัน แต่ชื่อที่ผิดคือชื่อนักศึกษา ซึ่งเป็นความผิดพลาดที่รายงานรายบุคคลรับไม่ได้
    "studentdrillstalewins": ("student",
        "      if (isCurrent()) setDrill(answer)\n",
        "      setDrill(answer)\n"),
    # คะแนนของกิจกรรมที่ครูย้ายไปแล้ว วาดอยู่ใต้ชื่อของกิจกรรมที่กำลังกรอก
    "scoresstalewins": ("scores",
        "      if (isCurrent()) setData(answer)\n",
        "      setData(answer)\n"),
    # คำตอบของการบันทึกก็เป็นการอ่าน และมันมาถึงหลังจากที่ตัวเลือกย้ายไปแล้วได้เหมือนกัน
    "savedrawsafterthemove": ("scores",
        "      if (onScreen.current === asked) setData(answer)\n",
        "      setData(answer)\n"),
    # ผู้เรียกที่สอง: รายการที่การบันทึกสั่งโหลดใหม่ วาดทับหลักสูตรที่ตัวกรองย้ายไปแล้ว
    # ยึดกับบรรทัด setNotice ข้างบนด้วย เพราะ `await load(…)` บรรทัดเดียวกันนี้มีสามที่
    "ploshandlerstalewins": ("plos",
        "      setNotice({ error: false, message: 'บันทึกข้อมูลเรียบร้อยแล้ว' })\n"
        "      await load(() => onScreen.current === program)\n",
        "      setNotice({ error: false, message: 'บันทึกข้อมูลเรียบร้อยแล้ว' })\n"
        "      await load(() => true)\n"),
}

main(FILES, MUTANTS)
