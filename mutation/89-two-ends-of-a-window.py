# -*- coding: utf-8 -*-
"""#89 - ปลายสองข้างของช่วงเวลา ต้องการคนละการกระทำ

บัญชีที่ยังไม่ถึงวันเริ่มใช้งาน กับบัญชีที่หมดช่วงเวลาไปแล้ว เคยได้ประโยคปฏิเสธ
เดียวกันทุกตัวอักษร คือ *บัญชีนี้อยู่นอกช่วงเวลาที่กำหนดให้ใช้งาน* และตั๋วเปิดไว้
เป็น**เรื่องที่ควรตัดสิน ไม่ใช่ข้อบกพร่อง** ด้วยเหตุผลที่ตรวจสอบได้: แถว
*ยังไม่ถึงวันเริ่ม ก็เข้าไม่ได้* ใน `docs/acceptance/11-user-accounts.md` เขียนสั่งไว้เอง
ว่า "ถูกปฏิเสธด้วยข้อความเดียวกัน" ระบบทำถูกตามที่ถูกสั่งทุกตัวอักษร
**ข้อบกพร่องอยู่ในใบตรวจรับ ไม่ได้อยู่ในโค้ด** ใบจึงถูกแก้ก่อน แล้วโค้ดจึงตามไป

แกนที่แยกสองประโยคออกจากกันคือ **สิ่งที่คนที่เจอต้องทำต่อ**

  ยังไม่ถึงวันเริ่ม -> รอ แล้วเข้าใหม่เมื่อถึงกำหนด
  หมดช่วงเวลาแล้ว  -> ติดต่อผู้ดูแลเพื่อขอต่ออายุ รอไปก็ไม่มีอะไรเปลี่ยน

ผู้ประเมินภายนอกเป็นคนนอกสถาบัน เดาเองไม่ได้ และไม่รู้ว่าควรถามใคร ประโยคเดียว
ที่ครอบคลุมทั้งสองสถานะ จึงไม่ได้บอกอะไรเขาสักอย่าง

ที่ค้านคือการบอกรายละเอียดของบัญชีก่อนพิสูจน์ตัวตนสำเร็จเป็นการรั่วข้อมูล - แต่
ประโยคเดิมก็ยืนยันอยู่แล้วว่าบัญชีนี้มีอยู่จริงและรหัสผ่านถูกต้อง การบอกเพิ่มว่าอยู่
ก่อนหรือหลังหน้าต่าง ไม่ได้เปิดเผยอะไรที่ยังไม่ถูกเปิดเผย

## ทำไมประโยคไม่บอกวันที่

ใบเสนอไว้ว่า "พิจารณาให้ข้อความฝั่ง *ยังไม่ถึงวันเริ่ม* บอกวันที่เริ่มไปเลย เพราะเป็น
วันที่คนคนนั้นรออยู่" ซึ่งฟังดูถูก และทำไม่ได้ - นี่เป็นสิ่งที่วัดแล้ว ไม่ได้เถียงเอา

กุญแจทั้งสอง (และ `outsideValidity` ก่อนหน้านี้) อยู่ใน `GOOGLE_REFUSAL_REASONS` ซึ่งเดินทางเป็น
`/login?error=<reason>` **ไม่มี body** และ `frontend/src/pages/Login.js` เก็บสำเนา
ถ้อยคำของตัวเองไว้ โดยมี reason เป็นกุญแจ ประโยคที่เอ่ยถึงค่าจึงไม่มีทางข้ามเส้นทางนั้นไป
ยิ่งกว่านั้น `refuse()` ใน `backend/auth/accounts.js`เขียนว่า `message: REFUSALS[reason]` - เป็น
`REFUSALS[` แบบเดียวกับที่ #125 เพิ่งแก้ที่ route สองเส้นเมื่อวาน ยื่นฟังก์ชันให้
`JSON.stringify` ทิ้ง

ทางเลือกที่เหลือคือ ให้ประตูที่มี body บอกวันที่ ส่วนประตูที่เป็น redirect ไม่บอก - ซึ่ง
**แย่กว่าการไม่บอกทั้งคู่** สถานะเดียวของบัญชีเดียว ตอบคนละอย่างตามประตูที่เข้ามา คือ
รูปของ #66 (*แถวที่บอกทางเข้าสองทาง คือสองแถว*) ในรูปของคำปฏิเสธ ถ้าเจ้าของ
ข้อกำหนดยังอยากได้วันที่ นั่นเป็นตั๋วของตัวเอง (#128) เพราะ redirect ต้องแบกวันที่เป็นพารามิเตอร์
ที่สอง ซึ่งคือข้อมูลของบัญชีใน URL

## สิ่งที่มัตแตนต์ในใบนี้พูดไม่ได้

`sessionAdmission` กับ `admit` เรียก `validityRefusal` ตัวเดียวกัน มัตแตนต์ที่ทำให้
สองเส้นนี้ตอบต่างกันจึงเขียนไม่ได้ - ไม่ใช่ช่องโหว่ แต่เป็นคำตอบข้อสองในสามข้อของ
#101 (*เอื้อมไม่ถึงโดยโครงสร้าง*) แถวที่ถือ cookie ข้ามปลายหน้าต่างยังคุ้มที่จะมี
เพราะมันเป็นคำกล่าวว่าสำเนาที่สองนั้น**เรียกตัวเดียวกันจริง** ซึ่งการแก้ครั้งหลังแยกออกจากกันได้

## หมายเหตุการกวาด

**ห้ามกวาดคู่กับ `50-sign-in-screens.py`, `66-sign-in-landing.py`,
`85-refusal-that-waits.py` และ `11-12-accounts-and-grants.py`** สามใบแรกถือ
`frontend/src/pages/Login.js` ใบสุดท้ายถือ `backend/auth/accounts.js`

รายการนี้สร้างจากการอ่าน `FILES` ด้วย AST ไม่ใช่จากการอ่านว่าใบไหนพูดถึงอะไร
ซึ่งเป็นความผิดพลาดที่ #125 ทำแล้วในใบของมันเอง - และร่างแรกของใบนี้
ก็ทำซ้ำ โดยเขียนไว้แค่สองใบ **`/code-review` จับได้** และผู้ตรวจเอง
ก็เสนอชื่อ `124` กับ `125` เข้ามาด้วย เพราะรัน `grep -l` บนทั้งไฟล์
ไม่ใช่บน `FILES` - `124` เอ่ยถึง `accounts.js` ในคำอธิบายเท่านั้น ส่วน `125`
ไม่ได้เอ่ยถึงเลย **รายการที่ยาวเกินไป กับรายการที่สั้นเกินไป มาจาก
การถามคนละคำถาม** และคำถามที่ถูกคือ *ใบไหนมี path นี้ใน `FILES`*

`50:googlerefusalmissesone` เคยยึดกับบรรทัด `outsideValidity` ใน `Login.js` และ
ตั๋วนี้ลบบรรทัดนั้นทิ้ง `mutation/anchors.py` ฟ้องทันที มันถูกเล็งใหม่ไปที่
`validityEnded` ซึ่งเป็นกุญแจตัวเดิมในชื่อใหม่ - เป็นการเล็งใหม่ ไม่ใช่การเอาไปใช้กับ
เรื่องที่มันไม่ได้ถูกเขียนถึง (#121)

## การกวาด

กวาดวันที่ 9 ก.ย. 2569 บนต้นไม้ที่สะอาด `cd backend && node --test "test/*.test.js"`
เขียว 729 ข้อ และ `cd e2e && E2E_FRONTEND_PORT=5300 npx playwright test 50a` เขียว 14 แถว
ตัวเลขข้างล่างนี้เป็นตัวเลขที่วัดได้ ไม่ใช่ตัวเลขที่ทำนายไว้

`onesentenceforboth` ฆ่า **4 ข้อ** คือทุกข้อที่อ่านว่าปลายไหนได้ประโยคไหน บวกกับ
*the list of reasons is the list the rules can actually produce* ซึ่งตายเพราะ
`validityNotStarted` หายไปจาก `refuse(` ที่มันสแกน ข้อที่ไม่ตายคือข้อที่ยืนยันปลายที่
ปิดไปแล้ว - มัตแตนต์นี้ไม่ได้แตะปลายนั้น

`endsareswapped` ฆ่า **5 ข้อ** จำนวนประโยคเท่าเดิม ทุกข้อที่นับว่ามีสองประโยคยังผ่าน
ตายเฉพาะข้อที่อ่านว่าปลายไหนได้ประโยคไหน รวมทั้งข้อที่ถือ cookie ข้ามปลายหน้าต่าง -
ซึ่งเป็นสิ่งที่ยืนยันว่า `sessionAdmission` เรียก `validityRefusal` ตัวเดียวกันจริง

`listmissesthenewend` ฆ่า **1 ข้อ** และเป็นข้อเดียวกันนั้น การลบกุญแจออกจากรายการ
ไม่ทำให้อะไรใน `50a` ล้มเลย - `50-sign-in-screens.py` เขียนไว้เป็นย่อหน้าของตัวเองแล้วว่า
แถว 1 วนตามรายการ ลบออกก็แค่ loop สั้นลง การกวาดครั้งนี้คือคำยืนยันเป็นตัวเลขของ
ย่อหน้านั้น: ปลายอีกข้างของสัญญาถูกจับที่ seam อื่น ไม่ใช่ที่นี่

`screenmissesthenewend` ฆ่า **1 แถว** คือแถว 1 ของ `50a` และไม่ฆ่าอะไรอื่นในไฟล์นั้น
เป็นคู่ของ `50:googlerefusalmissesone` ที่ถูกเล็งใหม่ - กุญแจคนละตัว ใบคนละใบ และ
ไม่มีตัวไหนแทนกันได้

ไม่มีอะไรนอก `users.test.js` กับ `auth.test.js` ตายจากสามตัวแรก

## สิ่งที่การกวาดจับได้ในตัวเทสต์เอง

ครั้งแรก `endsareswapped` ฆ่า **6** และหนึ่งในนั้นไม่ใช่การฆ่า สองข้อใน `auth.test.js`
ใช้ `U_EXT` ร่วมกัน และแต่ละข้อคืนค่าคอลัมน์ที่มันย้ายใน**บรรทัดถัดจาก assertion สุดท้าย**
พอข้อแรกล้ม การคืนค่าก็ไม่ได้ทำงาน ข้อที่สองจึงเจอบัญชีที่หน้าต่างยังอยู่ในอดีต และล้ม
ด้วยเรื่องที่มันไม่ได้ยืนยัน นี่คือกฎของ #97 (*มัตแตนต์ต้องล้มได้ทีละแถว*) ในขนาดของ
fixture เดียว และไม่เคยมีอะไรเห็นมันเพราะไม่เคยมีอะไรทำให้ข้อแรกล้ม `finally` คือทั้งหมด
ของการแก้ ตัวเลข 5 ข้างบนวัดหลังจากนั้น

    python mutation/89-two-ends-of-a-window.py save
    python mutation/89-two-ends-of-a-window.py <mutant>
    python mutation/89-two-ends-of-a-window.py restore
"""

from harness import main

FILES = {
    "accounts": "backend/auth/accounts.js",
    "screen": "frontend/src/pages/Login.js",
}

MUTANTS = {
    # ข้อบกพร่องเดิม ปลายทั้งสองข้างได้ประโยคเดียวกัน เขียนเป็นการคืนกุญแจตัวเดียวกัน
    # ไม่ใช่การลบด่าน - ด่านยังปฏิเสธเหมือนเดิม เปลี่ยนแค่ว่ามันพูดว่าอะไร ฆ่า 4 ข้อ
    "onesentenceforboth": ("accounts",
        "  if (user.valid_from && now < calendarDay(user.valid_from)) {\n"
        "    return refuse(403, 'validityNotStarted');\n"
        "  }\n",
        "  if (user.valid_from && now < calendarDay(user.valid_from)) {\n"
        "    return refuse(403, 'validityEnded');\n"
        "  }\n"),
    # สองประโยคสลับข้าง จำนวนประโยคเท่าเดิม ทุกแถวที่นับว่า *มีสองประโยค* ยังผ่าน
    # มีแต่แถวที่อ่านว่าปลายไหนได้ประโยคไหนเท่านั้นที่ตาย ฆ่า 5 ข้อ
    "endsareswapped": ("accounts",
        "  if (user.valid_until && now > calendarDay(user.valid_until)) {\n"
        "    return refuse(403, 'validityEnded');\n"
        "  }\n",
        "  if (user.valid_until && now > calendarDay(user.valid_until)) {\n"
        "    return refuse(403, 'validityNotStarted');\n"
        "  }\n"),
    # การแก้ที่ลงฝั่ง server แล้วลืมสำเนาในอีก package - รูปของ #111 กุญแจใหม่ตัวหนึ่ง
    # ไม่มีถ้อยคำบนหน้าจอ คนที่เจอจึงได้ประโยคสำรอง แทนเหตุผลที่ถูกปฏิเสธ ฆ่าแถว 1 ของ `50a`
    "screenmissesthenewend": ("screen",
        "  validityNotStarted:\n"
        "    'บัญชีนี้ยังไม่ถึงวันเริ่มใช้งาน กรุณาเข้าใช้งานอีกครั้งเมื่อถึงวันที่กำหนด',\n",
        ""),
    # กุญแจใหม่ไม่ได้อยู่ในรายการที่เป็นสัญญากับหน้าจอ `50a` วนตามรายการ การลบออก
    # จึงทำให้ loop สั้นลงและไม่มีอะไรล้มที่นั่น (#50 เขียนไว้แล้ว) ข้อที่จับได้คือข้อที่
    # อ่านรายการเทียบกับ `refuse` ที่กฎผลิตได้จริง ฆ่า 1 ข้อ และไม่มีอะไรใน `50a` ล้ม
    "listmissesthenewend": ("accounts",
        "  'unverified',\n  'validityNotStarted',\n  'validityEnded',\n",
        "  'unverified',\n  'validityEnded',\n"),
}

main(FILES, MUTANTS)
