# -*- coding: utf-8 -*-
"""#164 - ช่องที่เปิดใหม่ถูกวาดพร้อมสิ่งที่พิมพ์ทิ้งไว้ หนึ่งเฟรม

`draft` ของ `EntrySection` อยู่ยืนกว่าตัวช่อง การปิดคืน `seededFrom` เป็น `CLOSED` แต่ไม่แตะ
สิ่งที่พิมพ์ไว้ ช่องที่ เขียน ครั้งที่สองเปิดขึ้นมาจึง **mount พร้อมข้อความที่กดยกเลิกไปแล้ว**
แล้วถูกล้างโดย effect อีกหนึ่ง commit ถัดมา - และ `useEffect` ทำงาน**หลัง** paint เบราว์เซอร์
จึงได้รับ commit แรกนั้นไปวาด และมันวาดจริง

## การวัดที่ตัดสินตั๋วใบนี้

ตั๋วเสนอว่าให้**หน่วง renderer** เพื่อถ่างช่องว่างให้กว้างขึ้น วัดแล้วได้ตรงข้าม: อ่านครั้งเดียว
ได้ข้อความค้าง 1 ใน 5 รอบตอนไม่หน่วง และ **0 ใน 5** ทั้งที่ x4 x10 และ x20 การหน่วงบีบให้
การอ่านไปต่อคิวหลังงานที่ React ค้างอยู่ มันจึง**ปิด**การแข่งนี้ ไม่ใช่เปิด

คำถามที่ถูกต้องคือคำถามที่คนใช้ถาม - *ข้อความนั้นเคยขึ้นจอไหม* สุ่มตัวอย่างค่าของช่องทุก
animation frame ตลอดการเปิดใหม่ **แปดรอบจากแปดรอบ ได้หนึ่งเฟรมที่ถือข้อความนั้นพอดี**
สิ่งที่สุ่มคือการที่การอ่านครั้งเดียวของ Playwright ไปตกในเฟรมนั้นหรือไม่ (หกจากแปด) ตัวข้อบกพร่อง
ไม่ได้สุ่มเลย - `150a` แถว 2 จึงไม่ใช่แถวที่พึ่งพาไม่ได้ แต่เป็นแถวที่จับของจริงได้บางครั้ง

ทางแก้คือ `useLayoutEffect` ซึ่ง**ตัดสินใจเหมือนเดิมทุกอย่างจากค่าเดิมทุกตัว** ที่ย้ายคือ React
flush มันก่อนเบราว์เซอร์ paint เฟรมที่ถูกวาดจึงเป็นเฟรมที่ effect นี้แก้ไปแล้ว

## มัตแตนต์

แถวของตั๋วนี้อยู่ใน `e2e/tests/164a-a-reopened-editor-is-never-painted-stale.spec.js` แถวเดียว

* `paintedafter` - `useLayoutEffect` กลับไปเป็น `useEffect` คือ**พฤติกรรมก่อน #164** ทุกตัวอักษร
  สองแก้ในไฟล์เดียว เพราะ import กับจุดเรียกเป็นการตัดสินใจอันเดียว และย้อนอันเดียวได้โค้ดที่
  พังตอนโหลด ไม่ใช่โค้ดที่เคยมีอยู่ (มัตแตนต์ที่หยุดทั้งแอปฆ่ามากเกินไป - #97)
  **ฆ่าแถวเดียวของ `164a`** กวาดผ่านทุกใบที่วาด `EntrySection` ขึ้นจอ - `41a` `133a` `146a`
  `149a` `150a` `151a` `164a` - **ล้ม 1 ผ่าน 110** (5.9 นาที) และ `150a` แถวสองรอดในรอบนั้น
  ซึ่งคือเรื่องของตั๋วใบนี้ทั้งใบ: ยามที่อ่อนไม่ได้จับทุกครั้ง กวาดซ้ำอีกรอบหลังแถวถูกเขียนใหม่ตามผลรีวิว
  ยังตายที่ข้ออ้าง ไม่ใช่ที่เงื่อนไขนำ (`Expected: 0 · Received: 1`)

* `clearondraftclose` - **มีไว้ให้รอด** คือ**ทางแก้อีกทางหนึ่ง**: คง `useEffect` ไว้หลัง paint
  แล้วล้าง `draft` ตอนปิดแทน ช่องที่เปิดใหม่บนหัวข้อที่ยังไม่มีอะไรจึง mount ว่างตั้งแต่แรก
  ไม่มีเฟรมไหนถือข้อความเก่า `164a` จึงเขียว - **และมันรอดจริง** กวาด**กว้างเท่ากับตัวบน**
  เมื่อ 25 กันยายน 2569 คือทั้งเจ็ดใบ **111 แถว ไม่ตายสักแถว** (5.8 นาที) ผู้รอดที่กวาดแคบกว่า
  ผู้ฆ่าคือผู้รอดที่ยังไม่ได้ถูกถามให้ครบ (#51)

  สิ่งที่ผู้รอดตัวนี้บอกคือขอบเขตของแถว: `164a` ถือข้ออ้างว่า*กรณีหัวข้อว่างไม่แฟลช* ไม่ได้ถือ
  ข้ออ้างว่า*การ seed ถูก flush ก่อน paint* ทางแก้สองทางนี้ต่างกันที่กรณี**ถูกแทนที่** - เปิดช่อง
  บนหัวข้อที่มีรายการ A ปิด แล้วมีคนเปลี่ยนเป็น B ก่อนเปิดใหม่ - ที่ `clearondraftclose` ยังแฟลช A
  อยู่หนึ่งเฟรม แถวของกรณีนั้น**เขียนวันนี้ไม่ได้** ไม่ใช่เพราะสร้างสถานการณ์ไม่ได้ แต่เพราะมันจะ
  ผูกตัวเองเข้ากับ**คำถามที่ #150 เปิดค้างไว้** (การถูกแทนที่ควร seed ทับสิ่งที่พิมพ์หรือไม่)
  ถ้าเจ้าของงานตอบว่าไม่ควร แถวนั้นจะแดงทันทีทั้งที่จอถูก จุดที่ยึดข้ออ้างระดับชั้นไว้วันนี้จึงเป็น
  ตัวมัตแตนต์นี้กับคำอธิบายข้างโค้ด ไม่ใช่แถว - เขียนไว้ตรงนี้เพื่อให้มันเป็นสิ่งที่หาเจอ ไม่ใช่
  ร้อยแก้วที่ไม่มีใครหา (#119)

## ไฟล์ที่ใช้ร่วมกัน

`frontend/src/components/improvement/EntrySection.js` มี `41` `149` และ `150` ถืออยู่ด้วย
**ห้ามกวาดใบนี้พร้อมกับสามใบนั้น** (ถามสำมะโนด้วย `FILES` ไม่ใช่ด้วยความจำ - #125)

`150:closekeepstheid` เกาะบรรทัด `seededFrom.current = CLOSED` และ `150:arrivalreseeds`
กับ `neverreseeds` เกาะบรรทัด `replaced` ทั้งสามบรรทัดตั๋วนี้ไม่ได้แตะ - สิ่งที่ตั๋วนี้เปลี่ยนคือ
*เมื่อไร* effect ทำงาน ไม่ใช่ว่ามันตัดสินใจอะไร ทั้งสามจึงกวาดซ้ำแล้วตายเหมือนเดิม

## วิธีรัน

    python mutation/164-a-reopened-editor-painted-stale.py save
    python mutation/164-a-reopened-editor-painted-stale.py paintedafter
    cd e2e && E2E_FRONTEND_PORT=5300 npx playwright test 164a-a-reopened --reporter=line
    python mutation/164-a-reopened-editor-painted-stale.py restore
"""

from harness import main

FILES = {
    'entry': 'frontend/src/components/improvement/EntrySection.js',
}

LAYOUT = "import { useLayoutEffect, useRef, useState } from 'react'\n"
AFTER_PAINT = "import { useEffect, useRef, useState } from 'react'\n"
CALL = "  useLayoutEffect(() => {\n"
AFTER_PAINT_CALL = "  useEffect(() => {\n"
CLOSE = """    if (!editing) {
      seededFrom.current = CLOSED
      return
    }
"""
CLOSE_CLEARS = """    if (!editing) {
      seededFrom.current = CLOSED
      setDraft('')
      return
    }
"""

MUTANTS = {
    'paintedafter': [
        ('entry', LAYOUT, AFTER_PAINT),
        ('entry', CALL, AFTER_PAINT_CALL),
    ],
    'clearondraftclose': [
        ('entry', LAYOUT, AFTER_PAINT),
        ('entry', CALL, AFTER_PAINT_CALL),
        ('entry', CLOSE, CLOSE_CLEARS),
    ],
}

main(FILES, MUTANTS)
