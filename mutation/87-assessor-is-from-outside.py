# -*- coding: utf-8 -*-
"""#87 - ผู้ประเมินภายนอกที่อยู่ในโดเมนของสถาบันเอง

`U_EXT` อยู่ที่ `external.assessor@kmitl.ac.th` ตั้งแต่วันแรก ทั้งที่บทบาท
*ผู้ประเมินภายนอก* **โดยนิยามแล้วไม่ใช่คนของสถาบัน** และเหตุผลทั้งหมดที่บทบาทนี้เป็น
บทบาทเดียวที่ถูกบังคับให้ตั้งรหัสผ่าน ก็มาจากข้อเท็จจริงข้อเดียวคือ **Google ปฏิเสธอีเมล
นอก `@kmitl.ac.th`** บัญชีผู้ประเมินที่อยู่ในโดเมนของสถาบันจึงเป็นตัวอย่างของคนที่
มีอยู่จริงไม่ได้

ธรรมเนียมถูกตกลงไปแล้วทุกที่ที่ไม่ใช่ seed — ผู้ประเมินภายนอกทุกใบที่
`backend/test/users.test.js` สร้างขึ้นอยู่ที่ `tabee-review.org` (สิบสี่ที่อยู่ สองอัน
เป็น template ที่วนสร้างมากกว่าหนึ่งใบ) และ `U_NONKMITL` กับ `U_EXT_CLOSED` ก็อยู่ที่นั่น
**seed ถือข้อยกเว้นใบเดียวของทั้งคลัง**

## สิ่งที่การแก้เปิดออกมา

ตั๋วทำนายไว้เองว่า *"ถ้ามีที่ใดผูกกับกฎอีเมลต้องเป็น `@kmitl.ac.th` การแก้นี้จะเปิด
ออกมาเอง"* — และมันเปิดออกมาจริง

`auth.test.js` มีสองข้อที่ยิง `resolveGoogleAccount` **ทาง Google** ด้วยบัญชีผู้ประเมิน
คนนั้น แล้ววัดว่าได้ `validityEnded` กับ `validityNotStarted` ย้ายโดเมนออกไปแล้วรัน
ผลคือ `expected: 'validityEnded'` · `actual: 'domain'` เพราะ `resolveGoogleAccount`
ตรวจโดเมนเป็นบรรทัดแรกและตรวจช่วงเวลาอีกสามบรรทัดถัดมา **ผู้ประเมินภายนอกไม่มีวัน
ไปถึงช่วงเวลาของตัวเองทางประตูนั้นเลย** สองข้อนั้นไปถึงกฎช่วงเวลาได้ก็เพราะ seed ขัดกับ
บทบาทอยู่พอดี กฎถูก แต่ตัวประธานเป็นคนที่มีอยู่จริงไม่ได้

สองข้อนั้นย้ายไปอยู่บน `U_TEACH` แล้ว — `backend/routes/users.js` ไม่ได้กั้น
`valid_from`/`valid_until` ไว้ที่บทบาทใด ผู้ดูแลจึงกำหนดช่วงเวลาให้บัญชีไหนก็ได้ และคนที่
เจอกฎนี้ที่ประตู Google คืออาจารย์ที่ถูกกำหนดช่วงเวลาแล้วช่วงนั้นปิดไป **ช่วงเวลาของ
ผู้ประเมินเองถูกตัดสินที่ประตูรหัสผ่าน ซึ่งเป็นประตูเดียวที่บทบาทนี้มี**

## กฎโดเมนไม่เคยมีมัตแตนต์มาก่อนเลย — แต่มันมีข้อที่ยืนยันมันอยู่

`grep KMITL_DOMAIN mutation/` ไม่เจออะไรก่อนไฟล์นี้ กฎที่ทั้งบทบาทแขวนอยู่จึงไม่เคยมี
มัตแตนต์ ตั๋วนี้เพิ่ม assertion ตรง ๆ กับกฎนั้น (*an external assessor cannot come in
through Google at all*) มัตแตนต์ของกฎจึงเป็นของตั๋วนี้ ไม่ใช่ของ #8

**ร่างแรกของย่อหน้านี้เขียนต่อไปว่า *ไม่มีอะไรค้ำว่ามันยังทำงาน* ซึ่งผิด** และการกวาดเป็น
คนบอก `googleforgetsthedomain` ฆ่า *refuses an address outside the KMITL domain*
ซึ่งอยู่ใน `auth.test.js` มาตลอด — ที่ขาดคือมัตแตนต์ ไม่ใช่ข้อยืนยัน **grep เป็นหลักฐาน
ของรูปแบบที่พิมพ์ลงไป ไม่ใช่ของข้อความที่อยากให้จริง** และคราวนี้มันถูกจับได้ภายในไฟล์
เดียวกับที่เขียนมันลงไป เพราะตัวเลขถูกวัดหลังจากเขียนประโยค ไม่ใช่ก่อน

## การกวาด

| มัตแตนต์ | ฆ่า | ข้อที่เป็นคำกล่าวของตั๋วนี้จริง ๆ |
|---|---|---|
| `seedassessorisinhouse` | **3 ข้อ ใน 2 สวีท** (db 1 · auth 2) | **1** |
| `googleforgetsthedomain` | **4 ข้อ ใน 1 ไฟล์** (auth ทั้งสี่) | 4 |

**`seedassessorisinhouse` ฆ่าสามข้อ แต่มีข้อเดียวที่เป็นคำกล่าว** ข้อนั้นคือ
*no external assessor is at an address the institution owns* ใน `db/test/seed.test.js`
อีกสองข้อ — *in development, password sign-in is open to every role* กับ
*outside development, password sign-in is for the two admin roles only* — ตกเพราะ
`EMAILS.assessor` ใน `auth.test.js` เป็น**สำเนาที่คนดูแลเอง**ของที่อยู่ใน seed พอ seed
ขยับ สำเนาก็ชี้ไปที่บัญชีที่ไม่มีอยู่ แล้วการลงชื่อเข้าใช้ตอบ 401 นั่นไม่ใช่ข้อบกพร่องที่
มัตแตนต์นี้พูดถึงเลย มันคือรูปของ #85: **ที่อยู่นั้นเป็น*ข้อตั้ง*ของสองข้อนั้น ไม่ใช่*คำกล่าว*
ที่ข้อไหนอ้าง** เขียนสองเลขไว้คู่กันตามกฎสองตัวเลขของ #102

และข้อใหม่ *an external assessor cannot come in through Google at all* **ไม่ตาย**
ใต้มัตแตนต์ตัวแรก เพราะมันยิงที่อยู่ที่เขียนไว้ในไฟล์เทสต์ ไม่ใช่ที่อยู่ที่ seed ถืออยู่ ทั้งสองทาง
ได้ `domain` เหมือนกัน — ซึ่งถูกต้องแล้ว ข้อนั้นเป็นคำกล่าวเรื่อง*กฎ* ไม่ใช่เรื่อง*ชุดข้อมูล*
ส่วนคำกล่าวเรื่องชุดข้อมูลอยู่ที่ `db/` และมัตแตนต์ตัวแรกก็ฆ่ามันได้พอดีหนึ่งข้อ

**`googleforgetsthedomain` ฆ่าสี่ข้อ และทั้งสี่เป็นคำกล่าวของกฎนี้** —
*refuses an address outside the KMITL domain* (ข้อที่มีอยู่แล้ว),
*an external assessor cannot come in through Google at all* (ข้อใหม่ของตั๋วนี้),
*the list of reasons is the list the rules can actually produce* (#89 — ลิสต์ยังมีคีย์
`domain` อยู่ แต่โค้ดผลิตมันไม่ได้แล้ว) และ
*refuses an address outside the domain back onto the sign-in screen* (#119 — ปลายที่
พาคนกลับไปหน้าลงชื่อเข้าใช้) ไม่ใช่รูป *ฆ่ามากเกินไป* ของ #97 เพราะทั้งสี่ยืนยันกฎเดียวกันนี้
ตรง ๆ และที่เหลืออีก 725 ข้อยังเขียวหมด

รันที่ HTTP seam ทั้งคู่: `cd backend && node --test "test/*.test.js"` และ
`cd db && node --test "test/*.test.js"` — ไม่มีตัวไหนแตะเบราว์เซอร์ เพราะ e2e
ลงชื่อเข้าใช้ด้วยฟอร์ม และไม่มี OAuth credentials
"""
from harness import main

FILES = {
    "seed": "db/seed.js",
    "accounts": "backend/auth/accounts.js",
}

MUTANTS = {
    # ตัวข้อบกพร่องเอง คืนอีเมลกลับไปเป็นโดเมนของสถาบัน
    #
    # ข้อที่มันควรฆ่าคือข้อใน `db/test/seed.test.js` ที่ถามคำถามนี้กับ *สิทธิ์*
    # ไม่ใช่กับแถว - *no external assessor is at an address the institution
    # owns* - เขียนแบบนั้นเพราะเขียนว่า `ext01` อยู่ที่ที่อยู่นี้ ก็คือคัดลอก
    # `db/seed.js` มาไว้อีกที่ และจะผ่านบนชุดข้อมูลที่ผู้ประเมินคนถัดไปกลับเข้ามา
    # อยู่ในสถาบันอีก
    "seedassessorisinhouse": ("seed",
        "    email: 'external.assessor@tabee-review.org',\n",
        "    email: 'external.assessor@kmitl.ac.th',\n"),
    # กฎโดเมนหายไปทั้งบรรทัด ประตู Google เปิดให้ทุกที่อยู่ที่มีบัญชีอยู่จริง
    #
    # ไม่ใช่รูป *ฆ่ามากเกินไป* ของ #97: e2e ไม่ได้ใช้เส้นทางนี้เลย และที่ HTTP seam
    # บัญชีในสถาบันทุกใบยังเข้าได้เหมือนเดิม สิ่งที่เปลี่ยนคือบัญชีนอกสถาบันเข้าได้ด้วย
    "googleforgetsthedomain": ("accounts",
        "  if (!email?.toLowerCase().endsWith(KMITL_DOMAIN)) return refuse(403, 'domain');\n",
        ""),
}

main(FILES, MUTANTS)
