# -*- coding: utf-8 -*-
"""#131 - บัญชีที่ถูกลบแล้วต้องออกจากระบบได้ และคุกกี้ต้องหาย

`POST /api/auth/logout` เขียนบันทึก `LOGOUT` **ก่อน** ลบคุกกี้ และ `user_log.user_id`
เป็น foreign key ไปที่ `users` แบบ `ON DELETE CASCADE` บัญชีที่ถูกลบไปแล้วจึงไม่มีที่ให้
บรรทัดใหม่ลง INSERT ล้ม `next(error)` ตอบ 500 และ **การลบคุกกี้ไม่เคยได้ทำงาน** บน
route เดียวที่มีหน้าที่ลบมัน

สองอย่างที่คอมเมนต์ของ route นี้เขียนไว้เองอยู่แล้วคือคำตอบ - *"clearing is unconditional
and idempotent... recording needs a name"* - แต่ลำดับในโค้ดพูดตรงข้าม ตั๋วนี้จึงสลับลำดับ
และเพิ่มคำถามเดียวก่อนเขียน: ยังมีบัญชีชื่อนี้ให้ลงบรรทัดไหม (`accountStillExists`)

## ทำไมถึงสำคัญกว่าตอนเขียนตั๋ว

หลัง #52 เบราว์เซอร์ยิง `/api/auth/logout` **เอง** เมื่อได้ธง `accessEnded` และหนึ่งใน
เหตุผลของธงนั้นคือ `unknown` = *บัญชีถูกลบทั้งที่คุกกี้ยังดี* ซึ่งคือสถานะนี้ตรง ๆ
`AuthContext` ยิงแบบ `.catch(() => {})` จึงกลืน 500 เงียบ ๆ ผลคือวนรอบ
403 → logout → 500 ทุกครั้งที่โหลดหน้า โดยคุกกี้ยังอยู่

วันนี้ยังไม่มี route ลบบัญชี (`backend/routes/users.js` - ข้อเท็จจริงที่ #67 บันทึกไว้)
สถานะนี้จึงสร้างได้จากการ DELETE ในฐานข้อมูลเท่านั้น **วันที่มี route ลบบัญชี นี่คือทาง
ที่คนเดินถึง ไม่ใช่สถานะที่มีแต่การทดสอบเซ็น token เองถึงจะไปถึง**

## ครึ่งที่พิสูจน์ได้ กับครึ่งที่พิสูจน์ไม่ได้

- **คำถามก่อนเขียน** พิสูจน์ได้ที่ seam นี้ - ถอดออกแล้วแถวตาย (ดูตารางข้างล่าง)
- **ลำดับ** พิสูจน์ที่ seam นี้ไม่ได้ และพูดไว้ตรงนี้แทนที่จะปล่อยให้เข้าใจว่าพิสูจน์แล้ว
  เมื่อคำถามก่อนเขียนยังอยู่ ไม่มีอะไรโยน exception ลำดับจึงไม่เปลี่ยนผลของแถวไหนเลย
  สิ่งที่ลำดับกันไว้คือช่วงแคบระหว่างสองบรรทัด - บัญชีถูกลบหลังคำถามและก่อน INSERT -
  ซึ่งเป็นการแข่งกันที่การทดสอบไม่มีที่จับ เหมือนที่ #51 บันทึกไว้สำหรับ token renewal
  มันอยู่ในโค้ดเพราะ**ไม่มีราคา** ไม่ใช่เพราะมีแถวค้ำ (#107 ข้อที่สาม: บอกว่าข้อไหน
  พิสูจน์แล้ว ข้อไหนไปไม่ถึง ข้อไหนยังไม่ได้ทดสอบ)

## การกวาด

`cd backend && node --test "test/*.test.js"` (739 แถว) กวาดเมื่อ 12 ก.ย. 2569
ตารางนี้บันทึก **สิ่งที่มัตแตนต์ฆ่าได้จริง**

| มัตแตนต์ | แถวที่มันค้ำ | ตายที่ |
|---|---|---|
| `logstoanybody` | บัญชีที่ไม่มีอยู่แล้ว ออกจากระบบได้ | *is cleared on sign-out by an account that no longer exists* - ได้ 500 ที่ควรเป็น 200 · แถวอื่นของ `auth.test.js` ผ่านหมด เพราะบัญชีของมันมีอยู่จริง คำถามที่ถูกถอดจึงไม่เปลี่ยนอะไรสำหรับพวกมัน |

`13:N7` อยู่กับบรรทัดเดียวกันนี้และเล็งใหม่แล้วเป็นครั้งที่สอง - มันค้ำข้ออ้างคนละข้อ
(*ออกจากระบบแล้วต้องมีบรรทัดในประวัติ*) และยังฆ่าแถวของมันได้ตามเดิม
"""
from harness import main

FILES = {'auth': 'backend/routes/auth.js'}

MUTANTS = {
    # บรรทัดที่ถูกเพิ่มเข้ามาใน #131: ถามก่อนว่ายังมีบัญชีให้ลงบรรทัดไหม
    # ถอดคำถามออก แล้ว INSERT จะล้มกับ foreign key เหมือนเดิม
    'logstoanybody': ('auth',
        "      if (userId && (await accountStillExists(pool, userId))) {\n",
        "      if (userId) {\n"),
}

main(FILES, MUTANTS)
