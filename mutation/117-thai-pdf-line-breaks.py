# -*- coding: utf-8 -*-
"""#117 - PDF ขึ้นบรรทัดใหม่ตัดกลางคำไทย และแยกสระนำออกจากพยัญชนะ

`…และอินเทอร์เ` / `ฟซในการออกแบบ…` ในรายงานของ #40 — คนอ่านเจอตอนเดินมือใบ #40
ชุดทดสอบเขียว 3 รอบมองไม่เห็น เพราะแถว PDF ของ `40a` อ่าน**ไบต์ของฟอนต์** ซึ่งเป็นข้ออ้างว่า
กลิฟอยู่ในไฟล์ ไม่ใช่ข้ออ้างเรื่องประโยคที่วาดด้วยกลิฟนั้นถูกตัดที่ไหน

## จุดที่ตั๋วชี้ย้ายไปแล้ว แต่กลไกที่ตั๋วบอกถูกทุกตัวอักษร

ตั๋วชี้ `splitTextToSize` ใน `assessmentPdf.js` — `grep -rn splitTextToSize frontend/src`
ไม่เจออะไรเลย การตัดเกิดลึกลงไปหนึ่งชั้น ใน `jspdf-autotable` ที่เรียกตัวตัดของ jsPDF ให้ทุกเซลล์
ที่สไตล์เป็น `overflow: 'linebreak'` ตัวตัดนั้นเขียนไว้เองว่า *at this time works only on
Western scripts, ones with space char separating the words* และ `splitLongWord` ของมันเดิน
ทีละตัวอักษรจนความกว้างหมด ภาษาไทยไม่เขียนช่องว่างระหว่างคำ ประโยคทั้งประโยคจึงเป็น "หนึ่งคำ"
ในสายตามัน และรอยตัดไปตกที่ไหนก็ที่ที่คอลัมน์หมดพอดี

การแก้จึงมีรูปที่ตั๋วไม่ได้เสนอ — **ห่อคำก่อนส่งให้ autoTable** และ**ปล่อยตัวตัดของ autoTable
ไว้ข้างหลัง**เป็นทางถอย ไม่ใช่แทนที่มัน (รูปเดียวกับ #118: สองข้อเสนอของตั๋วไม่ใช่ทางเลือก
แต่เป็นคนละชั้น)

## สองในสี่เกณฑ์ของตั๋วมีอยู่ก่อนแล้ว และไม่ใช่เพราะเรา

เกณฑ์ 2 — *ไม่มีบรรทัดไหนเริ่มด้วยวรรณยุกต์หรือสระบน/ล่าง* — ถือไว้โดย **TH Sarabun**
ไม่ใช่โดยโค้ด วรรณยุกต์และสระบน/ล่างทั้ง 16 ตัวมี advance เป็น **0** และเงื่อนไขวนซ้ำของ
`splitLongWord` รับตัวที่กว้าง 0 เสมอ มาร์กจึงถูกดึงขึ้นไปอยู่บรรทัดของพยัญชนะมันทุกครั้ง
วัด 60 ความกว้าง — ไม่มีแม้แถวเดียวที่เริ่มด้วยมาร์ก **แถว 4 จึงเป็น ☑ ไม่ใช่ ⚙** ดูหัวข้อ
*แถวที่ไม่มีมัตแตนต์ตัวไหนเอื้อมถึง* ข้างล่าง

เกณฑ์ 4 — *มีเทสต์ที่อ่านข้อความในหน้า PDF ไม่ใช่แค่ไบต์ฟอนต์* — คือ
`e2e/support/pdf-text.js` เอง ไม่ใช่สิ่งที่มัตแตนต์ตัวไหนวัด มันคือเครื่องมือที่ทำให้แถวอื่น
วัดอะไรได้ (แถว 1 เป็นข้อตั้งของมัน)

## มัตแตนต์

* `notwrapped` — **โค้ดก่อนการแก้ ในรูปที่รันได้** ยื่นคอลัมน์ที่กว้าง 1000 เท่าให้ตัวห่อ
  `wrapThai` จึงคืนย่อหน้าทั้งย่อหน้าและ autoTable ตัดเองเหมือนเดิมทุกประการ
  **ฆ่าแถว 2, 3 กับ 9** — *ไม่ตัดกลางคำ* และ *ไม่มีสระนำค้างท้ายบรรทัด* ซึ่งเป็นเกณฑ์ 1
  และครึ่งแรกของใบนี้ทั้งใบ
* `notwrappedgrid` — อย่างเดียวกันกับคอลัมน์รายวิชาของ #20 **ฆ่าแถว 8 ตัวเดียว**
  สองตัวนี้แยกสองเอกสารออกจากกัน: ใบนี้แก้สองไฟล์ และข้ออ้างของแต่ละไฟล์ต้องมีตัวที่ฆ่ามันเอง
  (ตัวนี้**รอดในการกวาดรอบแรก** — อ่าน *ชื่อที่รอดได้ทั้งสองแบบ* ข้างล่าง)
* `bycharacter` — `piecesOf(paragraph, 'word')` เป็น `'grapheme'` ห่อบรรทัดโดยถือว่า
  **ทุกคลัสเตอร์คือหนึ่งคำ** ซึ่งคือการเติมบรรทัดให้เต็มแล้วตัด — พฤติกรรมเดิมที่เขียนด้วยมือเรา
  **ฆ่าแถว 2 กับ 8** เป็นตัวที่ตอบว่า *พจนานุกรม* คือสิ่งที่ทำงาน ไม่ใช่การห่อ
* `novowelretreat` — `LEADING_VOWELS` เป็นสตริงว่าง ถอดกฎข้อ 3 ออกทั้งข้อ
  **ฆ่าแถว 9 ตัวเดียว** อ่าน *กฎข้อ 3 กับแถวเดียวที่เอื้อมถึงมัน* ข้างล่างก่อนเชื่อบรรทัดนี้
* `nochop` — `chop` คืนคำนั้นทั้งคำเมื่อคำกว้างเกินคอลัมน์ (ใช้พารามิเตอร์ครบทั้งสามตัว)
  ถอดกฎข้อ 2 ออก **ฆ่าแถว 9 ตัวเดียว ไม่ใช่แถว 6 อย่างที่ไฟล์นี้ทำนายไว้รอบแรก** —
  อ่าน *กฎข้อ 2 ทำอะไรจริง* ข้างล่าง
* `nofallback` — `overflow: 'linebreak'` ของตารางหลักใน #40 เป็น `'visible'` ถอด**ทางถอย**ออก
  ไม่ใช่ถอดการแก้ **รอดทุกแถว** และรอดอย่างที่ควรรอด: กฎข้อ 2 ของเรายังตัดคำยาวอยู่
  มันเป็นหนึ่งช่องของตาราง 2×2 ไม่ใช่ตัวที่ควรฆ่าอะไรเดี่ยว ๆ
* `nochopnofallback` — `nochop` กับ `nofallback` พร้อมกัน (มัตแตนต์ที่มีสองการแก้)
  **ฆ่าแถว 6 กับ 9** เป็นช่องเดียวในตารางที่ไม่มีใครตัดคำยาวเลย และเป็นตัวเดียวที่แตะเกณฑ์ 3
  เขียนขึ้นหลังการกวาดรอบที่สาม ซึ่งบอกว่าทั้ง `nochop` และ `nofallback` เดี่ยว ๆ ไม่แตะแถว 6

## กฎข้อ 3 กับแถวเดียวที่เอื้อมถึงมัน

กฎข้อ 3 (*ไม่มีบรรทัดไหนจบด้วยสระนำ*) **เอื้อมไม่ถึงเลยเมื่อ `Intl.Segmenter` อยู่** วัดเมื่อ
25 ก.ย. 2569 สองทาง: (1) ทุกสตริงไทยใน `db/seed.js` ตัดด้วย ICU แล้ว **ไม่มีเซกเมนต์ไหน
จบด้วยสระนำเลยแม้แต่ตัวเดียว** (2) 141 รูปที่ปั้นขึ้นมาให้คำยาวเกินคอลัมน์ — **ถึงกฎข้อ 3
ศูนย์ครั้ง** เหตุผลเป็นโครงสร้าง ไม่ใช่โชค: ICU แยกหน้าสระนำทุกตัว สิ่งที่ `chop` ได้รับจึงเป็นชิ้น
ที่ถูกแยกหน้าสระนำมาแล้ว

ที่มันกัดคือ**ทางถอยเมื่อไม่มีพจนานุกรม** ซึ่งเป็นทางที่โค้ดรองรับไว้จริงและเขียนคอมเมนต์อธิบายไว้:
ย่อหน้าทั้งย่อหน้ากลายเป็นคำเดียว `chop` ทำงานทั้งหมด และรอยตัดไปตกที่ไหนก็ที่ที่ความกว้างหมด
รวมถึงหลังสระนำ **แถว 9 จึงลบ `Intl.Segmenter` ออกจากหน้าเว็บ** แล้วถามคำถามเดิม

ถ้าไม่มีแถว 9 กฎข้อ 3 จะเป็นโค้ดที่ไม่มีอะไรค้ำ — ลบทิ้งก็ไม่มีแถวไหนแดง และเกณฑ์ 1 จะพัง
เงียบ ๆ บนทางที่โค้ดประกาศว่ารองรับ (#48 — *เกณฑ์ที่ตอบว่าไม่ ต้องมีแถวเท่ากับเกณฑ์ที่ตอบว่าใช่*)

## แถวที่ไม่มีมัตแตนต์ตัวไหนเอื้อมถึง

**แถว 4** (*ไม่มีบรรทัดไหนเริ่มด้วยมาร์ก*) เอื้อมไม่ถึงด้วยมัตแตนต์ — วัดแล้ว ไม่ใช่เดา
ตัวที่ควรจะฆ่ามันคือ `bycodepoint` (ตัดทีละโค้ดพอยต์แทนคลัสเตอร์) และมันจะรอดทุกแถว: มาร์กกว้าง 0
เงื่อนไขของ `chop` รับชิ้นที่กว้าง 0 เสมอ มาร์กจึงถูกดึงขึ้นไปอยู่กับพยัญชนะมันทั้งสองแบบ
วัด 6 รูปที่ปั้นด้วยมาร์กหลายชนิด — **เอกสารที่ได้เหมือนกันทุกบรรทัดทั้ง 6 รูป**
ไฟล์นี้จึงไม่มี `bycodepoint` (#97 — ตัวที่รอดเพราะข้ออ้างไม่เคยเสี่ยง คือตัวที่ต้องลบ ไม่ใช่เก็บไว้)

ข้อยกเว้นที่วัดเจอระหว่างทาง และเป็นของใบอื่น: **`ำ` (U+0E33) กว้าง 1.56mm ไม่ใช่ 0**
มันไม่ใช่วรรณยุกต์และไม่ใช่สระบน/ล่าง จึงอยู่นอกเกณฑ์ 2 ตามตัวอักษรของตั๋ว และอยู่นอก
คลัสเตอร์ของ ICU ด้วย (U+0E33 เป็น Lo ไม่ใช่ Mn) — บรรทัดที่เริ่มด้วย `ำ` จึงเป็นไปได้
ในทางทฤษฎี เฉพาะข้างใน `chop` ของคำที่ยาวเกินคอลัมน์ ไม่มีแถวไหนในใบนี้ถาม

**แถว 1** (เครื่องมืออ่านไฟล์ได้จริง) และ **แถว 7** (`Intl.Segmenter` ตอบภาษาไทยจริง)
เป็น**ข้อตั้ง** ต้องยืนใต้ทุกมัตแตนต์ อย่านับเป็นตัวที่รอดตอนอ่านตารางข้างล่าง (#85)

**แถว 5** (ไม่มีบรรทัดไหนล้นกรอบเซลล์) ถามเอกสารที่เมล็ดสร้าง ซึ่งไม่มีคำไหนกว้างเกินคอลัมน์เลย
`nochop` จึงไม่ควรแตะมัน — มันเป็นแถวเฝ้าระวังบนเอกสารจริง ส่วนแถว 6 คือที่ที่การล้นถูกวัด

## ชื่อที่รอดได้ทั้งสองแบบ

การกวาดรอบแรก `notwrappedgrid` **รอด ผ่านครบเก้าแถว** ทั้งที่มันคือโค้ดก่อนการแก้ของ #20

ไม่ใช่เพราะแถว 8 อ่อน แต่เพราะ**ชื่อที่แถว 8 ฉีดเข้าไป**: ในคอลัมน์ 72mm ชื่อนั้นมีรอยตัด
**หนึ่งรอย** คือมีที่ให้ผิดได้ที่เดียว ตัวห่อของเราตัดที่ 49 ส่วน jsPDF ตัดที่ 53 — และ 53
**ก็เป็นขอบคำของ ICU เหมือนกัน** เอกสารสองใบจึงถูกต้องทั้งคู่ในสายตาคำถามของแถวนั้น

วัด 260 ชื่อที่เป็นไปได้ (25 ก.ย. 2569): **65 ชื่อแยกสองแบบออกจากกัน** ชื่อที่ใช้อยู่ตอนนี้
ตัดที่ 44 (ขอบคำ) ส่วน jsPDF ตัดที่ 50 ซึ่งอยู่กลางคำ `วิศวกรรม` — ของเสียวาด
`…สำหรับวิศวกร` / `รมคอมพิวเตอร…` ของที่แก้แล้ววาด `…ษาจาวาสำหรับ` / `วิศวกรรมคอมพ…`
และแถวนั้นยืนยัน**เงื่อนไขก่อน**ด้วย: ต้องมีรอยตัดอยู่ข้างในชื่อจริง ไม่ใช่แค่มีรอยตัด

บทเรียนคือกลับด้านของ #154 — ตัวที่แทนค่าดีฟอลต์มองไม่เห็นตรงที่ค่าจริงเท่ากับดีฟอลต์
ที่นี่ **ตัวที่แทนตัวตัดหนึ่งด้วยอีกตัวมองไม่เห็นตรงที่สองตัวตัดที่เดียวกัน**
ข้อมูลของแถวต้องถูกปั้นที่จุดที่มันต่าง ไม่ใช่ที่จุดที่มันยาว

## กฎข้อ 2 ทำอะไรจริง

รอบแรกไฟล์นี้ทำนายว่า `nochop` ฆ่าแถว 6 (*คำที่ยาวเกินคอลัมน์ยังถูกตัด ไม่ล้นกรอบ*)
**มันรอดแถวนั้น และไปฆ่าแถว 9 แทน**

เหตุผลอยู่ในรูปของการแก้เอง: `chop` ที่คืนคำมาทั้งคำส่งสตริงยาวหนึ่งบรรทัดให้ autoTable
แล้ว **autoTable ก็ตัดมันเองที่ความกว้าง** เหมือนที่มันทำมาตลอด เกณฑ์ 3 จึงถือไว้ด้วย
**ทางถอย** ไม่ใช่ด้วยกฎข้อ 2 — และการกวาดรอบแรกคือสิ่งที่บอก ไม่ใช่การอ่านโค้ด

กฎข้อ 2 ไม่ได้ไร้ประโยชน์: สิ่งที่มันทำคือทำให้**กฎข้อ 3 เอื้อมถึงคำที่ยาวเกินคอลัมน์ได้**
ถอดมันออกแล้วคำยาวไปตกอยู่กับตัวตัดของ jsPDF ซึ่งทิ้งสระนำค้างไว้ — แถว 9 ตายเพราะอย่างนั้น
**ข้ออ้างของมันวัดได้ผ่านกฎข้อ 3 เท่านั้น** ส่วนข้ออ้างที่คอมเมนต์ของมันเขียนไว้ (*ตัดระหว่าง
คลัสเตอร์*) วัดไม่ได้เลยในเอกสารนี้ ด้วยเหตุผลเดียวกับแถว 4 ข้างบน

`nofallback` จึงถูกเขียนขึ้นหลังจากนั้น เพื่อให้ประโยคที่ว่า *ปล่อยตัวตัดของ autoTable ไว้
ข้างหลังเป็นทางถอย* เป็นข้ออ้างที่มีแถวค้ำ ไม่ใช่ประโยคในคอมเมนต์

## สิ่งที่ไฟล์นี้ชนกับใบอื่น

`FILES` ของใบนี้มีสามไฟล์ และสองในสามซ้ำกับใบอื่น (#125 — สิ่งที่ทำให้การกวาดเพี้ยนคือ
**เส้นทางที่ซ้ำ** ไม่ใช่เนื้อเรื่องที่ใกล้กัน):

| ไฟล์ | ใบที่แตะด้วย |
|---|---|
| `frontend/src/components/results/assessmentPdf.js` | `mutation/40-clo-assessment.py` |
| `frontend/src/components/ploMapping/exportPdf.js` | `mutation/20-plo-mapping.py` |
| `frontend/src/lib/thaiWrap.js` | ใบนี้ใบเดียว |

`python mutation/anchors.py` เมื่อ 25 ก.ย. 2569 หลังการแก้: มัตแตนต์ 804 ตัว ปัญหา 0 —
การแก้ไม่ขยับ anchor ของสองใบนั้น และการกวาดใบนี้รันสเปกของใบนี้เท่านั้น

## การกวาด

กวาดเมื่อ 25 ก.ย. 2569 ทีละตัว บน `117a-thai-pdf-line-breaks` (ชื่อเต็ม ไม่ใช่ `117a` — #158)
`save` ก่อนเริ่ม และ `restore` ระหว่างทุกตัว อ่าน `git status` ทั้งหลัง `apply` และหลัง `restore`

| มัตแตนต์ | ผล | แถวที่ตาย |
|---|---|---|
| (ไม่ใส่) | ผ่าน 9 | — |
| `notwrapped` | ล้ม 3 ผ่าน 6 | 2, 3, 9 |
| `notwrappedgrid` | ล้ม 1 ผ่าน 8 | 8 |
| `bycharacter` | ล้ม 2 ผ่าน 7 | 2, 8 |
| `novowelretreat` | ล้ม 1 ผ่าน 8 | 9 |
| `nochop` | ล้ม 1 ผ่าน 8 | 9 |
| `nofallback` | ผ่าน 9 | — (อ่านย่อหน้าถัดไป) |
| `nochopnofallback` | ล้ม 2 ผ่าน 7 | 6, 9 |

**`nofallback` รอด และนั่นคือคำตอบ ไม่ใช่ช่องว่าง** เกณฑ์ 3 ถือไว้ด้วยสองกลไกที่ค้ำกันเอง
จึงต้องวัดเป็น**ตาราง** ไม่ใช่เถียงว่าอันไหนทำ (#148, #149):

| | มีตัวตัดของ autoTable | ถอดออก |
|---|---|---|
| **มี `chop` ของเรา** | ตัวควบคุม: เขียว 9 | `nofallback`: เขียว 9 |
| **ถอดออก** | `nochop`: แถว 9 | `nochopnofallback`: **แถว 6** |

ตารางนี้เป็น**การกวาดรอบที่สี่** และสามรอบก่อนหน้าคือสิ่งที่ทำให้มันมีความหมาย:

* **รอบแรก** — `notwrappedgrid` **รอด** ทั้งที่มันคือโค้ดก่อนการแก้ของ #20 (ดู *ชื่อที่รอดได้ทั้งสองแบบ*)
  และ `nochop` ฆ่าแถว 9 ไม่ใช่แถว 6 อย่างที่ไฟล์นี้ทำนายไว้ (ดู *กฎข้อ 2 ทำอะไรจริง*)
* **รอบที่สอง** — หลังเปลี่ยนชื่อที่แถว 8 ฉีด: `notwrappedgrid` ฆ่าแถว 8 และอีกสี่ตัวไม่ขยับเลย
* **รอบที่สาม** — `nofallback` ที่เพิ่งเขียนขึ้นเพื่อค้ำเกณฑ์ 3 **ก็รอด** และตัวควบคุมยังเขียว
  หลังเปลี่ยน `cellPadding: 1.6` เป็น `cellPadding: CELL_PADDING` (ค่าเท่ากัน)
* **รอบที่สี่** — หลังเปลี่ยน `UNBREAKABLE` จาก `ก`×60 เป็น `ก้`×60 และเพิ่ม `nochopnofallback`
  ชุดที่ตายของหกตัวแรกเหมือนรอบก่อนทุกตัว
* **รอบที่ห้า** — หลังรีวิวสองแกน ซึ่งแก้หกจุดในโค้ดกับสเปก: ลบ export ที่ไม่มีใครนำเข้า
  สามตัว รวมด่าน `typeof` ของ `thaiWrap` ให้เหลือประตูเดียว คำนวณคอลัมน์รูบริกจาก
  ความกว้างหน้าจริงแทนเลข `210` ให้แถว 6 ใช้ `holding` และให้แถว 8 ถามเกณฑ์ข้อ 2
  กับตารางของ #20 ด้วย **ชุดที่ตายเหมือนรอบที่สี่ทั้งเจ็ดตัว** ซึ่งเป็นคำตอบที่ต้องการ:
  ไม่มีอันไหนในหกจุดนั้นเปลี่ยนสิ่งที่วัดได้

**`UNBREAKABLE` ที่เปลี่ยนคือเหตุผลที่แถว 6 ไม่เคยวัดอะไร** ถาม ICU แล้วมันตอบว่า `ก`×60 เป็น
**สามสิบคำ คำละสองตัวอักษร** — พจนานุกรมยอมตัดกลางแถวพยัญชนะซ้ำที่ไหนก็ได้ กฎข้อ 1 จึงห่อมัน
และ `chop` ไม่เคยอยู่บนเส้นทางนั้นเลย ส่วน `ก้`×60 ตอบว่า **หนึ่งคำ ยาว 120 ตัวอักษร**
(วรรณยุกต์กว้าง 0 ความกว้างที่วาดจึงเท่าเดิม) ชื่อของฟิกซ์เจอร์เป็นคำกล่าวอ้าง ไม่ใช่คำอธิบาย

**ทุกแถวที่เป็นเกณฑ์มีตัวที่ฆ่ามัน ยกเว้นแถว 4** ซึ่งอธิบายไว้ข้างบนว่าทำไมไม่มีทางมี
และไม่มีสองตัวที่ฆ่าชุดเดียวกัน: `notwrapped` กับ `notwrappedgrid` แยกสองเอกสาร
`bycharacter` แยก *พจนานุกรม* ออกจาก *การห่อ* `novowelretreat` กับ `nochop` ฆ่าแถว 9
เหมือนกันแต่คนละเหตุผล — ตัวแรกถอดกฎที่ทำงาน ตัวหลังถอดสิ่งที่พากฎนั้นไปถึงที่ทำงาน

**แถว 5 ยืนอยู่ใต้ทุกตัว** ทั้งแปดตัว — เอกสารที่เมล็ดสร้างไม่มีคำไหนกว้างเกินคอลัมน์
มันจึงเป็นแถวเฝ้าระวัง ไม่ใช่แถวที่ค้ำอะไร อย่านับเป็นตัวที่รอด

## วิธีรัน

    python mutation/117-thai-pdf-line-breaks.py save
    python mutation/117-thai-pdf-line-breaks.py <mutant>
    cd e2e && E2E_FRONTEND_PORT=5300 npx playwright test 117a-thai-pdf-line-breaks
    python mutation/117-thai-pdf-line-breaks.py restore

แถวอยู่ใน `e2e/tests/117a-thai-pdf-line-breaks.spec.js`
"""
from harness import main

FILES = {
    'assessment': 'frontend/src/components/results/assessmentPdf.js',
    'grid': 'frontend/src/components/ploMapping/exportPdf.js',
    'wrap': 'frontend/src/lib/thaiWrap.js',
}

# The call, never the comment beside it (#121). A column a thousand times wider
# than the one drawn is a column nothing can exceed, so the wrap returns the text
# whole and autoTable's own splitter does what it did before the fix - and both
# `column` and `CELL_PADDING` stay referenced, which a `Number.MAX_SAFE_INTEGER`
# would not leave true.
WRAPPED = 'wrapped(text, column - CELL_PADDING * 2, one => doc.getTextWidth(one))'
UNWRAPPED = 'wrapped(text, (column - CELL_PADDING * 2) * 1000, one => doc.getTextWidth(one))'

GRID_WIDTH = '      SUBJECT_WIDTH - CELL_PADDING * 2,'
GRID_UNWRAPPED = '      (SUBJECT_WIDTH - CELL_PADDING * 2) * 1000,'

BY_WORD = "  const words = piecesOf(paragraph, 'word')"
BY_CLUSTER = "  const words = piecesOf(paragraph, 'grapheme')"

VOWELS = "const LEADING_VOWELS = 'เแโใไ'"
NO_VOWELS = "const LEADING_VOWELS = ''"

# Rule 2 removed while every parameter stays used: an over-wide word comes back
# whole, which is the one thing autoTable's splitter used to guarantee for us.
CHOP = """const chop = (word, maxWidth, widthOf) => {
  const clusters = piecesOf(word, 'grapheme')"""
NO_CHOP = """const chop = (word, maxWidth, widthOf) => {
  if (widthOf(word) > maxWidth) return [word]
  const clusters = piecesOf(word, 'grapheme')"""

# The fallback the fix leaves in place, taken away: autoTable stops splitting the
# cell at all, so our own newlines still draw but an over-wide word runs out of
# the box. The `halign` line is what makes this the main table and not the rubric.
LINEBREAK = """      halign: 'center',
      valign: 'middle',
      textColor: 20,
      lineColor: [180, 180, 180],
      lineWidth: 0.1,
      overflow: 'linebreak',"""
VISIBLE = """      halign: 'center',
      valign: 'middle',
      textColor: 20,
      lineColor: [180, 180, 180],
      lineWidth: 0.1,
      overflow: 'visible',"""

MUTANTS = {
    'notwrapped': ('assessment', WRAPPED, UNWRAPPED),
    'notwrappedgrid': ('grid', GRID_WIDTH, GRID_UNWRAPPED),
    'bycharacter': ('wrap', BY_WORD, BY_CLUSTER),
    'novowelretreat': ('wrap', VOWELS, NO_VOWELS),
    'nochop': ('wrap', CHOP, NO_CHOP),
    'nofallback': ('assessment', LINEBREAK, VISIBLE),
    'nochopnofallback': [('wrap', CHOP, NO_CHOP), ('assessment', LINEBREAK, VISIBLE)],
}

main(FILES, MUTANTS)
