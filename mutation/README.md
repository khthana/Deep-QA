# mutation/

การกลายพันธุ์ที่ใช้พิสูจน์ชุดทดสอบก่อนติด ⚙ ให้แถวใน `docs/acceptance/`

## ทำไมต้องมี

`docs/06-implementation-plan.md` §Acceptance กำหนดว่าแถวหนึ่งจะติด **⚙** — ครอบคลุมด้วย browser
seam — ได้ก็ต่อเมื่อ mutation test แสดงว่า *assertion ใหม่ตัวนั้น* (ไม่ใช่ตัวก่อนหน้า) ตายจริง
เมื่อโค้ดที่มันพูดถึงถูกทำให้พัง

ถ้าไม่มีขั้นตอนนี้ ชุดทดสอบที่ผ่านทั้งหมดพิสูจน์ได้แค่ว่า *มันรันแล้ว* ไม่ได้พิสูจน์ว่ามันจะสังเกตเห็นอะไร
assertion ที่ยิงผิดที่ก็เขียวเหมือนกัน

รอบกวาดของ #57 เป็นตัวอย่างที่ดีว่าขั้นตอนนี้จับอะไรได้จริง — `nofloor` ควรไม่มีผลกับรายการที่ไม่ว่างเลย
แต่กลับฆ่าแถวหนึ่งทิ้ง สาเหตุคือแถวนั้นอ่านตารางระหว่างที่คำออกมากับตอนที่หน้าจอยังวาดไม่เสร็จ จึงอ่านได้แถว
*กำลังโหลด…* มาเป็นคำตอบ ซึ่งไม่ซ้ำกับอะไรเลย ข้ออ้าง "หน้าถัดไปคือคนละชุด" จึงเป็นจริงโดยบังเอิญ
แก้ที่ `e2e/support/pager.js` ด้วยการรอให้ตารางวาดเสร็จก่อนอ่าน — นี่คือสิ่งที่ mutation test มีไว้เพื่อ

ก่อนหน้านี้สคริปต์เหล่านี้อยู่ใน scratchpad ของแต่ละ session แล้วหายไปพร้อมกัน — เอกสารตรวจรับอ้างถึง
หลักฐานที่ไม่มีใครเปิดดูได้อีก โฟลเดอร์นี้คือการเก็บมันไว้ **ให้ทำซ้ำได้ แทนที่จะให้เชื่อ**

## ใช้อย่างไร

```
python mutation/18-program-subjects.py save        # เก็บไฟล์ต้นฉบับไว้ก่อน
python mutation/18-program-subjects.py list        # มีตัวไหนบ้าง
python mutation/18-program-subjects.py nolimit     # ทำพัง หนึ่งตัว

cd e2e && npx playwright test 18b                  # ต้องตาย และต้องตายตัวเดียว

python mutation/18-program-subjects.py restore     # คืนโค้ดเดิม
```

`save` คัดลอกไฟล์ที่ยังไม่ถูกแตะไปไว้ที่ `mutation/.backup/` (อยู่ใน `.gitignore`)
ทุกครั้งที่ apply จะ restore ก่อนเสมอ ตัวกลายพันธุ์จึงซ้อนกันไม่ได้

**`restore` ก่อน commit เสมอ แล้วดูว่า `git status` สะอาดจากไฟล์ใน `backend/` กับ `frontend/`** —
นั่นคือหลักฐานเดียวว่าต้นไม้กลับมาเป็นต้นไม้แล้ว

**และ `save` ใหม่ทุกครั้งก่อนเริ่มกวาด ไม่ว่าจะเคย save ไปแล้วหรือไม่** — `restore` เขียนทับ
*ทุกไฟล์ใน `FILES`* ด้วยสำเนาที่ backup ถืออยู่ ไม่ใช่เฉพาะไฟล์ที่มัตแตนต์ตัวนั้นแตะ ดังนั้นการแก้ไฟล์
ในชุดนั้นหลัง `save` ครั้งล่าสุด — แม้จะแก้คนละไฟล์กับที่กำลังกวาด — จะหายไปเงียบ ๆ ตอน restore
ครั้งถัดไป เรื่องนี้กินงานไปสองรอบใน #33 (ครั้งแรกคือการแก้ระหว่างกวาด ครั้งที่สองคือแก้หลังกวาด
แล้วกวาดซ้ำโดยไม่ save ใหม่) วิธีที่ปลอดภัยคือ **อย่าแก้ไฟล์ในชุดระหว่างที่กวาดค้างอยู่** และ
**`save` เป็นคำสั่งแรกของสคริปต์กวาดเสมอ**

## ไฟล์

| ไฟล์ | ตั๋ว | จำนวน |
|---|---|---:|
| `10-application-shell.py` | #10 เปลือกหน้าจอ | 15 |
| `11-12-accounts-and-grants.py` | #11 ข้อมูลผู้ใช้งาน, #12 สิทธิ์การใช้งาน | 9 |
| `13-user-activity-history.py` | #13 ประวัติการใช้งาน | 9 |
| `16-subjects.py` | #16 ข้อมูลรายวิชา | 14 |
| `18-program-subjects.py` | #18 รายวิชาในหลักสูตร | 23 |
| `57-pager.py` | #57 ตัวควบคุมการแบ่งหน้า | 14 |
| `55-notice-in-view.py` | #55 แถบแจ้งผลที่อยู่เหนือขอบจอ | 2 |
| `56-wrong-template.py` | #56 ไฟล์ผิดกล่อง | 3 |
| `91-stale-notice.py` | #91 แถบแจ้งผลที่ค้างอยู่ | 3 |
| `23-offerings.py` | #23 การเปิดรายวิชาในภาคการศึกษา | 17 |
| `24-teacher-dashboard.py` | #24 หน้าหลักผู้สอน | 11 |
| `27-course-outcomes.py` | #27 ผลการเรียนรู้รายวิชา | 23 |
| `95-route-not-found.py` | #95 404 ที่ตอบด้วยฟิลด์ที่ไม่มีใครอ่าน | 2 |
| `19-plos.py` | #19 ผลการเรียนรู้ระดับหลักสูตร | 12 |
| `21-rubrics.py` | #21 ข้อมูล Rubric กลาง | 13 |
| `22-rubric-criteria.py` | #22 เกณฑ์การให้คะแนนของ Rubric | 14 |
| `98-narrow-window.py` | #98 ตารางกว้างที่ถูกตัดทิ้ง | 2 |
| `20-plo-mapping.py` | #20 การเชื่อมโยงผลการเรียนรู้กับรายวิชา | 8 |
| `25-section-enrolment.py` | #25 รายชื่อนักศึกษาของรายวิชา | 14 |
| `28-measurable-behaviors.py` | #28 พฤติกรรมที่วัดผลได้ตาม CLO | 13 |
| `29-achievement-criteria.py` | #29 เกณฑ์การบรรลุผลตาม CLO | 14 |
| `30-weighting-scheme.py` | #30 สัดส่วนคะแนน | 15 |
| `31-teaching-plan.py` | #31 แผนการสอน | 13 |
| `32-activity-list.py` | #32 กิจกรรมการเรียนรู้ | 15 |
| `33-activity-editor.py` | #33 การสร้างแก้ไขกิจกรรมและการเชื่อมโยง CLO | 14 |
| `26-work-groups.py` | #26 กลุ่มงานนักศึกษา | 15 |
| `34-activity-scores.py` | #34 คะแนนกิจกรรมการเรียนรู้ | 14 |

รวม **321 ตัว** — **320 ตัวยังใช้ได้กับโค้ดปัจจุบัน** (ที่เหลือคือ `13:N6` ดูหัวข้อ *เมื่อ MISS*)
ดูรายละเอียดของแต่ละตัวว่าฆ่า assertion ไหนได้ที่หัวข้อ mutation ของเอกสารตรวจรับใบนั้น ๆ

ตัวเลขรวมนี้คือ**ผลบวกของตารางข้างบน** อย่าบวกเพิ่มทีละตั๋วด้วยมือ — เคยหลุดไปสามตัวเพราะทำแบบนั้น
นับใหม่ทุกครั้งที่แก้ตาราง:

```bash
cd mutation && python -c "import ast,io,glob; print(sum(len(n.value.keys) for f in glob.glob('*.py') if f!='harness.py' for n in ast.parse(io.open(f,encoding='utf-8').read()).body if isinstance(n,ast.Assign) and getattr(n.targets[0],'id','')=='MUTANTS'))"
```

`harness.py` คือส่วนที่ใช้ร่วมกัน — save / restore / apply ไฟล์ที่เหลือมีแต่ *ข้อมูล* คือ
"แก้ไฟล์ไหน จากข้อความนี้ เป็นข้อความนี้"

## เมื่อ MISS

ตัวกลายพันธุ์ที่ตอบ `MISS` **ไม่ได้แปลว่าโค้ดผิด** แปลว่าข้อความที่มันถูกเขียนไว้ไม่อยู่ตรงนั้นแล้ว —
ไฟล์ถูก refactor หลังจากที่ข้ออ้างนั้นถูกพิสูจน์ ต้องเขียนตัวกลายพันธุ์ใหม่ให้ตรงกับโค้ดวันนี้
แล้วรันใหม่ ก่อนจะเชื่อแถวที่มันค้ำอยู่

harness ปฏิเสธตัวที่ข้อความตรงกับโค้ด **มากกว่าหนึ่งที่** ด้วย เพราะ "แก้ที่แรกที่เจอ" คือการแก้จุดที่
ไม่มีใครตั้งใจ ตัวที่กำกวมต้องผูกกับข้อความที่ยาวพอจะชี้จุดเดียว

ตอนย้ายเข้ามาที่นี่ (2026-08-20) มีสองตัวที่สะดุด

- **`13:N4`** — ข้อความเดิมสั้นเกินไป ตรงกับทั้งคอลัมน์เป้าหมายและคอลัมน์เวลา เขียนใหม่ให้ผูกกับ
  ทั้ง expression แล้วรันซ้ำ ตายที่ `e2e/tests/13a-user-history.spec.js` บรรทัด 100 ตามเดิม
- **`13:N6`** — ตกยุคจริง ๆ แผงเคยรีเซ็ตเลขหน้าใน `useEffect` และถูกแทนที่ด้วย `key` ที่ผู้เรียก
  ทั้งสองแห่ง ซึ่งเป็นสิ่งที่ `N9` พูดถึง เก็บไว้เป็นบันทึกแต่ apply ไม่ได้แล้ว

ที่ #79 (2026-08-21) อีกสองตัว — `MAINTAINERS` ของ `backend/routes/programSubjects.js`
ตัด `FACULTY_ADMIN` ออก ข้อความที่ `18:maintainerall` กับ `10:maintainer` ผูกไว้จึงหายไป
เขียนใหม่ทั้งคู่ให้ตรงกับโค้ดวันนี้ แล้วรันซ้ำ — `18:maintainerall` ตายที่
`e2e/tests/18c-program-subjects-refusals.spec.js` บรรทัด 152 ตามเดิม และ `10:maintainer`
ตายที่ `e2e/tests/10a-shell.spec.js` บรรทัด 104 ตามเดิม ตัวเดียวที่เหลือ
เป็น MISS จึงยังคงเป็น `13:N6` ตัวเดียว

ที่ #71 (21 ส.ค. 2569) เพิ่ม `11-12:M8` หนึ่งตัว — ตัดด่านสถานะบัญชีออกจาก `sessionAdmission` เท่านั้น ไม่แตะสำเนาที่ `admit` ใช้ตอนลงชื่อเข้าใช้ ผูกกับบรรทัด `unknown` ที่อยู่เหนือมันเพราะสำเนาทั้งสองเขียนเหมือนกันทุกตัวอักษร ตายที่ `e2e/tests/11c-suspension-and-a-live-session.spec.js` บรรทัด 109

ที่ #92 (22 ส.ค. 2569) เพิ่ม `10:guardedlogout` หนึ่งตัว — เอา `requireSession` กลับไปครอบ `POST /auth/logout` คือสภาพก่อนแก้เอง มันเขียนด้วย `require('../auth/session').requireSession` ในบรรทัดเดียว เพราะหลังแก้แล้วไฟล์นั้นไม่ได้ import มันไว้อีกแล้ว — ตายที่ `e2e/tests/10a-shell.spec.js` บรรทัด 173 และตายที่นั่นที่เดียว อีกสี่แถวของ row 6 ยังผ่านทั้งหมด

ที่ #23 (22 ส.ค. 2569) มีตัวหนึ่ง — `23:firstrole` — ที่แก้ `backend/auth/authorise.js` ซึ่งเป็นโค้ดร่วมของ #10 ไม่ใช่ของตั๋ว #23 เอง ตั้งใจให้เป็นอย่างนั้น เพราะแถวที่มันค้ำอยู่คือ *บัญชีสองบทบาทเข้าได้ในฐานะกรรมการ* ซึ่งเป็นข้ออ้างเกี่ยวกับการเลือกบทบาทโดยปริยาย ที่ตัดสินอยู่ในไฟล์นั้น ไม่ใช่ในเส้นทางของ #23 มัตแตนต์ที่แก้เฉพาะไฟล์ของตั๋วจะไม่มีทาง พูดถึงแถวนี้ได้เลย ตัวไหนที่ออกนอกไฟล์ของตั๋วแบบนี้ต้องเขียนเหตุผลกำกับไว้ที่เอกสารตรวจรับ

ที่ #28 (28 ส.ค. 2569) — การยก `offeringOf` กับ `cloOf` ของ `clos.js` ขึ้นเป็น module level
(เพื่อให้ #28 import แทนการเขียน join ซ้ำ) ทำให้ `27:anyclo` MISS เพราะ indentation ที่มันผูกไว้
เปลี่ยน เขียนใหม่แล้วรันซ้ำ — ตายที่ subtest 17 ของ `backend/test/clos.test.js` ตามเดิม
อีก 22 ตัวของ #27 ตรวจแล้ว apply ได้ครบ และ `28:orderbyid` ฉบับแรกถูก harness ปฏิเสธเพราะ
ข้อความตรงสอง query — เขียนใหม่ให้ผูกทั้ง SELECT ก่อนกวาดจริง

ที่ #31 (29 ส.ค. 2569) — การยก `sectionOf` ของ `enrolment.js` ขึ้นเป็น module level (#104
เพื่อให้ #31 import แทนการเขียน join ที่สี่) ทำให้ `25:anysection` MISS เพราะ indentation
ของ query เปลี่ยนกับการย้าย — สาเหตุเดียวกับ `27:anyclo` ตอน `offeringOf` ถูกยก เขียนใหม่
แล้วรันซ้ำ — ตายที่ row 9 (แรก) ของ `e2e/tests/25a-section-enrolment.spec.js` ตามเดิม
และตายแถวเดียว อีก 13 ตัวของ #25 ตรวจแล้ว apply ได้ครบ

ที่ #24 (23 ส.ค. 2569) มีตัวหนึ่ง — `24:notoken` — ที่ทำให้ต้องเขียนแถวทดสอบเพิ่มก่อน ไม่ใช่หลัง บรรทัดที่แทนที่ `%SECTION%` ด้วย `section_id` คือสิ่งที่ ADR-0004 เปลี่ยนทั้งบรรทัด แต่ไม่มี assertion อยู่ที่ไหนเลย โทเคนที่ไม่ถูกแทนที่พาไปที่ `NotBuiltYet` ซึ่งบนจอ แยกไม่ออกจากหน้าที่ยังไม่ได้สร้างจริง — สิ่งที่ mutation มีไว้เพื่อหา คือแถวที่ขาดไป ไม่ใช่แค่มัตแตนต์ที่ขาดไป

ที่ #34 (2 ก.ย. 2569) — การรวมด่านเพดานคะแนนของสองทาง (หน้าจอกับไฟล์) เข้าเป็น `readShares`
กับ `readWhole` ตัวเดียว ทำให้ `34:noactivityceiling`, `34:nocloceiling` และ
`34:importmarkunchecked` MISS พร้อมกันทั้งสามตัว เขียนใหม่ให้ตรงกับโค้ดวันนี้แล้วรันซ้ำ —
และ `34:importmarkunchecked` ถูก**ลบทิ้ง** เพราะหลังรวมแล้วมันคือบรรทัดเดียวกับ
`34:noactivityceiling` ทุกตัวอักษร ตัวที่เหลือจึงฆ่าทั้ง row 5 (หน้าจอ) และ row 9 (ไฟล์)
ตัวเดียว ซึ่งเป็นเรื่องปกติเมื่อโค้ดที่เคยมีสองสำเนากลายเป็นสำเนาเดียว — **มัตแตนต์นับตามจุดที่
พังได้ ไม่ใช่ตามแถวที่มันค้ำ**

เหตุที่รวม: /code-review ของตั๋วนี้ชี้ว่าสำเนาฝั่งไฟล์ไม่เคยได้ด่านที่สำเนาฝั่งหน้าจอมี ซึ่งเป็น
สิ่งที่ *Duplicated Code* ทำนายไว้ตรง ๆ และเป็นเหตุผลว่าทำไมสองมัตแตนต์นั้นถึงเคยต้องมีสองตัว

## ข้อควรระวัง

- `save` ต้องเป็นคำสั่งแรกของรอบ ไม่ใช่ของที่ค้างจากรอบก่อน
  `save` เก็บสำเนาไฟล์ และ `restore` เอาสำเนานั้นกลับมาทับทั้งไฟล์ สิ่งที่เขียนเพิ่มหลังจาก `save` จึงหายไปโดยไม่มีอะไรบอก
  ใน #92 เหตุนี้กลืนคอมเมนต์ไปหนึ่งย่อหน้าใน `backend/routes/auth.js` โดยที่ `git status` เพิ่งมาฟ้องหลัง push ไปแล้ว
  ครั้งนั้นเสียแค่คอมเมนต์ แต่ลำดับเดิมนี้กินโค้ดจริงได้เงียบ ๆ เท่ากัน — ถ้าไม่แน่ใจว่า snapshot เป็นของรอบนี้ ให้ `save` ใหม่ก่อน
- ตัวกลายพันธุ์ฝั่ง frontend ต้องรอ dev server compile ใหม่ ถ้ารันเองอย่าเพิ่งยิงทดสอบทันที
  (`e2e` ตั้ง `reuseExistingServer: false` จึงเริ่ม server ใหม่ทุกครั้งอยู่แล้ว)
- ไฟล์ spec หลายไฟล์เป็น `serial` การตายที่ข้อแรกจะบังข้อที่เหลือ ถ้าจะพิสูจน์ assertion ที่อยู่ท้าย ๆ
  ให้กรองด้วย `-g` หรือดูว่ามีกี่ข้อผ่านก่อนตัวที่ตาย
- **`list` พิมพ์บรรทัดลงท้ายด้วย `\r` บนเครื่องนี้** (Python เปิด stdout เป็น text mode บน Windows)
  ลูป shell ที่เขียนว่า `for m in $(... list)` จะได้ชื่อที่มี `\r` ติดมา แล้ว apply ทุกตัวจะตอบว่า
  *no mutant named ...* ซึ่งอ่านเหมือน MISS ทั้งแผงทั้งที่ไม่มีตัวไหนพัง — เจอตอนกวาด #32
  ให้ใส่ `| tr -d '\r'` ต่อท้ายเสมอ และถ้าเห็น MISS พร้อมกันทุกตัว ให้สงสัยลูปก่อนสงสัยโค้ด
- อย่าใช้ `| tail -20` กับผลของ Playwright — จะเห็นแค่ "1 failed" ไม่เห็นว่าตายที่บรรทัดไหน
  ให้เขียนลงไฟล์แล้ว grep หา `Error: ` กับ `> <บรรทัด> |`
