# -*- coding: utf-8 -*-
"""#84 - ปุ่มที่กดแล้วล้มเสมอ ไม่ควรถูกวาดให้กดได้

ตาราง *ข้อมูลผู้ใช้งาน* วาดปุ่ม *ระงับ* บนแถวของคนที่กำลังลงชื่อเข้าใช้อยู่ เหมือนแถวของ
คนอื่นทุกประการ ทั้งที่กดแล้วไม่มีทางสำเร็จ — `backend/routes/users.js` ปฏิเสธเสมอ
**ปุ่มที่ล้มเสมอ บอกคนใช้ว่าการกระทำนี้เป็นไปได้ ทั้งที่ไม่เป็น**

## เพิ่ม ไม่ใช่แทนที่

ADR-0002 บอกว่าการซ่อนไม่ใช่การกัน ด่านฝั่ง server ไม่ถูกแตะเลยในตั๋วนี้ และ `84a` แถวที่สาม
ยิง `PUT` ตรงไปที่ route ด้วยคุกกี้ของเบราว์เซอร์เอง ผ่านหลังปุ่มที่ปิดไป แล้ววัดว่ายังได้
`403` กับประโยคของ #83 อยู่ **แถวนั้นมีไว้ดักความคิดที่ว่า "หน้าจอกันแล้ว ลบด่านได้"**
ซึ่งเป็นความคิดที่จะเกิดขึ้นบนหน้าจอนี้ ไม่ใช่ในไฟล์เทสต์ของ backend

## ทำไมแผงบทบาทไม่อยู่ในตั๋วนี้

รูปเดียวกันอยู่บนปุ่ม *ยกเลิกบทบาท* ของ `GrantsPanel` และ **จงใจไม่แตะ** เพราะสามแถวใน
เบราว์เซอร์กดปุ่มนั้น *เพื่อให้ได้คำปฏิเสธของมัน* — `12a` แถว 6, `111a` แถว 3 และ `121a`
ซึ่งคอมเมนต์ของมันเองเขียนว่านั่นคือ *"the one refusal this panel can be given without
writing anything"* ปิดปุ่มนั้นแปลว่าต้องไปหาคำปฏิเสธใหม่ให้ #121 **ซึ่งเป็นการตัดสินใจ
ไม่ใช่การติ๊ก** จึงเป็นตั๋วของมันเอง

ปุ่ม *ระงับ* มีคุณสมบัติตรงข้าม และวัดไว้แล้วตอน #83: **ไม่มีแถวไหนใน `e2e/` กดมันบนแถว
ของตัวเอง** ซึ่งเป็นเหตุที่ `statusisjustforbidden` ฆ่า 0 แถวในเบราว์เซอร์ ปิดปุ่มนี้จึงไม่ทำ
อะไรพังเลย **ความต่างนี้เป็นตัวเลขที่วัดได้ ไม่ใช่รสนิยม และมันคือสิ่งที่แบ่งสองครึ่งนี้ออกจากกัน**

## ประโยคบนปุ่มเป็นสำเนา และเป็นสำเนาไม่ได้ไม่ได้

create-react-app ไม่ยอมให้ import ข้ามออกไปนอก `frontend/src` หน้าจอจึงอ่าน
`backend/auth/refusals` ไม่ได้เลย ประโยคใน `Users.js` จึงเป็นสำเนาของ `REFUSALS.selfStatus`
**สิ่งที่ปิดวงสำเนาไว้คือ seam ของเบราว์เซอร์ ซึ่ง import ตัวจริงได้** `84a` แถว 1 วัดว่า
`title` เท่ากับค่าคงที่ตัวนั้น สองที่จึงแยกจากกันได้ในเทสต์ที่แดง ไม่ใช่ต่อหน้าคนใช้
`titledrifts` คือมัตแตนต์ที่พิสูจน์ว่าการวัดนั้นมีจริง

## การกวาด

`cd e2e && E2E_FRONTEND_PORT=5300 npx playwright test` (372 แถว) วัดเมื่อ 10 ก.ย. 2569

| มัตแตนต์ | ฆ่า |
|---|---|
| `alwaysenabled` | **1** — `84a` แถว 1 |
| `everyrowisdead` | **3** — `84a` แถว 2, `11c` แถว 3, `13a` แถว 1/2/4 |
| `titledrifts` | **1** — `84a` แถว 1 |

**ตัวข้อบกพร่องเองฆ่าได้แถวเดียว และเป็นแถวที่เพิ่งเขียนในตั๋วนี้** ก่อนตั๋วนี้ชุดเบราว์เซอร์มี
**369** แถว และไม่มีแถวไหนแยกออกเลยว่าปุ่มบนแถวของตัวเองกดได้หรือกดไม่ได้ ซึ่งเป็นรูปเดียวกับ
ที่ #83 เจอกับ `statusisjustforbidden` เมื่อเช้าวันเดียวกัน — **แถวที่ผ่านทั้งก่อนและหลังการแก้
ไม่ได้พูดถึงการแก้นั้น**

(369 ไม่ใช่ 372 — 372 คือยอดหลังใส่สามแถวของ `84a` เข้าไปแล้ว ร่างแรกของย่อหน้านี้เขียน 372
ซึ่งเป็นการเอายอดหลังตั๋วไปเล่าเรื่องก่อนตั๋ว `/code-review` จับได้ **เลขที่คนดูแลด้วยมือในไฟล์
ที่โตทุกตั๋ว คือเลขที่จะผิด** และรอบนี้มันผิดในไฟล์ที่เขียนกฎข้อนั้นไว้เอง)

**ส่วน `everyrowisdead` ฆ่าสามแถว และสองในสามอยู่ในไฟล์ที่ไม่เกี่ยวกับตั๋วนี้เลย** `11c` แถว 3
กับ `13a` แถว 1/2/4 กดปุ่ม *ระงับ* บนแถวของ**คนอื่น** เพื่อทำงานของมันเอง การปิดทั้งคอลัมน์จึง
ล้มมันไปด้วย **นี่คือเหตุผลที่ต้องกวาดทั้งชุด ไม่ใช่เฉพาะไฟล์ที่คิดว่าเกี่ยวข้อง** — ถ้าดูแต่ `84a`
ตัวเลขนี้จะเป็น 1 และจะอ่านได้ว่าความผิดพลาดแบบ *ปิดทั้งคอลัมน์* มีแถวคุมอยู่แถวเดียว
ทั้งที่จริงมีสามแถว และสองแถวมีอยู่ในคลังมาก่อนตั๋วนี้

**และตัวเลข 3 นั้นต้องวัดสองรอบกว่าจะเชื่อได้** `13a` เป็น `mode: 'serial'` และแถวที่ตายคือ
**แถวแรกของไฟล์** อีกห้าแถวจึงไม่ได้รันเลย (3 ตาย + 364 ผ่าน + 5 ข้าม = 372 พอดี) ซึ่งตาม
กฎของ #67 แปลว่าเลขนั้นเป็น**ขอบล่าง** ไม่ใช่คำตอบ รันซ้ำด้วย `--grep-invert` ที่ชื่อแถวที่ตาย
ห้าแถวที่เหลือ**ผ่านหมด** ขอบล่างกับคำตอบจึงบังเอิญเท่ากันในรอบนี้ **การรันแล้วไม่เจออะไร
เป็นผลลัพธ์ ไม่ใช่การไม่ได้รัน** และสองอย่างนี้อ่านย้อนหลังแล้วหน้าตาเหมือนกันทุกประการ
"""
from harness import main

FILES = {"users": "frontend/src/pages/Users.js"}

MUTANTS = {
    # ตัวข้อบกพร่องเอง ปุ่มกลับมากดได้ทุกแถว รวมแถวของตัวเอง
    "alwaysenabled": ("users",
        "                          disabled={isSelf(user)}\n",
        "                          disabled={false}\n"),
    # ปิดทั้งคอลัมน์แทนที่จะปิดทีละแถว ซึ่งเป็นวิธีที่ง่ายกว่าและผิด แถวคุมของ `84a`
    # มีไว้ล้มตัวนี้ - ถ้าไม่มีมัน การปิดทุกปุ่มจะผ่านทุกข้ออ้างของตั๋วนี้
    "everyrowisdead": ("users",
        "                          disabled={isSelf(user)}\n",
        "                          disabled={true}\n"),
    # สำเนาที่เลื่อนออกจากต้นฉบับ ซึ่งเป็นสิ่งเดียวที่สำเนาทำได้และไม่มีใครเห็น
    "titledrifts": ("users",
        "const CANNOT_SUSPEND_SELF = 'ระงับบัญชีของตัวเองไม่ได้'\n",
        "const CANNOT_SUSPEND_SELF = 'ไม่สามารถระงับบัญชีนี้ได้'\n"),
}

main(FILES, MUTANTS)
