# -*- coding: utf-8 -*-
"""#150 - รายการที่มาถึงใต้ช่องที่เปิดอยู่ ไม่เอาสิ่งที่กำลังพิมพ์ไป

#148 ผูกการ seed ของ `EntrySection` ไว้กับ `entry_id` แทนที่จะผูกกับ object การโหลดใหม่ที่
สร้างรายการเดิมขึ้นมาใหม่จึงไม่เขียนทับสิ่งที่พิมพ์อยู่อีก สิ่งที่การตัดสินนั้นไม่ได้มอง คือ id ที่
เปลี่ยนจาก **ไม่มี เป็น มี** ขณะช่องเปิดอยู่ ซึ่งคือหน้าตาของการที่ save ของตัวเองเดินทางกลับมา:

กด เขียน ในหัวข้อที่ยังไม่มีอะไร พิมพ์ กด บันทึก → ฟอร์มปิด แต่การโหลดใหม่ยังไม่กลับ หัวข้อนั้น
จึงยังไม่มีรายการ และยังเสนอ เขียน อยู่ → กดอีกครั้ง ช่องเปิดบน `undefined` → พิมพ์ → การโหลดใหม่
ลง `entry_id` เปลี่ยนจาก `undefined` เป็น id → effect seed ช่องจาก `detail_text`
**สิ่งที่พิมพ์หายไป กลายเป็นสิ่งที่บันทึกไว้เมื่อครู่**

ทางแก้แยกสองการเปลี่ยนออกจากกันด้วย ref ที่จำ id ที่ draft ถูก seed มา: *มาถึง* (`undefined` → id)
ไม่ seed · *ถูกแทนที่* (id → id อื่น) ยัง seed เหมือนเดิม ค่า `CLOSED` เป็น Symbol เพราะ
`undefined` เป็นสถานะจริงที่ ref ต้องถืออยู่แล้ว - หัวข้อที่ยังไม่เคยเขียนไม่มี id - `null` จึงชนกัน

## คำถามที่ยังเปิดอยู่

ครึ่งที่สอง (คนอื่นลบหรือแทนรายการขณะช่องเปิด) ตั๋ว #150 เขียนเองว่า seed **อาจจะถูกแล้ว**
`docs/06` §Out of Scope จึงบอกว่านั่นเป็นคำถาม ไม่ใช่งาน การแก้นี้จึงไม่แตะการเปลี่ยนแบบนั้นเลย
และ `neverreseeds` ข้างล่างคือเครื่องมือวัดคำถามนั้น ไม่ใช่ตัวมัตแตนต์ธรรมดา

## มัตแตนต์

แถวของตั๋วนี้อยู่ใน `e2e/tests/150a-an-entry-arriving-under-an-open-editor.spec.js` สองแถว
แถว 1 คือรายการที่มาถึงใต้ช่องที่เปิดอยู่ · แถว 2 คือช่องที่เปิดใหม่บนหัวข้อที่ยังไม่มีอะไร ต้องว่าง

* `arrivalreseeds` - `replaced` ไม่ดู id เดิม คือ**พฤติกรรมก่อน #150** ทุกการเปลี่ยน id ขณะ
  ช่องเปิดอยู่ seed ใหม่หมด ฆ่าแถว 1 ของ `150a` แถวเดียว แถวของ `149a` ไม่ตาย ซึ่งถูก: แถวเหล่านั้น
  ไม่มีแถวไหนที่ id เปลี่ยนเลย - อันหนึ่ง id คงเดิม อีกอันเป็นหัวข้อคนละอันที่ id ยังเป็น
  `undefined` ตลอด

  กวาดซ้ำ 25 กันยายน 2569 หลัง #164 ได้ **ล้ม 1 ผ่าน 40** (`41a` `149a` `150a` `164a`)
  แถวเดิมแถวเดียวเหมือนวันแรก - สิ่งที่ #164 ย้ายคือ *เมื่อไร* effect ทำงาน ไม่ใช่ว่ามันตัดสินใจอะไร
* `neverreseeds` - `replaced` เป็นเท็จเสมอ คือ**ทางเลือกอีกทางของคำถาม** (ผูกกับ `editing`
  อย่างเดียว ไม่ seed ซ้ำเลยไม่ว่าอะไรมาถึง) เขียนเป็น `seededFrom.current === CLOSED` เพราะ
  `opening` คือการทดสอบเดียวกันนั้น `!opening && opening` จึงเป็นเท็จเสมอโดยที่ทุกชื่อยังถูกอ้าง
  **ตัวนี้มีไว้ให้รอด** และ **มันรอดจริง** วัดเมื่อ 23 กันยายน 2569: กวาดผ่านทุกใบที่วาด
  `EntrySection` ขึ้นจอ - `133a` `146a` `149a` `150a` `151a` `41a` รวม 109 แถว - ไม่ตายสักแถว
  แปลว่าวันนี้ไม่มีแถวไหนแยกสองคำตอบของคำถามออกจากกันได้ ซึ่งเป็นการวัดที่คำถามต้องการ
  ไม่ใช่ความบกพร่องของแถว - ถ้าเจ้าของงานเลือกทางนี้ การแก้คือลบ `replaced` ทิ้ง
  และแถวที่ต้องเขียนเพิ่มคือแถวของการถูกแทนที่

  กวาดซ้ำ 25 กันยายน 2569 หลัง #164 กับ `164a` ที่เพิ่งเกิดด้วย - `41a` `149a` `150a` `164a`
  **41 แถว ไม่ตายสักแถว** คำถามจึงยังไม่มีแถวไหนแยกคำตอบได้เหมือนเดิม และ `164a` ก็แยกไม่ได้:
  มันถามว่าช่องถูกวาดพร้อมอะไร ไม่ได้ถามว่าอะไรควรถูก seed ลงไป
* `closekeepstheid` - ปิดช่องแล้วไม่คืน ref เป็น `CLOSED` การเปิดใหม่จึงไม่นับเป็น *opening*
  หัวข้อที่ยังไม่มีรายการ (id เป็น `undefined` ทั้งก่อนและหลัง) จะไม่ถูก seed ตอนเปิดใหม่ ช่องจึง
  ยังค้างสิ่งที่พิมพ์ทิ้งไว้รอบก่อน **ฆ่าแถว 2 ของ `150a` แถวเดียว** เก้าแถวของ `41a` รอดหมด

  กวาดซ้ำ 25 กันยายน 2569 หลัง #164 ได้ **ล้ม 2 ผ่าน 39** (`41a` `149a` `150a` `164a`) แถวที่
  เพิ่มมาคือแถวเดียวของ `164a` ซึ่งยังไม่มีอยู่ตอนวัดครั้งแรก มัตแตนต์ตัวนี้ทำให้ช่องค้างข้อความเก่า
  **ทุกเฟรม** ไม่ใช่เฟรมเดียว จึงตายทั้งที่การอ่านครั้งเดียวและที่การสุ่มตัวอย่างรายเฟรม

  ใบนี้เคยเขียนไว้ว่ามันฆ่าแถวที่ 3 ของใบ #41 **ซึ่งผิด และการกวาดเป็นผู้บอก** แถวนั้นเปิดช่องซ้ำ
  บนรายการที่ตัวเองเพิ่งบันทึก draft ที่ค้างอยู่จึงเท่ากับ `detail_text` ที่ควรถูก seed ลงไปพอดี
  สองค่าตรงกัน มัตแตนต์จึงมองไม่เห็น (#154 - ค่าที่แทนที่ด้วยค่าปริยายมองไม่เห็นทุกที่ที่สองค่าเท่ากัน)
  แถว 2 จึงถูกเขียนขึ้นที่จุดเดียวที่สองค่า **ต่าง** กัน คือหัวข้อที่ยังไม่มีอะไร: ค่าที่ควร seed คือ
  ว่าง ส่วนค่าที่ค้างคือสิ่งที่พิมพ์แล้วกดยกเลิก - บรรทัดที่ #150 เพิ่มเข้ามาต้องมีมัตแตนต์ของตัวเอง
  ไม่งั้นมันเป็นคำกล่าวอ้างที่ไม่มีใครค้ำ

## ไฟล์ที่ใช้ร่วมกัน

`frontend/src/components/improvement/EntrySection.js` มี `41` `149` และ `164` ถืออยู่แล้ว
**ห้ามกวาดใบนี้พร้อมกับสามใบนั้น**

ร่างแรกของบรรทัดนี้เขียนว่า `146` ถืออยู่ด้วย ซึ่ง**ผิด** `146` เอ่ยชื่อ `EntrySection` ไว้ในคำอธิบาย
ของมันบรรทัดเดียว แต่ `FILES` ของมันไม่มี path นี้ - **สำมะโนอ่าน `FILES` ไม่ได้อ่านชื่อ** และ
คำถามคือ path ที่ทับกัน ไม่ใช่เรื่องที่พูดถึงกัน (#105 พลาดรูปเดียวกันนี้คนละทาง)

`41:editorstartsempty` เคยเกาะบรรทัด `if (editing) setDraft(...)` ที่ตั๋วนี้เขียนใหม่ จึงถูกเล็งใหม่
ไปที่ `if (opening || replaced) setDraft(...)` ซึ่งมีความหมายเดิม และกวาดซ้ำในวันเดียวกัน

## วิธีรัน

    python mutation/150-entry-arriving-under-an-open-editor.py save
    python mutation/150-entry-arriving-under-an-open-editor.py arrivalreseeds
    cd e2e && E2E_FRONTEND_PORT=5300 npx playwright test 150a-an-entry --reporter=line
    python mutation/150-entry-arriving-under-an-open-editor.py restore

ชื่อไฟล์เต็ม `150a-an-entry` ไม่ใช่ `150` เฉย ๆ - Playwright จับแบบ substring (#158)
"""

from harness import main

FILES = {
    'entry': 'frontend/src/components/improvement/EntrySection.js',
}

REPLACED = "    const replaced = !opening && seededFrom.current !== undefined\n"
RESET = "      seededFrom.current = CLOSED\n"

MUTANTS = {
    'arrivalreseeds': ('entry', REPLACED, "    const replaced = !opening\n"),
    'neverreseeds': ('entry', REPLACED,
                     "    const replaced = !opening && seededFrom.current === CLOSED\n"),
    'closekeepstheid': ('entry', RESET, ''),
}

main(FILES, MUTANTS)
