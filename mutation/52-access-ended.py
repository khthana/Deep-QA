# -*- coding: utf-8 -*-
"""#52 - บัญชีที่ใช้ต่อไม่ได้กลางคัน ถูกทิ้งไว้บนหน้าจอ

`sessionAdmission` ปฏิเสธคำขอถัดไปของบัญชีที่ถูกระงับกลางคันมาตั้งแต่ #11 (`11c` พิสูจน์ไว้)
สิ่งที่ขาดคือ**คนที่ปลายทางของคำปฏิเสธนั้น** ถ้ากดเมนู เขาได้แถบแดงบนหน้าจอเดิมที่ใช้ต่อไม่ได้แล้ว
ถ้าพิมพ์ที่อยู่เอง เขาถูกส่งกลับ `/` **โดยไม่มีอะไรบอกเลย** เพราะคำปฏิเสธของ `GET /api/me`
ถูกอ่านว่า *ไม่มีใครลงชื่อเข้าใช้* ข้อหลังนี้ไม่อยู่ในตัวตั๋ว การเดินเจอและเขียนไว้ในคอมเมนต์ของมัน

## ทำไมเป็น field ไม่ใช่รายการที่ client จำไว้

`403` ของ route นี้มีสองความหมาย และคนหน้าจอต้องทำสองอย่างตรงข้ามกัน: *บทบาทของคุณไม่ถึง
ส่วนนี้* คือหน้าจอยังใช้ได้ อยู่ที่เดิม (เกณฑ์ข้อหกของ #10) ส่วน *บัญชีนี้ใช้ต่อไม่ได้แล้ว* คือไม่มี
หน้าจอไหนเหลือให้อยู่ ทางที่ client จะแยกได้มีสามทาง และสองทางแรกคือสิ่งที่ตั๋วห้าม:

- จับคำภาษาไทย — ประโยคเป็นของ `refusals.js` และถูกแก้ถ้อยคำมาแล้วหลายรอบ (#89 แยก
  `outsideValidity` เป็นสองประโยค) ประโยคที่แก้วันไหนก็ได้ ไม่ใช่สัญญาณ
- รายการ `reason` ที่ client จำไว้ — เหตุผลใหม่ที่ `sessionAdmission` เพิ่มวันหน้าจะไม่อยู่ใน
  รายการนั้น และ client จะทิ้งคนไว้บนหน้าจออีกครั้งโดยไม่มีเทสต์ไหนแดง นี่คือรายการที่คนดูแลด้วย
  มือ ในที่ที่ห่างจากกฎที่มันคัดลอกมาที่สุด
- **`accessEnded: true` ที่ server ตั้งเอง ณ จุดที่ตัดสินใจ** — `endAccess` ใน `authorise.js` คือ
  ทางเดียวที่ `attachRoles` ใช้ปฏิเสธคนที่ถือคุกกี้ดีอยู่ เหตุผลใหม่ที่ผ่านทางนั้นได้ธงไปเอง

## ข้อที่ตั๋วไม่ได้ระบุ: บัญชีที่ไม่เหลือบทบาท

ตั๋วเอ่ยเฉพาะเหตุผลของ `sessionAdmission` แต่ `attachRoles` มีทางปฏิเสธที่สามอยู่ข้างๆ คือ
`noRole` บัญชีที่ถูกถอนบทบาทสุดท้ายไปกลางคัน **สำหรับคนหน้าจอ สองอย่างนี้คือสถานะเดียวกัน**:
ทุกคำขอถูกปฏิเสธ ไม่มีเมนูไหนพาไปไหนได้ มันจึงได้ธงด้วย `noroleisordinary` คือมัตแตนต์ที่ถอนมัน
ออก และฆ่า**ข้อเดียวที่ HTTP seam และไม่มีอะไรเลยในเบราว์เซอร์** — ไม่มีแถวไหนขับกรณีนี้ ข้อนั้น
จึงเป็น ☑ ไม่ใช่ ⚙ และเขียนไว้ตรงนี้เพื่อไม่ให้ใครอ่าน 0 ว่าเป็นช่องโหว่ที่ถูกลืม

## ออกจากระบบจริง และสิ่งที่มันทิ้งไว้ในประวัติ

การพากลับหน้าเข้าสู่ระบบโดยเก็บคุกกี้ไว้ คือการซ่อน ไม่ใช่การจบ: บัญชีที่ถูกคืนสถานะจะเดินกลับเข้า
ไปได้ในฐานะใครก็ตามที่ถือคุกกี้นั้น `AuthContext` จึงเรียก `POST /api/auth/logout` ซึ่งอยู่ก่อนด่าน
และลบคุกกี้ที่ตายแล้วได้ `cookiekept` ฆ่า `52a` แถว 4 **ที่บรรทัดสุดท้ายของมัน** (158) และแถว 5–6
ซึ่งนับการออกจากระบบ

ผลข้างเคียงที่ตั้งใจรับไว้: route นั้นบันทึก `LOGOUT` ลงประวัติของบัญชีที่อ่านได้จากคุกกี้
(`accountInDeadCookie`) บัญชีที่ถูกระงับแล้วเบราว์เซอร์ออกจากระบบให้เอง จึงมี `LOGOUT` หนึ่งแถวใน
ประวัติของตัวเอง — ตามจริง session นั้นจบลงตรงนั้นจริงๆ แต่คนอ่านประวัติจะเห็นแถวเดียวกับที่คนกด
*ออกจากระบบ* เอง `13a` แถว 3 (*nothing about the suspension appears in the suspended account's own
history*) ยังเขียว เพราะแถวนั้นไม่ได้เปิดเบราว์เซอร์ของคนที่ถูกระงับไว้

## สิ่งที่รีวิวเจอ: *หนึ่งแถว* เป็นจริงเฉพาะหน้าจอที่ขอทีละอย่าง

ประโยคข้างบนเขียนก่อนรีวิว และจริงเฉพาะหน้าจอที่ขอสิ่งเดียว หน้าจอที่ขอสามอย่างพร้อมกันได้คำปฏิเสธ
สามครั้ง แต่ละครั้งเรียก `logout` และแต่ละครั้งบนคุกกี้ที่ยังมีชีวิตบันทึก `LOGOUT` — **การจบครั้งเดียว
อ่านในประวัติเป็นการออกจากระบบสามครั้งที่คนไม่ได้กด** รีวิวสองแกนเจอข้อนี้ ไม่ใช่การกวาด เพราะไม่มี
แถวไหนนับ `AuthContext` จึงได้ ref ที่ทำให้ออกครั้งเดียว และ `52a` แถว 5 นับคำขอ (*ข้อมูลนักศึกษากลาง*
ขอรายการกับตัวเลือกสองชุดพร้อมกัน) `signsoutpereach` ถอนมันและฆ่าแถว 5 แถวเดียว

**ref ที่ทำให้ครั้งเดียวคือข้อบกพร่องถัดไป ถ้าไม่มีที่คืนค่า** ไม่คืนค่า มันคือครั้งเดียว*ต่อแท็บ* ไม่ใช่ต่อ
การจบ บัญชีที่จบ ลงชื่อเข้าใหม่ผ่านฟอร์มในแท็บเดิม (ไม่มีการโหลดหน้าใหม่ ref จึงยังอยู่) แล้วจบอีกครั้ง
จะถูกวาดหน้าเข้าสู่ระบบทับคุกกี้ที่เก็บไว้ — `cookiekept` ในรูปที่มาทีหลัง ดังนั้น `load()` ที่สำเร็จจึงคืนค่ามัน
และ `52a` แถว 6 ขับลำดับนั้นทั้งหมด `onceonly` ถอนการคืนค่าและฆ่าแถว 6 แถวเดียว ทั้งสองไม่ฆ่าอะไรใน
`11b` `11c` `12a` ซึ่งเป็นสเปกอื่นทั้งหมดที่ทำให้บัญชีใช้ต่อไม่ได้ระหว่างที่ลงชื่อเข้าอยู่ (หาด้วย grep
`'inactive'`, `valid_until`, `is_active = false` ใน `e2e/tests/`) **guard ที่บอกว่า *ครั้งเดียว* ต้องบอก
ด้วยว่าครั้งเดียวต่ออะไร และที่คืนค่าคือที่ที่มันบอก**

`load()` ที่สำเร็จล้าง `endedBecause` ด้วย ประโยคนั้นเป็นของช่วงที่ไม่มีใครลงชื่อเข้าอยู่ และถ้าไม่ล้าง
หน้าเข้าสู่ระบบที่มาถึงทีหลังในแท็บเดิมจะแสดงมันกับคนที่บัญชีปกติดี **ข้อนี้ไม่มีอะไรพิสูจน์** ทางเดียวที่
ไปถึงคือ `load()` ล้มหลังการลงชื่อเข้าใหม่ในแท็บเดิม ซึ่งนอกจากหน้าเข้าสู่ระบบเองมีผู้เรียกเดียวคือ
`SubjectStudents` และต้องให้
`/api/me` ล้มด้วยเหตุอื่นที่ไม่ใช่การจบ จึงไม่มีมัตแตนต์ของมันในใบนี้ — ในสามคำตอบ (พิสูจน์แล้ว,
ไปไม่ถึง, ไม่มีเทสต์) ข้อนี้คือข้อที่สาม

แถว 4 ต้องรอการออกจากระบบก่อน `goto` เพราะ `goto` ยกเลิกคำขอที่ยังไม่เสร็จได้ รอบแรกเขียนเป็น
`await` ธรรมดา แล้วการกวาดบอกว่า `cookiekept` ฆ่าแถว 4 **ที่บรรทัดรอ** ไม่ใช่ที่บรรทัดสุดท้าย —
การรอกลายเป็นข้ออ้างของแถวไปเงียบๆ ตอนนี้มันรอได้ห้าวินาทีแล้วปล่อยผ่าน และมัตแตนต์ตายที่บรรทัด 158
ซึ่งเป็นข้ออ้างของแถวนั้นจริง **ของที่เขียนเพื่อจังหวะต้องไม่ตัดสินอะไร** — ถ้ามันล้มได้ มันคือ assertion

## การแก้ที่ง่ายกว่าและผิด

`everyrefusalends` คือ *403 ทุกตัวคือการจบ session* บรรทัดเดียว ไม่ต้องแตะ server เลย และผ่าน
ทุกแถวของ `52a` สิ่งที่ล้มมันคือแถวในไฟล์อื่นที่คาดว่า 403 ธรรมดาจะอยู่ในที่ เช่น `10a`
แถว 6 ซึ่งลงชื่อเข้าเป็นครูแล้วเปิดหน้าจอที่ครูถูกปฏิเสธ — ใต้มัตแตนต์ เบราว์เซอร์ออกจากระบบให้ แล้ว
แถวนั้นหาคุกกี้ไม่เจอ จำนวนที่วัดได้อยู่ในตารางข้างล่าง

## สิ่งที่การกวาดเจอนอกตั๋ว

**แถวที่ใบนี้บอกว่าไม่ต้องแก้ คือแถวเดียวที่แดง** `docs/acceptance/11-user-accounts.md` เขียนไว้
ว่า `11c` ไม่ได้ผูกกับพฤติกรรมนี้ จึงไม่ต้องแก้เมื่อ #52 ถูกปิด ชุดเต็มหลังการแก้ได้ 375/376 และแถว
ที่แดงคือ `11c` แถว 3 ที่**บรรทัดเก็บกวาด** ซึ่งถาม `/api/me` บนคุกกี้เดิมหลังคืนสถานะ — คุกกี้ที่
#52 ลบไปแล้ว การกวาดมัตแตนต์ตอบไม่ได้ว่าการแก้ทำอะไรพัง มีแต่ชุดสะอาดที่ตอบได้ (#83)

**ตัวเลขของ `everyrefusalends` รอบแรกเป็น 171 และเป็นเท็จ** `12a` แถว 2–3 ให้บทบาทกรรมการ
หลักสูตรกับ `teacher.one@` ก่อนการยืนยันแรกฝั่งเบราว์เซอร์ และถอนคืนหลังการยืนยันสุดท้าย มัตแตนต์ที่
ล้มมันตรงกลางจึงทิ้งบทบาทนั้นไว้ `actingFrom` เลือกบทบาทที่สูงกว่า และ**ทุกแถวของครูในทุกไฟล์หลัง
จากนั้น**ถูกปฏิเสธในฐานะกรรมการ รูปเดียวกับที่ #89 เจอใน fixture ฝั่ง backend: แถวที่คืนสิ่งที่ย้าย
ไปเฉพาะเมื่อมันผ่าน ทำให้ตัวเลขของมัตแตนต์ถัดไปพอง `12a` จึงได้ `afterAll` ที่ถอนบทบาทนั้นทาง
ฐานข้อมูล และตัวเลขในตารางวัดหลังจากนั้น

**การรันเขียวรอบแรกแดงเพราะจังหวะ ไม่ใช่เพราะโค้ด** `signIn` คืนค่าเมื่อที่อยู่นิ่งแล้ว ซึ่งก่อน
รายการของหน้าแรกจะกลับมา รายการที่กลับมา*หลัง*การระงับถูกปฏิเสธ และตั้งแต่ #52 มันพาคนไปหน้า
เข้าสู่ระบบก่อนที่แถวจะกดอะไร ภาพตอนล้มคือหน้าเข้าสู่ระบบพร้อมประโยคที่ถูกต้อง แต่ลิงก์เมนูที่แถวจะกด
หายไปแล้ว `settledAsDepartmentAdmin` ให้แถวนิ่งบนหน้าจอหนึ่งก่อน **พฤติกรรมใหม่ที่ทำงานเร็วกว่า
ที่เทสต์คาด ก็คือพฤติกรรมใหม่ที่เทสต์เก่ายังไม่รู้จัก**

## การกวาด

`cd backend && node --test "test/*.test.js"` (738 ข้อ) และ
`cd e2e && E2E_FRONTEND_PORT=5300 npx playwright test` (376 แถว) วัดเมื่อ 11 ก.ย. 2569
ก่อนการแก้ `52a` แดงครบสี่แถว

แถว 5–6 และมัตแตนต์สามตัวล่าง (`forbidends` `signsoutpereach` `onceonly`) มาจากรีวิว หลังจากนั้น
มัตแตนต์ฝั่ง client ทุกตัวถูกวัดใหม่บน `52a` ทั้งหกแถว สองตัวใหม่วัดบน `11b` `11c` `12a` `52a` และ
`forbidends` บนชุด backend เต็ม ส่วนที่เหลือของชุดเบราว์เซอร์ใช้ตัวเลขจากการกวาดเต็มก่อนหน้า
เพราะนอก `52a` ไม่มีอะไรเปลี่ยน — `notflagged` และ `flagignored` คือโค้ดก่อน #52 ครึ่งละฝั่ง และฆ่า
แถวใหม่ทั้งสองด้วย ชุดเต็มบนโค้ดสุดท้าย: backend 738/738 เบราว์เซอร์ **378/378** รอบก่อนหน้านั้นได้
375/376 โดย `30a` แถว 4 หมดเวลารอการอ่านแรกของหน้าจอ — รอบนั้นวิ่งอยู่ตอนแก้ `AuthContext.js` และ
frontend ของ e2e เป็น dev server ที่คอมไพล์ใหม่ทุกครั้งที่ไฟล์เปลี่ยน **อย่าแก้ `frontend/src` ระหว่าง
ที่ชุดเบราว์เซอร์วิ่ง** รอบนั้นไม่ใช่ทั้งโค้ดเก่าและไม่ใช่โค้ดใหม่

| มัตแตนต์ | backend | เบราว์เซอร์ |
|---|---|---|
| `notflagged` | **3** — *says so when the account was suspended while signed in*, *and when an assessor’s window closed while they were signed in*, *and when the account was left holding no grant* | **6** — `52a` แถว 1–6 |
| `noroleisordinary` | **1** — *and when the account was left holding no grant* | **0** |
| `forbidends` | **5** — *but not when the endpoint is merely not for this role* และสี่ข้อที่เทียบตัวคำปฏิเสธทั้งก้อน: *names neither internal details nor other users* (the role guard), *refuses the same way the role guard does* (the scope guard), *is refused at the route as well as hidden from the sidebar*, *is refused the screen entirely by a role that is not an administrator* | ไม่ได้กวาด |
| `doorendstoo` | **1** — *nor when a suspended account is refused at sign-in* | **0** |
| `flagignored` | — | **6** — `52a` แถว 1–6 |
| `everyrefusalends` | — | **13** — `10a` แถว 6 (สามข้อ) และแถว 7, `111a` แผงบทบาท, `121a`, `12a` *rows 2 and 3* (เทสต์เดียว) และแถว 6, `16a` แถว 75, `18c` แถว 8, `19a` และ `21a` แถวที่บัญชีที่หน้าจอไม่ได้มีไว้ให้ถูกปฏิเสธ, `24a` แถว 7 |
| `cookiekept` | — | **3** — `52a` แถว 4 ที่บรรทัด 158, แถว 5 (ออก 0 ครั้ง), แถว 6 |
| `leftonscreen` | — | **5** — `52a` แถว 1, 2, 4, 5, 6 |
| `saysnothing` | — | **3** — `52a` แถว 1, 2, 3 |
| `signsoutpereach` | — | **1** — `52a` แถว 5 · ไม่มีอะไรใน `11b` `11c` `12a` |
| `onceonly` | — | **1** — `52a` แถว 6 · ไม่มีอะไรใน `11b` `11c` `12a` |

`everyrefusalends` ยังผ่าน `52a` ครบหกแถวหลังเพิ่มแถว 5–6

**13 ของ `everyrefusalends` วัดสองชั้น** ชุดเต็มรายงาน 13 ตาย + 13 ไม่ได้รัน เพราะ `12a` `16a`
`19a` เป็น `mode: 'serial'` (#67) สิบสามแถวที่ถูกข้ามรันใหม่ใต้มัตแตนต์ด้วย `--grep-invert` ที่ชื่อ
แถวที่ตาย ได้ `12a` แถว 6 ตายเพิ่มหนึ่ง (การยกเลิกบทบาทของตัวเองถูกปฏิเสธ แล้วเบราว์เซอร์ออกจาก
ระบบให้) อีกสิบสองผ่าน และมี `44a` *reads one column per year* ที่ตายในชุดเต็มแต่**ไม่นับ** — ดูข้างล่าง
ทุกแถวที่นับตายด้วยเหตุเดียวกัน: 403 ธรรมดาที่ควรอยู่ในที่ พาคนออกจากระบบ

**`44a` ไม่ใช่การฆ่า และตรงนี้เขียนไว้เพราะมันดูเหมือน** แถวนั้นล้มด้วย `columnheader` 8 แทน 4 ในชุด
เต็มใต้ `notflagged` และใต้ `everyrefusalends` ทั้งสองรอบ แต่ `notflagged` ไม่เปลี่ยนอะไรฝั่ง client เลย
(มันคือโค้ดก่อน #52) และแถวเดียวกันล้มแบบเดียวกันในชุดสะอาดรอบแรกด้วย รันเดี่ยวใต้ `notflagged`
ด้วย `--repeat-each=3` ผ่าน 21/21 **ล้มสี่ในสิบเอ็ดรอบของชุดเต็มที่รันวันนี้ ไม่ขึ้นกับมัตแตนต์** (สองในสี่รอบนั้นเป็นรอบที่ปนเปื้อน —
ชุดสะอาดรอบแรก และ `everyrefusalends` รอบที่ได้ 171 — จึงเป็นหลักฐานที่อ่อน แต่อีกสองรอบไม่ใช่) จึงเป็นแถวที่
ไม่นิ่งในบริบทของชุดเต็ม ไม่ใช่ข้ออ้างของใบนี้ และไม่ถูกนับในตารางไหน

ต้นเหตุวัดแล้วในวันเดียวกันและเขียนไว้ใน [#129](https://github.com/khthana/Deep-QA/issues/129):
8 คือรายงานของช่วง `2561..2566` ที่ `showRange` ยิงระหว่างทาง บนทะเบียนที่ `17b` กับ `17c`
ทิ้งปี 2561 และ 2568 ไว้ คำตอบนั้นมาถึงช้าแล้วเขียนทับรายงานที่ขอ เพราะ `load` ของ
`ProgramLevelCompare.js` ไม่ทิ้งคำตอบที่ล้าสมัย สเปกทิ้งขว้างที่หน่วงเฉพาะคำตอบนั้นได้ 8
ขณะที่ตัวเลือกบอก `2565..2566` (ไม่หน่วงได้ 4) **แถวนี้ไม่ได้ไม่นิ่ง มันจับข้อบกพร่องของหน้าจอได้เป็นบางรอบ**

(backend นับจากบรรทัด `not ok` ที่ย่อหน้า ข้อทั้งหมดอยู่ใต้ *a refusal that ends the session*
ซึ่งเป็น suite แม่ และนับใน `# fail` ด้วย — `notflagged` จึงอ่าน `# fail 4` ได้สามข้อ)

**`leftonscreen` ไม่ฆ่าแถว 3 และถูกแล้ว** แถว 3 เป็นการโหลดใหม่ทั้งหน้า `load()` ล้างสถานะเอง
อยู่แล้วตั้งแต่ก่อน #52 และคนถูกพาไปหน้าเข้าสู่ระบบอยู่แล้ว สิ่งที่แถวนั้นขาดคือประโยค ซึ่งเป็นข้อ
อ้างของ `saysnothing` ในทางกลับกัน `saysnothing` ไม่ฆ่าแถว 4 เพราะแถวนั้นไม่ได้อ่านประโยคเลย
`flagignored` `cookiekept` `leftonscreen` `saysnothing` `signsoutpereach` `onceonly` ฆ่าชุดแถวที่ไม่
ซ้ำกันเลยสักคู่ ข้ออ้างหกข้อของฝั่ง client — *ฟังธง*, *ลบคุกกี้*, *พาออกจากหน้าจอ*, *บอกเหตุผล*,
*ออกครั้งเดียว*, *ครั้งเดียวต่อการจบ* — จึงล้มแยกกันได้ ซึ่งคือสิ่งที่ทำให้ ⚙ ของแต่ละแถวชี้ไปที่ข้ออ้าง
ของแถวนั้นเอง ไม่ใช่ที่ทั้งไฟล์
"""
from harness import main

FILES = {
    "authorise": "backend/auth/authorise.js",
    "auth": "backend/routes/auth.js",
    "client": "frontend/src/api/client.js",
    "context": "frontend/src/context/AuthContext.js",
    "login": "frontend/src/pages/Login.js",
}

MUTANTS = {
    # ตัวข้อบกพร่องเองฝั่ง server: คำปฏิเสธที่จบ session ไม่บอกว่ามันจบ
    "notflagged": ("authorise",
        "  res.status(403).json({ message: REFUSALS[reason], reason, accessEnded: true });\n",
        "  res.status(403).json({ message: REFUSALS[reason], reason });\n"),
    # บัญชีที่ไม่เหลือบทบาทเลย กลับไปเป็นคำปฏิเสธธรรมดา - ข้อที่ตั๋วไม่ได้ระบุ
    "noroleisordinary": ("authorise",
        "      if (roles.length === 0) return endAccess(res, 'noRole');\n",
        "      if (roles.length === 0) return res.status(403).json({ message: REFUSALS.noRole });\n"),
    # ธงหลุดไปอยู่ที่ด่านบทบาท: 403 ธรรมดาที่ควรอยู่ในที่ อ่านเป็นการจบ - ฝั่ง server ของ everyrefusalends
    "forbidends": ("authorise",
        "const forbid = (res) => res.status(403).json({ message: REFUSALS.forbidden });\n",
        "const forbid = (res) => res.status(403).json({ message: REFUSALS.forbidden, accessEnded: true });\n"),
    # ธงหลุดไปอยู่ที่ประตู: การลงชื่อเข้าใช้ที่ถูกปฏิเสธ ไม่มี session ให้จบ
    "doorendstoo": ("auth",
        "          .json({ message: admission.message, reason: admission.reason });\n",
        "          .json({ message: admission.message, reason: admission.reason, accessEnded: true });\n"),
    # ตัวข้อบกพร่องเองฝั่ง client: ธงมาถึงแต่ไม่มีใครฟัง
    "flagignored": ("client",
        "    if (payload.accessEnded === true) accessEndedListener?.(payload.message)\n",
        ""),
    # การแก้ที่ง่ายกว่าและผิด: 403 ทุกตัวคือการจบ session ซึ่งทำลายเกณฑ์ข้อหกของ #10
    "everyrefusalends": ("client",
        "    if (payload.accessEnded === true) accessEndedListener?.(payload.message)\n",
        "    if (response.status === 403) accessEndedListener?.(payload.message)\n"),
    # พากลับหน้าลงชื่อเข้าใช้ แต่เก็บคุกกี้ไว้
    "cookiekept": ("context",
        "      ended.current = true\n      post('/api/auth/logout').catch(() => {})\n",
        "      ended.current = true\n"),
    # ลบคุกกี้ แต่ปล่อยคนไว้บนหน้าจอเดิม
    "leftonscreen": ("context",
        "      setEndedBecause(message)\n      setState(null)\n",
        "      setEndedBecause(message)\n"),
    # ออกจากระบบครั้งต่อคำปฏิเสธ ไม่ใช่ครั้งต่อการจบ - ประวัติได้ LOGOUT หลายแถว
    "signsoutpereach": ("context",
        "      if (ended.current) return\n",
        ""),
    # ครั้งเดียวต่อแท็บ ไม่ใช่ครั้งเดียวต่อการจบ - การจบครั้งที่สองเก็บคุกกี้ไว้
    "onceonly": ("context",
        "      setEndedBecause(null)\n      ended.current = false\n",
        "      setEndedBecause(null)\n"),
    # พากลับไปแล้ว แต่ไม่บอกว่าทำไม - ทางเงียบที่การเดินของ #52 เจอ
    "saysnothing": ("login",
        "    if (endedBecause) setErrorMessage(endedBecause)\n",
        "    if (endedBecause) setErrorMessage('')\n"),
}

main(FILES, MUTANTS)
