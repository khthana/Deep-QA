# -*- coding: utf-8 -*-
"""#129 - รายงานของช่วงที่ตัวเลือกออกมาแล้ว ต้องไม่ทับรายงานของช่วงที่ตัวเลือกอยู่

`ProgramLevelCompare` ยิงคำขอหนึ่งครั้งต่อปลายหนึ่งข้าง การย้ายปลายสองข้างจึงมีคำขอ
กลางทางที่ **เก่าไปครึ่งหนึ่ง** - `to` ย้ายแล้ว ส่วน `from` ยังเป็นค่าเดิม และไม่มีอะไรผูก
คำตอบเข้ากับคำขอที่ทำให้มันเกิด `load` เขียน `setData(await ...)` ตรง ๆ **คำตอบที่มาถึง
ทีหลังจึงชนะ ไม่ใช่คำตอบที่ขอทีหลัง** คนที่เปลี่ยนปลายสองข้างเร็ว ๆ บนเน็ตช้าจึงอ่าน
รายงานของช่วงที่ตัวเลือกไม่ได้แสดงอยู่ ซึ่ง docstring ของไฟล์เดียวกันเรียกตรงตัวว่า
*"showing a report about one range while saying another"*

## ตั๋วนี้ถูกวินิจฉัยผิดสองรอบ ก่อนจะวัดกลไกจริง

รอบแรกอ่านว่าเป็นสเปกที่ไม่นิ่ง (`44a` แถวแรกได้ 8 คอลัมน์ในชุดเต็ม และ 7/7 เมื่อรันเดี่ยว)
รอบที่สองอ่านว่าเป็นข้อมูลที่ `17b` ทิ้งไว้ ทั้งสองครึ่งจริง แต่ไม่ใช่ข้อบกพร่อง: การ query
ทะเบียนหลังชุดเต็มบอกว่า `17c` รั่วด้วย (ปี 2561) และ **8 ไม่ใช่ช่วงที่กว้างขึ้น มันคือ
รายงานของช่วงที่ไม่ได้ขอ** ที่มาถึงช้ากว่าและทับของจริง **สเปกที่ล้มเฉพาะในชุดเต็ม ยังไม่ใช่
สเปกที่ไม่นิ่ง จนกว่าจะวัดกลไก** - แถวนั้นเป็นแถวเดียวที่ข้อมูลของมันไปถึงข้อบกพร่องได้

## แถวที่พิสูจน์ ต้องสร้างสถานการณ์เอง

บนทะเบียนของ seed ช่วงที่หน้าจอเปิดมาคือช่วงที่แถวเหล่านี้ขออยู่แล้ว `showRange` จึงคืนค่า
ทันทีโดยไม่ยิงคำขอ ไม่มีการแข่งกันเลย และนั่นคือเหตุที่รันไฟล์เดี่ยวผ่านทุกครั้ง สองแถวของ
#129 จึง **ใส่นักศึกษาสองคนคนละปลาย** (สองปีก่อน seed และสองปีหลัง) เพื่อให้ช่วงเปิดหน้าจอ
กว้างกว่าช่วงที่ขอทั้งสองข้าง แล้ว **หน่วงคำตอบด้วย `page.route`** ผลลัพธ์จึงเป็นข้อเท็จจริง
เรื่องโค้ด ไม่ใช่เรื่องเน็ต - รูปเดียวกับที่ #121 ใช้ย่อหน้าต่างแทนการเขียนข้อมูลลง schema
ที่สเปกอื่นใช้ร่วมกัน

## ทำไมถึงเป็นใบของตัวเอง ไม่ใช่ของ `44`

ใบ `44` ถือหน้าจอเดียวกัน และ path นี้จึงเป็น path ที่ชนตั้งแต่รอบนี้ **ห้ามกวาด `129`
พร้อมกับ `44`** (รายการนี้สร้างจาก `FILES` ด้วยสคริปต์ใน `mutation/README.md` ไม่ใช่จาก
การอ่าน) มัตแตนต์อยู่กับตั๋วที่เขียนมัน ส่วนแถว ⚙ อยู่บนใบตรวจรับของหน้าจอ ตามกฎของ #97

## สองด้านของผู้คุ้มกันเดียวกัน และตัวหนึ่งกว้างกว่าอีกตัว

`staleanswerwins` คือตัวข้อบกพร่องเอง - คำตอบที่ล้าสมัยวาดทับ `staleclearsloading` คือ
ด้านที่อยู่ถัดไปหนึ่งสถานะ: คำตอบที่ไม่มีใครรออีกแล้วปิดสถานะ *กำลังโหลด* ของคำขอที่ยัง
ไม่กลับ ซึ่งเห็นได้เฉพาะตอนที่ยังไม่มีรายงานบนจอ (`loading && !data`) แถวที่สองของ #129
จึงเป็นแถวที่แทนที่คำขอ**แรก**ก่อนมันจะตอบ แล้ววัดว่าจอยังบอกว่ากำลังโหลดอยู่ - ถ้าไม่มี
แถวนี้ ครึ่งนี้จะเป็นข้อที่สามของ #107 (ไม่มีอะไรทดสอบ) มากกว่าจะเป็นข้อที่หนึ่ง

ผู้คุ้มกันมีด้านที่สามคือ cleanup ของ effect (`current = false`) ซึ่งถอดออกแล้วได้ผลเท่ากับ
ถอดสองตัวข้างบนพร้อมกัน จึงไม่เขียนเป็นมัตแตนต์ตัวที่สาม - มันฆ่าแถวชุดเดียวกันและไม่ได้
พูดถึงข้ออ้างใหม่

## การกวาด

`cd e2e && E2E_FRONTEND_PORT=5300 npx playwright test` (380 แถว) กวาดเมื่อ 12 ก.ย. 2569
ตารางนี้บันทึก **สิ่งที่มัตแตนต์ฆ่าได้จริง** ไม่ใช่สิ่งที่ควรจะฆ่าได้

| มัตแตนต์ | แถวที่มันค้ำ | ตายที่ |
|---|---|---|
| `staleanswerwins` | รายงานเป็นของช่วงที่ตัวเลือกแสดงอยู่ | แถว 8 ที่การนับหัวคอลัมน์ - ได้ 8 ในช่วงที่ขอไว้ 4 · **และฆ่าแถว 9 ไปด้วย** เพราะคำตอบที่ล้าสมัยวาดรายงานลงไป ตัวหมุนที่วาดเฉพาะตอนยังไม่มีรายงานจึงหายไป |
| `staleclearsloading` | คำตอบที่ไม่มีใครรอ ไม่ปิดสถานะกำลังโหลด | แถว 9 ที่การนับ *กำลังโหลดข้อมูล…* - ได้ 0 ที่ควรเป็น 1 · **แถว 8 ยังผ่าน** รายงานยังถูกต้อง เพราะครึ่งนี้ไม่ได้แตะว่าอะไรถูกวาด |

**ไม่มีตัวไหนเอาหน้าจอไปทั้งหน้า** รอบแรก 378 ผ่าน 2 ตก รอบที่สอง 379 ผ่าน 1 ตก และ `44a`
อีกเจ็ดแถวผ่านทั้งสองรอบ

**แถวที่ *เคย* ล้มเพราะข้อบกพร่องนี้ ไม่ได้ล้มภายใต้มัตแตนต์** แถว 1 ล้มเป็นบางรอบเมื่อสเปกก่อนหน้า
ทิ้งรุ่นอื่นไว้พอดี - ในรอบที่กวาด `staleanswerwins` มันผ่าน ทั้งที่ผู้คุ้มกันถูกถอดออกแล้ว
**แถวที่จับข้อบกพร่องได้เป็นบางรอบ ไม่ใช่แถวที่ค้ำข้ออ้างนั้นไว้** - แถวที่ค้ำคือแถวที่สร้าง
สถานการณ์ขึ้นมาเอง

## การกวาดรอบแรกฆ่าทั้งสองแถวที่เงื่อนไขนำ และนั่นคือข้อค้นพบเกี่ยวกับแถว

ร่างแรกของสองแถวเปิดด้วยการยืนยันว่าตัวเลือกเปิดมาที่ปีของนักศึกษาสองคนที่มันใส่เอง (2563 กับ
2568) ในชุดเต็มปลายต้นเป็น **2561** เพราะรุ่นที่ `17c` ทิ้งไว้เก่ากว่าทุกอย่างที่ไฟล์นี้ใส่ แถวแรก
ตายตรงบรรทัดนั้น แถวที่สองหมดเวลารอคำตอบที่ระบุด้วยชื่อช่วงซึ่งไม่มีใครขอ - **ทั้งคู่ไปไม่ถึง
ข้ออ้างของตัวเอง การกวาดรอบนั้นจึงไม่ได้พิสูจน์อะไร** และทั้งคู่จะล้มในรอบสะอาดเหมือนกัน

ตอนนี้ทั้งสองแถวอ่านช่วงเปิดหน้าจอออกจากจอ แล้วยืนยันแค่*รูปร่าง*ที่ตัวเองต้องการ คือเริ่มก่อน
ช่วงที่ขอและจบหลังช่วงที่ขอ ส่วนการแยกคำขอสองอันใช้ปลายที่ขยับ **แถวที่สร้างสถานการณ์เอง
ก็ยังได้รับโลกที่มันไม่ได้สร้างมาด้วย** และการกวาดคือที่แรกที่แถวใหม่ได้เจอทะเบียนแบบที่ชุดเต็ม
ทิ้งไว้ - เป็นเหตุผลข้อที่สองของการกวาด
"""
from harness import main

FILES = {"trend": "frontend/src/pages/ProgramLevelCompare.js"}

MUTANTS = {
    # ตัวข้อบกพร่องเอง: คำตอบที่มาถึงทีหลังชนะ ไม่ว่าจะขอไว้เมื่อไหร่
    "staleanswerwins": ("trend",
        "      if (isCurrent()) setData(answer)\n",
        "      setData(answer)\n"),
    # ด้านที่อยู่ถัดไปหนึ่งสถานะ: จอบอกว่าโหลดเสร็จแล้ว ทั้งที่คำขอที่มันรออยู่ยังไม่กลับ
    "staleclearsloading": ("trend",
        "      if (isCurrent()) setLoading(false)\n",
        "      setLoading(false)\n"),
}

main(FILES, MUTANTS)
