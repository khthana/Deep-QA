# -*- coding: utf-8 -*-
"""#68 - รายการของคำขอที่ถูกแทนที่ไปแล้ว ต้องไม่ถูกวาด และต้องไม่บอกว่าโหลดเสร็จ

ทุกแผงที่ paging ฝั่งเซิร์ฟเวอร์อ่านคำตอบด้วยรูปแบบเดียวกัน คือ
`setData(await listSomething({page, per_page}))` โดยไม่มีอะไรผูกคำตอบเข้ากับคำขอที่ทำให้
มันเกิด ถ้ามีสองใบวิ่งพร้อมกัน - กด *ถัดไป* สองครั้งรัว ๆ ย้ายตัวกรองก่อนใบแรกกลับ หรือ
เน็ตช้าใบเดียว - **ใบที่มาถึงทีหลังชนะ ไม่ใช่ใบที่ขอทีหลัง** ตารางจึงวาดหน้าที่ pager
ไม่ได้อยู่แล้ว และ `setLoading(false)` ของใบเก่าก็ปิดสถานะกำลังโหลดของใบที่ยังไม่กลับ

เป็นกฎเดียวกับ #129 ซึ่งเจอมันบนหน้าจอรายงาน (ที่นั่นคำขอสองใบเกิดจากการย้ายปลายช่วง
สองข้าง) #68 คือแผงที่เหลือ

## สิบห้าไฟล์ แต่มัตแตนต์อยู่ไฟล์เดียว

ตั๋วให้รายชื่อไว้เจ็ดไฟล์พร้อมคำสั่งว่า *รายชื่อจาก `grep` ให้ยืนยันทีละไฟล์ว่ารูปแบบตรงกัน
จริงก่อนแก้* ทั้งเจ็ดตรงจริง แต่คำถามที่ยังไม่มีใครถามคือ grep นั้น**พลาดใครไปบ้าง** -
คำตอบคืออีกห้าที่อ่านรายการเหมือนกัน (`Rubrics` · `Offerings` · `SubjectStudents` ·
`UserHistory` · `groups/GroupHistory`) **และอีกสามที่ความเห็นของเจ้าของตั๋วเองวัดไว้แล้ว**
(`ProgramLevelByIntake` · `ProgramLevelIndividual` · `ProgramLevelAllStudents` ซึ่งขับด้วย
`CohortPickers` ตัวเดียวกับที่ #129 แก้บน `ProgramLevelCompare`) รวมเป็นสิบห้า นับจาก
ตัวโค้ดและจากตั๋ว*ทั้งใบ* ไม่ใช่จากรายชื่อในตัวตั๋วอย่างเดียว

ตั๋วเสนอทางเลือกว่าถ้าซ้ำเกินสามที่ให้ยกขึ้นเป็น hook เดียว (`usePagedList`) คำตอบคือ
**ไม่** และเหตุผลอยู่ตรงนี้พอดี: มัตแตนต์ในโค้ดที่ทุกจอใช้ร่วมกันจะฆ่าแถวของทุกจอ
พร้อมกัน ซึ่งเป็นบทเรียนของ `intakefrozen` ใน #42 - มัตแตนต์ที่ฆ่าทุกอย่างไม่ได้พิสูจน์
อะไรเกี่ยวกับอันไหนเลย และไฟล์ส่วนใหญ่ในนั้นมี anchor ของใบอื่นอยู่แล้ว (13 · 16 · 18 ·
57 · 84 · 91 · 124)

`frontend/src/pages/Students.js` เป็นไฟล์เดียวในสิบห้าไฟล์ที่**ไม่มีใบไหนถืออยู่** จึงกวาด
ได้โดยไม่ชนใคร (ตรวจจาก `FILES` ด้วยสคริปต์สำมะโนใน `mutation/README.md` ไม่ใช่จาก
ความจำ) และทะเบียนของ seed มี 173 คน หน้าที่สองจึงมีอยู่โดยไฟล์ทดสอบไม่ต้องเขียนเอง
อีกสิบสี่จอเปลี่ยนด้วยกฎเดียวกันทุกตัวอักษร และพิสูจน์ด้วย*รูปแบบ* ไม่ใช่ด้วยแถวของตัวเอง -
ซึ่งเขียนไว้บน `docs/acceptance/57-pager.md` ตรง ๆ แทนที่จะปล่อยให้เข้าใจเอาเอง

## สามอนุประโยค สามมัตแตนต์

ผู้คุ้มกันตัวนี้มีสามอนุประโยค และ**ทุกอนุประโยคต้องบอกได้ว่าเป็นอย่างไหนในสาม: พิสูจน์แล้ว
เข้าไม่ถึงโดยโครงสร้าง หรือยังไม่ได้ทดสอบ** ที่นี่ทั้งสามเป็นอย่างแรก ตัวละแถว

`staleanswerwins` คือตัวข้อบกพร่องเอง - คำตอบที่ล้าสมัยวาดทับ `staleclearsloading` คือ
ด้านที่อยู่ถัดไปหนึ่งสถานะ: คำตอบที่ไม่มีใครรอปิดสถานะกำลังโหลดของคำขอที่ยังไม่กลับ
บนทะเบียนนี้ครึ่งหลังดังกว่า เพราะตารางวาด *กำลังโหลด…* อย่างเดียวขณะรอ ปิดธงแล้ว
จึงเหลือ *ยังไม่มีนักศึกษาในหลักสูตรนี้* - ประโยคเรื่องหลักสูตรว่างเปล่าที่ถือนักศึกษา
อยู่ 173 คน

`staleerrorwins` คืออนุประโยคที่รอบแรกไม่มีมัตแตนต์ และเป็นอนุประโยคที่อ่านผ่าน ๆ แล้ว
ดูเหมือนไม่ต้องมีผู้คุ้มกัน: `report` คือ `if (!error.expired) setNotice(…)` คำขอที่ถูก
แทนที่ไปแล้วซึ่ง*ล้มเหลว*จึงขึ้นแบนเนอร์แดงทับรายการที่มาถึงเรียบร้อยดี - บอกคนอ่านว่า
สิ่งที่อยู่ตรงหน้าล้มเหลว และไม่มีอะไรบนจอนี้เอาแบนเนอร์นั้นออกนอกจากการกระทำถัดไป
ทางเดียวที่จะเข้าอนุประโยคนี้พร้อมคำขอที่ถูกแทนที่แล้วคือทำให้มันล้มเหลว ข้อ 26 จึงทำเอง
ด้วย `route.fulfill` สถานะ 500 แทนการหน่วง

ด้านที่สี่คือ cleanup ของ effect (`current = false`) ถอดออกแล้วได้ผลเท่ากับถอดสามตัว
ข้างบนพร้อมกัน จึงไม่เขียนเป็นมัตแตนต์ ตามเหตุผลเดียวกับ #129

## การกวาด

`cd e2e && E2E_FRONTEND_PORT=5300 npx playwright test` (383 แถว) กวาดเมื่อ 12 ก.ย. 2569
ตารางนี้บันทึก **สิ่งที่มัตแตนต์ฆ่าได้จริง** ไม่ใช่สิ่งที่ควรจะฆ่าได้

| มัตแตนต์ | แถวที่มันค้ำ | ตายที่ |
|---|---|---|
| `staleanswerwins` | ตารางเป็นชุดของหน้าที่ pager อยู่ | ข้อ 24 ที่การเทียบรหัสบนตาราง — ได้ชุดของหน้า 2 (`66010110`…`66010101`) ที่ควรเป็นชุดของหน้า 1 (`67010002`…`66010111`) · **ข้อ 25 ยังผ่าน** เพราะครึ่งนั้นพูดถึงสถานะกำลังโหลด ไม่ใช่สิ่งที่ถูกวาด |
| `staleclearsloading` | คำตอบที่ไม่มีใครรอ ไม่ปิดสถานะกำลังโหลด | ข้อ 25 ที่การนับ *กำลังโหลด…* — ได้ 0 ที่ควรเป็น 1 · **ข้อ 24 ยังผ่าน** เพราะตอนคำตอบที่ล้าสมัยมาถึง สถานะกำลังโหลดถูกปิดไปแล้วโดยใบที่มาก่อน |
| `staleerrorwins` | คำตอบที่ไม่มีใครรอ ไม่ขึ้นแบนเนอร์ | ข้อ 26 ที่การนับ `role="alert"` — ได้ 1 ที่ควรเป็น 0 คือแบนเนอร์แดงของคำปฏิเสธที่ถูกแทนที่ไปแล้ว ทับรายการของหน้า 1 ที่มาถึงเรียบร้อยดี · **ข้อ 24 กับ 25 ยังผ่าน** เพราะทั้งสองข้อไม่มีคำขอไหนล้มเหลวเลย |

**แต่ละตัวฆ่าแถวเดียว และคนละแถวกัน** รอบละ **382 ผ่าน 1 ตก** จาก 383 แถว ไม่มีตัวไหนเอาหน้าจอ
ไปทั้งหน้า และไม่มีแถวของสเปกอื่นตกเลยสักแถวในทั้งสามรอบ — ซึ่งเป็นสิ่งที่ต้องวัด ไม่ใช่สิ่งที่
เดาได้ เพราะทะเบียนนี้ถูกสเปกอื่นอ่านอยู่หลายไฟล์ และรอบนี้โค้ดของอีกสิบสี่จอก็เปลี่ยนไปด้วย

**ตัวเลขทั้งหมดนี้วัดใหม่หลังการตรวจรอบสอง** ไม่ใช่ยกมาจากการวัดครั้งแรก — เพราะหลังจากนั้น
สามจอรายงานถูกคุ้มกันเพิ่ม เจ็ด handler ถูกแก้ และสามแถวถูกรวมข้อยืนยันเข้าเป็นก้อนเดียว
ชุดสะอาดของโค้ดชุดที่ commit จริง **383 ผ่าน 383** (19.4 นาที) และรอบกวาดทั้งสามใช้เวลา
ใกล้เคียงกัน ที่ `staleanswerwins` ตายนั้นตายทั้งสองครึ่งของข้อยืนยันเดียวกัน — ทั้งชุดรหัส
และบรรทัด *ทั้งหมด 180 รายการ · หน้า 2 จาก 18* ที่ pager ไม่ได้อยู่ — ซึ่งเป็นเหตุผลที่รวม
สองครึ่งไว้ใน `expect` เดียว

**รันเดี่ยวให้ผลเดียวกับชุดเต็มทุกรอบ** ซึ่งไม่ใช่เรื่องธรรมดาบนทะเบียนนี้ - `17b` กับ `17c`
เขียนนักศึกษาเพิ่มก่อนไฟล์นี้จะรัน ทั้งสามแถวจึงไม่ยึดกับตัวเลขของ seed เลย: ข้อ 24 กับ 26 อ่าน
ชุดของหน้า 1 ออกจากจอก่อนกด และยืนยันแค่ว่ามีหน้าที่สองอยู่จริง ส่วนข้อ 25 อ่านรายชื่อที่ควรได้
จากตัวคำตอบเอง
"""
from harness import main

FILES = {"register": "frontend/src/pages/Students.js"}

MUTANTS = {
    # ตัวข้อบกพร่องเอง: คำตอบที่มาถึงทีหลังชนะ ไม่ว่าจะขอไว้เมื่อไหร่
    "staleanswerwins": ("register",
        "      if (isCurrent()) setData(answer)\n",
        "      setData(answer)\n"),
    # ด้านที่อยู่ถัดไปหนึ่งสถานะ: จอบอกว่าโหลดเสร็จ ทั้งที่ใบที่มันรออยู่ยังไม่กลับ
    "staleclearsloading": ("register",
        "      if (isCurrent()) setLoading(false)\n",
        "      setLoading(false)\n"),
    # คำขอที่ถูกแทนที่ไปแล้วล้มเหลว ขึ้นแบนเนอร์ทับรายการที่มาถึงเรียบร้อยดี
    "staleerrorwins": ("register",
        "      if (isCurrent()) report(error)\n",
        "      report(error)\n"),
}

main(FILES, MUTANTS)
